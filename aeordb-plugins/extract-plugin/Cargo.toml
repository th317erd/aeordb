[package]
name = "aeordb-extract-plugin"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
aeordb-plugin-sdk = { path = "../../aeordb-plugin-sdk" }
serde = { version = "1", features = ["derive"] }
