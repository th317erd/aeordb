# Child 08 Progress: Evidence

## Landing State

- **Status:** P0 through the safe P8 native candidate gates are complete; P9 repository debt, operator/documentation closure, repository-wide strict Clippy, exact Linux/Windows, live/resource, long-duration, and final 100-case qualification are green. Native macOS awaits the usable SSH identity; copied-production, canary, install/deploy, cutover/acceptance, and destructive GC remain separately gated.
- **Current landing unit:** P9 native macOS qualification and adversarial repository-only DoD/evidence closure.
- **Entry commit:** `094d83f3646d24e0caeddad55ca96077c146004d` (`harden final qualification tests`).
- **Last pushed green commit:** `535004f166ad4cfaf8e7ab740458f2ef4733d2bc` (`refresh checkpoint audit inventory`) on `development` and `origin/development`; recovery authority remains the preceding `d93b4b22` landing.
- **Owner:** Codex, campaign integration/evidence owner.
- **Start gate:** Exact successor Linux and Windows native release candidates, live documentation routes, resource qualification, S1/S2/S3, and the final 100-case restart-resilience target are green and preserved.
- **Plan:** [Child 08](../children/08-verification-operations-docs-and-debt.md).
- **Owned files:** this ledger; suppression scanner/inventory; architecture target; narrowly required production corrections and regression tests; affected operator documentation.
- **Forbidden files and actions honored:** no v4 service authority, migration/cutover activation, production or evidence database mutation, deployment, `.codex/DETAILS.md`, `.codex/wip.md`, or `downloads/` changes.

## P9 Final Inventory-Coherence Correction

- Exact `4f72579e` Linux qualification attempt 2 passed format and shell gates, then stopped in the broad AeorDB matrix because the checkpoint-reader refactor had made the checked error-squelch inventory stale. The truthful four-entry failure manifest is `/media/Data/AeorDB/Tests/p9-exact-4f72579e/evidence/linux-final-attempt2/artifacts.sha256`, SHA-256 `51a71b063e76b330b478f9c42b2bec9ec53c1a169b79b9cc5150949b558a5f53`.
- The narrow scanner reproduced the mismatch. Scoping the new reader import to its sole consumer restored every unrelated source location. A generated refresh then removed only the obsolete `diff_checkpoint` fallback occurrence, moved one later diagnostic row from line 1113 to 1108, retained the complete reviewed policy map, and shrank the exact ceiling from 1,506 to 1,505. Local and isolated desktop hashes match for the changed probe source (`dd4f471d6896307044a514c5d9dd098787cdcae5328dcfb83fb3586ad79c17c3`), unchanged shared reader (`61a85968025cc1574ce0a7e728c28b23046b07fef34ca91878971d1cc2e239ab`), and inventory (`55b70958d660e404ec5f99ee769ef22103ad8003e7001da3cd5a34e48c3ccec8`).
- The isolated desktop landing matrix passed formatting, scanner validation and tool tests, all 29 error-squelch architecture tests, the focused checkpoint-reader targets, the complete 202-test CLI suite with seven intentional ignores, and strict CLI all-target Clippy. Across the deliberately overlapping targets, 247 tests passed, zero failed, and fourteen were ignored. The result SHA-256 is `b8c8bf2c0d4350cdc15e998b041bbf33c31c911a477c42afe49de6e95cfe20c2`; the sealed artifact manifest SHA-256 is `47d775892c06efe0780a5623bcc61a372222a0804a705fa937d883dc72b9f656`.
- The first service launch failed before test execution because its configured output directory did not yet exist; that setup failure is preserved at manifest SHA-256 `a825b33456890f83c1f1e190190967a34ab23b615bec40495cabe6c24b141f4c`. The corrected launch completed every gate and wrote exit zero before its post-run manifest command used the wrong working directory; the empty attempted manifest and service record remain inside the final sealed evidence rather than being hidden.
- Exact `4f72579e` native Windows qualification independently passed formatting, the storage matrix, the CLI matrix, and its one-job release build: 990 tests passed across 17 result groups, zero failed, and seven were intentionally ignored. Its result SHA-256 is `3838f790177c9c60d318566219d6220067a144276d54b0c162d8d2c87bc67a44`, release binary SHA-256 is `03d05f95eb757f87233fddcc27e94ff79c089a56b252c29c9791a54688049f1c`, and centralized artifact manifest SHA-256 is `7bc690417eb8da6d8a76d4a1383f8bfc64786eac43d536e33d3f1d717c4a9c32`.
- The shrinking inventory landing is pushed as `535004f166ad4cfaf8e7ab740458f2ef4733d2bc` (`refresh checkpoint audit inventory`). Its exact source archive SHA-256 is `1f534f95840121689b6ccb36dd13eeca7dc0c0aef7f8332ef036078b73b50f05`; the frozen root lock remains `06e6c7a8eb6dbccf52a0b97a4ee6edeece7297866305b0930314fd47b987faec`. Fresh Linux WebAssembly preparation passed under manifest SHA-256 `75233ad215c6240748b94ccebc8f1bcd33d6277e589ad68a0d196ae3fbc0c105`.
- Exact successor Linux qualification passed formatting, shell behavior, 7,080 AeorDB tests, 202 CLI tests with seven intentional ignores, 7,403 workspace tests with seven intentional ignores, strict workspace Clippy, the 1,505-entry inventory, all 29 inventory architecture tests, contracts, debt, docs, and both release builds. Zero tests failed. Its result SHA-256 is `87d80d19ba3cabd178f9f747f8c1a20cfbd271f3bf2dceaf061c1914ac418984`, release binary SHA-256 is `775f56f1bba3d73f97d0339b77e2bc3a3bc3726465b57abe8fa0e5d91a32fdbe`, and artifact manifest SHA-256 is `c7dbb076bcb03a9c0de4ca7f89686db58ab40b6790a07e41a0ef6fda66b43fc1`. A provenance-only first launch stopped before tests because the desktop history clone lacked the new commit object; its preserved manifest is `ef8a96970213e5307ba16763cb4e00d0dd5f1907b7283d59c13da96b7993d8b5`.
- Exact successor native Windows qualification passed formatting, storage, CLI, and one-job release gates: 990 tests passed across 17 result groups, zero failed, and seven were intentionally ignored. Its result SHA-256 is `88e387a3c392e4a9afd8748d93dfa024eb5e762abd73fdfc91968ee8bc6c81cc`, release binary SHA-256 is `167a64b97e830e9aea8231df9734036f349c2ca57810310322c503e042e75cf3`, and centralized artifact manifest SHA-256 is `708d34aebbd70d7578298a5e2fa7f87b33263197b7648d7dca2b4e0497aa5928`.
- Git-object proof shows the complete engine source tree and long-run scripts are byte-identical from `9a71d4ce` through `535004f1`; its manifest SHA-256 is `8e01d8de7f81fd70dfdba97c71b19460cebb7b1170220e5158dcc87a7dbc1221`. Because the rebuilt worker's full executable hash differed, equivalence was not assumed: the exact successor reran all 100 restart-resilience cases and passed every one with zero failures. Its worker SHA-256 is `ca1c71c702ac5686ca8e9bb7cad03ff1912fea03d2b337573750d4192ce27f16`, result SHA-256 is `c4230b0c2f854167575cbefe3383e1d5de25858a46b3a7fcd04c3acf9f08ce08`, and artifact manifest SHA-256 is `fa64f09dd2d7a0d39f8bdbd67368cf25d41e80272e248801de3617a9a75b8fb2`.
- **Next exact action:** run the prepared exact successor native macOS package after the usable SSH alias/identity is supplied, then publish the final adversarial DoD/evidence closure. No separately gated operational boundary has been crossed.

## P9 Crash-Qualification Recovery Slice

- The first short S3 run made the harness itself fail closed instead of printing and forgetting defects. It durably publishes pending overwrite/delete checkpoint intent before database mutation, parses every verifier defect family, retains diagnostics, and returns nonzero if any kill cycle fails. The preserved 127-cycle run found 12 invalid reusable-void rows at iteration 9 while every external checkpoint comparison remained clean; runner log `/media/Data/AeorDB/Tests/p9-recovery-fix-c4591572/s3-short-20260829T2302/runner.log` has SHA-256 `b2a1dd663b7069a9370f516ba747de23243f0ae5e7de3e67704fc2a3518c1759` and is not claimed green.
- A separately preserved interrupted run repeatedly reproduced one stale stable-directory locator after dirty startup selected the older header while post-frontier namespace authorities were still present. Its runner log `/media/Data/AeorDB/Tests/p9-recovery-fix-c4591572/s3-short-20260829T2345/runner.log` has SHA-256 `5acc223929015deed8e61f365173d93d0a02a8daffc172b08422dd1b6486890f`; interruption and every observed failure remain explicit rather than being converted into a passing soak.
- Territory tracing identified the mixed-authority mechanism across file, directory, symlink, snapshot, fork, deletion, and task locators. Dirty rebuild now classifies stable namespace authorities beyond the selected hot-tail frontier, excludes them from rebuilt KV authority, overwrites exactly those complete rows in place with durable Void records, and only then publishes the rebuilt header. Unknown or malformed FileRecord/Symlink rows remain fail-closed. A torn recovery Void is permitted to discard only the non-authoritative terminal tail, while complete hash-bad or structurally corrupt WAL remains unrepaired. Recovery/GC Void registration normalizes the non-overlapping union. All v3 direct reusable-space consumption is now disabled until the receipt-backed v4 catalog/claim authority can make allocation crash-atomic.
- Dirty-recovery locator tests failed first at log SHA-256 `bf2e53244f96b54c9e34b0a84be21d370d769fdc84eb073c110049a68c927078` and pass after the correction at `9362100714a947928fd0afd7d1ce6af806d4f817a44c71e9eb9eac1e9005ca61`. Retained locator payloads are admitted before allocation through the process coordinator as `repair` / critical bounded recovery, charged at `3 * raw + 512`, capped at 64 MiB per value, and released on every success/error path; red/green log hashes are `486ab09bfb98570c4926da12eb8e4a1c23c280f5b72e0a5757302bf0a57af906` and `d1e4f98d78ef56e044c8fc64539684440e9b75ded241a5412f8954480dda5929`. The first adjacent attempt under desktop `/tmp` truthfully failed the frozen capacity oracle, and the Data-volume attempt truthfully failed private-mode tests because its FUSE/NTFS mount is fixed at `0777`; final qualification keeps build/log I/O on Data and uses ext4 `~/.cache` for permission-capable `TMPDIR`.
- After the namespace repair, a fresh 101-cycle S3 run had 100 clean verifier passes and 101 clean checkpoint comparisons but found six otherwise-valid hot-tail Void rows out of offset order at iteration 8. The evidence and diagnostic database remain under `/media/Data/AeorDB/Tests/p9-recovery-fix-c4591572/s3-short-dirty-rollback-final/`; runner log SHA-256 is `4a32d20eb2bcef07fb4d0f6849945f95cc2cabe3bd537262d7a95626eefa292a`. Binary inspection traced the defect to both KV-expansion producers: online adjustment and interrupted-expansion recovery could relocate only a low subset past retained high rows without restoring canonical order.
- Both expansion producers now sort their transformed reusable-space snapshots by offset. The direct online and private interrupted-recovery regressions failed first at SHA-256 `ea0fe996ab1f88e2677bb065b75fca37579389359ff29e703814ec52939f4c6d` and pass at `c3611fbf02167cdd7f1cdf08ded1643cb35d0627c87c557790fbd02cdbfb7241`. The post-fix adjacent matrix passes 799 tests with zero failures (`164c48eb987304f7341ebfef9818e5e821c6dc94c3d62640af364295e7fb88e8`); the release crash matrix passes seven campaigns and 23 real SIGKILL windows with zero failures (`87505f6418fcd66408bc1314d065ef92889551e7aec28b41bce2d7e0ac2d9469`); and the adversarial debt self-test/gate remains green at eight reviewed entries and 164 retained matches (`7154b91a84043fd58d569439469591aed8ff65c297174c0bf8c66205b7303fd8`).
- The final fresh short-S3 rerun passes all 101 SIGKILL/reopen cycles, all 101 complete verifier reports, and all 101 external checkpoint comparisons with zero retained failures. The unit exited 0; runner log `/media/Data/AeorDB/Tests/p9-recovery-fix-c4591572/s3-short-void-order-final/runner.log` has SHA-256 `bf1daf41bdb3d625bad061f57cc46ba12b77e9eaec043c31cda2f96bbf9cd5f0`, and the complete artifact manifest remains beside it. No production, production-derived, canary, install/deploy, cutover, acceptance, first-write, or destructive-GC boundary was touched.
- Complete malformed FileRecord and Symlink rollback-authority fixtures failed first at `b98548644c79827e834e098a6e7b1c08075f999863a60ae1a8a3ccab81c05fd6` and pass fail-closed at `931c676b96c4a1a59c0bf7fdfd84b3af9649191c7491794d0644862e42e526d3`. Refreshing the reviewed error-squelch inventory removed the obsolete crash-worker success-only probe and shrank the exact ceiling from 1,507 to 1,506 (`ca07eb6a6cf4dedd1ca2e5bb784bb6d8d4b329b00c16d8f9cbc67f3798115165`). The first complete static/package gate then passed formatting, workspace all-target checking, strict Clippy, affected CLI/harness tests, all 29 architecture tests, 454 fixtures, eight debt entries/164 retained matches, 95 routes, and 39 documentation pages; runner SHA-256 is `074d069f3576ef06f215187f55bcabc8b31f955a18076b60cb2192623d3dc451`.
- The first final pre-commit S3 attempt truthfully failed all 149 post-crash cycles after its 1–3 second window killed the first worker before `StorageEngine::create` returned; its checkpoint remained empty and the new database was corrupt at offset 512. The preserved log `/media/Data/AeorDB/Tests/p9-recovery-fix-c4591572/final-precommit/short-s3.log` has SHA-256 `8aa53691c981e07149bb29ebba92aeae961a873315c4303cf528c847cdb49420`. A failing-first harness guard (`99a7600e8a0870b9e0a942c21f709bf44f7030beea617a85f4e44452756c6e73`) now requires S3 to observe the exact durable `# worker up mode=stress` marker before starting its first crash timer; its green proof is `99f20a97bbb3975f908dde806ae789c3c0b35473a117036169ec9e3a07b6420d`. Missing-checkpoint marker counts also failed first at `b17a2915f83f279868053bfdb54cc58a741a51d06716cbd1b7438a59d400b89f` and normalize to zero at `8db611eec79fd68f59480d50ce202e193533b0ddd9f6d3e1c25bff8b1ffdf010`.
- The next complete S3 attempt exposed a real repeated-crash allocator defect at iteration 3: a direct Chunk consumed a reusable extent, the remainder Void tore, the prior hot-tail snapshot temporarily masked the hole, and a later hot-tail publication made the same offset fatal on reopen. Thirty-one diagnostic copies and `/media/Data/AeorDB/Tests/p9-recovery-fix-c4591572/final-precommit-r3/short-s3.log` remain preserved; runner SHA-256 is `7baf6110e937fcceeae71c6bd24546bdd0fb4bfd0dee8f20fd909c2bd1ba09bb`. The no-direct-reuse target failed first at `3ab3e19c432f31d5ae2cf05da0e689544f22249fe01cb29a5ed548c30f5e45c8`; focused no-reuse and crash/reopen proofs pass at `18f78212aaee02ede47324d909eed8e33f92238bbe0d937f09a1b9229626cd13` and `ff43033cd5c70d64b39ef0542b08c65b0fd424c5ca2938d99dd1d2ce2d6ff383`.
- Disabling consumption made three legacy tests reveal their stale premise rather than hiding the mismatch; that truthful adjacent failure is `dd937859e1d3cedf46ea7ca59accfd67f22ef2f620e1f73634a235deb300ebee`. The reconciled 75-test corruption suite passes at `508c37fd48994f4bf03342a11db6fb0d4c708ba9fc978983176ad335293bd9d0`: chronology is still proven with a deliberately lower physical replacement, expansion still relocates and orders reusable-space metadata, and both encodable and unencodable v3 extents remain intact while new Chunks append.
- The fresh final pre-commit unit passes 800 adjacent tests (`bbe988326747eb7f5094296f6785c415dcda8cc91d12082f2a53300555573941`), all seven release crash campaigns and 23 real SIGKILL windows (`8facf75488e7c7f133c86fe64a543ea2b2091147d5c48023d75d7867726ff65c`), and 99 fresh S3 kill/reopen cycles with 99 complete verifier reports, 99 checkpoint comparisons, and zero retained diagnostics (`6c22db2af74e8cf1b7900cb6a4c2be7dcd28861491d09bc8c2ebb69275a9e136`). The first S3 worker visibly published its durable startup marker before the crash window began. A final generated location-only inventory refresh retained exactly 1,506 entries, 35 policies, and an identical semantic hash before/after; the complete static/package/contract rerun exited 0 with runner SHA-256 `9f7544dd63ee693e9096805f7e5b840e0f6de836a99c479027e35b4faef68541`.
- The coherent 22-file recovery landing is commit `d93b4b229769acf1ad80fc0e82ee4fb19f779c86` (`harden crash recovery authority`) and is pushed to `origin/development`. Its first exact-source Windows CLI matrix truthfully failed because Windows rejects an unreadable checkpoint directory while opening it and Unix reports the failure while reading it. Direct Windows execution returned Cargo exit 101; the original `Start-Process` wrapper falsely recorded zero, so that attempt is rejected and preserved. The narrowed cross-platform oracle requires either exact fail-closed prefix plus the checkpoint path, passes on Linux, macOS arm64, and Windows x86_64 MSVC, and is the only tracked change in pushed test-only commit `4a64d020be76f898b28a76823240134e3c0c4ca2`. The hardened Windows qualifier converts any zero exit accompanied by a terminal Cargo failure marker into exit 125. Final ordinary, native, live/resource, and long-run qualification is pinned to archive SHA-256 `c2383ee0f3e29d3682f1d645ca311f6e2fb6e2e5303a680d693a5febea683735` for `4a64d020`, not a pre-commit working directory.

## P9 Resource-Qualification Correction Slice

- The first exact 8 GiB/no-swap resource run is rejected and preserved, not repaired in place. Its original database is `/home/wyatt/.cache/codex/aeordb-p9-d93b4b22-resource/live.aeordb` (SHA-256 `ab128e15eef020bbfbe28283b230438435335557c2842dbeabe78d8622530f3e`); the byte-identical diagnostic copy and immutable logs are under `/media/Data/AeorDB/Tests/p9-exact-d93b4b22/resource-attempt1-diagnostic/`. Read-only verification of the copy found one otherwise healthy WAL-backed identity missing from KV authority: `/.aeordb-indexes/@content_type.string.idx`, key prefix `c6fd8534509de3e1`, WAL offset `323729080`, length `260`. The run also recorded two 30-second snapshot-drain timeouts before immediate retry success; one affected blob commit reported `namespace_publication_ms=30369`.
- The strengthened sequential expansion regression reads every stored path and requires zero verifier-reported missing/stale KV rows. It passes on `wyatt-desktop` in 10.11 seconds, so ordinary serial expansion is not the falsifier. A deterministic page-provider interleaving then failed first in 0.01 seconds: after update preparation evicted an old page but before pending-generation publication, an ordinary reader could begin an old disk load; after replacement commit it could repopulate the current cache with those stale bytes. Red log: `/media/Data/AeorDB/Tests/p9-resource-fix2-4a64d020/evidence/page-race-red.log`.
- The page-provider correction enforces that an update owner may capture old bytes while `preparing_update` is set, but an ordinary cache miss waits until pending old-generation bytes are published or preparation exits. Every successful, aborted, failed, or poisoned exit wakes waiters; admitted old snapshots still read their retained pending/historical bytes after publication is poisoned. The deterministic interleaving and missing-pending poison targets pass, and the exact desktop adjacent rerun passes 4 internal provider tests, 3 transaction-admission tests, all 13 public provider tests, the exhaustive expansion verifier, the expansion overflow regression, and both cache/hardlink expansion tests. Preserved adjacent log SHA-256: `c037f4a3d5f768db404040ba85ba400aec5a9e89ed14e9753f4316d9a789bac0`.
- Snapshot contention before layout mutation is now a typed resource-busy result that restores the current provider view and retains the requested expansion. The committed write returns immediately instead of waiting 30 seconds; after the retained reader drops, the queued expansion completes and the committed entry remains readable before and after it. The failing-first target returned the former `InvalidInput` in 0.02 seconds; the corrected target passes in 0.03 seconds and is included in the adjacent proof above.
- The first broad run truthfully stopped at the error-squelch architecture gate because source locations moved and the `read_page_at` helper split changed two stable identities. The checked refresh retains exactly 1,506 occurrences and 35 policy definitions; the multiset of file/kind/pattern/review assignments and every policy body are identical before and after. The two replacement identities retain `optional-data-default` and `format-or-header-probe`, while three unrelated sites keep their prior manually reviewed policies rather than accepting current classifier churn. The focused audit plus all 29 architecture tests pass with runner SHA-256 `274e5e5c777170229e8d95f09adf9c61acb7d6543f6045ef738e37dfa3ba5878`.
- The corrected exact desktop source passes formatting, workspace all-target checking, strict workspace all-target Clippy, and the complete workspace all-target suite: 7,373 tests pass across 343 result groups, zero fail, seven are intentionally ignored, and 126 are filtered. The same runner passes 454 independent reference fixtures, 95 routes, 39 documentation pages, and the eight-entry/164-match debt ceiling. It ran from `2026-08-31T01:12:16Z` through `2026-08-31T01:49:23Z`; preserved runner SHA-256 is `6881582043bce2e289301c13b9ea39733831723d1f065694ed19f5aa6bae7360`. Native macOS/Windows, a fresh exact-source Linux release matrix, and the final 8 GiB/no-swap rerun remain required before this slice resumes the long campaigns.
- Native qualification first exposed a Windows-only test-fixture mismatch: the anonymous delete-on-close race file could not be reopened with production sharing semantics, so the full attempt failed after 608 passes with sharing-violation code 32. The rejected attempt remains under `C:\Users\wyatt\.cache\codex\aeordb-p9-51840f68\evidence\windows-attempt3-pinned-anonymous-file-failure` with runner SHA-256 `edd75bc352cbf2df065f1873c3a24ac44e5311127eac68b1f4ffa083063e610e`. The fixture now uses a named file whose temporary directory owns its lifetime; the exact regression passes on Windows in 0.02 seconds (`a3093757b7972a2a81ff9920f5fc773d66c3612b5e3a4a8c8afa38ae8e5be263`) and macOS in 0.03 seconds (`cb3c5606839aea5b58e681f162b13c77e10c5a7517d040235fe47656786a813b`).
- The first Linux workspace qualifier then exposed two scheduler-sensitive qualification deadlines rather than product failures: configuration-authority owner convergence exceeded a two-second test wait, and initialization-panic supervision exceeded a one-second test join. Both synchronization points retain explicit fail-closed deadlines, widened only in test code to the project-standard ten seconds. Their exact targets and the complete 19-test CLI library pass, and the final full workspace rerun passes 7,373 tests across 343 result groups with zero failures, seven intentional ignores, and 126 filtered tests; all three hardened cases pass in that run. Preserved Linux log SHA-256 is `fafd2a1d7c18636caf8443523624e81bdc9161b5aecea7d8f574ed23ebbe4c04`; the rejected link-only outer-timeout and CLI-timeout attempts remain preserved at `597a79e4e0dc6b041476452125575903b467bfe3fc598215d61f413673045fb6` and `633d0c200ff53e9b6b3b570070a0f8ecc67703ca10cc6a0a3b477e87265cc91a`. These corrections change qualification fixtures and deadlines only; they do not change production behavior or erase any failed evidence.

## P9 Dry-Run GC Stable-Layout Correction

- The first exact 8 GiB/no-swap run at `094d83f3` completed 950 writes, 4,968 reads, 749 blob commits, 34 reindexes, 114 searches, and 35 dry-run GC requests before one GC request returned HTTP 500. The server completed a stage-three online KV expansion at `2026-08-31T05:19:59.544624525Z` and reported the GC failure 2.3 milliseconds later. The original database and its byte-identical diagnostic copy both retain SHA-256 `0ccbaa00369a23a33aeb53ac323cf913515896f35fdbe70a2fb6b2501cd6475a` under `/home/wyatt/.cache/codex/aeordb-p9-094d83f3-resource/` and `/media/Data/AeorDB/Tests/p9-final-094d83f3/diagnostics/resource-attempt1-functional-gc-500/`; no failed evidence was mutated or reclassified as passing.
- Territory tracing found that `gc_sweep_internal` copied immutable KV rows and their physical WAL offsets, released the snapshot lease, and only then read WAL headers. Online expansion could therefore relocate the WAL between selection and inspection. The correction adds one `StorageEngine`-owned stable KV/WAL visitor that acquires the writer-layout read lock before leasing the immutable KV generation, validates every row against that retained writer, and keeps both authorities alive through header inspection. GC now uses that visitor for its first pass; destructive candidate revalidation and concurrent-write sparing remain unchanged. Visitor failure propagates and releases both the writer guard and snapshot lease.
- The stable-layout regression failed first because the authority did not exist; red log SHA-256 is `9082485c4384029d2dfb52b1be708eea18230af1d26f3d2dc039ae6ac308577a`. Both success and injected-visitor-failure paths pass (`98b0ab2d1a90bb2a2fbdad306420415ba391468dfc05fa07d9f45921d2474684`), and the static ownership/lock-order guard passes (`7312ecef52d9b077e099fd87913a769f39fe96f6d920a56f087f6c63b25fda7f`). The adjacent corruption, disk-KV, GC HTTP, GC, v4 execution, observability, qualification-harness, and KV-concurrency targets pass; one combined attempt truthfully encountered the desktop `/tmp` reserve-capacity oracle, and its isolated GC rerun passes all 45 tests with SHA-256 `21bbc302e7916cacadf2480ffc5f192d984d09142384a9de0227ace3d8d453fc`.
- A fresh 120-second diagnostic live run under concurrent writes, reads, blob commits, searches, reindexes, dry-run GC, cancellation, and three online KV expansions exits zero with no functional failures: 917 writes, 4,492 reads, 691 blob commits, 104 searches, 38 reindexes, 39 GC runs, and 60 cancellation attempts. Peak process memory is 1,646,231,552 bytes under the 8 GiB ceiling with zero swap. Offline verification reports 122,727 valid WAL entries, 73,325 KV entries, and zero corrupt headers/hashes, stale/missing/invalid KV rows, directory defects, or invalid voids. Evidence is preserved at `/media/Data/AeorDB/Tests/p9-final-094d83f3/evidence/resource-diagnostic-attempt3-gc-expansion-fix/`.
- The first broad attempt placed `TMPDIR` on the Data volume, whose fixed `0777` mode invalidated 16 private-permission assertions and whose FUSE performance exceeded two B-tree timing bounds; it then reached its outer timeout while the disk-KV suite was still doing heavy I/O. That rejected environmental run remains at SHA-256 `ea7d243af40ea9e5626f2bd21f6da5f6c52fcd91ea9562b045dfbc5d2deb6db0`. Permission-capable ext scratch independently passes the entire 614-test library and all 78 B-tree tests (`b2fbd14fa7a7916ad4fb21cecc0029367d807545b9bca0b4bdf7315aef7bcdc7`, `ac7f82076129e76497212675c441de61bd1482d3e368f8174aee9ed4b7018e66`). A subsequent complete run found only six stale line-number fields in the checked suppression inventory; the generated refresh retained all 1,506 semantic entries with identical normalized SHA-256 `c2a5cddb01f3339fe427cb17ecb365a31a58f48328971423a3b788b0361d6d46`, and all 29 architecture tests pass.
- The final uninterrupted workspace/all-target run passes 7,376 tests across 343 groups with zero failures, seven intentional ignores, and 126 filtered tests; preserved log SHA-256 is `c50da9563f0e48e1791d58977758c5372a03b8cf2cdc67601129a4e20c174274`. Strict workspace/all-target Clippy is green with zero diagnostics (`941560b8e9fba68797acdcfcc7ab9acfe2e619ed89d0390f86a1686501daa6dd`). Rust formatting and diff hygiene pass. The Git-aware independent contract gate passes 454 fixtures, 95 routes, and 39 documentation pages (`79ce73569b5fc32116d7d116633cc760b205e57a7c8a32438fc475dc7c0aef86`); the standalone debt gate remains green at eight reviewed entries and 164 retained matches (`a1cdc0cc2a89df2d046000e027cb12db537b927b5637b661b05ebc21a4b4ad37`). An initial archive-directory invocation was correctly refused because an archive has no Git history; that truthful wrapper refusal remains preserved at `17af91a2a418dcd43936467d0a04511ea31ec088cdfb46afcd91a69e1fcc08de`.
- This slice touches no production or production-derived database, deployment, install, canary, service activation, cutover, acceptance, first v4 write, or destructive GC boundary. Native exact-commit release qualification and the formal 8 GiB/no-swap rerun remain the next gates before the serial long S1/S2/S3/crash campaigns.

## P9 Streaming-Read Layout Correction

- The formal exact-commit 8 GiB/no-swap rerun at `4d3048af4483867d0b568e34f8026dad9046f92e` is rejected and preserved after 120 seconds because one otherwise-successful seed-file GET ended as Node/Undici `TypeError: terminated`. The joined workload recorded 961 writes, 4,694 successful file reads, 726 blob commits, 40 dry-run GC runs, 38 reindexes, 109 searches, 60 cancellation attempts, and 4,597 health samples; p99 health latency was 77.66 ms and the server had a 1.5 GiB cgroup peak with zero swap. The failed database (1,420,631,227 bytes, SHA-256 `1332ebc38846f905aa2847a30f52f5b77e5f6e6440b932dcc522c037157aa9fb`), load report (`4f5fb96f003b88836f8d93bd73594c7b31239abe013394e76ba6d06c87a7200c`), complete journal (`b480bec1e78585b9d2bf72c65e8381411a6d531b83941ec50cc13ef3dad3c1bc`), and startup response are immutable under `/media/Data/AeorDB/Tests/p9-exact-4d3048af/evidence/resource-attempt1-runtime/`.
- The workload joins every producer before report publication, and the shell sent SIGTERM only after the terminal dry-run GC completed and the failure report was durable; the server then shut down cleanly. The failure was therefore not an in-flight harness request racing cleanup. Territory tracing instead found that `CoalescedEngineByteRangeStream` retained physical chunk offsets while an HTTP body waited to be consumed. Online expansion could relocate those chunks between plan construction and the first or a later response frame. `read_chunk_span_verified` correctly rejected the stale locations, but the response-body adapter converted that internal error into a truncated HTTP stream, exactly matching Undici's `terminated` result without an error-level server log.
- The deterministic pre-expansion-plan/post-expansion-read regression failed with the same concrete mismatch: planned offset `66048`, relocated offset `854188`, unchanged length `262239`. Its red log is `/media/Data/AeorDB/Tests/p9-stream-layout-fix/evidence/failing-first-red.log`, SHA-256 `8bafd60547b588a61b889648ff218858bfb4f5434940cad2a4c3cb761d2342d0`. The correction treats a still-live hash at a changed physical location as a transient layout result, refreshes the remaining coalesced plan, releases the abandoned attempt's memory reservation, and retries at most the finite KV-stage count. It deliberately does not retain exclusive maintenance admission across a potentially slow network client; missing, compressed, length-changed, corrupt, shutdown, and resource-refused chunks remain terminal.
- Both expansion-before-first-frame and expansion-between-frames regressions now return byte-exact content; the first also proves streaming reservation count and reserved bytes return exactly to baseline. Focused green log SHA-256 values are `61450661fd8cded04e8c5ac60ba26e4e65de536ed7df5f711c9c586f1aab0c30` and `d66b2720e93db6241fa0a71b549bde07712a7caa40474aae362047c53521f8cf`. These focused proofs were then carried into the final affected and broad/static qualification below.
- The storage-layer helper exposes the layout transition only to the bounded stream adapter; its existing public verified-span contract still returns the exact detailed corruption diagnostic for callers that supplied a stale physical span. The final focused runner passes all three coalesced-stream regressions and that direct public-contract regression.
- The first broad/static runner correctly rejected six stale suppression identities after the implementation moved their enclosing methods and source locations; its log SHA-256 is `c480e50024761d3ee6515a6c1434feea1c133cd22623604259cacb1af89d477f`. Regeneration retained exactly 1,506 reviewed occurrences. Three unrelated classifier suggestions were rejected rather than silently changing policy, and the old/new normalized `{file, kind, pattern, review}` multiset remains identical at SHA-256 `2e532ee8923dcf395eb8fc18a80adbfb81aeeb1bee5964e698ff82158517da05`.
- The corrected broad/static runner exits zero. It passes Rust formatting, workspace/all-target checking, strict workspace/all-target Clippy, the exact 1,506-entry suppression check, all 29 suppression architecture tests, 7,379 workspace tests across 343 result groups with zero failures, seven intentional ignores, and 126 filtered tests, the 454-fixture/95-route/39-document contract gate, and the debt gate at eight reviewed entries and 164 retained matches. Preserved runner log SHA-256 is `a25d5501ae745467f3ec51f3ea6a5e32ac74c857bf3a750d59b646f1c3435b1d`; result SHA-256 is `9e8b5b36365c8ea14a7bdc8f84e74c85ccd66cb9f5ca3573d78c6f581719b0b5`. Exact-source native, real-server resource, and long-run gates remain required on the landed revision.

## P9 Debt Gate Qualification

- The new checked policy classifies four still-required compatibility surfaces with exact owners, allowed files, non-growing match ceilings, rationales, and removal gates: selected-root v3 reads, explicit legacy-v3 root service mode, the active v0 whole-index manager, and the embedded JSON/base64 query cursor. Three forbidden classes must remain at zero: the dead auto-heal stub, the misleading `legacy_position_component` name, and any v4 `IndexManager::new` bypass. The gate passes at 7 reviewed entries and 87 retained matches and is now part of the independent v4 contract wrapper.
- The opt-in auto-heal module and feature were safely deleted because peer retrieval had no transport and always returned an error. Its two stale suppression rows were removed, shrinking the reviewed ceiling from 1,510 to 1,508, and the persisted producer/consumer inventory now records the retired writer explicitly. Required v3 readers and writers remain intact.
- The v0 whole-index conversion helper is now named for its input representation, and `FromStr` is imported only on Linux. Identical changed-source hashes were verified on Linux, macOS arm64, and Windows x86_64 MSVC; the deleted source is absent on all three. Release-profile library checks pass natively in 49.16 seconds on macOS and 175 seconds on Windows, with the former unused-import warning absent on both.
- The adversarial shell self-test covers the valid policy and every validation family: missing/malformed/schema metadata, duplicate identities/paths, classification and ceiling rules, unsafe/missing/out-of-root paths, invalid patterns, new paths, match growth, forbidden matches, and stale review paths. It passes under an outer 20-second deadline; the standalone gate passes under 120 seconds.
- The affected Linux matrix passes 176 aggregation, suppression-architecture, query-engine, and pagination tests with zero failures. Workspace all-target checking passes with only the historical test-only `require_wasm_parser` warning. The chained independent contract gate passes 454 fixtures, 7 debt entries/87 retained matches, 95 routes, and 38 documentation pages.
- Durable command, failure-first, native, hash, and boundary evidence is recorded in `evidence/p9-debt-gate-report.json`. The unit touches no production or production-derived database, install, download artifact, deployment, canary, service activation, cutover, acceptance, first v4 write, or destructive GC boundary.

## P9 Documentation and Supersession Qualification

- The active 39-page mdBook now states the exact current authority: ordinary service operation remains on the v3 compatibility runtime, no public CLI/HTTP migration or automatic v4 activation exists, and legacy layout convergence plus forced FileRecord backfill are not v3-to-v4 migration. The new operator page freezes preflight, clone/capture/reconciliation, read-only cutover, acceptance, first-write, rollback, evidence, monitoring, and destructive-GC boundaries without exposing an internal command as public API.
- Architecture, storage, indexing, configuration, CLI, installation, backup, deployment, GC, observability, API-versioning, client-release, and served bot guidance agree on the current-v3/staged-v4 boundary. The old external `.aeordb.kv` and hot-file recovery advice is removed from active instructions; the current single file, lock, external spill evidence, and logs are preserved instead.
- Thirty-three identified historical design/implementation documents now carry explicit fully-superseded, partially-incorporated, or active-v3-history banners with valid links to the current campaign. The external hot-file transaction documents are explicitly pre-single-file history rather than current recovery authority.
- Territory tracing found that `--hot-dir`, `storage.hot_dir`, and the public Rust constructor family remain accepted but are ignored by the current engine. Their public descriptions and startup output now say so, and `legacy-hot-dir-option` is a timed compatibility entry with owner, exact five-file production surface, 77-match ceiling, and removal gate. The complete debt policy passes at eight entries and 164 retained matches.
- Failure-first proof records the absent migration page, machine inventory drift, stale external-hot-file claims, and missing debt-policy entry. mdBook, all 39 source links, JSON/YAML syntax, formatting, diff hygiene, 33 banner links, the debt self-test/gate, and the independent 454-fixture/95-route/39-document contract gate pass.
- On `wyatt-desktop`, 64 focused CLI configuration/start/live-route tests pass, workspace all-target checking is green except for the historical test-only macro warning, and the exact release CLI builds in 2m23s. Its generated help preserves the flag without advertising external hot files and exposes no `migrate`/`cutover` command.
- A real release server under `/home/wyatt/.cache` proved healthy startup, embedded docs/migration/CLI/SKILL `200` responses, a missing-doc `404`, byte-exact file PUT/GET, missing-value parser refusal before database creation, no creation of the deliberately nonexistent compatibility hot path, clean SIGINT exit, and offline verification of 195 valid entries with zero reported defects. The first harness attempt is truthfully retained: public health returned `200` with `status=starting` while every protected route returned the expected `503`; the final harness waits for `status=healthy`.
- Durable failure, build, link, contract, live, hash, and authorization evidence is recorded in `evidence/p9-documentation-report.json`. No production or production-derived database, install, download artifact, deployment, canary, cutover, acceptance, first v4 write, or destructive GC boundary was touched.

## P9 Strict Clippy Qualification

- The exact clean-worktree baseline at `f386403f5656c10295cc48c9a2ce10e90bf93947` reported 132 diagnostics across 61 source/test files: 25 `result_large_err`, 21 production `too_many_arguments`, 15 `nonminimal_bool`, and 71 diagnostics across 32 smaller lint families. The run used untracked lockfile SHA-256 `06e6c7a8eb6dbccf52a0b97a4ee6edeece7297866305b0930314fd47b987faec`; the baseline log SHA-256 is `0a15b0fc3d383999988ad27342ec74d180e23ec7d684d3b60fccbe18284b539e`.
- The cleanup uses no lint suppression and does not weaken the lint policy. Mechanical fixes cover the smaller lint families; typed request/options/context structures replace all 36 oversized signatures across 21 production helpers and 15 test fixtures. Large Axum route failures use one boxed `RouteResponseError`, and the migration destination error boxes its large artifact while retaining exact status, response, and diagnostic behavior.
- `cargo clippy --locked -j6 --workspace --all-targets -- -D warnings` is green with zero diagnostics in 2m25s. Preserved log: `/home/wyatt/.cache/codex/aeordb-tests/logs/p9-clippy-f386403f-strict-final.log`, SHA-256 `b6be66392879b3012e00d998bf8d211d16a5c5e7874a12cb5e1e64795684ba8f`.
- The reviewed error-squelch inventory shrinks from 1,508 to 1,507 occurrences with no pending review, no changed review assignment for any retained stable identity, and no policy change. All 29 architecture/error-squelch tests pass; the final complete workspace run independently includes the same green target.
- Broad verification used build artifacts under `/media/Data/AeorDB/Tests/` and a permission-capable ext4 `TMPDIR` under `wyatt-desktop:~/.cache`. This avoids the desktop `/tmp` filesystem's frozen reserve-capacity mismatch and the data volume's fixed `0777` mode, while leaving all heavy compilation and target I/O on the designated data disk. The three required default/echo WASM fixtures were built in their remote-only target trees and were never synchronized.
- Failure-first broad evidence caught and corrected one Clippy rewrite that changed the architecture-visible reindex flush propagation shape. The corrected exact guard passes 1/1 and retains `preserve_reindex_partial_on_terminal` as the failure owner; log SHA-256 `d4d67f7321fd9b05dcce2eaf9d4e2572ade2ba4d731be3483f8bf108578f0d17`. The echo-plugin target passes 30/30 after its documented fixture build; log SHA-256 `76881de760fe1ed9d8509c2a2f77e9137d0b7a9798d95caa8b2407d9a95e2a14`.
- The final uninterrupted `cargo test --locked -j6 --workspace` passes 7,361 tests across 347 result groups with zero failures, nine intentional ignores, and 126 filtered tests. Preserved log: `/home/wyatt/.cache/codex/aeordb-tests/logs/p9-clippy-f386403f-workspace-tests-green.log`, SHA-256 `f6f7161ce1bb3ee6902e5ab7b4afde1c9663867cd2b0fae42e8e795fe2eb698f`.
- The landing touches no production or production-derived database, install, download artifact, deployment, canary, service activation, cutover, acceptance, first v4 write, or destructive GC boundary.

## P9 Next Action

Commit and push the coherent green crash-qualification recovery landing, then qualify that exact revision across Linux, native macOS, and native Windows before the resource/live and long S1/S2/S3/crash campaigns. Mechanically inspect every retained long-run log, publish the command-level DoD and completion packet, and close the repository-only campaign boundary. Keep copied-production, canary, installation/deployment, operational cutover, acceptance, monitoring, first v4 write, and destructive GC behind their exact separate authorization gates.

## Inventory Contract

- Added the syntax-aware `aeordb-error-squelch-audit-v1` workspace tool and exact checked JSON inventory.
- The scanner covers discarded results, `Result`-to-`Option`, default-on-error, broad error patterns, success-only conditionals, status/variant probes, error conversion/recovery, logged continuation, and panic methods/macros. It scans nested macro expressions while excluding error-preserving `map_err` closures and test-only source.
- Stable identities do not depend on leading line shifts. The validator rejects unknown classifications, duplicate identities, pending reviews, stale or unused entries, missing policy metadata, and any ceiling above the exact discovered count.
- Every retained occurrence receives a named semantic policy containing class, bounded rationale, owner, guarding test, and removal condition. The reviewed ceiling may shrink but cannot grow without an explicit override.
- Final review/check found exactly **1,420** reviewed production occurrences with no baseline growth.

## Correctness Work

The audit corrected high-risk suppression classes instead of merely documenting them:

- durability admission, permits, tickets, WAL/hot-tail reads and rebuild, transaction state, spill preservation, and poisoned authority;
- namespace, permission, cancellation, reindex, repair, backup/import, GC/Void, KV page, cache, and lifecycle invariants;
- malformed FileRecord/binary/deletion data, locator/traversal omissions, future versions, wrong hash widths, and offset overflow;
- CLI prompt/checkpoint/deployment/soak evidence, startup/shutdown, logging, CORS, metrics, health, and database-lock failures;
- plugin SDK host envelopes, WASM query options, parser/registry configuration, and runtime action state machines; and
- typed SSE `stream_gap` publication so lag is observable and clients can refresh instead of silently remaining stale.

Optional cleanup and telemetry failures retain the acknowledged primary result only where the error is bounded and visible through warning, event, metric, or debt evidence.

## Regressions Found During Verification

- Forced reindex originally treated task/system JSON records sharing the legacy `KV_TYPE_FILE_RECORD` bucket as malformed FileRecords. Current-file discovery now starts from the strict live namespace, supplements it only with canonical decodable FileRecord keys, and still fails closed on malformed live records. All 34 reindex specs pass.
- Embedded routers initialized `AppState.db_path` to an empty string and relied on the CLI to inject a path extension. `AppState` now obtains the real engine database path, while an explicit extension remains a supported override. All 31 portal specs pass, including the deliberate missing-path failure.
- A v3 architecture fixture depended on a local variable being mutable even though the contract does not. The fixture boundary now recognizes the correct immutable spelling without weakening its behavior assertions. All 66 v3 facade specs pass.

## Verification Evidence

- Scanner: `cargo run -j4 -p aeordb-error-squelch-audit -- review` and `check` both pass at 1,420 exact entries.
- Architecture: `cargo test -j4 -p aeordb --test error_squelch_architecture_spec -- --test-threads=4` passes all 27 tests.
- Storage matrix: 238 tests pass across backup, cache, directory operations, emergency spill, GC, KV snapshot, lifecycle, and reindex targets.
- Boundary matrix: logging 27, SSE 48, records 24, auth provider 21, auth middleware 37, and CORS 54 all pass.
- Runtime and contract matrix: health 36, metrics pulse 14, portal 31, RSS sampler 2, v3 facade 66, virtual clock 21, WASM query E2E 30, library unit harness 405, CLI 164, plugin SDK 122, and 221 remaining affected integration tests pass.
- Final broad workspace gate after the reviewed fixture and ledger edits: 5,434 tests pass across 205 harness groups, zero fail, and seven pre-existing stress cases remain intentionally ignored. Preserved log SHA-256: `36b33a3b330dbad8e001776fcfe143eb438a17ac42be086b3cd79d69bfbbbaec`.
- Formatting, scanner-tool Clippy, plugin-SDK Clippy, and mdBook are green. mdBook log SHA-256: `dc137df35b17344ae33785de9240f26750b43bbe9c9f952b0cd56621b18aa8f4`.
- Full workspace Clippy with `-D warnings` remains red on historical repository debt: 117 diagnostics spanning long-standing argument-count, large-error, type-complexity, documentation, and style lints outside this landing unit. No broad `allow` attributes or unrelated refactors were used to hide it. Preserved log SHA-256: `95634245816dfadbaa037115407989866eb49bffee81684f00e36a5138c87ed0`; final P9 qualification still owns a green repository-wide lint gate.

## Release And Real-World Proof

- A final exact-tree `cargo build -j4 --release --bin aeordb` produced `aeordb 0.9.5`, SHA-256 `fa2581b9134567ffba1d49783d9b838a25c451229759c41b82c366f8f30de2fc`. Build log SHA-256: `f6fdd3e31fb3cdd22f1777aa833fee634d3ea809488e7ec6989f867d11806113`.
- A disposable release-mode database at `/home/wyatt/.cache/codex/aeordb-tests/p2f-live-7OlLSb/live.aeordb` proved malformed `AEORDB_LOG` fails before creating a database, valid startup reaches ready, text and JSON writes list/fetch byte-exactly, `/system/stats` reports real engine state, and SSE emits `server_ready` only after readiness.
- SIGTERM completed graceful worker/engine shutdown with exit code 0. Reopening the same database returned byte-identical content and shut down cleanly again.
- Offline verify reports 198 valid entries, zero corrupt headers/hashes, zero missing/dangling/B-tree/unlisted entries, zero stale/missing/invalid KV entries, and `Status: OK`. Verify log SHA-256: `47edec058973b719a207bac77ca1671916052f3dd37e0bf56b649fe0c8908bce`.
- The disposable server was stopped; no Cargo, Rust compiler, AeorDB, or soak process remains active. No transient artifact, secret, evidence database, or production data is part of the landing diff.

## Current P3a Evidence

- P3a-1's checked common encoders byte-match both independent hash-width fixtures and remain disconnected from production authority.
- P3a-2's serialized header publisher uses the shared durability coordinator, exact inactive-slot publication, full barriers/read-back, and fail-closed two-slot fencing/adoption. Its fault suite covers roughly 6,150 crash/failure prefixes.
- Native qualification found and corrected a Windows shared-cursor regression in positional reads. Exact commit `e41e45d` passes four internal fault tests and the 216-test adjacent matrix on Linux, macOS arm64, and Windows x86_64 MSVC. Both native hosts pass the 436-fixture, 95-route, and 38-document contract gate.
- The corrected Linux workspace/all-target closure passes 5,463 tests across 207 result groups with zero failures and seven intentionally ignored stress cases. Its log SHA-256 is `417c3bfe37314d69f7a51e975373f12e733c9fee75281eca4d1dd4433deaec88`; the suppression inventory remains exactly 1,420 reviewed occurrences after refreshing four line-only metadata entries.
- P3a-3 adds disconnected immutable AIDX/AGCA envelope writers only. All 54 immutable index and 92 immutable GC fixtures byte- and key-match the independent oracle; every mutable pointer/control fixture is excluded or rejected; every kind cap and allocation boundary is frozen independently. The complete workspace/all-target closure passes 5,468 tests across 207 result groups with zero failures and seven intentionally ignored crash-injection cases. Its log SHA-256 is `36fe2ba3e784e61ee19890602dcfcc3c927900e35a91ba7bcfb41d2b31a14878`; the reviewed suppression inventory remains exactly 1,420 entries.
- P3a-4 publishes every validated mutable and immutable SystemControlV1 payload through one typed FileRecord v1 adapter, shared namespace authority, and hard durability acknowledgement. Legacy v0 wrappers remain migration input only. The exact native ten-target matrix passes 203 tests on both macOS arm64 and Windows x86_64 MSVC, and the final Linux workspace closure passes 5,478 tests across 208 result groups with zero failures and seven intentional ignores.
- P3a-5 advertises only complete common-format writers: WholeEntityV1 bit 0 and SystemControlV1 bit 4. Partial immutable-only IndexArtifactV1 and GcArtifactV1 writers do not advertise bits 7/12 because their mutable pointer/control families remain absent. Current writable and peer admission still fail closed on the nine missing baseline writer capabilities.
- No service authority, route, migration destination, deployment, or production database byte is active.

## Next Action

Begin Child 03 P3b-2c with an exhaustive first-authority visibility map and a failure-first transaction target. Keep prepared roots unadmitted, then publish the authority selector and admission witness through one shared visibility/durability boundary without selecting HEAD or activating service reads. Keep capability bits 1, 2, and 5 disabled, read-view/lifecycle/pins in P3b-3, and migration work behind its P7 gate.

## P3b-2b Semantic Object Storage Closure

- Exact commit `0b739c71b434101f35660c051bd657f73148681e` adds disconnected, deterministic, write-once v4 semantic-object publication through the ordinary system FileRecord v1 namespace and hard-durability authorities. It activates no HEAD, service, startup, route, migration, or capability caller.
- The focused target passes 8 tests; the adjacent twelve-target v4 matrix passes 267. The final Linux workspace/all-target run passes 5,501 tests across 210 harness groups with zero failures, seven intentional ignores, and 126 filtered tests. Preserved broad log SHA-256: `e66b2627c53974c7dc7293100d4ee0fa5fdfbee9f6de20349dca3e769de6beab`.
- Workspace checking, both formatters, mdBook, `git diff --check`, the independent 436-fixture/95-route/38-document contract gate, the exact 1,420-entry suppression check, and all 27 suppression architecture tests are green. Strict focused Clippy remains blocked only by the established 110-diagnostic crate baseline and reports nothing in the changed semantic-store/spec files; preserved non-strict log SHA-256 is `dde4e74b7b9ab0d5ac613bcd8d058eead4e4df9889abcffeb52a3b9335168b9d`.
- The exact twelve-target, 267-test matrix passes on native macOS arm64 and Windows x86_64 MSVC using lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`. macOS also passes the portable contract gate. Retained native log SHA-256 values are `8094691c6e3ba24ac552b03f71c764d290faa685b24444c212f31bdc53b255f4` and `6b33820ae59540f0b2ef00ed1ae49b9c4be18f27e3452a9026ab158b46453471`.
- Real-storage proof creates actual engine files, hard-publishes every semantic kind, shuts down cleanly, reopens, and loads exact bytes on all three native platforms. The isolated native worktrees, build targets, logs, and disposable tooling were removed after qualification; no server, Cargo process, or user checkout was left active or modified.

## P3b-1 Immutable Root Authority Closure

- Commit `ba60b4d9447e89576a0ce3665b0570c41566c34a` adds disconnected immutable namespace-root, namespace-tree, semantic-state, and admission-witness readers plus an independent reference model. No startup, route, HEAD selector, lifecycle, authorization, migration, or service caller is activated.
- The focused target passes 11 tests; the complete adjacent v4 matrix passes 121 tests on Linux, macOS arm64, and Windows x86_64 MSVC. macOS passes the independent 436-fixture/95-route/38-document contract wrapper; Windows natively exercises all 436 fixtures but does not claim the POSIX wrapper because its `jq` and Python/PyYAML dependencies are absent.
- The final Linux workspace/all-target gate passes 5,490 tests across 209 result groups with zero failures and seven intentional ignores. Preserved log SHA-256: `d5eff5ef36265578aa2eb3b95cdef6c93532e234c32cc2328c26fc4f95f8e13a`.
- Workspace checking, both Rust formatters, mdBook, `git diff --check`, the exact 1,420-entry suppression check, all 27 suppression architecture tests, and the independent contract gate are green. Real storage proof performs store, clean shutdown, reopen, bounded verified reads, authority resolution, and final shutdown on a disposable database.

## P3a-4 Entry Territory

- **Authority:** P2's `V3TransitionControlStore` is the sole current control producer and intentionally writes system FileRecord v0 wrappers. Its typed context owns the outer namespace guard, canonical A/B path derivation, one shared namespace transaction, hard acknowledgement, read-back, and selected-state verification. Generic file, sync, plugin, and index APIs remain unable to mint this typed authority.
- **Target representation:** P3a-4 must preserve each validated SystemControlV1 payload byte-for-byte while publishing the frozen ordinary FileRecord v1 wrapper with `FLAG_SYSTEM`, `application/vnd.aeordb.system-control`, normal path/content/identity locators, and current content hash. Mutable kinds use inactive A/B rollover; immutable kinds use idempotent `i.ctrl` publication at sequence one.
- **Compatibility:** the new shadow writer may accept a validated legacy FileRecord v0 slot as migration input, but every new slot it publishes is v1. Two ordinary mutable publications therefore converge both A/B wrappers without an out-of-band rewrite. The v3 transition writer and every current service caller remain unchanged until a later activation gate.
- **Proof order:** fail first on the absent v4 writer/context; cover all twenty kinds, strict metadata and body validation before mutation, exact payload read-back, one hard acknowledgement, legacy rollover, immutable idempotency, concurrent mutable sequence selection, restart, protected typed authority, and zero production callers; then run adjacent transition/namespace/durability/system-family gates before broad closure.

## P3a-4 Local Qualification

- **Red proof:** `v4_control_store_writer_spec` first failed only on the absent `V4ControlStore`, typed publication context, and frozen system-control MIME constant. The preserved failure log is `/home/wyatt/.cache/codex/aeordb-tests/logs/p3a4-control-writer-red.log`.
- **Implementation:** the disconnected `V4ControlStore` validates complete SystemControlV1 bytes before acquiring namespace authority; publishes every mutable and immutable kind through one typed `DirectoryOps` FileRecord v1 adapter; derives the canonical A/B/I path only from the typed context; uses the shared namespace/hard-durability coordinator; reads back the exact captured FileRecord chunk inventory; requires the frozen MIME, empty metadata, exact content hash, body and wrapper bounds; accepts FileRecord v0 only as validated migration input; and leaves the existing v3 transition writer unchanged and v0-only.
- **Focused proof:** all 10 writer tests pass. They cover all twenty kinds, exact wrappers and payloads, one hard sequence per publication, mutable v0 A/B rollover, immutable v0 rollover and v1 idempotency, shutdown/reopen, invalid database/kind/mutability/CRC input before mutation, sixteen concurrent publishers with exactly one winner, noncanonical MIME/metadata/path/hash/version/flags/lengths, missing chunks, torn control payloads, typed authority, and zero production callers.
- **Adjacent proof:** the ten-target storage/control matrix passes 203 tests; the internal library harness passes 409. The complete workspace/all-target suite passes 5,478 tests across 208 result groups with zero failures and seven intentionally ignored crash-injection cases. Preserved broad log SHA-256: `a91c46d0e06d1a9bbcbb477c6c2df9bf0dbeec1f84661fa822c13f484b153614`.
- **Static and contract proof:** workspace/all-target checking, both Rust formatters, mdBook, `git diff --check`, the 436-fixture/95-route/38-document contract gate, the exact 1,420-entry suppression check, and all 27 suppression architecture tests are green. The suppression inventory changed only 31 source line numbers after adapter insertion; normalized records and every stable ID remain byte-equivalent.
- **Native falsifier:** the first Windows adjacent matrix exposed a mixed-separator expectation in `validated_external_spills_seed_one_persistent_recovery_incident`. Production returned the canonical native path; the test had constructed one path component containing a literal slash. Commit `14fbace` corrects only the independent oracle to join `earlier` and `manifest.json` as separate components. The corrected v3 transition target passes 20 tests locally and on Windows.
- **Native proof:** exact commit `14fbacec8ebfdff5e51b89b633a887d1ff5a2fa7` passes the same ten-target, 203-test matrix on macOS arm64 and Windows x86_64 MSVC. Both hosts pass all ten v4 ControlStore writer tests, including storage-engine create, hard publication, shutdown, reopen, exact read-back, malformed input, concurrency, and legacy rollover. macOS also passes the portable contract gate with 436 independent fixtures, 95 routes, and 38 documentation pages. The Windows native behavior/fixture matrix is green; the POSIX contract wrapper is not claimed there because that host lacks its `jq`, `rg`, and Python/PyYAML command dependencies.
- **Exact Linux closure:** the final `14fbace` workspace/all-target run passes 5,478 tests across 208 result groups with zero failures and seven intentionally ignored crash-injection cases. Preserved log SHA-256: `84dd209b4a042db380cf50dbfd65ba41f4e37f5d3310206e86c66e1e455d1cc8`.
- **Real storage boundary:** the disconnected writer has deliberately no HTTP, startup, migration, or service caller, so an HTTP live test would falsely activate authority. The focused target instead exercises the real `StorageEngine` and namespace/durability coordinators against disposable database files, including shutdown/reopen and byte-exact read-back on all three native platforms.
- **Boundary:** P3a-4 activates no v4 writer capability, service/storage caller, startup admission, route, migration destination, deployment, or production database byte. P3a-5 owns the capability-profile change.

## P3a-5 Capability Profile Qualification

- **Red proof:** the first exact profile test failed because the binary advertised no writers. An adversarial contract pass then rejected the initial proposed `[0, 4, 7, 12]` profile: bits 7/12 cover complete artifact families, while P3a-3 deliberately has no mutable A/B pointer/control writers. A second preserved red run proves those extra bits were present before the profile was tightened. Logs: `/home/wyatt/.cache/codex/aeordb-tests/logs/p3a5-capability-profile-red.log` and `/home/wyatt/.cache/codex/aeordb-tests/logs/p3a5-partial-artifact-capability-red.log`.
- **Exact profile:** `BinaryCapabilityProfileV1::current()` continues to advertise all 24 readers and now advertises only WholeEntityV1 bit 0 and SystemControlV1 bit 4. Generated capability constants feed one private const bit builder. All other bits remain absent; IndexArtifactV1 and GcArtifactV1 stay disabled until their complete mutable families exist.
- **Fail-closed proof:** writable admission reports the exact missing baseline bits `[1, 2, 3, 5, 6, 18, 19, 21, 22]`. A peer source/destination pair using the current profile fails as `peer_destination_writer_capability_mismatch`. Source architecture proves no production `admit_v4_header` or current-profile caller exists, and the storage engine still has no P3 writer caller.
- **Qualification:** the eight-target P3a matrix passes 150 tests locally, on macOS arm64, and on Windows x86_64 MSVC at exact commit `ec20bd4a9627f48d52ddf38d0f4a723fa4203d4a`. The complete Linux workspace passes 5,479 tests across 208 groups with zero failures and seven intentional ignores; log SHA-256 is `2bba8e296ce453d2ff7387ec5c77e385f749a4e539dd498865a87304ced6a6d7`. Workspace/all-target checking, both Rust formatters, mdBook, `git diff --check`, the 436-fixture/95-route/38-page contract gate, all 27 suppression architecture tests, and the exact 1,420-entry suppression check pass. Three suppression rows moved by line only; normalized entry SHA-256 remains `16b0368f9d415224028baa5657d16975964da4559717731d144fca526de205a8`.
- **Boundary:** capability self-description changes, but no v4 database header is selected by service startup and no service, storage, route, peer, migration, index, GC, or root authority caller consumes the profile. No persistent byte or production database changed.
