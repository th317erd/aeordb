# Child 07 Progress: Migration

- **Status:** active; Child 03 start gate, P3c-1 lease/progress/source-GC ownership, P3c-2a destination initialization, all P3c-2b2 bounded capture/checkpoint/restart work, entity-version framing, bounded immutable/successor publication, bounded base-clone streaming, ordered selected-capture replay, the live exclusive source-write freeze, bounded strict final namespace reconciliation, and frozen retained-root/SystemFamily closure are complete; P3c-2b3e/P3c-3 root-map, restart, verification, resource, and source-invariance closure is active
- **Current landing unit:** P3c-2b3e/P3c-3 migration closure
- **Entry commit:** `c119713` (`docs: record bounded migration capture proof`)
- **Last green commit:** `c1e81c254e4ba81b1850fbe7f844b730e6dde515` (`feat: close frozen migration authority`); exact-commit Linux, macOS arm64, and Windows x86_64 MSVC qualification is green
- **Owner:** current Codex thread after the shared-owner handoff; migration/control hotspots remain serialized
- **Start gate:** satisfied for P3c because Child 03 is complete; P8 still requires Children 04 and 06
- **Plan:** [Child 07](../children/07-side-by-side-migration-cutover-and-rollout.md)
- **Owned files:** `engine/v4/migration_control.rs`, migration capture runtime modules, their dedicated specs, `engine/v4/mod.rs`, the already-landed optional `StorageEngine` subscription boundary, explicit test manifests, and this progress ledger
- **Forbidden/hotspot files:** new v3 source mutation pathways, server/CLI/service activation, production databases, deployment, cutover, first-v4-write admission, and shared first-authority/control-store hotspots without a separately green handoff
- **Hotspot handoff commit:** none
- **Narrow gate:** first add the dedicated P3c-2b3 execution target, then prove bounded source streaming, idempotent destination publication, selected-capture replay, exact final authority/SystemFamily diff, and final-freeze refusal on any gap or drift
- **Broad gate:** P3c-2b3p/a complete Linux workspace/all-target proof and exact pushed-commit macOS/Windows qualification are green
- **Drift/risks:** no production mutation without explicit P8 operator gate
- **Ratification:** on 2026-08-13 the operator ratified the remaining recommendations and policy decisions. This authorizes the disconnected P3c substrate and later planned implementation gates, but not deployment, destructive migration, production cutover, or first-v4-write acceptance.
- **Evidence:** P3c-1a first demonstrated the absent typed module through an unresolved-import compile failure. `migration_control.rs` now provides the one typed AMLE/AMPR v1 body authority over the existing `SystemControlV1` envelope, including exact fixed layouts, every frozen enum/flag, nonzero identity/fencing/policy validation, time and hash-width bounds, fallible allocation, and encode/decode identity round trips at 32-byte and 64-byte hash widths. Four independent checked-in binary fixtures remain the byte oracle. Seven focused tests cover exact bytes, widest-hash round trip, every enum, all known flags, wrong-kind/truncation, invalid identities/fencing/times/flags/hash widths, and zero required policy hashes. The neighboring format/writer/control-store/platform/error-squelch matrix passes 155 tests. Workspace all-target checking passes with only the historical unused `require_wasm_parser` test macro. The complete workspace/all-target gate passes 6,262 tests across 276 result groups with zero failures and seven intentional ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1a-migration-control-full.log` has SHA-256 `1f0fbf3c3cd7448352d796136fb23e1a0afd7e92c34777378b6acba3fdbd3680`. The first broad run correctly exposed an obsolete Wave-5 architecture guard that treated any typed writer as runtime activation. Its replacement permits exactly `migration_control.rs` and recursively rejects storage engine, directory, control-store, first-authority, filesystem, publication, and capture dependencies; source migration configuration capture remains callerless and migration runtime modules remain absent. P3c-1b then demonstrated the absent preflight module through an unresolved-import compile failure and added one bounded, storage-neutral observation contract. Its private permit closes exact v3 source/header/checksum identity, strict verifier and transition-inspector evidence, protected-family inventory, native source/destination qualification, immutable configuration, binary/capability/SystemFamily registries, joint memory admission, and four capacity roles aggregated by physical volume. The 11-test target exercises all 24 typed refusal codes, both valid and malformed order independence, duplicate roles, inconsistent shared-volume observations, arithmetic overflow, resource pressure, a valid zero v3 HEAD, and a real disposable file-backed v3 database. Adversarial review found that duplicate malformed capacity rows originally retained request order in the evidence fingerprint; a red regression demonstrated it and the canonical sort now includes every observation field. The affected nine-target matrix passes 155 tests. The final complete workspace/all-target gate passes 6,273 tests across 277 result groups with zero failures and seven intentional ignored crash-injection tests in 33m07s; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1b-final-full.log` has SHA-256 `6e543a84a442e8453c3fe291ed8833ea2f2e91bac18003286430da43125f54ac`. Formatting and diff checks pass. Workspace Clippy still stops on the pre-existing `range_extract.rs:370` `never_loop` denial after reporting 112 historical warnings; no P3c-1b file is implicated and the retained Clippy log has SHA-256 `8f6535b7b91f4632061a5cca28868b225e054832cb1490d9591db6ac2ead926c`.
- **Dependency correction:** the parent graph requires P3c's separate v4 shadow authority before P6 can install its native recovery store into lifecycle. A live v3 `StorageEngine` cannot safely stand in for that authority. No source file, production database, deployment, or cutover is authorized by this correction.
- **Territory refresh:** the complete control registry already freezes MigrationLease `0x0030/AMLE`, MigrationProgress `0x0031/AMPR`, LegacyRootMap control/page `0x0032/0x0033`, and SideBySideCutover `0x0052/ACUT`. Fail-closed envelope/body readers, A/B/I selection, the v4 control wrapper, v4 first-authority publication, exact 56-byte native file identity, filesystem capacity, migration configuration, and platform durability probes exist. Typed migration body codecs, a lease/progress owner, source-GC suspension, preflight aggregation, capture/clone/reconciliation, and a generic physical-v4 migration control publication path do not exist. `StorageEngine` remains v3 and cannot be substituted for the destination.
- **Landing decomposition:** P3c-1a freezes typed lease/progress codecs against independent 32/64-byte-hash fixtures. P3c-1b adds preflight evidence. P3c-1c adds the durable fenced state owner. P3c-1d connects the exact lease to every mutating GC/retention entry point while leaving diagnostics available. P3c-2 owns bounded clone/capture/reconciliation; P3c-3 owns root mapping and source-invariance exit proof.
- **P3c-1b entry map:** the existing producers are the read-only v3/v4 header dispatcher, exact 56-byte platform identity descriptor, native durability probe, checked verifier, read-only transition/spill inspector, immutable migration run configuration, process memory snapshot, embedded capability/SystemFamily registries, and filesystem-capacity observation. The new layer must compose their bounded outputs; it must not add a second probe, verifier, configuration resolver, or writer. Capacity observations are coupled by physical volume: independently passing destination/workspace/backup/capture rows can still overcommit one filesystem, so admission sums required bytes once per volume and applies the strongest remaining-space floor. Source verification, recovery, and authority inventory must all close against the same selected header sequence, size, and complete-file checksum. The permit remains disconnected from `StorageEngine`, routes, CLI, destination creation, lease publication, and source mutation.
- **P3c-1b proof:** complete. The contract does not own `StorageEngine`, `DirectoryOps`, a control store, first authority, files, routes, destination creation, lease publication, source mutation, or service activation. The evidence fingerprint exposes no raw path and the bounded report can represent every frozen refusal class without truncation.
- **P3c-1c territory:** AMLE/AMPR identities are the frozen `migration_id[16]`. The physical v4 first authority already owns exact system FileRecord/chunk loading, stable dependency publication, header/KV serialization, replacement-incarnation evidence, and retirement-journal hard publication, but its only mutable `SystemControlV1` entry is index-operation-specific. `V4ControlStore` still depends on live v3 `StorageEngine` namespace authority and is forbidden here. Add one generic physical mutable-control compare-and-swap boundary rather than copying another entire publication path. The migration owner then owns typed lease/progress transition policy and must compare-and-swap exact selected bytes under first-authority serialization. Destination initialization must precede acquisition; absent first authority, degraded header redundancy, malformed A/B state, wrong database/physical instance, stale expectations, and post-commit ambiguity all fail closed.
- **P3c-1c state direction:** acquisition consumes the exact preflight permit and writes Held lease before initial Pending/Preflight progress, so a crash can conservatively leave an explicit lease with missing progress. Renewal re-reads the exact token and holder. Progress phases are sequential and all counters/watermarks/times are nondecreasing under one token; terminal canceled/failed work may release only before final freeze, while later rollback release remains forbidden until P3c-3 supplies verified cutover evidence. Expired takeover is explicit, advances the token before rebinding progress, and treats a crash between those publications as a typed rebind-required state. Restart never silently clears Held, Releasing, mismatched, or partial state. P3c-1c records the lease but cannot claim the source-GC-suspended progress flag; P3c-1d owns that activation.
- **P3c-1c proof plan:** first compile-red on the absent generic physical control authority and absent migration owner. Prove A/B/A selection and reopen on a real v4 file, exact-idempotent retry, concurrent CAS, replacement lineage, every header/KV/control corruption refusal, and committed-state error classification. Then prove acquire/partial acquire/restart, renewal, stale holder, explicit takeover/rebind, phase/state/flag/counter/time regressions, safe early release, forbidden late release, terminal behavior, and architecture isolation without a live service caller.
- **P3c-1c-a proof:** complete. `V4FirstAuthorityPublisher` now exposes one generic physical mutable `SystemControlV1` loader and compare-and-swap publisher over the selected A/B slots. The boundary validates exact kind/database/identity, non-degraded first authority, selected slot/sequence/domain-separated digest expectations, sequential control sequences, native write/read-back, header/KV dependency publication, replacement-incarnation retirement lineage, hard retirement flushing, committed-state error classification, and exact post-commit read-back. Exact retries are idempotent. The pre-existing index-operation control path delegates its physical publication to this boundary, removing the second copy of that mutable-control writer. The dedicated real-file target passes 6 tests covering A/B/A replacement and reopen, widest-hash operation, malformed and stale expectations, foreign identities and sequence gaps, concurrent CAS contenders, and persisted-slot corruption. The affected seven-target matrix passes 80 tests. Workspace all-target checking and formatting pass; strict Clippy reaches 113 historical repository findings led by the known `range_extract.rs:370` `never_loop` denial and reports no finding in the changed authority or target. The complete workspace/all-target gate passes 6,279 tests across 278 result groups with zero failures and seven intentional ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1c-a-full.log` has SHA-256 `06e6849fdddbc358276cc7bb51039e6d626a4eead7691da4b35e9f2a6b14165b`.
- **P3c-1c-b proof:** complete. Acquisition first failed to compile because the migration owner did not exist and the preflight configuration fingerprint could not represent the destination's 64-byte hash profile. The opaque permit now carries the exact hash algorithm, source capture head, effective configuration fingerprint, and SystemFamily registry fingerprint admitted by preflight; fingerprint length is validated against the selected database hash width. `MigrationStateOwnerV1::acquire` validates destination first authority and physical identity, publishes a Held lease before Pending/Preflight progress, uses fencing token 1 only for the absent-control case, resumes an explicit lease-only crash prefix, retries exact selected controls idempotently, and rejects orphan progress, malformed timing, expired same-boot state, foreign holders, and concurrent foreign acquisition. It never claims source GC suspension. Adversarial review found two post-lease timing overflows and one redundant fallible read-back window that could misclassify durable state; compile-red regressions now require every derived timestamp to validate before the first publication and forbid fallible owner read-back after the generic publisher has already verified durable selection. The real-file target passes 9 tests, including reopen, SHA-512, partial restart, concurrency, and architecture isolation. The four-target migration/namespace matrix passes 60 tests. Workspace all-target checking and formatting pass; strict Clippy still stops on 113 historical findings and reports none in the migration owner/preflight files. Two complete-workspace attempts correctly exposed stale exact-consumer architecture inventories for the retirement owner and first-authority publisher; their allowlists now name exactly index recovery plus migration ownership and retain live-service prohibitions. The final complete workspace/all-target gate passes 6,288 tests across 279 result groups with zero failures and seven intentional ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1c-b-full.log` has SHA-256 `99e8c5586a1b2024e1179235990f044af4806c8a62964486ab1991b1cbb9a28b`.
- **P3c-1c-c territory:** renewal and progress transitions share the selected AMLE/AMPR controls, the generic physical first-authority CAS publisher, exact opaque preflight policy, and the same retirement-journal owner. Every operation must re-read lease plus progress, match holder/token/permit, reject expiry before publication, publish with the exact selected-slot/sequence/digest expectation, and perform no fallible owner work after the generic publisher's durable read-back. Renewal may only extend time. Progress phases are same-or-next only; a completed phase enters the next phase as Pending; counters, watermarks, flags, time, and established optional evidence never decrease or clear. Copy cannot begin before durable source-GC suspension, destination verification cannot be left before write-freeze plus full-verification evidence, and failed/canceled states are terminal. Source-GC activation remains P3c-1d authority.
- **P3c-1c-c proof:** complete. `MigrationStateOwnerV1` now renews only the exact held lease and advances AMPR through a sequential, monotonic state machine. Each mutation re-reads both controls, validates permit/holder/token/expiry against publication wall time, compare-and-swaps its target, and cross-guards the exact selected slot/sequence/digest of the other control under the same first-authority lock. The generic mutable-control publisher bounds guards to eight, rejects duplicate/malformed/stale guards before target publication, preserves exact retry idempotence after a dependent control advances, and validates the durable `FileRecord` timestamp range before any retirement flush or header mutation. Real-file regressions cover reopen and exact retry, SHA-512 guard digests, concurrent fencing, foreign-holder fencing, stale cross-control guards, out-of-range clocks with unchanged bytes, sequence/phase/state/flag/counter/watermark/time/evidence regressions, missing GC/write-freeze/full-verify prerequisites, and terminal states. The seven-target affected matrix passes 91 tests; the exact error-squelch architecture target passes 28 tests. The first full gate correctly rejected three stale line-number records whose stable IDs, patterns, columns, enclosing items, and review classifications were unchanged after the new guard type shifted source lines; only those three metadata locations moved, and the non-growing inventory then passed in the uninterrupted rerun. Workspace all-target checking and formatting pass. Strict Clippy still stops on 115 historical library findings and 122 total lib-test findings, with no diagnostic in either changed production module. The complete workspace/all-target gate passes 6,297 tests across 279 result groups with zero failures and seven intentional ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1c-c-full.log` has SHA-256 `ea691acd85a21c5be7ba30ceaa13a2398f5c0ce6defa34173092a00de2a51f28`.
- **P3c-1c-d territory:** early release is a hard-published `Held -> Releasing -> Released` lease sequence guarded by the exact terminal AMPR. Only `Failed` or `Canceled` progress before `FinalFreeze` may enter it; a persisted `Releasing` prefix is resumable by the same holder/token even after lease expiry, while `Expired`, foreign, active, completed, and late-phase states fail closed. Expired takeover is a separate explicit operation. It requires the caller's expected token, publishes a strictly larger held token for the new boot before changing progress, then rebinds AMPR while guarding the exact new lease. A crash after the lease publication leaves AMLE ahead of AMPR and is never ordinary ownership. An exact retry may explicitly finish that rebind; a later boot may advance another token after expiry and rebind progress across multiple abandoned lease-only prefixes. Every derived timestamp, expiry, sequence, and token must validate before its first publication, and no fallible owner readback may follow a committed target.
- **P3c-1c-d proof:** complete. Early terminal release now publishes the exact `Held -> Releasing -> Released` sequence under an unchanged terminal AMPR guard, resumes a durable `Releasing` prefix after expiry, retries an already selected `Released` lease idempotently, and rejects active, late-phase, foreign, expired-state, and malformed-clock attempts before publication. Explicit takeover accepts a time-expired Held or durably Expired lease, advances AMLE's fencing token before AMPR, rebinds progress under the exact new lease guard, resumes the lease-only crash prefix, and can advance across multiple abandoned lease generations while ordinary acquire/renew/progress/release operations return the typed `migration_progress_rebind_required` refusal. Concurrent expired takeovers select exactly one holder and fence every loser. All control encodings, clock increments, sequence arithmetic, and token arithmetic complete before the first publication, and committed/partial errors preserve the durable direction without a fallible owner readback. The real-file focused target passes 24 tests including reopen, SHA-512, concurrent takeover, durable Expired state, early-release crash recovery, multi-generation rebind, and malformed/foreign/overflow boundaries. The affected eight-target matrix passes 128 tests. Formatting, diff hygiene, and the exact error-squelch architecture target pass. Strict Clippy remains blocked by 115 historical library and 122 historical lib-test findings; its two `migration_owner.rs` helper-arity diagnostics are already present in entry commit `0cc79d9`, and this landing adds no new diagnostic. Retained Clippy log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1c-d-clippy.log` has SHA-256 `cbafb823fb6e4408c004a3dbe19f1f6383b23a8780ce0f22a359f05949702d33`. The complete workspace/all-target gate passes 6,306 tests across 279 result groups with zero failures and seven intentional crash-injection ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1c-d-full.log` has SHA-256 `7f737f3b82afedbbdc0c845eaec5f66cd1c8b90f0f585849ceece5e0f4616f59`.
- **P3c-1d entry territory:** destructive legacy-v3 GC is reachable through embedded, CLI, scheduled-task, HTTP, cancellation, and hook adapters that converge on `execute_gc_run`; direct `gc_sweep` remains a separate public mutation doorway. Snapshot-retention pruning is independently public and the destructive GC inventory phase also creates/deletes pre-GC snapshots and invokes retention. Dry-run mark/sweep paths are currently non-mutating and must remain available. Suspension admission must therefore serialize against an entire in-flight destructive operation rather than perform a one-time boolean check, reject every later mutating doorway under the exact migration binding/token, remain latched across lease expiry, and clear only through explicit fenced recovery/release. Normal reads, writes, and diagnostic dry-runs remain admitted.
- **P3c-1d ownership:** production hotspots are `storage_engine.rs`, `gc.rs`, `lifecycle_config.rs`, `errors.rs`, `v4/migration_preflight.rs`, `v4/migration_owner.rs`, a new dedicated suspension owner, their exact module/test manifests, and this ledger. The source interlock is one engine-owned authority; adapters may not cache a boolean or implement independent suspension policy. No route, CLI, scheduled-task, destination activation, persistent-format change, source-byte mutation, deployment, or production database is authorized in this landing.
- **P3c-1d proof:** complete. One `StorageEngine`-owned permit spans each destructive legacy-v3 GC operation; direct destructive sweep and independent snapshot-retention pruning use the same interlock, while dry-run mark/sweep, normal reads, and normal writes remain available. Suspension closes new admission before draining admitted work, binds the exact database/migration/source/holder/token, remains latched across lease expiry, supports a strictly larger takeover token, and clears only after an exact early-terminal lease release. Publication races conservatively retain the in-memory latch for explicit recovery. Restart reopens and suspends the exact platform file identity before exposure even after AMPR has advanced to Copy. Foreign-source release is rejected before durable lease mutation, malformed clocks and identities fail before latching, and HTTP sanitization exposes only the retryable 503 plus fencing token. The focused migration/response gate passes 33 tests; the 15-target affected matrix passes 236 tests with SHA-256 `894667101f6ff89e10881a27c245ae185614d9108f6fb57277d9dc20750be1ad`. Workspace/all-target checking and formatting pass. The non-growing error-squelch audit passes at exactly 1,474 reviewed sites; its only final refresh moved the new RAII Drop log from line 370 to 377. Strict Clippy remains at the frozen 115 library and 122 library-test findings, reports no finding in `migration_source_gc.rs`, and retains only the two pre-existing migration-owner helper-arity findings. The final complete workspace/all-target gate passes 6,314 tests across 279 result groups with zero failures and seven intentional crash-injection ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-1d-full-final.log` has SHA-256 `5940724450d815c6da1821f23c0be0005b6ab602153ee10977b6509698bb6679`.
- **Retained boundary:** generic `StorageEngine::open` does not discover migration controls. The migration recovery owner must use `MigrationSourceGcSuspensionOwnerV1::reopen_source_suspended` before source exposure; P3c-2 owns lifecycle construction around that safe boundary. No live server exercise is claimed because the disconnected substrate has no route, CLI, scheduler, or service activation; real disposable file-backed create, mutation, suspend, shutdown, reopen, and verification paths are covered by the automated targets.
- **P3c-2a entry map:** the v3 side already exposes bounded KV, namespace, and historical-tree visitors, while strict preflight binds one selected source header, HEAD, complete-file checksum, authority inventory, configuration, capability profile, and SystemFamily registry. The v4 side exposes exact dual-slot headers, in-file KV creation, native no-follow file and durability primitives, one atomic first-authority publisher, semantic content-only initialization, and the selected SystemFamily policy resolver. No production v4 file initializer or migration-policy stream exists. The new landing must compose those authorities rather than add another header, KV, namespace-root, or durability writer.
- **P3c-2a execution boundary:** first add a constant-state migration-policy planner that classifies every source subject as required copy, destination-local reset, destination rebuild, owner conversion, declared omission, or structural traversal and fails unknown protected state closed. Then add one create-new, destination-first initializer that consumes the exact preflight permit, creates a separately identified v4 file, installs two identical initial headers and an empty KV, hard-publishes one shadow-only empty first authority through `V4FirstAuthorityPublisher`, syncs the file and parent namespace, and returns exact file/header/root evidence. Source storage is never opened for write and no payload clone, final root replacement, service admission, route, CLI, cutover, or production activation is part of this landing.
- **P3c-2a central falsifiers:** malformed or foreign permit/profile/registry/path/time/identity inputs refuse before destination creation; existing paths and symlinks are never replaced; failure after file creation is classified as identified shadow cleanup rather than source rollback; both hash widths reopen exactly; cancellation and memory pressure stop planning without claiming completeness; GC operational families are destination-local/omitted and never copied; unknown protected subjects fail closed; a source-frontier or inventory mismatch cannot produce a complete plan; and architecture tests retain one physical writer plus no service caller.
- **P3c-2a baseline:** `migration_owner_spec` passes 31 tests and `migration_preflight_spec` passes 11 at entry commit `956ab36` with one Cargo job and home-backed scratch.
- **P3c-2a planner proof:** the dedicated target first failed to compile because the migration-clone owner did not exist and the opaque preflight permit did not expose its admitted authority inventory or required capability sets. The constant-state, failure-latching planner now classifies caller-owned streams through the selected SystemFamily registry, covers every frozen migration policy and all five subject domains, excludes destination-local GC/task/workspace state from copy totals, checks all counters before one atomic state update, and closes only against the exact admitted database, source physical instance, selected header, HEAD, authority digest, and authority counts. Both hash widths, unknown protected state, zero/item limits, cancellation before and during planning, byte overflow, frontier drift, all six policy branches, and disconnected architecture are covered. The eight-target affected matrix passes 115 tests with one Cargo job; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-2a-planner-affected.log` has SHA-256 `396b859de2264003c5056e8c41b6edb8094e68acac58912a61c5d64f47b3d2f5`.
- **P3c-2a destination proof:** the target first failed to compile on the absent destination module, then on the absent open-handle platform identity primitive and committed first-authority receipt. The opaque preflight permit now retains its admitted source file identity, destination path digest, destination-parent physical identity, hash profile, capability sets, and registry/configuration evidence. One nonconstructible observation canonicalizes an absolute absent destination under an exact parent identity. The initializer validates every fallible timestamp/layout/registry/header/semantic value before creation, creates one private no-follow read/write file, installs stage-zero DiskKV before two identical v4 headers become valid, publishes one content-only empty shadow authority exclusively through `V4FirstAuthorityPublisher`, and syncs/read-backs the file and parent namespace. It never receives or opens a source pathname and never removes, renames, or automatically cleans a failed artifact. Every post-create failure returns the canonical path, path digest, expected logical and physical IDs, exact open-handle physical identity or its explicit observation error, and any durable first-authority receipt.
- **P3c-2a adversarial closure:** exact file-handle identity remains stable across pathname replacement and is implemented for Unix and Windows from the already-open handle. Parent and pathname identities are rechecked before and after parent sync; create-new/no-follow behavior rejects existing paths and symlinks. Both hash widths close/reopen with exact dual headers, empty KV, one content-only semantic state, and matching logical/new physical identities. Tests cover malformed permit/path/registry/fence/time inputs, signed timestamp bounds, cancellation at every safe pre-publication phase, all nine post-create crash prefixes, committed first-authority failures, path substitution, parent replacement, private Unix mode, and source-byte invariance. The final 13-target affected matrix passes 237 tests; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-2a-destination-affected-post-review.log` has SHA-256 `31b8339e8acff5585ddf09e707f7f8dd97e742fac9cf905eaac17d77498cff15`.
- **P3c-2a qualification:** the first exact-tree broad rerun exposed two non-reproducible index-cache eviction assertions; each exact test and the normal 43-test target passed unchanged, so no cache semantic was weakened. A later broad run exposed a genuine test-oracle defect: streaming-reservation cleanup was bounded by 100 zero-time scheduler yields. One shared one-second poll now preserves the exact accounting assertion while allowing the cleanup task to be scheduled; the 67-test HTTP target passes under normal parallel execution. The final exact-tree workspace gate passes 6,335 tests across 285 result groups with zero failures and nine intentional ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-2a-destination-full-final-2.log` has SHA-256 `61c7a3a170b3e68b637fc12f5c7e0171b007c072aecc4d283f5aa49377a079ea`. Workspace all-target checking passes with only the historical unused `require_wasm_parser` macro warning; the reviewed suppression inventory remains exactly 1,474 and all 28 error-squelch architecture tests pass. Formatting and diff hygiene pass. No live server test is claimed because this remains disconnected internal storage substrate with no route, CLI, scheduler, service, or `StorageEngine` activation; the tests use disposable real files and native durability operations.
- **P3c-2b ratified boundary:** the 2026-08-13 owner ratification closes the remaining policy gate. AMPR remains the small selected authority for capture/reconciliation watermarks and one selected checkpoint hash; bulk capture is the already-registered external `migration_capture` dynamic artifact. Capture segments and checkpoints are private, bounded, restart-verifiable disk state. Producer capture is optional acceleration: capacity, reserve, or sink failure must preserve the acknowledged source write, hard-latch `needs_full_reconcile`, and stop later optional capture. Final freeze may publish completeness only after exact source authority and SystemFamily closure; a capture gap, malformed checkpoint, or unavailable exact diff aborts the shadow rather than guessing. No service, route, CLI, cutover, deployment, source mutation, or first-v4-write admission is authorized by this landing.
- **P3c-2b decomposition:** P3c-2b1 freezes independently checked capture segment/checkpoint bytes plus private bounded workspace create/reopen. P3c-2b2 adds one optional producer-facing capture owner, hard exhaustion/pressure evidence, checkpoint cadence, and exact resume. P3c-2b3 streams base clone output through the initialized destination authority, replays retained capture, and performs guarded full reconciliation with mandatory exact-diff fallback. Each slice receives a failing focused target, real disposable native files, architecture isolation, affected regressions, and a complete workspace gate before handoff.
- **P3c-2b1 territory:** producers are migration-bound source publication observations supplied by the later explicit migration runner; consumers are checkpoint resume, capture replay, AMPR checkpoint/watermark transitions, final reconciliation, verification/status, and identified shadow cleanup. Existing durable patterns are the private no-follow GC mark workspace, soft mutation segments, immutable checkpoint manifests, native file/directory barriers, `MemoryOwner::Migration`, and the external dynamic-record registry. The landing must reuse platform/durability primitives while defining migration-specific identity, closure, capacity, and malformed-input rules; it must not reuse GC artifact identity or make GC state authoritative for migration.
- **P3c-2b1 proof:** complete. The new fixed-size `AMCM` v1 checkpoint body is independently generated and checked at 500 bytes for BLAKE3 and 724 bytes for SHA-512, binds distinct source/destination physical instances, migration/fence/generation/checkpoint identity, state and terminal flags, times, source and capture watermarks, segment closure, source/destination roots and heads, policy fingerprints, evidence, reserve policy, and CRC, and uses the selected database hash profile. Its external `migration_capture` workspace reuses immutable `AINX` task-owned mutation-journal segments while retaining migration-specific owner identity and closure. One shared private/no-follow workspace substrate now serves capture and GC without duplicating platform policy. Create, append, checkpoint, and reopen enforce deterministic canonical names, private modes, capacity and free-space reserve, `MemoryOwner::Migration` accounting, read-only source-path validation, pending-file plus durable-install publication, exact readback, single-file bounded verification, contiguous sequence/root/predecessor chains, selected-manifest ancestry, and physical-byte accounting that includes crash prefixes and unselected canonical files. Unknown entries, malformed names, gaps, root discontinuity, foreign identities, source/destination aliasing, predecessor loss, over-capacity state, and post-mutation writer failures fail closed; terminal `needs_full_reconcile` checkpoints are representable without activating a service path. The exact affected matrix passes 221 tests. The complete workspace/all-target gate passes 6,342 tests across 283 result groups with zero failures and seven intentional ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-2b1-full-exact.log` has SHA-256 `cbc8f288c1a2ee523fb9f81abc8d34b57e93468fa294c8f7e7bd2152e6fcf9ce`. Workspace all-target checking, formatting, diff hygiene, the 1,495-site non-growing error-squelch audit, the closed-reader registry, and the independent 438-fixture contract gate pass. The retained check and contract logs have SHA-256 `9b560e2c38429bf164aaebfd5d4a2bca7f3e477380e7ff0ae877d75e8c3b521f` and `01eb3fd41b17498b1dd8241bd8c1c4fc5c7a68f29f8e6a36a74d4f0f6cadb8d9`. Strict Clippy remains at the exact historical 125-error-line baseline with no changed-file diagnostic; retained log SHA-256 is `2553a422fd807f8445a34d6b24b540a3e9c3d3cf2cf0198215737af9b6b4ea17`. Broad testing also corrected one stale generated-format count and one scheduler-sensitive HTTP cleanup oracle without weakening production semantics. No route, CLI, scheduler, `StorageEngine` activation, deployment, production database, or first-v4-write admission is present.
- **P3c-2b1 native qualification:** exact pushed commit `ccc7cdb` is green in clean detached qualification trees on macOS arm64 and Windows x86_64 MSVC. macOS passes all 115 applicable tests across capture format/workspace, shared GC workspace, platform durability, reader hardening, namespace architecture, and error-squelch architecture; it also passes all 438 independent reference fixtures and the 95-route/38-page contract-evidence gate. Windows passes all 112 platform-applicable tests, including its private-path, workspace-security, no-clobber durable-install, open-handle identity, and atomic-replacement paths. Both builds used the frozen lockfile and one Cargo job. The first macOS contract attempts failed before verification because noninteractive SSH omitted installed Homebrew tools and PyYAML from `PATH`; the exact rerun used the existing qualification virtual environment and explicit toolchain path, then passed. No product failure, source edit, service activation, deployment, production database, or cutover occurred.
- **P3c-2b2 territory and landing shape:** every acknowledged v3 namespace producer converges through `NamespaceMutationCoordinator`, which performs one post-hard-commit handoff into `StorageEngine`; caller-specific fanout follows and cannot become migration authority. The engine's existing `SoftMutationHubV1` is a destructive index-runtime queue, so migration must receive a separate optional bounded subscription rather than consume or share that queue. Registration occurs under direct hard namespace authority, records the exact selected source root and boot-local hard frontier, and ignores only late handoffs at or below that observed frontier. Concurrent offers may arrive out of order; capture sorts and deduplicates one bounded drain, then requires an exact publication/root chain or falls back to full reconciliation. P3c-2b2a first reserves the durable AMPR `needs_full_reconcile` flag and specialized owner and permits a sequence-zero empty initial checkpoint. P3c-2b2b adds the independent optional engine subscription and proves subscriber pressure cannot affect either acknowledged source writes or the primary index queue. P3c-2b2c owns AINX encoding, 300-second checkpoint publication, hard failure latching, and conservative restart. Source publication sequences are process-local today, so a boot change or unselected crash tail never guesses continuation: retained selected capture remains replayable, but exact final reconciliation becomes mandatory.
- **P3c-2b2a proof:** complete. AMPR bit 3 is now the sole durable `needs_full_reconcile` authority and bits 4 through 31 remain rejected. Generic migration-progress transitions can neither set the bit nor mutate capture checkpoint identity/watermark; one specialized checkpoint publication keeps those fields monotonic, and one specialized irreversible latch requires nonzero database-profile failure evidence. Once latched, optional checkpoints stop, while repeated latch requests preserve the already-selected evidence without reopening state. The AMCM and private capture workspace accept publication sequence zero only for an exact empty initial checkpoint; any nonempty segment chain still requires a positive publication sequence and complete predecessor/root closure. Independent fixtures add both 32-byte and 64-byte initial-empty cases, bringing the oracle to 440 cases and 149 reference-tool tests. The focused migration matrix passes 57 tests, the affected matrix passes 163, the 28-test non-growing error-squelch architecture target passes after moving one unchanged reviewed site by one source line, and the complete workspace/all-target gate passes 6,345 tests across 283 result groups with zero failures and seven intentional crash-injection ignores. Retained full and Clippy logs have SHA-256 `3419ed32b678724469254e0e7577677a1e53f8e7b07d934741515a4c059b9618` and `4b9324d0fe1acce7f1021c7ab1c8297f7b3a9e74947012d8627cbd299582c67b`; strict Clippy remains at the exact historical 125-error-line baseline with only the two previously recorded `migration_owner.rs` helper-arity findings in changed code. Workspace all-target checking, formatting, diff hygiene, the 440-fixture/95-route/38-document contract gate, and real file-backed A/B/reopen tests pass. No route, CLI, service activation, deployment, production database, source mutation, cutover, or first-v4-write admission is present.
- **P3c-2b2 central falsifier:** once hard source authority acknowledges a write, migration queue contention/fullness, allocation, workspace capacity/I/O, checkpoint encoding/durability, or AMPR publication failure cannot change the successful source result. The migration subscriber terminates, records an in-memory conservative latch even if durable latching fails, and requires the final exact source-authority/SystemFamily diff before any shadow can be considered complete.
- **P3c-2b2b entry gate:** all acknowledged directory, version, backup/import, and task-queue namespace producers converge through `NamespaceMutationCoordinator::execute_with_namespace`, which issues exactly one `StorageEngine::offer_soft_namespace_mutation` call after the hard receipt. The engine-owned primary `SoftMutationHubV1` remains the destructive index-runtime queue. This landing adds one independent optional `ArcSwapOption` subscription whose caller supplies a separately reserved hub; registration observes exact HEAD plus the idle durability hard frontier under direct namespace authority, late acknowledgements at or below that boundary are ignored, and exact-token removal closes only the selected subscription. Queue pressure, malformed/oversized notices, closure, or panic in that optional sink may require migration reconciliation but cannot alter source success or the primary-hub result. No default subscription, route, CLI, scheduler, service activation, persistent writer, or source-format change is in scope.
- **P3c-2b2b proof:** complete at pushed commit `8f43a48`. `StorageEngine` now owns exactly one optional `ArcSwapOption` migration subscription in addition to, and never instead of, its primary index-runtime hub. Registration is serialized under direct hard namespace authority, observes exact HEAD plus the boot-local hard frontier, requires a nonzero migration/boot/fence/unique-registration identity and an exclusive pristine caller-budgeted hub, and refuses replacement while a selected registration remains active. Exact-token unregister removes admission, closes only that selected queue, and returns the retired drain handle plus any explicit close failure; stale owners cannot remove a replacement registration. Sequence zero always reaches validation, while only positive publications at or below the observed frontier are ignored. Queue full, contention, oversized notices, poisoned locks, and injected panics preserve both the committed source mutation and the primary index handoff while latching conservative reconciliation on the migration side. An outer soft-handoff panic latches both independent consumers. Six external and eight internal focused tests include a disposable real file-backed engine, concurrent registration, exact replacement fencing, hard-publication failure, and every modeled sink failure. The final complete workspace/all-target gate passes 6,360 tests across 284 result groups with zero failures and seven intentional crash-injection ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/p3c-2b2b-full-final.log` has SHA-256 `ece567a216eecdf9deaa01f1826d6415019cb21761c5084b0763fff0a1ac7927`. Workspace all-target checking, formatting, diff hygiene, and the non-growing 1,496-site error-squelch audit pass. Strict Clippy remains byte-for-byte identical to the pre-review P3c-2b2b historical 125-error-line baseline with SHA-256 `b81e095a0bf27a721d0659bd6657076daff458d4120d4e31f28400bb08a2f492`; it reports no diagnostic in the new subscription module or specs. The sole new reviewed continuation is the required post-hard-commit panic containment and is classified as `postcommit-derived-reconciliation`. No route, CLI, scheduler, service activation, persistent capture writer, deployment, production database, source mutation, cutover, or first-v4-write admission is present.
- **P3c-2b2b native qualification:** exact pushed commit `d48d429` is green in the existing clean detached qualification trees on macOS arm64 and Windows x86_64 MSVC. Each platform passes all 20 applicable subscription and namespace-boundary tests: six external contract/real-file tests, eight engine-internal pressure/fencing/concurrency tests, and six hard-publication/soft-handoff regressions. Both use one Cargo job and isolated home-backed temporary directories. macOS required a Python subprocess timeout because GNU `timeout` is not installed; Windows used the controlling Linux host's timeout because neither Python nor GNU `timeout` is available in its noninteractive environment. The failed wrapper attempts occurred before Cargo started. Windows rebuilt its cold qualification cache in 10m21s and reported only pre-existing platform-specific unused-code warnings. No source edit, service activation, deployment, production database, or cutover occurred.
- **P3c-2b2c entry gate:** the runtime owner must structurally retain the `MemoryOwner::Migration` reservation for its separately bounded subscription through unregister and retired drain. Each bounded drain is sorted and deduplicated before append, then accepted only as one exact predecessor/root/publication chain. The existing migration capture workspace remains the sole AINX writer. Checkpoint publication occurs at the frozen 300-second maximum cadence and at explicit lifecycle boundaries; capacity, reserve, allocation, queue, encoding, workspace, durability, or AMPR publication failure permanently stops optional capture, preserves source success, and attempts the specialized durable `needs_full_reconcile` latch while retaining an in-memory conservative latch if that publication also fails. Selected restart state remains replayable, but a boot change or unselected crash tail makes exact final reconciliation mandatory rather than guessing continuation.
- **P3c-2b2c implementation:** `MigrationCaptureRuntimeV1` is the one optional owner that reserves `MemoryOwner::Migration` before queue construction, registers the independent source subscription at its exact hard-publication frontier, creates the sole durable capture workspace, sorts and deduplicates bounded drains, appends exact task-owned AINX chains, and selects checkpoints through the specialized AMPR authority at no more than 300-second intervals. Startup, polling, stop, and drop preserve source acknowledgement independently of allocation, pressure, queue, encoding, filesystem, checkpoint, or AMPR failures. Any loss permanently stops optional capture, attempts the monotonic durable `needs_full_reconcile` latch, and retains a conservative local latch if authority publication also fails. Stop unregisters before its final drain and keeps the migration memory reservation until the retired queue is resolved. A catastrophic unregister failure deliberately retains both the forced reconciliation state and its queue accounting rather than under-reporting live memory.
- **P3c-2b2c recovery:** restart opens only the exact AMPR-selected AMCM and complete predecessor/segment closure. A missing or damaged selected checkpoint, watermark disagreement, holder boot/fence change, or any physically present unselected crash tail never promotes later bytes or guesses continuity; selected capture remains replayable when valid, but exact final reconciliation becomes mandatory. Every physical capture-workspace directory walk shares a fixed one-million-entry scan budget, and nested inventory helpers consume the parent budget instead of resetting it.
- **P3c-2b2c concurrency correction:** migration lease and progress were previously observed through separate first-authority loads, allowing a concurrent takeover loser to combine an old lease with new progress. `V4FirstAuthorityPublisher::load_mutable_system_control_selected_pair` now holds one root-state exclusion and one aligned KV/header observation across both selected controls. Every acquire, renew, progress, release, takeover, and capture observation uses that pair. Repeated two-thread takeover proof requires exactly one winner and one fenced loser.
- **P3c-2b2c proof:** commit `431002e` is pushed on `development`. The exact affected matrix passes 175 tests; workspace all-target checking, formatting, diff hygiene, the non-growing 1,508-site error-squelch audit, the 440-fixture/95-route/38-document contract gate, and the exact historical 125-line strict-Clippy baseline pass. The cold all-target run reached its 50-minute wrapper after 204 result groups and 4,671 passing tests with zero failures; an artifact-manifest continuation reran the interrupted target and exactly the 81 remaining current executables. Combined evidence is 285 result groups, 6,378 passing tests, zero failures, and seven intentional crash-injection ignores. Retained logs `/home/wyatt/.cache/codex/aeordb-tests/p3c-2b2c-full-commit-candidate.log` and `/home/wyatt/.cache/codex/aeordb-tests/p3c-2b2c-full-remaining.log` have SHA-256 `e7757b6568dfbad76d563f3f3568d49b25602e911b0322b843d9363e16edcbd6` and `a3ec8ab31437c7f2b52c50eef722972250069afad07088600f423fd929252ef3`.
- **P3c-2b2c native qualification:** exact pushed commit `431002e` passes 145 applicable focused tests on macOS arm64 and 144 on Windows x86_64 MSVC; Windows omits the one Unix-only private-mode case. The first Windows attempt failed before tests with MSVC `LNK1318` because `C:` had only 22.5 MB free. Removing only the disposable qualification clone's `target` recovered 6.4 GB; the exact retry disabled test/debug PDB generation, rebuilt cold at one Cargo job, and passed. No source edit, service activation, deployment, production database, cutover, or first-v4-write admission occurred.
- **P3c-2b3 ratified decomposition:** the owner ratified the recommended single-authority design. P3c-2b3a first exposes one bounded immutable-entity and successor-root publication boundary by reusing the existing physical v4 dependency, KV, header, durability, prepare, and admission-witness machinery. P3c-2b3b then streams the exact preflight-pinned v3 authority plus retained-root and SystemFamily closure without a whole-tree collection. P3c-2b3c replays only the AMPR-selected capture chain as idempotent ordered deltas. P3c-2b3d acquires the explicit final write freeze and requires an exact namespace, retained-root, and SystemFamily reconciliation before destination verification; incomplete capture only increases that proof obligation. P3c-2b3e owns crash/restart, cancellation, commit-unknown, resource, source-invariance, widest-hash, and native-platform proof. No subunit may create a second root writer, activate a service path, mutate the source, or infer completeness from a journal gap.
- **P3c-2b3 entity-version prerequisite:** implementation territory review found that the ratified Round 10 contract requires v4 physical framing to preserve per-type EntityVersion, including migrated `(DirectoryIndex, EntityVersion 0)` nodes, while the current `WholeEntityV1` implementation hardcodes byte 4 to `1` and the proposed immutable batch has no version field. This is a pre-activation persistent-format discrepancy, not a live-compatibility exception: no service path or first-v4-write admission exists. P3c-2b3p must restore version-aware whole-entity encode/decode, independent both-width fixtures, role-specific validation, and exact migration publication before base-clone execution can be honest.
- **P3c-2b3p proof:** complete. `WholeEntityV1` byte 4 now carries the registered per-type EntityVersion rather than an accidental constant framing version. The closed matrix admits v0-only Chunk/DeletionRecord/Snapshot/Void/Fork/Symlink, v0-or-v1 FileRecord/DirectoryIndex, and v1-only IndexArtifact/GcArtifact; unknown versions and unsupported type/version tuples fail before allocation or publication. Namespace-root, raw namespace-tree, and system-FileRecord readers require their exact role versions. Two independently generated both-width v0 directory-tree fixtures bring the manifest to 442 cases while preserving 149 independent reference-tool tests, and every writer now supplies its explicit registered version.
- **P3c-2b3a proof:** complete. `V4FirstAuthorityPublisher` exposes one 511-entry/64-MiB bounded immutable batch and one successor-root publication boundary over the existing root lock, selected header, atomic KV visibility, native append/readback, shared durability coordinator, prepare record, and admission witness. Generic migration publication accepts only ordinary unflagged canonical Chunk/FileRecord/DirectoryIndex/Symlink content, preserves v0/v1 FileRecord identity, rejects specialized roles and collisions, converges exact retries, and classifies post-authority failures as committed. Adversarial landing review demonstrated that a post-authority immutable-batch readback I/O failure could still escape through the ordinary pre-commit error channel; the red regression now requires and receives the exact committed receipt for every missing or failed readback after header selection. Successor publication requires the exact predecessor, selected semantic state, complete target tree entity, one winner under concurrency, prior-root retention, and exact retry witnesses. The source audit confirms compressed v3 chunks must be decoded before publication and that B-tree directory roots intentionally use the `btree:` domain. The focused migration execution target passes 9 tests; the affected version/root/writer/authority matrix passes 111 plus 101 internal authority tests; the reference oracle passes 149 tests and all 442 fixtures; workspace checking, formatting, the 28-test non-growing error-squelch architecture gate, and the 95-route/38-document contract gate pass. A scheduler-sensitive ZIP cleanup assertion exposed by the broad loaded-host run now uses the neighboring engine test's real-time bounded wait rather than a fixed count of task yields; isolated repetition, the complete 26-test download target, and the loaded broad rerun all pass without changing production behavior. The final complete workspace/all-target gate passes 6,393 tests across 286 result groups with zero failures and seven intentional crash-injection ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3a-full-final4.log` has SHA-256 `a0c1892c9d8242b2ba4861d93c2e599b2eeb001973685c0ed894c412444e40cc`.
- **P3c-2b3a native qualification:** exact pushed commit `c5607249de38e121f5292645e37805159429ff2d` is green in clean detached qualification trees on macOS arm64 and Windows x86_64 MSVC. Each host passes the same 241 applicable focused tests: 9 migration-publication tests, 103 version/format/root tests, 28 error-squelch architecture tests, and all 101 internal first-authority crash/failure tests. Both new committed-state regressions pass natively. macOS also passes the complete 442-fixture/95-route/38-document contract gate. Windows passes all 149 standalone reference-tool tests and directly verifies all 442 fixture binaries; its run used one Cargo job with test/debug PDB generation disabled and retained more than 3 GiB free. No source edit, service activation, deployment, production database, cutover, or first-v4-write admission occurred.
- **P3c-2b3b source-stream direction:** a migration-long pinned KV snapshot is forbidden because it could retain replaced KV page generations across a multi-terabyte clone. Source GC suspension instead preserves immutable retained-root closure while execution uses short-lived bounded historical reads. The new traversal must decode compressed chunks before canonical v4 publication, retain only a depth/frontier-bounded walk plus one 511-entry/64-MiB destination batch, and treat concurrent authority movement as capture/final-reconciliation work rather than source mutation or inferred completeness.
- **P3c-2b3b entry map:** the source producer is a caller-streamed preflight inventory of current HEAD, retained snapshot/fork/sync/maintenance roots, and detached protected seeds. `StorageEngine` supplies only short-lived header-plus-verified-value reads; it must never expose a migration-long `ReadSnapshot`. Directory bodies are immutable `dirc:` or `btree:` entities, FileRecords own ordered chunk identities, and compressed v3 chunks must be verified then decoded before v4 `chunk:` identity publication. The pure `MigrationBaseClonePlannerV1` remains the sole SystemFamily policy resolver. `V4FirstAuthorityPublisher::publish_immutable_entity_batch` remains the sole destination content writer and enforces the 511-entry/64-MiB batch limit. Root mapping, snapshot/fork record publication, semantic wrapping, capture replay, final freeze, service activation, and cutover remain later owners.
- **P3c-2b3b test gate:** first compile-red on an absent `migration_base_clone_execution` module. The real-file target must prove flat and B-tree traversal, compressed chunks, v0/v1 FileRecords, symlinks, duplicate retained roots, policy-required copy versus destination-local/rebuild/owner-converted omission, strict missing/wrong-type/key/hash/version failures, cancellation and memory limits, idempotent restart, both hash widths where the v3 engine permits them, bounded batch/frontier accounting, and byte-for-byte source invariance. Architecture proof forbids whole-tree collectors, migration-long KV snapshots, direct destination file/KV/header writes, routes, services, and source mutation.
- **P3c-2b3b implementation:** complete. `MigrationBaseCloneEntrySourceV1` exposes only short-lived header and verified bounded historical reads; its private `StorageEngine` adapter does not leak a migration-long snapshot. One disconnected executor consumes caller-streamed HEAD, retained roots, and detached protected seeds; applies the existing SystemFamily planner; walks flat and B-tree directories with explicit ancestry, depth, frontier, cancellation, work, and memory bounds; validates canonical legacy identities and versions; decodes compressed chunks; recomputes full-file hashes incrementally across every supported database hash algorithm; rebuilds parent metadata from translated children; and publishes ordinary immutable content only through the existing 511-entity/64-MiB first-authority batch writer. It streams root mappings to a caller sink, never activates the translated HEAD, and treats duplicate/restarted publication idempotently. The shared flat-directory visitor now gives verification and migration the same count, zero-progress, and malformed-input checks.
- **P3c-2b3b adversarial corrections:** compile-red first proved the execution owner absent. Focused review then added authoritative parent-metadata reconstruction, realistic legacy SystemFamily chunk identities, inherited B-tree range and duplicate-child validation, strict v0/v1 FileRecord parsing, header reread/TOCTOU checks, checked cancellation/work/memory accounting, and a pre-I/O rejection for caller-supplied decoded-chunk bounds above the destination publication ceiling. The malformed-bound regression proves the source performs zero chunk reads and destination authority remains byte-for-byte unchanged. The dedicated target passes 12 tests. The affected 14-target matrix passes 346 tests with zero failures; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3b-affected-final.log` has SHA-256 `5dacf1a6d67e64f9bbfb0a3bd89b5bf7a5b08a50b574c30f527b5c7870b7e1d4`. The final complete workspace/all-target gate passes 6,405 tests across 287 result groups with zero failures and seven intentional crash-injection ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3b-full-final2.log` has SHA-256 `d50ca0a330c67e604085d7a4936994c227dd728cdb59942bd63113f7ae8932ea`. Formatting, diff hygiene, the 1,508-site non-growing error-squelch audit, the 442-fixture/95-route/38-document contract gate, and `mdbook build docs` pass. Strict Clippy retains exactly the prior 126 `error:` lines including its final summary, with no added or removed normalized diagnostic signature and no diagnostic in the new clone modules; retained log SHA-256 is `c10da694efd7d9458898183c211f7b5e8f9c024f5f898c6230d399f34b1bd6ee`.
- **P3c-2b3b residual resource proof:** the executor's bounded vectors may retain their peak capacity until the executor returns even after logical reservation charges are released. The number and logical content of those vectors remain bounded, and all reservations drop on every return path. P3c-2b3e must measure allocator/RSS high-water under the frozen limits and introduce explicit capacity reservation/trimming or bounded containers if physical memory exceeds the declared accounting; P3c-2b3b does not weaken the resource contract by guessing at allocator behavior.
- **P3c-2b3b native qualification:** exact pushed commit `c68249c16814abf7c2fdf7b8a53c93d21fabeb6d` is green in clean detached qualification trees on macOS arm64 and Windows x86_64 MSVC. Each host passes 344 platform-applicable tests across the same 14-target base-clone, directory/B-tree, entity/version, migration-authority, memory, and error-squelch matrix with zero failures or ignores. Both used the frozen lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`, one Cargo job, disabled incremental/debug data, and isolated home-backed temporary storage. The retained native log SHA-256 values before cleanup are macOS `0864a5f967a9ab75f4fae8d036152fcb4d94d7763b6cecc889c5d3dd57bb6fb7` and Windows `803a6a6e54d4face3ae32536d3d415aa5bca6f05325b9c44a2a237a0d69a3ca1`. The first macOS attempt failed before compilation because the untracked qualification lockfile was absent; supplying the exact verified lockfile corrected the harness without changing source. Windows reclaimed only an old disposable reference-tool cache before building and retained more than 2 GiB free. No service activation, deployment, production database, cutover, or first-v4-write admission occurred.
- **P3c-2b3c entry map:** replay must reuse the AMPR-selected `MigrationCaptureWorkspaceV1::for_each_segment` and validated `MutationJournalV1` atomic batches; no second capture format is permitted. Source historical `root_before` and `root_after` remain the correctness authority. Replay should collapse overlapping changed paths to their shallowest authoritative paths, verify every before/after revision against those historical roots, translate only authoritative after-subtrees, and copy-on-write only destination ancestors. A source publication that affects only omitted or destination-local SystemFamily state advances the reconciliation checkpoint without fabricating a destination successor root. Each committed source publication must be checkpointed through a specialized monotonic migration-owner operation; an uncheckpointed but committed destination successor is recovered through deterministic recomputation plus the existing idempotent successor witness. Root mappings stream to the later P3c-3 sink instead of accumulating in memory.
- **P3c-2b3c implementation:** complete. One disconnected replay executor opens only the exact AMPR-selected capture manifest and validated predecessor segment closure, reconstructs the destination from the base-clone root on every invocation, coalesces complete atomic batches by source publication, verifies captured before/after revisions and mutation-kind absence against immutable historical roots, collapses overlapping paths, translates authoritative after-subtrees through the base-clone policy/traversal owner, and copy-on-writes only affected destination ancestors. Flat and B-tree directories share the existing bounded parser and storage-neutral B-tree reader/planner; immutable entities and successor roots publish only through `V4FirstAuthorityPublisher`. Root-map rows stream to a caller sink before a specialized monotonic AMPR replay checkpoint. Commit-unknown root publication, sink failure, checkpoint failure, cancellation, and restart converge by deterministic transaction identity and exact authority retry rather than by inferred completion.
- **P3c-2b3c root-revisit decision:** the operator ratified one immutable admission per distinct NamespaceRoot, reused for later root selections, plus one fresh transaction-specific prepare for every `A -> B -> A` transition. The reusable admission proves only immutable database/root closure; it does not pretend to be the later transaction witness. Exact retry validates the selected transaction prepare, while read-view and retirement consumers continue accepting the older admission sequence only when it does not exceed selected authority. Direct real-file proof covers both hash widths, exact retry, and reopen without replacing the first admission locator.
- **P3c-2b3c adversarial review:** a capture publication cannot straddle segment boundaries: selected-workspace validation requires each segment's first publication sequence to equal the prior durable watermark plus one, while repeated same-sequence batches are legal only within one segment and must agree on roots. Carrying a pending publication across segments would therefore weaken the frozen format. The apparent deletion hazard for omitted or destination-local SystemFamily state is also excluded by the base-clone projection: those families are absent from the migrated namespace tree, required-copy descendants remain present and must be deleted, and structural containers exist only to carry retained descendants. Replay-owned allocation is bounded by the 16 MiB/10,000-record journal ceiling plus a conservative 32 MiB admission; the workspace separately charges the encoded segment read. P3c-2b3e retains allocator/RSS high-water measurement rather than treating modeled allocation as physical proof.
- **P3c-2b3c proof:** the dedicated real-file replay target passes 14 tests, including exact selected-checkpoint/basis refusal, same-publication coalescing and divergence, nested copy-on-write, shared B-tree planning, all mutation kinds, historical absence, omitted SystemFamily projection, base-only capture, memory/work bounds, cancellation, source divergence, commit-unknown sink recovery, exact restart retry, both hash widths, and source byte invariance. The exact final affected 20-target matrix passes 330 tests with zero failures or ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3c-affected-final.log` has SHA-256 `6d08f2f21734e77a802c89a9645473c28e1cb756c0f4f8c4f25dfdafa2ffdb2a`. Architecture gates now enumerate replay as the fifth reviewed first-authority consumer and sixth retirement-owner caller, while retaining route/service/spawn/source-writer prohibitions and exact namespace-root encoder ownership. Workspace all-target checking, formatting, diff hygiene, the 28-test non-growing error-squelch gate, and the 442-fixture/95-route/38-document independent contract gate pass. Strict Clippy retains exactly the prior 126 `error:` lines and adds no replay diagnostic; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3c-clippy-final.log` has SHA-256 `bb925df04af0bf2d5265eca6f6880f1b2f9b150d54592607df957940e7887e14`. The exact-final-source workspace/all-target gate passes 6,422 tests across 288 result groups with zero failures and seven intentional crash-injection ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3c-full-final2.log` has SHA-256 `e58792a53d96a0ff8cb5c8eca8845a652439b5cdb012678977b6792e7f4c3f32`. This remains a disconnected storage substrate: no route, service, deployment, production database, cutover, or first-v4-write admission was activated.
- **P3c-2b3c native qualification:** clean detached worktrees at exact pushed commit `2d31e35bc9f6e7a1aa0faaac49c6a57ec61a1d7f` used frozen lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`, one Cargo job, one test thread, disabled incremental/debug data, and isolated home-backed temporary/target storage. The same 20-target matrix passes 328 platform-applicable tests on macOS arm64 and 327 on Windows x86_64 MSVC, with zero failures or ignores. Linux's two additional memory-coordinator tests are Linux-specific; Windows also omits the one Unix-only private capture-workspace test. Retained native log SHA-256 values before cleanup are macOS `99c96969fa64821e0b80cdc714d935227c1bd258a9782b3e16cdd6f785fcaf71` and Windows `553e5aa1d121c1f36ca24e25127ac1faccbb684084c3e63d6bae9c4aaf7baa13`. The first Windows wrapper treated normal Cargo compile progress on stderr as a PowerShell error and exited before source compilation; the corrected wrapper preserved Cargo's actual exit code and completed the cold run. No source edit, service activation, deployment, production database, cutover, or first-v4-write admission occurred.
- **P3c-2b3d entry map:** every source storage operation enters `EngineOperationTracker`, while visible namespace writers additionally converge through `NamespaceMutationCoordinator`; raw chunk/control/KV writes therefore make the namespace mutex alone an incomplete final-write freeze. The existing exclusive maintenance admission is the sole all-operation drain/block mechanism and remains reentrant for the owning thread's bounded historical reads. A committed namespace writer can release its operation guard before optional capture handoff, so final completeness must always close against frozen HEAD/hard-frontier authority rather than inferring success from an apparently gap-free queue. Producers are the exact source header/HEAD/hard frontier, AMPR-selected capture basis, current retained snapshot/fork/sync/maintenance roots, detached protected seeds, and the selected embedded SystemFamily registry. Consumers are bounded source Merkle diff, the existing subtree translator and directory/B-tree copy-on-write path, first-authority successor publication, streamed legacy-root mappings, specialized AMPR final-freeze completion, later destination verification, and P3c-3 root-map/cutover proof. No durable AMPR flag is a live post-restart lock token; downstream work must hold the nonconstructible process-local freeze owner. The landing is split into d1 all-write freeze/frontier capture, d2 bounded exact namespace projection, and d3 retained-root/SystemFamily closure plus final AMPR authority. Service, route, scheduler, deployment, source mutation, production database, cutover, and first-v4-write admission remain forbidden.
- **P3c-2b3d1 proof:** complete. `EngineOperationTracker` now classifies every admitted operation as read or write, supports cancellation/timeout while blocked behind exclusive maintenance, and owns one process-local exclusive read-only guard. The guard drains all prior admitted work, blocks new operations on other threads, permits bounded nested reads by its owner, and rejects nested owner writes before mutation. A bypass audit brought legacy void recovery, emergency replay recovery, hot-tail flush, index flush, and KV-void synchronization under the same write admission; raw entry/header/KV mutations and namespace transactions are explicitly classified. The nonconstructible `MigrationSourceWriteFreezeV1` validates the exact preflight source identity and hash profile, captures physical identity, selected header sequence, HEAD, hard-publication frontier, and SystemFamily fingerprint only after the durability coordinator is idle, and cannot be recreated from AMPR state. Five disposable real-file tests prove source-byte invariance, owner-read/write separation, blocked raw and namespace writers, malformed/canceled/timeout refusal, and competing-freeze cancellation/timeout; four operation-tracker tests include canceled drain recovery. All 541 library tests and the 160-test affected migration/namespace matrix pass. The complete workspace/all-target gate passes 6,428 tests across 289 result groups with zero failures and seven intentional crash-injection ignores. Workspace all-target checking, formatting, diff hygiene, the 1,508-site non-growing error-squelch audit, and the 442-fixture/95-route/38-document contract gate pass. Strict Clippy remains blocked only by the historical 124-error-line repository baseline, reports no diagnostic in the new freeze module/spec, and the two new suppression-shaped expressions found by the audit were removed rather than admitted. No route, service, scheduler, deployment, production database, cutover, source mutation, or first-v4-write admission was added.
- **P3c-2b3d1 native qualification:** the exact pushed commit `a2c3aecfdf9b7e113b148e9a3b63dc3dafa018d3` passed the same ten-target 160-test migration/namespace matrix plus four operation-tracker tests on macOS arm64 and Windows x86_64 MSVC with one Cargo job and one test thread. Both hosts used frozen `Cargo.lock` SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`. The macOS evidence log SHA-256 is `2c1dff8950f9ce58f03b05197b6c0712c3bfd753539b8916ab3653192c77656b`; its only platform-specific source warning was the historical unused `str::FromStr` import. The Windows evidence log SHA-256 is `40005716fc569907092a416162eb241735eba3e4ffdc388526fcc4f417fe8dcd`; its platform-specific source warnings were the historical `health.rs` path/import/argument warnings and `rss_sampler.rs` import warning. Both cold native builds also reported the expected absent-`mdbook` fallback warning. The first macOS attempt stopped before compilation because the isolated detached worktree did not contain the intentionally untracked lockfile; the exact frozen lockfile was then installed and verified before the green run. The first Windows SSH wrapper exited before creating a log; the UTF-16LE PowerShell encoded-command rerun is the qualified run. Isolated worktrees and target directories were removed after hashing the retained host-local logs.
- **P3c-2b3d2 implementation:** complete. One strict streaming Merkle diff compares the last reconciled v3 directory root with the live-frozen HEAD without collecting the namespace. Flat and B-tree cursors validate canonical identities, ordering, inherited ranges, cycles, counts, and source double-read stability under explicit depth, work, cancellation, and migration-memory bounds. Changed paths reuse the selected-capture projection machinery: content changes translate only the authoritative after-subtree, metadata-only changes reuse the already translated destination identity, and destination ancestors are copy-on-written through the shared flat/B-tree planner. A separate aggregate subtree-work budget prevents many individually valid paths from exceeding the admitted run. Only the final complete projection can publish one deterministic successor; any earlier failure may leave unreachable immutable entities but cannot move selected destination authority.
- **P3c-2b3d2 adversarial corrections:** red tests first demonstrated that a malformed 257-entry flat directory escaped the base-clone compatibility ceiling and that the retained previous-name allocation was absent from reported diff memory. Both now share the 256-entry bound and charge/release retained ordering state. Emitted paths and owned change entries are conservatively charged while the sink uses them, and projection counter overflow now latches the exact typed final-reconciliation failure rather than returning a generic sink error. Review also confirmed equal-root zero-I/O behavior, zero-root add/remove projection, exact deterministic retry after successor publication, source byte invariance, and unchanged selected destination authority on cancellation, malformed input, missing state, and resource refusal.
- **P3c-2b3d2 proof:** the dedicated disposable real-file target passes 17 tests. The exact affected 21-target matrix passes 347 tests with zero failures or ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3d2-affected-final2.log` has SHA-256 `cb0d64aabffeb2b8d5b3acec783a59d63fffe55ad78b5f9581ea2dd80e9a8136`. Workspace all-target checking passes; retained log SHA-256 is `aab639c35d4793cedcfa80c909f99e5513665d4fdcfc283da4ed814874150c49`. The independent contract gate passes all 442 fixtures, 95 routes, and 38 documents; retained log SHA-256 is `167e24ee48d66ccd9feb34355f3bbaf6f30e05e81d74d6a6fae92c1c62be9dc8`. Strict Clippy retains exactly 124 historical `error:` lines and reports no diagnostic in the changed reconciliation, replay, base-clone, or architecture files; retained log SHA-256 is `2019aba2481c877e0d70feba89ba05439c2df3b449b51a1638f70448c141c8fd`. The exact-source workspace/all-target gate passes 6,440 tests across 289 result groups with zero failures and seven intentional crash-injection ignores; retained log SHA-256 is `437b529802ee23ece267248b4ad9615140bc10ef5e8bf2063c406a12b5f59584`. Formatting, diff hygiene, and `mdbook build docs` pass; the retained mdBook log SHA-256 is `dc137df35b17344ae33785de9240f26750b43bbe9c9f952b0cd56621b18aa8f4`. This remains a disconnected storage substrate: no route, service, scheduler, deployment, production database, cutover, source mutation, or first-v4-write admission changed.
- **P3c-2b3d2 native qualification:** exact pushed commit `84f7cb3d3947c120768e726b3fee5715dfbf77d0` passes the same 21-target matrix on clean detached worktrees: 345 platform-applicable tests on macOS arm64 and 344 on Windows x86_64 MSVC, with zero failures or ignores. Windows omits the one Unix-only private-workspace case. Both hosts used frozen `Cargo.lock` SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`, one Cargo job, one test thread, disabled incremental/debug data, and isolated home-backed temporary/target storage. Retained native log SHA-256 values are macOS `0ab552df71a401ecb4a2cdedd4bcfa602cea9f947593557236d10de6f9ff6d68` and Windows `451d6d82075a5993f5d7aac8e2e93215ff6306c29e0914ec33ab1207c277daa2`. Exact qualification worktrees and generated target/tmp trees were removed after hashing; host-local logs were retained. No source edit, service activation, deployment, production database, cutover, or first-v4-write admission occurred.
- **P3c-2b3d3 entry map:** the live freeze, not the durable AMPR flag, remains the only authority that can close final reconciliation. The caller must stream the complete frozen HEAD, retained snapshot/fork/sync/maintenance roots, detached protected seeds, and SystemFamily closure in canonical order under bounded work and memory. The exact current HEAD mapping comes from the nonconstructible d2 receipt. Existing retained mappings may be reused only after bounded destination validation; newly retained seeds reuse the shared subtree translator. Every final mapping is handed to an idempotent caller sink whose closure must finalize before AMPR completion. Generic progress transitions must not complete `FinalFreeze` or enter later phases; one specialized owner operation validates the still-live source freeze, exact destination authority, lease/fence, mapping closure, and watermark before setting the monotonic final-freeze state. A restart discards the process-local proof and therefore requires a new live freeze/reconciliation even if AMPR records historical completion.
- **P3c-2b3d3 implementation:** complete. A new disconnected final-authority reconciler consumes one canonical caller stream for frozen HEAD, retained snapshot/fork/sync/maintenance roots, and detached protected SystemFamily seeds. It validates the complete closure, all source authority counters, exact source/freeze/permit identity, and selected destination authority; reuses only bounded validated prior mappings; translates missing required roots through the shared subtree owner; applies embedded SystemFamily migration policy; and stages deterministic rows plus one exact closure in a caller-owned idempotent sink. The resulting proof retains the live nonconstructible source freeze. Generic AMPR transitions cannot complete `FinalFreeze` or enter later phases; specialized completion validates the proof twice and publishes `SOURCE_WRITE_FREEZE_HELD` only through a first-authority expectation checked under root serialization after retirement flush.
- **P3c-2b3d3 adversarial corrections:** cancellation is checked on both sides of inventory and sink closure, destination authority is revalidated before and after sink finalization, all eleven source authority counters are included in the domain-separated digest, and sink-driven authority movement is rejected. Compile-red regressions also demonstrated that logical-length accounting accepted a zero-length seed with 1 MiB retained capacity, a 32-byte prior mapping with 1 MiB capacity, and an exact closure fingerprint with 1 MiB capacity. Final reconciliation now charges all caller-owned capacities, normalizes the accepted closure fingerprint, and shares the subtree owner's 1,000-directory-depth ceiling. Optional family IDs use an explicit presence tag plus value, removing a `None`/`Some(0)` digest collision without adding an error-suppression construct.
- **P3c-2b3d3 narrow proof:** the dedicated disposable real-file target passes 29 tests, including canonical ordering, malformed closure mutations, valid and invalid prior mappings, actual retained-root translation, destination-local omission, cancellation at stream/sink closure, component failures, source invariance, destination mutation, aggregate work, depth, and all three excess-capacity boundaries. Migration ownership passes 43 tests, first authority passes 4, and the non-growing error-squelch gate passes 28. The exact affected 21-target matrix passes 361 tests with zero failures or ignores; retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3d3-affected-final2.log` has SHA-256 `f8182a757914926a4bba4fd2c66e7350af05f5f2ca27851ee6f4141edae26b6d`. Workspace all-target checking, formatting, diff hygiene, the 442-fixture/95-route/38-document independent contract gate, and `mdbook build docs` pass. Strict Clippy retains exactly the historical 124 `error:` lines and reports no diagnostic in the new final-authority module; retained log SHA-256 is `09e0b5bdc9460818fef184f715e36cf1599673476d8141442dd3ff37c4be943d`.
- **P3c-2b3d3 Linux proof:** the exact-source complete workspace/all-target gate passes 6,454 tests across 289 result groups with zero failures and seven intentional crash-injection ignores. Retained log `/home/wyatt/.cache/codex/aeordb-tests/logs/p3c-2b3d3-full-final.log` has SHA-256 `510ebc9a988740aa36e611a0bf5a288e0d3d8d9e1dbf553ece5781a2aec36694`. This remains a disconnected migration substrate: no route, service, scheduler, deployment, production database, source mutation, cutover, or first-v4-write admission changed.
- **P3c-2b3d3 native qualification:** exact pushed commit `c1e81c254e4ba81b1850fbe7f844b730e6dde515` passes the same 21-target affected matrix on clean detached worktrees: 359 platform-applicable tests on macOS arm64 and 358 on Windows x86_64 MSVC, with zero failures or ignores. Windows omits the one Unix-only private-workspace case. Both hosts used frozen `Cargo.lock` SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`, one Cargo job, one test thread, disabled incremental/debug data, and isolated home-backed temporary/target storage. Retained native log SHA-256 values are macOS `d31390d0eb2d50469eb342f550dc46b1e89ab1b944f8144509fdb2a80753f74c` and Windows `7aa6a85481e10d09610bb06f3341786bbef4fed0a5be57b74119cfedd9c0ac29`. Expected native warnings were limited to historical platform-specific unused imports/arguments and the absent-`mdbook` fallback. Exact qualification worktrees and generated target/tmp trees were removed after hashing; host-local logs were retained. No source edit, service activation, deployment, production database, cutover, or first-v4-write admission occurred.
- **Next action:** execute P3c-2b3e/P3c-3: add the bounded persistent legacy-root map owner and selected reader, prove cancellation/restart/commit-unknown recovery and exact destination verification, measure allocator/RSS high-water at both hash widths, and close source byte invariance before any service or cutover activation.
