# Child 02 Progress: Runtime

- **Status:** P2a-1 complete; P2a-2 next while native P1c host executions remain pending
- **Current landing unit:** P2a-2 grouped hard-frontier commit and platform failure matrix
- **Entry commit:** `c2f373e`
- **Last green checkpoint:** current P2a-2 explicit transaction-acknowledgement landing unit atop `ba22f59`
- **Owner:** Codex, durability/runtime integration owner
- **Start gate:** Child 01 bounded readers and platform probes green
- **Plan:** [Child 02](../children/02-durability-controls-config-and-memory.md)
- **Owned files:** `aeordb-lib/src/engine/durability_coordinator.rs`; `aeordb-lib/spec/engine/v3_contract_facade_spec.rs`; the corresponding module/target declarations; this ledger; `TODO.md`
- **Forbidden/hotspot files:** no live writer routing or v4 authority activation in P2a-1; preserve `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/`
- **Hotspot handoff commit:** none
- **Narrow gate:** P2a-2 first failed on four out-of-order/gapped hard-authority proofs, missing native classification APIs, missing group-policy APIs, missing coordinated header publication, separate transaction/timer soft barriers, a trailing shutdown barrier, and eleven raw production barrier calls. It now passes 30 facade tests, including shared sequence-space, one-ticket transaction/timer commits, duplicate-authority ownership, explicit fallible transaction completion, and recursive source architecture gates that leave raw barriers only in `native_durability.rs`.
- **Broad gate:** The current landing snapshot passes 206 library tests, 30 v3 contract tests, 16 transaction tests, 15 SDK bulk-write tests, 43 directory operation tests, 14 symlink operation tests, and 10 content-addressed-file tests. Earlier P2a-2 evidence also includes 25 append/header tests, 19 shutdown tests, all 49 DiskKVStore tests (the two long dataset/resize cases were rerun independently), 6 header-repair tests, 9 native/control-store tests, formatting, and `cargo check -j 4 --workspace --all-targets` with only the historical unused test macro warning. Test scratch remains `/home/wyatt/.cache/codex/aeordb-tests` because `/tmp` is 99% full. The prior P2a-1 131-test and 436-case campaign gates remain green evidence and will be rerun at the P2a-2 boundary.
- **Drift/risks:** native Linux P1c execution and Windows cross-compilation are green; native `wyatt-mac` and `win11vm` execution remains unavailable and cannot be waived. A fresh retry still returned no route to `wyatt-mac` and connection refused for `win11vm`. Current-source durability work remains distributed across append writer, DiskKVStore, hot-tail timer/transaction/shutdown, KV expansion/resize, header repair, emergency spill, integrity/cluster/version helpers, and route-level temporary-file publication.
- **Evidence:** `../evidence/durability-operation-inventory.json` freezes current producer/consumer ledgers and known gaps. The coordinator enforces exact contiguous hard-prefix publication, canonical group order, 64 MiB/100 ms bounded selection, oversized singleton handling, one grouped barrier, all-waiter failure, typed bounded retry, and uncertain-completion no-replay. Native errors preserve OS evidence and map all platform classes explicitly. Initial creation and later updates retain one `AppendWriter` coordinator; StorageEngine supplies it to DiskKVStore across create/open/rebuild; DiskKVStore no longer publishes expansion authority. Transaction, timer, and forced hot-tail paths now share one `publish_hot_tail_authority` owner: the hot-tail dependency write executes inside the hard header plan before one dependency barrier, then authority write/barrier/read-back. Successful dependency commit clears the hot buffer; failed dependency commit leaves it available to the serious-failure spill path and never touches the inactive slot. Shutdown no longer adds a fallible soft barrier after hard authority is already proven. Header repair uses one operation-scoped coordinator, emergency spill uses typed native barriers, and crash-soak checkpoint barrier failures now abort rather than being discarded. Every production namespace and batch mutation now explicitly consumes its transaction guard with a fallible `commit`/`finish`; `Drop` is cleanup-only, a poisoned completion test proves the error reaches the caller, and a recursive source gate rejects the former underscore/drop acknowledgement pattern. Straightforward counters and SSE events are published only after successful hard completion.
- **Next action:** implement the live bounded grouping/waiter execution owner rather than limiting grouping to facade tests; then run the P2a-2 campaign gate and continue into persistent latch/spill/repair authority in P2a-3
