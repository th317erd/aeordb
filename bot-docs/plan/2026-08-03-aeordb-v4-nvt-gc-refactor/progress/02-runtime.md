# Child 02 Progress: Runtime

- **Status:** P2a and P2b complete; P2 exit real-server, stress, dirty-restart, and deployment-readiness evidence is active while native P1c host executions remain pending
- **Current landing unit:** P2 exit real v3 stress, memory pressure, dirty restart, verify, and deployment readiness
- **Active sub-unit:** execute one bounded authenticated v3 workload across the frozen route and embedded-operation surface under the reference memory envelope.
- **Entry commit:** `a9db742`
- **Last green checkpoint:** P2b-4 verify-accounting and complete runtime-observability gate
- **Owner:** Codex, durability/runtime integration owner
- **Start gate:** Child 01 bounded readers and platform probes green
- **Plan:** [Child 02](../children/02-durability-controls-config-and-memory.md)
- **Owned files:** strict configuration resolver/authority, lifecycle compatibility adapter, v3 transition ControlStore integration, server configuration routes, CLI startup overrides/status, metrics/SSE/portal observability, verify accounting, focused specs, docs, this ledger, and `TODO.md`
- **Forbidden/hotspot files:** no v4 authority/capability publication; preserve `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/`
- **Hotspot handoff commit:** none
- **P2b-3 query-admission evidence:** query, pagination, aggregate, and EXPLAIN entry points now hold the shared operation gate and one `MemoryOwner::Query` budget. Index deep clones and disk deserialization, lookup/candidate sets, FileRecord loads, recursive listings, field-value maps, result vectors, stable sorts, aggregate groups, legacy buffered JSON rechecks, and fuzzy-scoring scratch reserve before growth. Returned query/aggregate/EXPLAIN values retain the final reservation until drop. Hard-pressure failures are retryable `ResourceExhausted` errors and release every reservation. Damerau-Levenshtein scoring was reduced from a quadratic full matrix to three rows and exhaustively matched against an independent matrix oracle. The focused query/aggregate/EXPLAIN/fuzzy/multi-index boundary passes 175 tests, including pre-fix red proofs for index cloning, buffered JSON, and scorer amplification.
- **Narrow gate:** P2a-2 first failed on out-of-order/gapped hard-authority proofs, missing native classification/group-policy APIs, missing coordinated header publication, separate transaction/timer soft barriers, a trailing shutdown barrier, raw production barriers, and facade-only grouping. It now passes 36 facade tests and 17 transaction tests. Live callers release namespace authority after ticket admission, elect one bounded driver, reacquire namespace authority, publish one dependency/header proof for the selected prefix, and wake exact waiters only after the hard frontier advances. Bounded-group, driver-setup, executor-unwind, and authority failures halt the hard frontier, fail every pending waiter, reject later hard admissions, and retire terminal records. Batch SSE/counter publication is source-gated after hard completion.
- **Broad gate:** The P2a-2 boundary passes 207 library tests; 36 v3 contract; 17 transaction; 15 SDK bulk-write; 21 blob-commit; 34 event-emission; 24 append-writer; 6 header-repair; 43 directory operation; 14 symlink; 10 content-addressed-file; 19 shutdown; 10 lifecycle; 14 KV snapshot; 23 cache/hard-link; 33 health; and 10 metrics tests. `cargo check -j 4 --workspace --all-targets` is green with only the historical unused test macro warning. The CLI crash-injection target compiled and reported all seven destructive cases explicitly ignored. Strict library Clippy remains blocked by the repository's historical warning backlog (73 errors under `-D warnings`); no new warning from the P2a-2 paths is retained. Scratch moved to `/home/wyatt/.cache/codex/aeordb-tests`; 6.6 GiB of campaign Cargo targets were removed from `/tmp`, reducing tmpfs use from 94% to 9%.
- **Drift/risks:** native Linux P1c execution and Windows cross-compilation are green; native `wyatt-mac` and `win11vm` execution remains unavailable and cannot be waived. A fresh retry still returned no route to `wyatt-mac` and connection refused for `win11vm`. Current-source durability work remains distributed across append writer, DiskKVStore, hot-tail timer/transaction/shutdown, KV expansion/resize, header repair, emergency spill, integrity/cluster/version helpers, and route-level temporary-file publication.
- **Evidence:** `../evidence/durability-operation-inventory.json` freezes current producer/consumer ledgers and known gaps. The engine now owns the shared coordinator independently of the writer lock, so writer/KV/namespace setup failure can still wake and retire waiters. The coordinator enforces exact contiguous hard-prefix publication, canonical group order, 64 MiB/100 ms selection bounds, a 1 ms adaptive live admission window, oversized singleton handling, one grouped barrier, all-pending failure, typed bounded retry, and uncertain-completion no-replay. Initial creation and later updates retain one database coordinator shared by AppendWriter and DiskKVStore. Transaction, timer, and forced hot-tail paths share one `publish_hot_tail_authority` owner; dependency append/barrier precede authority write/barrier/read-back. Successful dependency commit clears the hot buffer; failed dependency commit leaves it available to the spill path. Every production namespace/batch mutation explicitly consumes its guard through fallible completion; `Drop` is cleanup-only. A live debug server at `~/.cache/codex/aeordb-tests/p2a2-live` accepted 64 concurrent HTTP PUTs, returned exact content, shut down cleanly, reopened with all 64 files, and passed `aeordb verify` with 831/831 valid entries, zero directory/KV/void issues, and no corruption. The disposable DB was removed after evidence was recorded.
- **P2a-3 entry territory:** serious failure is produced by transaction, timer, forced hot-tail, explicit WAL sync, and shutdown hard-completion paths in `StorageEngine`; the in-memory string latch feeds write admission, route health, engine health, and timer suppression. External spill production currently snapshots hot-tail, WAL-tail, and buffered-index state into the configured/user-data/temp roots; CLI start scans those roots before server admission, while `verify --repair` prompts, replays WAL tails, runs dirty recovery/verification, and only then writes an applied marker. The v4 bounded readers and A/B selector consume the frozen control bytes, but no production encoder, FileRecord ControlStore publisher, persistent latch state, or downgrade inspector exists yet. Backup/peer-sync can currently include system files and must preserve transition controls without making generic APIs their writer.
- **P2a-3 identity ruling:** v3 headers contain no stable logical database ID, and a node ID, path hash, or mutable root is not an acceptable substitute. A first spill incident therefore carries a random nonzero 128-bit identity; explicit recovery persists it in the approved DurabilityLatch transition control, and the cleared latch remains as the identity source for later incidents and v4 migration. A database with no historical latch receives its logical identity during v4 migration. This avoids an unratified v3 header/config mutation while preserving identity after the first safety incident.
- **P2a-3 codec evidence:** production now has typed bounded decoders and encoders for `DurabilityLatch` and `EmergencySpillCatalog`. All four Blake3-256/SHA-512 outputs match the independent reference fixtures byte-for-byte, while zero control sequence, reversed failure times, and malformed complete-file digest width fail with the frozen malformed classes. The focused platform/control target passes 12 tests, the fixture and mutation-reader targets pass 71 + 2, and the library passes 208 tests. No live ControlStore writer or v4 authority is activated by this slice.
- **P2a-3 ControlStore evidence:** the sole v3 transition writer now stores canonical A/B controls as system-flagged FileRecord v0 entries through the shared namespace transaction and hard durability path. Selection verifies kind, database, identity, sequence, record version/type/flags, exact path, encoded cap, content size, full entry integrity, inactive-slot read-back, and final selected state. FileRecord decoding proves chunk-count byte closure before allocation, and bounded verified reads reject oversized FileRecord values before reading their bodies. A red 16-publisher race initially acknowledged all 16 copies of sequence 7; publication now holds reentrant namespace authority across read-select-write-read-back and acknowledges exactly one. The focused target passes 7 tests; adjacent FileRecord, records, directory, backup export/diff, content-addressed file, corruption-hardening, and platform targets pass 198 tests; the library passes 208 tests; and `cargo check -j 4 --workspace --all-targets` is green with only the historical unused test macro warning. `/tmp` remains at 9% after campaign scratch cleanup.
- **P2a-3 external-spill evidence:** v2 spill artifacts now carry stable random database/incident identities, coordinator creation sequence, first/latest failure times, native lossless database path bytes, deterministic ordering, and complete BLAKE3/length evidence for every hot-tail, WAL-tail, and index-buffer component. WAL preservation streams from the engine's pinned file handle, not a reopened pathname. Artifacts and files are private on Unix, use create-new/no-follow publication, reject symlink/reparse substitution at scan and replay, stream large replay comparison, validate applied markers against exact manifest evidence, and retain legacy v1 replay compatibility. A durable pending record makes a crash before final manifest publication startup-visible. Twelve adversarial integration tests cover tamper, symlink, incomplete, native-path, foreign-database isolation, legacy, ordering, and applied-marker paths; six focused engine tests cover first/latest evidence, spill retry, writer-lock loss, and private writer behavior. The 212 library tests, 19 shutdown tests, 9 repair/resilience tests, workspace all-target check, and Windows GNU cross-check pass. Native `wyatt-mac` and `win11vm` execution is still unavailable and remains an explicit P1c gate.
- **P2a-3 persistent-admission evidence:** v3 open now discovers selected durability-latch and emergency-spill-catalog controls through the bounded A/B ControlStore, validates database identity and the domain-separated catalog payload reference, and restores one typed persistent recovery state. Active, orphaned, or missing-companion recovery authority keeps reads and health/status available but rejects every ordinary writer through the existing shared `ensure_writable` gate. A selected cleared latch plus complete catalog retains the stable v3 database identity without blocking writes. Contradictory database identities and present-but-wrong cleared catalog references fail during open before mutation authority is exposed. The focused target passes 10 tests; emergency spill, health, shutdown, and v3 durability regressions pass 100 tests; and `cargo check -j 4 -p aeordb` is green. Missing companion authority is intentionally read-only rather than unreadable so operators retain diagnostics and explicit repair access.
- **P2a-3 explicit-repair evidence:** one thread-owned RAII repair session is the only write bypass while persistent recovery is active; all foreign threads remain read-only. Begin hard-publishes catalog `replaying` then latch `repair_verifying` with monotonic generations/sequences. A crash between those two A/B controls reopens as a partially advanced read-only state and resumes. Completion accepts only an opaque proof constructed by a clean full verify pass, rejects intervening header/frontier mutation, performs a fresh hard hot-tail publication probe, publishes a receipt-backed complete catalog, and finally publishes the exact cleared latch. Dropping or failing the session never clears admission. The focused target passes 15 success/failure/crash tests; 133 adjacent corruption/spill/health/shutdown/durability tests and all 212 library tests pass; workspace all-target checking is green except the historical unused test macro warning.
- **P2a-3 spill/CLI convergence evidence:** validated external artifacts now seed one canonical oldest-first catalog and typed first/latest latch evidence before engine repair mutations. V2 identity disagreement, partial typed evidence, and invalid embedded typed IDs fail before either control publication; first-failure selection is deterministic across tied timestamps/sequences. Legacy v1/v2 artifacts remain readable. Catalog-first publication immediately refreshes current-process read-only admission and a catalog-first crash reopens read-only. Repeated recovery recognizes immutable row identity independent of applied replay state, so a crash after in-DB completion but before external marker publication does not create a second incident. `verify --repair` owns replay recovery, repair, KV-expansion close/reopen, final clean verification, hard probe, receipt, and marker ordering; normal verify and writable startup refuse unresolved in-DB authority with the exact repair command. A disk-backed live CLI database replayed one legacy artifact, cleared receipt-backed authority, marked it applied, and restarted writable. The final focused boundary passes 20 transition-control, 13 spill-hardening, 36 coordinator-contract, 94 adjacent repair/shutdown/health/corruption, and 213 library tests; workspace all-target checking and the complete workspace test boundary are green. The first complete-suite attempt correctly surfaced three ENOSPC failures because maximum-stage KV fixtures inherited the 7.7 GiB `/tmp` tmpfs; rerunning the same 49-test target and complete workspace with `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp` passed and left that scratch directory at 4 KiB.
- **P2a-3 shutdown-drain evidence:** every production `TransactionGuard` now acquires a fallible top-level `namespace_transaction` operation guard before incrementing transaction depth and retains it through hard completion or failure cleanup. Shutdown closes top-level admission, waits those transactions/readers/writers, then independently waits for all nonterminal coordinator records and active drivers before flushing. Coordinator snapshots remain callable during shutdown and now report driver state plus oldest pending age; terminal receipts do not block drain. Operation guards are explicitly thread-bound because their nesting authority is thread-local. Deterministic concurrent tests prove a pre-admitted transaction remains visible, a foreign-thread transaction is rejected after shutdown begins, shutdown cannot return before release/hard proof, and the resulting entry survives reopen. A disk-backed live server accepted 600 concurrent HTTP file writes, handled SIGTERM with a graceful storage shutdown, and then passed offline verification with 4,764/4,764 valid entries, zero corrupt entries, zero directory/B-tree/KV/void issues, and no dangling records. The focused boundary passes 21 shutdown, 17 hot-transaction, 15 SDK batch, 43 directory, 21 blob-commit, and 38 coordinator-contract tests; workspace all-target checking and the complete disk-backed workspace test boundary are green.
- **Downgrade-gate entry territory:** candidate self-inspection is not sufficient because the candidate may be the pre-P2 binary that ignores transition controls. The installed P2-compatible binary must classify selected in-database A/B authority plus every approved external spill root without opening mutable engine authority; a candidate only advertises a fixed machine capability. A compatible upgrade is safe regardless of active recovery state. A candidate lacking that capability is allowed only after the installed inspector proves no active latch, repair/catalog state, or external spill. Inspection corruption or ambiguity fails closed. The deploy script must restore the old service if refusal happens after its clean stop; the local installer needs explicit database targets because it cannot safely guess every local database.
- **P2a-3 downgrade-gate evidence:** the candidate exposes the fixed `aeordb.v3-transition-recovery.v1` machine capability, while the installed compatible binary owns a bounded read-only inspector for selected header, hot-tail, KV page, canonical A/B controls, and every configured external spill root. The inspector never opens `StorageEngine` or writer authority, validates framing/checksums/identity/closure, and fails closed on corrupt, ambiguous, v4, or unsupported input. Compatible candidates may replace during active recovery; candidates without the capability require an exclusive database lock and a clean installed-binary inspection. Local install accepts explicit repeatable database targets. FS-Server1 deployment stops the service, gates replacement, restores the untouched old service on refusal, and restores the old unit and binary on installation failure. Focused guard and CLI targets pass 8 + 4 tests; shell safety/install/deploy targets cover first upgrade, active/repairing/cleared/external-spill/hot-tail/corrupt/unsupported/hung-probe/refusal/rollback paths; shellcheck, formatting, docs, Windows GNU cross-checking, 214 library tests, and the complete disk-backed workspace suite pass. A live disposable server proved compatible inspection while open, incompatible refusal while locked, clean downgrade after shutdown, checked installation, reopen, and offline verification with 79/79 valid entries and zero structural issues. Strict Clippy remains blocked by the historical `range_extract.rs` `never_loop` denial, and Windows all-target cross-check remains blocked only by the existing Unix-only crash-injection target. Native `wyatt-mac` and `win11vm` execution remains unavailable and is not waived.
- **P2b-1 entry territory:** the frozen machine registry already names 41 runtime/lifecycle properties and their generated environment/CLI names, but generated Rust omits each frozen default and constraint. Live operational values are still split between `lifecycle_config`, `index_store`, `emergency_spill`, `storage_engine`, and CLI start/TOML assembly. Five approved legacy environment aliases are parsed independently; malformed lifecycle JSON is warning-defaulted; runtime v1 and durable LKG activation do not yet exist. Producers are compiled/auto defaults, strict stored runtime/lifecycle documents, process environment, CLI overrides, validated LKG, and append-history recovery. Consumers span memory/KV/cache/index/query/read/durability/task/recovery/shutdown/migration/GC/lifecycle owners plus API, CLI, health, metrics, SSE, Dashboard, backup, migration, and repair diagnostics. P2b-1 adds a duplicate-aware strict resolver and diagnostics only; current live consumers remain unchanged until later P2b activation units.
- **P2b-1 strict-resolver evidence:** generated configuration rows now expose every frozen default and constraint without changing any of the 41 property identities. The new resolver uses a duplicate-aware recursive JSON AST, explicit runtime v1 and lifecycle v0/v1 adapters, property-level CLI/environment/stored/LKG/history/default precedence, complete-layer semantic admission, exact legacy-alias conflict handling, checked binary quantities, normalized absolute paths, truthful fallback/default source attribution, and visible degraded/blocking issues. Fourteen focused tests cover all registry rows and exact defaults, malformed and structural inputs, every precedence/fallback branch, all five aliases, optional/native-path values, per-property and cross-property constraints, unreadable source ownership, and cross-platform path fixture compilation. The boundary passes those 14 tests, 10 lifecycle regressions, 56 CLI config regressions, 145 reference tests, 436 independent fixture cases, the 93-route/37-doc architecture manifest, Linux all-target and Windows GNU test-target checks, and the complete disk-backed workspace suite. Clippy reports no finding in the new resolver/spec after allowing the historical `range_extract.rs` `never_loop` denial; the crate retains 77 unrelated baseline warnings.
- **P2b-1 startup-shadow evidence:** both engine constructors now capture one immutable read-only `ConfigShadowReport` before ready admission. The report derives bounded native RAM, CPU, filesystem, chunk, canonical database, private GC-workspace, and OS user-data spill context; reads only the two registered configuration FileRecords; accepts only registered environment names and the five frozen aliases; and never activates a resolved value in a legacy owner. Configuration metadata is capped at 64 KiB, declared documents at 1 MiB, stored chunk bodies before allocation, and streaming Zstd expansion before collection. Nine engine specs cover missing, valid, malformed, oversized, dishonest-compressed, missing-chunk, immutable, registered-environment, and unregistered-environment behavior; five bounded-decompression and three bounded-storage tests cover exact limits, malformed input, zero limits, stored-body excess, and expansion bombs. The focused 111-test boundary, 82 adjacent lifecycle/spill/shutdown/durability tests, 436-fixture/93-route/37-doc architecture gate, Linux and Windows GNU all-target checks, and the complete disk-backed workspace suite pass. Strict Clippy remains blocked only by the frozen historical 77-warning backlog. A real disk-backed CLI server captured a complete report, accepted and returned a 4,667-byte file, shut down, reopened with `AEORDB_MEMORY_HARD_LIMIT_BYTES=7GiB`, returned identical bytes, and passed offline verification with 191/191 valid entries and zero directory, B-tree, KV, or void issues; the disposable database was removed. Native `wyatt-mac` and `win11vm` remain unreachable and the P1c gate remains open.
- **P2b-2 entry territory:** policy is produced by the immutable startup configuration shadow and later dynamic config convergence; host RSS/availability samples and every allocator feed one process coordinator. Consumers are allocation admission, cache eviction/flush, maintenance pause/cancel, streaming/durable emergency headroom, health/metrics/SSE/Dashboard, and shutdown. Current material owners are full resident KV page sets and retained snapshots, KV/hot-tail/Void buffers, durability waiters, directory contents, clean and dirty index state, query candidates/sorts, parser/plugin amplification, tasks, GC recheck/mark state, migration/backup/repair, generic auth/config caches, and allocator/RSS remainder. Existing instrumentation attributes only index and directory estimates while RSS catches the rest; cache limits parse environment independently and no shared ceiling exists. P2b-2 freezes a pure coordinator and observes current owners without changing allocation behavior. P2b-3 alone activates mandatory reservations, eviction, flushing, and bounded KV residency.
- **P2b-2 evidence:** one fixed twenty-owner registry now covers KV residency/generations/write buffers, durability waiters, directory and clean/dirty index state, query/read/parser/task/GC/migration/backup/repair/Void/server caches, health, spill, and shutdown. A pure coordinator enforces checked soft/hard/emergency policy, host-floor pressure, exact emergency-purpose ownership, mutation-atomic reservation growth/shrink/release, and immutable owner/RSS-remainder snapshots. Both engine constructors derive policy only from the immutable configuration shadow; malformed or unresolved memory properties retain observation while admission remains unavailable. Current allocators stay behaviorally unchanged and report bounded estimates for pages, NVT/snapshot/buffers, durability, directory/index caches, GC recheck, Void, and generic caches. Fifteen focused tests cover registry stability, invalid/overflow observations, impossible policies, soft/hard/host-floor behavior, the complete critical matrix, concurrent ceilings, RAII, malformed startup configuration, and real-engine attribution. Linux and Windows GNU all-target checks pass; Windows uses native process and host-memory probes. The contract gate passes 436 fixtures, 93 routes, and 37 docs. The complete disk-backed workspace suite passes with four test threads. A real CLI database under `~/.cache/codex/aeordb-tests` accepted 16 concurrent writes under a 7 GiB configured hard limit, returned exact bytes before and after clean reopen, and verified 311/311 entries with zero corruption, missing/dangling records, B-tree, KV, or Void issues. Native `wyatt-mac` still has no route and `win11vm` still refuses port 22220, so P1c remains open rather than waived. No v4 authority or allocation policy was activated.
- **P2b-3 entry territory:** a KV stage currently creates or opens one `Arc<[u8]>` per bucket and publishes full cloned page vectors through `ReadSnapshot`; ordinary lookups are lock-free because every snapshot owns immutable old page bytes while writers update the file in place. Replacing that with naive positioned reads would silently destroy snapshot isolation. The bounded replacement therefore needs a shared version-aware page provider: current clean pages are coordinator-reserved LRU entries, pre-overwrite page versions remain pinned only while an older snapshot can observe them, and retention admission happens before disk overwrite. Full rebuild/expansion must quiesce or spill old generations rather than reinterpret overwritten bytes. KV consumers include normal hash reads, counts, backup/export/import, verify/integrity, GC, task/reindex, portal stats, rebuild, and resize. Beyond KV, current growth owners include unbounded directory/generic caches, clean/dirty index maps, all-result query candidates/sorts/aggregates, parser/plugin buffers, task queues, GC recheck/mark state, backup/restore, migration/repair scans, streaming/ZIP buffers, durability waiters, spill, and shutdown. Existing independent cache environment parsers must disappear as each owner adopts the strict resolved policy and one coordinator.
- **P2b-3 bounded-provider primitive:** one shared provider now performs exact native positioned reads, validates complete page framing without allocating entries, coalesces concurrent misses, and retains only coordinator-admitted clean LRU pages under a local byte ceiling. Logical snapshots share one generation lease across clones. A page update serializes writers, captures and workload-reserves every old page before overwrite admission, serves in-flight/committed historical generations exactly, prunes them when the last applicable view drops, and poisons new publication if an admitted overwrite is abandoned or cannot be committed exactly. Coordinator admission occurs outside the page-cache lock. Twelve focused tests cover bounds, cache pressure, malformed/truncated pages, concurrent misses, update-set validation, reservation refusal before mutation, abandoned writes, exact multi-generation reads, and reservation release. This is a green standalone primitive; `DiskKVStore` integration and full-layout rewrite quiescence remain the next slice.
- **P2b-3 snapshot/error contract:** `ReadSnapshot` now supports both resident compatibility pages and bounded provider generations behind one exact interface. Every page-backed lookup, raw lookup, type iteration, count override, and full scan returns `EngineResult`; page corruption or I/O failure can no longer silently become a miss, empty iteration, zero startup counters, or skipped bulk flush. Errors propagate through storage reads, backup, GC, counter initialization/reconciliation, stats, metrics pulse, portal responses, and CLI diagnostics. Bulk insertion now returns flush failures rather than warning and continuing. Seventeen snapshot tests include bounded exact-read and truncated-file regressions; 231 adjacent snapshot/KV/counter/GC/backup/cache/portal/shutdown tests pass, and the workspace all-target check is green. Production snapshots remain resident until the next activation slice.
- **P2b-3 startup-recovery ordering:** KV page CRC detection now reaches WAL rebuild before any startup counter or snapshot consumer is allowed to read the known-corrupt page. A targeted regression damages a flushed non-empty page, reopens, proves all four files survive authoritative WAL reconstruction, and proves counters initialize from the rebuilt KV. The full corruption-hardening target passes 34/34 tests, the complete disk-backed `cargo test -j4 --workspace --all-targets` boundary passes after the fix, and Windows GNU library/CLI checking is green with only two historical `health.rs` warnings.
- **P2b-3 bounded-activation territory:** production construction must retain resident pages only through bootstrap configuration reads and any dirty WAL rebuild, then resolve `cache.kv_resident_max_bytes`, create one coordinator-owned positioned-read provider, reuse the already-validated page type counts, atomically publish a bounded snapshot, and release the resident page set before ready admission. Ordinary and no-publish flushes currently duplicate read/deserialize/write loops, mutate disk before overflow is known, and reset corrupt pages; they must become one prepare-before-overwrite path that reserves every retained generation, rejects corruption or pressure before mutation, writes exact replacements, crosses the data barrier, commits the provider generation, and only then clears the buffer/publishes. Full resize, expansion, and rebuild overwrite or reinterpret the complete page layout, so they require a reversible exclusive operation gate plus provider retirement/drain; a current ArcSwap view must be replaced by a typed maintenance view before waiting, and pre-overwrite refusal must restore bounded reads.
- **P2b-3 bounded-activation implementation:** normal open now validates and accounts one page at a time through a zero-retention provider instead of materializing the KV block, then atomically replaces its private 8 MiB generation-safety coordinator with the strict process coordinator when configuration resolves. Ordinary and no-publish flushes share one prepare-before-overwrite authority, preserve exact old generations, reject reservation/corruption before mutation, and publish only after the data barrier. Runtime rebuild and full-layout expansion use one reversible exclusive operation gate and drain the old provider before offsets are reinterpreted. Expansion now walks validated WAL entry boundaries, preserves final straddling entries, places short-WAL relocation after the expanded block, durably journals target-page overflow, requests a later stage when possible, and latches read-only with emergency evidence after any post-marker failure. Focused proofs cover zero-resident reopen plus write, bounded refusal, old/new generation reads, corruption without reset, provider drain, runtime rebuild, short and straddling WAL relocation, collision overflow across reopen, preflight readability, and real post-marker copy failure.
- **P2b-3 hard-frontier/expansion completion:** a fresh live CLI run exposed a real first-start race between startup writes and the 100 ms hot-tail timer: the timer admitted a later header ticket and tried to execute it ahead of the transaction's pending hard prefix, latching a new database read-only. All hard header publishers now share namespace/frontier admission; the timer defers on contention, direct publishers wait before namespace acquisition, and a reentrant non-transaction publisher fails closed instead of deadlocking or leapfrogging. Transactional HEAD and backup metadata stay in memory until grouped hard completion. KV expansion requests remain queued while transaction depth is nonzero and run only after the exact hard ticket succeeds; expansion itself owns the exclusive maintenance and direct-header authorities. Deterministic tests first reproduced all three old failures, then passed with 19 storage-engine tests plus 18 transaction, 21 backup-header, 30 GC, and 37 versioning regressions. The corrected disk-backed live run accepted 800 authenticated-disabled HTTP JSON writes, expanded stage 0 to stage 1 (590,204-byte aligned block), returned exact files 1/400/800 before and after restart, reopened at about 49 MiB RSS with zero write-buffer depth, and passed offline verify with 8,773/8,773 valid entries, 6,340 KV entries, and zero corruption, dangling records, B-tree issues, invalid offsets, or invalid voids.
- **P2b-3 bounded-activation landing gate:** an adversarial review found that taking `needs_expansion` before read-only preflight could lose the only retry signal when boundary validation refused without mutation. One shared execution helper now restores only a valid future-stage request when no durability failure latched; post-marker failures remain owned by read-only startup recovery. The regression first failed with `needs_expansion=None` after a deliberately corrupt growth-zone header and now preserves `Some(1)` while leaving resize markers clear. Twenty storage-engine tests, 60 DiskKVStore tests, 44 corruption-hardening tests, the Windows GNU library/CLI check, formatting, diff checks, and the complete disk-backed workspace/all-targets suite with four harness threads pass. Seven destructive crash-injection cases remain explicitly ignored by their pre-existing test annotations.
- **P2b-3 clean-cache/index territory:** directory records, permission documents, user-group membership, API-key prefix lookups, index configuration, grants, and plugin authorization all retained immutable data in independently implemented maps with inconsistent limits, eviction, poison handling, and loader failure semantics. Field indexes additionally mixed clean retained state, dirty mutation state, publication snapshots, and module-local environment parsing. Every migrated owner now resolves limits from the immutable startup shadow, reserves exact supplied weights through the coordinator, and uses one reusable clean LRU with deterministic eviction and explicit loader errors. Dirty indexes and publication snapshots retain non-evictable reservations through durable publication; failed publication restores the exact dirty generation before releasing transient reservations.
- **P2b-3 cache/index proof:** focused cache, API-key, permission, sharing, index-store, pipeline, reindex, metrics, portal, response, and error-display targets prove bounded clean eviction, exact reservation release, malformed-loader propagation, no double counting, dirty retention, failed-flush rollback, concurrent mutation/flush safety, publication limits, and retryable HTTP 503 resource exhaustion. Adversarial review additionally found that deliberate zero-retention made waiting cache callers reload independently; its new 16-caller test failed with 16 physical loads, then passed with one explicit loading/completed result handoff. The new shared clean-cache API has one fallible read path, so lock poison cannot become a cache miss. Linux and Windows GNU all-target checks are green. The complete disk-backed `cargo test -j4 --workspace --all-targets -- --test-threads=4` gate passes, including 72 v4 format fixtures, 225 library unit tests, all HTTP/storage integrations, and the explicitly ignored destructive crash suite.
- **P2b-3 namespace/maintenance deadlock proof:** a real tiny-cache HTTP pressure run exposed expansion holding the maintenance gate while waiting for namespace authority, with the namespace holder waiting for operation admission after maintenance had closed it. Changing expansion lock order would have created the inverse cycle. Namespace authority itself is now operation-admitted before either blocking or try-lock ownership, so every namespace holder is visible to maintenance drain before the mutex can be acquired. A deterministic regression first observed zero active operations while namespace ownership was held and now observes one. Adjacent storage-engine, expansion/corruption, directory, DiskKVStore, hot-file transaction, and shutdown targets pass.
- **P2b-3 real-world cache/index evidence:** a debug server under `~/.cache/codex/aeordb-tests/live-namespace-fix-MVeMu1` ran with 32 KiB directory cache, 128 KiB clean-index cache, 16 MiB dirty-index limit, four-mutation flushes, and eight concurrent clients. All 800 indexed JSON writes completed while KV expanded through stages 0 to 2; health had zero failures and 14 ms maximum latency. Index telemetry recorded 10,400 mutations, 1,957 flushes, 2,493 clean evictions, 198,842,277 evicted bytes, and zero dirty entries at completion. Exact reads at files 0, 399, and 799 passed before and after restart. Offline verification reported 61,173/61,173 valid entries, 39,461 KV entries, and zero corrupt headers/hashes, missing or dangling records, B-tree errors, invalid offsets, or invalid voids. The 562 MiB disposable database was removed after recording this evidence; only small logs remain.
- **P2b-3 query/parser/plugin proof:** query, pagination, aggregate, and EXPLAIN use one reservation-owning work budget for index copies, candidates, recursive listings, FileRecords, legacy JSON fallback, fuzzy scratch, result vectors, sorting, and grouping. Returned values retain their final reservation until drop. Cooperative cancellation is checked before admission and at bounded query, fuzzy-distance, parser-field, and reindex quanta. Parser envelopes, mapper inputs/outputs, parsed JSON, stored plugin loading, deploy/compile state, guest linear memory, and output handoff reserve through `parser_plugin`; traps, fuel exhaustion, malformed output, and refusal release exactly. Host file reads preflight base64/JSON response capacity, extraction clamps source reads to guest capacity, and large B-tree directory responses use a new early-stopping live window walk rather than materializing the full tree. Unbounded oversized listings and serialized host responses return structured errors instead of truncating or returning an empty pointer. Authenticated HTTP invocation transfers the output reservation into the response body until completion or abort, and pressure refusal is a retryable HTTP 503 rather than a false 500. The affected boundary passes 289 query, aggregate, explain, fuzzy, parser, plugin, WASM, reindex, and B-tree tests, plus 103 route-handoff regressions, including hard-pressure overlap, cancellation, malformed/trap/fuel, CRLF/UTF-8 ranges, oversized file reads, damaged B-tree branches, bounded large-directory pages, response-lifetime accounting, and HTTP refusal semantics. The subsequent disk-backed `cargo test -j4 --workspace --all-targets -- --test-threads=4` gate also passes in full with no failures; its temporary files were rooted under `~/.cache/codex/aeordb-tests/tmp` rather than tmpfs.
- **P2b-3 plugin enumeration proof:** plugin listing now pages 64 live directory names at a time, preflights and reserves each stored record before decoding it, retains metadata rather than WASM bodies, and propagates read failures instead of silently omitting plugins. HTTP listing holds its reservation through response completion/abort and returns retryable 503 under pressure; rule collection holds the same accounted list while filtering. The 111-test HTTP/manager/rule/system-store boundary passes, including independent key-window pagination and exact reservation release.
- **P2b-3 task scheduler admission proof:** the worker now acquires coordinator-owned `task` maintenance workspace before the atomic Pending-to-Running transition and checks shutdown cancellation on both sides of admission. Soft/host-floor and ordinary hard-limit pressure leave the task persisted as Pending, emit no false lifecycle event, and retry after pressure clears; unavailable or internally broken policy remains an explicit engine error. Two focused pressure tests first reproduced the old dequeue behavior and now prove deferral/rejection counters, exact reservation release, retry, and event ordering. The complete 53-test task-worker, queue, reindex/GC-dispatch, and backup boundary passes. This reservation covers scheduler bookkeeping only; material GC, backup, reindex, migration, repair, and streaming work remains separately owned by the following slices.
- **P2b-3 GC admission/cancellation proof:** every public, HTTP, and task-worker GC entry now acquires coordinator-owned `garbage_collection` maintenance workspace before namespace authority, events, snapshots, or scans. The retained v3 mark/sweep footprint is conservatively admitted from the immutable KV row count, and the exported low-level mark API returns a reservation-owning `GcLiveSet` so embedded callers cannot bypass accounting. Cooperative cancellation is checked across root discovery, BFS, B-tree and system recursion, path-record marking, recheck drain, candidate construction, and reverification, with a final safe boundary before the first destructive KV mutation; after mutation begins, void registration and the durability flush must finish. Concurrent-write recheck growth reserves before inserting, never rejects an already committed user write, and latches a fail-closed GC error when headroom is exhausted. Malformed task registries, poisoned recheck state, and unavailable Void authority now abort rather than becoming reachability loss or leaked reclaim state. Adversarial review also removed the infallible Void snapshot wrapper: startup, spill repair, transaction completion, and GC now share one fallible synchronization path, with a poisoned-lock regression proving errors cannot become false success. The focused 341-test GC/HTTP/task/reindex/resilience/symlink/content-addressing/upload boundary, the subsequent 319-test library/corruption/GC/resilience boundary, all-target workspace check, formatting gate, and complete disk-backed workspace/all-target suite pass with four Cargo jobs and four harness threads. v3 GC remains admitted rather than structurally disk-bounded; the partitioned v4 mark design remains Child 04 authority.
- **P2b-3 backup/restore admission proof:** every embedded, CLI, scheduled-task, and HTTP full/snapshot/patch export and import path now acquires the shared `backup_restore` reservation and performs cancellable, pre-admitted traversal, diff, inventory, and transient-entry reads. Child backup/restore databases inherit the process coordinator; malformed or missing B-tree/system entries fail closed; imports validate all body checksums before the first target mutation; and resource pressure remains a retryable `ResourceExhausted`/HTTP 503. HTTP exports stream same-filesystem artifacts and imports stream request bodies to disk with both declared-length and cumulative enforcement of the documented 10 GiB limit. Unique create-new partial artifacts and ownership-aware RAII remove data plus `.lock` sidecars after success, errors, disconnects, and panic unwind without deleting a path substituted before ownership. The focused 322-test library/export/diff/import/HTTP boundary, Linux and Windows GNU all-target checks, formatting, docs, and the complete disk-backed workspace/all-target suite pass. A live server exported a 13,750,073-byte full backup, snapshot, and patch; restored the >10 MiB HTTP body; retained exact 270-file listings and a 12,582,912-byte incompressible file before/after patch; rejected malformed input without mutating existing data; shut down cleanly; and passed offline verification for source and both restores with zero corruption, missing/dangling records, B-tree/KV errors, or invalid voids. A separate live filesystem check proved an in-flight 13,752,238-byte response artifact stayed beside the database, created no OS-temp payload, and disappeared immediately after disconnect. The full-suite gate also exposed a harness-only `RLIMIT_FSIZE` interaction: the disk-write-failure child inherited an already oversized redirected log and could not report success, so it now captures child output through pipes and passes both isolated and under a synthetic 100 MiB parent log. Historical v3 snapshot roots that still name stale system children during user-only export remain an explicit migration/compatibility follow-up rather than an unverified claim in this slice.
- **P2b-3 durability-waiter admission proof:** configured engines reserve the bounded operation ledger and every admitted commit record through `durability_waiters` critical durable-write headroom; reservations survive success, failure, and panic unwind until the exact waiter receipt is retired. Hard group selection and direct execution are capped at 1,024 records even for zero-byte estimates, while every coordinator has a 4,096-record structural ceiling so malformed/unavailable configuration preserves the P2b-1 compatibility path without permitting unbounded growth. Admission is checked before and after reservation, so concurrent pressure cannot cross emergency headroom or consume sequence/queue state on refusal. Retryable pre-mutation pressure returns `ResourceExhausted`/HTTP 503 without latching the engine; timer refusal retains the recoverable hot tail for retry. Failure evidence is UTF-8-safe and capped at 4 KiB per retained field, and serious hard-frontier failures still block only new hard-authority commits rather than disabling recoverable/disposable barriers. The 69-test focused contract/config/memory gate, 318-test library/header/hot-file boundary, 60-test DiskKVStore suite, Windows GNU library/CLI all-target check, formatting/diff checks, and complete disk-backed workspace/all-target suite pass with four Cargo jobs and four harness threads. A fresh CLI server accepted 200 JSON files through four concurrent HTTP writers, returned exact files 1/100/200, remained healthy at about 62 MiB RSS with zero swap, shut down cleanly, reopened at about 47 MiB RSS with zero write-buffer depth, returned the same exact files, and shut down cleanly again. The public stats schema does not yet expose the coordinator per-owner table; exact owner reservation/release remains proven by the focused integration tests and is retained for P2b-4 observability activation.
- **P2b-3 streaming-read admission proof:** embedded full/range streams now reserve hash inventories, planned/coalesced spans, and decoded chunk frames through `streaming_read`; each frame owns its reservation until the consumer drops it. Resolved 2.5 MiB prefetch and 16 MiB coalesce limits replace fixed batching, and a bounded blocking producer plus one-frame async channel prevents synchronous database I/O from running in body polling. Direct chunk/hash, historical/deleted, ZIP, batch-fetch, and generic range-extraction paths use the same admission authority. ZIP and batch responses spool beside the database, stream reserved 64 KiB frames, and remove response artifacts on success, failure, panic, or disconnect. Generic line/character extraction retains its result reservation, and JSON-pointer extraction reserves parser/output amplification before loading the source. Adversarial review found range-batch serialization dropped that reservation while retaining its copied JSON value and could turn pressure into HTTP 200 under `continue_on_error`; successful items now serialize directly from their reservation-owning object, and resource exhaustion always remains route-level retryable 503. Startup transition-control reads preserve the pre-coordinator compatibility window without bypassing configured runtime policy; plugin cache snapshots no longer mutate host-pressure sampling during reindex construction. The final disk-backed workspace/all-target gate reports 4,547 passed, zero failed, and seven intentionally ignored tests across 188 summaries; focused streaming/download/fetch/range, reindex, transition-control, configuration-shadow, and plugin regressions pass, as do formatting, docs, Linux all-target, and Windows GNU all-target checks.
- **P2b-3 streaming live evidence:** a normal debug CLI server on a disposable disk-backed database stored a 32 MiB incompressible file and a UTF-8/CRLF/quote fixture. Full file, cross-chunk range, direct blob-hash, batch JSON, and ZIP responses were byte exact; the ZIP validated and reproduced both source hashes. A throttled client disconnect left no same-filesystem response artifact and health remained ready. The process stayed near 67 MiB RSS with zero swap, shut down cleanly, reopened cleanly, reproduced the exact full/range bytes, and passed offline verification with 326/326 valid entries and zero corrupt headers/hashes, missing/dangling records, B-tree/KV errors, or invalid voids. The disposable workspace was removed and `/tmp` remained at 10% use because all campaign scratch is rooted under `~/.cache/codex/aeordb-tests`. The run also exposed a pre-existing diagnostics error: offline `verify` labels serialized `FileRecord` bytes as logical data, so P2b-4 now owns explicit current/live versus history semantics and a corrected metric.
- **P2b-3 repair scan foundation:** `ReadSnapshot` now provides one page-at-a-time live visitors for complete and type-filtered scans, preserving buffer overrides while exposing explicit early-stop and callback/page errors. The hourly integrity scanner uses this shared path instead of materializing every KV row, pre-admits only its capped 1,000-entry sample through `repair` maintenance ownership, drops the snapshot before quarantine writes, and cooperatively checks shutdown cancellation during selection and verification. Focused proofs cover override/filter semantics, early stop, error propagation, soft-pressure deferral before sampling, pre-cancellation, bounded sample size, and exact reservation release; all 19 KV snapshot and 3 external integrity-memory tests plus 5 existing scanner unit tests pass.
- **P2b-3 current migration admission:** forced FileRecord reindex now scans live path records through the page-at-a-time visitor instead of first materializing every matching KV value. Its path vector and retained strings reserve through `migration`, each transient FileRecord is admitted before its WAL read/deserialization, and legacy whole-file hash backfill reserves encoded plus decoded chunk bounds before bounded reads. Direct migration shares the same scoped budget and refuses under soft pressure before namespace mutation. Focused proofs first observed an uncharged successful rewrite under pressure and zero migration ownership; they now prove refusal leaves the v0 record authoritative, normal migration raises and exactly releases the owner reservation, and all 24 reindex plus 2 scheduler-pressure tests pass.
- **P2b-3 pre-open repair scratch:** legacy v1/v2 header repair now shifts arbitrarily large data regions through one fallibly allocated 256 KiB backward-copy buffer instead of a 64 MiB allocation. Offset arithmetic is checked, empty scratch is rejected, and read, write, and recoverable full-barrier failures remain explicit before repaired-header publication. A multi-window v2 fixture proves exact payload, file length, hot-tail, and KV-offset migration; five unit fault/bound proofs plus all seven external header-repair regressions pass.
- **P2b-3 common KV-rebuild proof:** runtime and ordinary dirty-startup rebuild now stream WAL metadata into checksummed same-filesystem external sorted runs under an 8 MiB `repair` reservation, using a 4 MiB record window and fanout-eight merge. Runs validate version, algorithm, dimensions, counts, per-record CRCs, EOF closure, deletion order, and the legacy nonempty-directory preference before any live KV mutation. Database-specific stale scratch is removed only while the caller owns the exclusive database lock; symlink and non-directory lookalikes fail closed; shutdown cancellation is cooperative throughout append, merge, and replay. Resolved rows stream into the existing bounded KV page path. Intermediate 512-row page windows defer barriers, but every production bulk caller now publishes one explicit durable hot-tail/page checkpoint before authority can be observed. Rebuild first durably removes the valid hot-tail checkpoint, so a crash during page replacement re-enters WAL recovery instead of accepting a partial index; any post-marker failure latches the database. New-store creation also uses the zero-resident positioned page provider immediately rather than allocating an `Arc` slot for every bucket. Nine workspace unit tests, 63 DiskKVStore tests, 47 corruption/recovery tests, formatting, and the Linux all-target check pass; the only check warning is the historical unused `require_wasm_parser` test macro. The pre-single-file no-KV bootstrap/migration branch and scanner's accumulated corruption-region diagnostics remain explicitly open rather than being hidden under this completed slice.
- **P2b-3 bounded verification/repair proof:** ordinary and checked verification now run under admitted exclusive maintenance, hash-stream every WAL payload through a 64 KiB buffer, cap and account skipped-region diagnostics, and resolve expected versus actual KV state through two independent checksummed same-filesystem external-sort workspaces instead of resident maps and sets. Current reusable-space authority suppresses reclaimed WAL rows and is republished into rebuilt KV state before its durable checkpoint. Hot-tail counts and versions are preflighted and visited without allocating from untrusted cardinalities. Live directory verification visits raw flat/B-tree children without normal filtering or healing; B-tree lookup and walks detect cycles/depth/node excess, bound warning evidence, and preserve operational failures. FileRecord and snapshot verification page through live KV rows, reserve each bounded decode, avoid materializing file bodies, distinguish sanctioned task-queue records, and report malformed/corrupt snapshot bodies without converting I/O, shutdown, durability, or memory failure into a clean report. Repair propagates failed rebuilds/publication, returns a final post-repair report, keeps the engine available, and never calls shutdown as a persistence shortcut. The CLI refuses incomplete checked verification and refuses to create a repair copy after an open engine fails its hot-tail flush; an unopenable source remains copyable for the deliberate low-level header-repair path.
- **P2b-3 verification/repair evidence:** focused gates pass 270 library units, 45 durability/architecture guards, 48 corruption-hardening cases, 30 B-tree directory cases, 23 resilience/verification cases, 43 sharing cases, 21 void-manager cases, 21 task-queue cases, and 6 CLI units. A red corruption proof first showed a snapshot tree body hash failure escaping `verify_checked`; it now returns bounded broken-snapshot evidence while operational failures still abort. Linux and Windows GNU workspace/all-target checks pass with only historical platform/test warnings. Formatting, diff hygiene, the raw-barrier architecture gate, and the complete disk-backed `cargo test -j4 --workspace --all-targets -- --test-threads=4` run pass with zero failures.
- **P2b-3 verification/repair live evidence:** a normal debug CLI database at `~/.cache/codex/aeordb-tests/live-verify-b0ELXS/live.aeordb` verified 209 entries, 172 KV rows, and four directories with no issues. Copy repair at `~/.cache/codex/aeordb-tests/live-repair-XNXaZw/source.aeordb.repaired` completed cleanly; the repaired database started through the normal CLI on `127.0.0.1:18641`, reported healthy, returned exact `/docs/state.json` content and the expected directory listing, and shut down gracefully.
- **P2b-3 legacy-layout bootstrap proof:** engine open now recognizes sidecar-era no-KV and the known post-WAL repair layout before opening any KV page handle. It validates the complete authoritative legacy WAL and the advertised post-WAL physical extent before the first mutation, then uses the existing A/B resize journal to distinguish pre-relocation from relocation-durable state. The first phase preserves the original WAL; the second preserves and revalidates the relocated WAL until the new block is zeroed and finalized. Recovery is idempotent at both selected phases, selected-frontier rebuilds ignore later uncommitted bytes, empty legacy databases migrate cleanly, and corrupt, ambiguous-hot-tail, truncated-post-WAL, or inconsistent inputs fail byte-identically. The old resident startup rebuild branch and its unbounded record maps are gone; bootstrap, expansion, page-corruption recovery, and true dirty recovery converge on the bounded external rebuild path with an explicit selected-WAL versus physical-EOF boundary. An independently encoded v2 fixture proves explicit v2-to-v3 header repair composes with this migration and preserves two exact files through shutdown, reopen, and verification.
- **P2b-3 physical-Void invariant proof:** reusable extents are selected only when they are exact fits or leave enough bytes for a complete physical Void record. Successful splits write that full remainder in bounded 64 KiB zero/hash windows while holding the Void authority lock, and publish the changed void snapshot before KV insertion can flush. Failed in-place writes restore the original in-memory extent and latch durability failure; malformed split offsets cannot overflow. Exact reopen and sequential verification proofs cover valid splits, micro-remainder refusal, and overflow candidates. This fixes the previously possible state where the hot tail named a remainder that had no parseable on-disk entry. WAL-first writes that later receive a pre-durability KV refusal retain the established v3 recovery semantics; the broader returned-error/latent-WAL authority contract remains assigned to the P2c mutation-contract phase rather than being forked here.
- **P2b-3 legacy-layout live evidence:** an independently generated v2 no-KV database migrated through the actual debug CLI. The server returned exact `{"phase":"legacy-bootstrap","exact":true}` content and both legacy files, accepted a new `/docs/after.txt` write, shut down gracefully, and passed offline verification with 336/336 valid entries, 253 KV rows, three directories, and zero corruption, missing/dangling records, B-tree errors, invalid offsets, or invalid voids. A normal second CLI start returned both old and new bytes exactly. The source fixture was checksummed before migration, and the disposable live database used disk-backed campaign scratch rather than `/tmp`.
- **P2b-3 legacy-layout landing gate:** the final affected boundary passes 58 corruption/recovery, 22 Void-manager, 8 header-repair, 24 append-writer, 25 cross-restart, 63 DiskKVStore, 39 GC, 23 resilience, and 7 single-file tests. Formatting, diff hygiene, Linux and Windows GNU workspace/all-target checks, the raw-barrier architecture proof, and the complete disk-backed workspace/all-target suite pass with four Cargo jobs and four harness threads; the latter now covers 4,550 passing tests, zero failures, and seven intentionally ignored destructive crash-injection tests. Only the historical unused test macro and Windows health warnings remain.
- **P2b-3 bounded directory-repair proof:** full directory repair no longer clones the live KV or retains the database-wide directory graph. It visits current file/symlink path records one page at a time, admits each bounded decode, streams canonical child records into checksummed same-filesystem depth spools, externally sorts with 4 MiB windows and fanout-eight merges under a 32 MiB critical repair reservation, keeps at most 16 raw spool descriptors open, and publishes each directory once bottom-up. Scratch frames/runs validate dimensions, canonical paths, hash widths, checksums, counts, ordering, and EOF closure; stale workspace cleanup refuses symlink/non-directory substitution and drop removes every artifact. Targeted B-tree repair uses page-wise path visitation, a bounded B-tree visitor for readable child directories, per-child retention admission, and a pre-mutation 256 KiB entry gate. Both paths reject initially exhausted repair headroom before changing HEAD or directory keys and release reservations exactly. A later per-directory growth refusal can leave already appended non-root path-key repairs while HEAD remains unchanged; normal reads continue to prefer the authoritative HEAD tree and rerunning repair is idempotent. Full rebuild now preserves current symlink path records, while the unused older full-KV/brute-force/depth-50 repair implementation and its squelched errors are removed. The v3 source contract still cannot recover an empty directory that has no surviving descendant path record because historical directory content bodies do not encode their logical path; targeted repair preserves readable empty child directories when that evidence remains.
- **P2b-3 scanner-diagnostic proof:** recovery scanners retain at most 1,024 skipped-region coordinates while saturating total region/byte counters and preserving the latest region; verification reports omitted evidence explicitly. A direct saturation proof covers the cap and aggregate totals.
- **P2b-3 directory-repair live evidence:** a fresh database was started through the normal debug CLI on `127.0.0.1:18643`, populated through HTTP with root/deep files, exact JSON, and a symlink, and shut down cleanly. One disposable `/docs` directory path-key value byte was then changed without touching its surviving canonical content record. Offline verification reported exactly one corrupt hash plus one stale and one missing KV row; `verify --repair --force-fix-in-place --yes` rebuilt the KV and four directories through the bounded path. After a normal restart, deep JSON and symlink reads returned exact bytes and `/docs` listed both its directory and symlink. A final offline verification reported zero stale/missing/invalid KV rows, zero directory consistency issues, and only the deliberately damaged append-only historical entry.
- **P2b-3 directory-repair landing gate:** six direct scratch-workspace tests cover ordering, exact duplicate collapse, conflicting-child refusal, frame checksum corruption, run trailing bytes, stale lookalike refusal, cancellation cleanup, exact reservation release, and descriptor bounds. The affected corruption suite passes 62/62, including deep ancestor reconstruction, symlink retention, dangling-chunk exclusion, and pre-mutation pressure refusal for both repair paths. Formatting and diff hygiene pass; Linux and Windows GNU workspace/all-target checks pass with only the documented historical warnings. The complete disk-backed workspace/all-target suite passes 4,629 tests with zero failures; seven destructive crash-injection tests remain intentionally ignored.
- **P2b-3 emergency spill/shutdown proof:** hot-tail preservation and normal hot-tail publication now share the frozen streaming encoder rather than allocating a second complete payload. Dirty-index spill writes one index at a time through a streaming Base64/JSON component writer, reserves NVT rebuild and serialization scratch before allocation, removes incomplete optional components, and preserves authoritative hot-tail/WAL evidence when derived-index admission or I/O fails. Spill text, error lists, and final manifests are bounded to the startup scanner's accepted limits. Graceful shutdown reserves orchestration, KV page assembly, hot-tail clones, and retained pre-overwrite generations through the `Shutdown` critical owner; normal flushes retain their existing workload class. Shutdown collects failures while locks and workspace are held, releases all shutdown memory, then latches and spills. A mutually exclusive-headroom fault proof demonstrates the spill succeeds only because shutdown memory was released first. Destination failures, ordinary hard pressure, emergency-reserve exhaustion, partial optional spill, shutdown refusal/retry, and exact reservation release are covered. The affected library, index, DiskKVStore, KV-page, resilience, spill-hardening, and shutdown targets pass; Linux and Windows GNU workspace/all-target checks are green with only historical warnings.
- **P2b-3 emergency spill/shutdown live evidence:** a fresh disk-backed database was started through the normal debug CLI on `127.0.0.1:18644`, received CRLF text and JSON through HTTP, returned exact indexed filename search results, and shut down gracefully with dirty KV/index work. A normal reopen returned both byte sequences exactly and preserved indexed lookup. The second graceful shutdown completed, and offline verification reported 199/199 valid entries, zero corrupt entries, zero directory/B-tree consistency issues, and zero stale, missing, invalid-offset, or invalid-Void KV rows.
- **P2b-3 maintenance-pressure live evidence:** a normal debug CLI database on `127.0.0.1:18645` was populated through HTTP with a 270-file B-tree and one byte-exact 538,181,632-byte logical file. Root metadata reindex completed normally. After clean restart with a 512 MiB soft limit, 1 GiB hard limit, 256 MiB host floor, and 64 MiB emergency reserve, full reindex refused the large buffered body, remained durably `Pending`, kept `/system/health` responsive, and emitted an exact retryable `tasks_deferred` reason. The first implementation reclaimed and rewrote that task once per second; a red live finding and focused tests added durable `retry_at`/`deferral_count`, eligible-task FIFO skipping, legacy JSON defaults, and exponential 5-second-to-5-minute backoff. A subsequent eight-second SSE window before `retry_at` emitted zero events and left the task record unchanged. Normal-policy restart resumed the same task to `Completed`, cleared `retry_at`, preserved its checkpoint and deferral history, returned the final large-file byte exactly, retained all 270 ordered files, and settled near 60 MiB RSS. Offline verification passed 31,229/31,229 entries with zero corruption, directory/B-tree issues, stale/missing/invalid KV rows, or invalid Voids. The run independently reproduced the already-assigned P2b-4 logical-data accounting defect.
- **P2b-3 maintenance-pressure landing gate:** the affected backup, buffered-file, recursive-listing, GC, memory-coordinator, reindex, task-queue, and task-worker boundary passes 177 focused tests. The complete disk-backed workspace/all-target suite passes with zero failures, as do formatting, Linux and Windows GNU workspace/all-target checks, documentation, the 436-fixture/93-route/37-document v4 contract gate, and diff hygiene. Adversarial review confirmed that flat-directory corruption preserves the existing all-or-empty compatibility contract, B-tree visitors retain bounded warnings, backup refuses before destination side effects, and GC performs its last cancellation/pressure check before destructive KV removal and then completes void publication without an unsafe mid-commit pause.
- **P2b-3 historical-root normalization proof:** user-only version, snapshot, and full exports now normalize only legacy roots that still name `/.aeordb-system` or `/.aeordb-config`, publish the resulting root consistently through `ExportResult`, backup base/target metadata, and `HEAD`, and preserve ordinary flat and B-tree root identities. User-tree B-tree copying follows only nodes reachable from retained directories, so excluded system subtrees do not survive as unreferenced metadata. Flat snapshot and 264-entry B-tree HEAD compatibility fixtures prove filtering, changed root identity, absent protected content, strict tree walking, promoted import, exact user data, and clean offline verification. The complete 80-test export/import/task/HTTP boundary and full workspace all-target suite pass with zero failures, as do Linux and Windows GNU workspace/all-target checks, formatting, documentation, the 436-fixture/93-route/37-document v4 contract gate, and diff hygiene. Clippy reaches only the pre-existing `range_extract.rs` `never_loop` denial amid the repository's recorded warning backlog; the changed backup path introduces no clippy error. A normal debug CLI server accepted exact CRLF text and JSON over HTTP, shut down cleanly, exported and verified a user-only artifact, promoted it into a second database, restarted normally, returned both payloads byte-for-byte, and left source, artifact, and target with zero corruption, missing/dangling relationships, B-tree/KV errors, or invalid Voids. The disposable 20 MiB workspace was removed after recording this evidence.
- **P2b-3 exact-result matrix:** 384 focused tests pass across clean-cache eviction, transient no-retention reads, retained KV generations, malformed/truncated pages, typed disk I/O errors, tiny-budget pre-mutation refusal, concurrent read/write isolation, query/reindex cancellation, restart, and checked verification. The same source remains green under the complete workspace/all-target suite. Exact command-level evidence is frozen in `../evidence/p2b3-exact-results-and-pressure.json`.
- **P2b-3 swap-independent live exit:** a normal debug server ran inside a Linux cgroup capped at 2 GiB with `MemorySwapMax=0`. It accepted 5,402 exact writes, completed reindex and GC while 80 searches and 80 health requests ran without failure, kept health p95 at 5.4 ms and max at 27.1 ms, peaked at 409.4 MiB, shut down cleanly, and verified 61,999 entries with zero corruption, directory/B-tree, KV, or snapshot issues. A restart with a 16 MiB clean-index cap forced 716 eviction cycles and 3,915 index evictions while exact CRLF, JSON, and indexed-path results survived; RSS settled near 157 MiB, swap remained zero, health stayed responsive, shutdown was clean, and 139,291 entries verified without issue.
- **P2b-3 live finding assigned to P5:** the intentionally harsh but legal 256-mutation/1-second reindex override repeatedly published complete v0 index blobs once the active index set exceeded the 16 MiB clean cap. The database grew from 100.2 MiB to 7.0 GiB despite remaining exact. P2b cannot change v3 persistent index bytes; P5 already requires replacing whole-index blobs with immutable copy-on-write pages and now carries an explicit amplification regression. The 7.0 GiB disposable artifact was removed after evidence capture.
- **P2b-4 entry territory:** configuration inputs are CLI, registered environment, current runtime/lifecycle JSON, approved LKG/history, and compiled host-derived defaults. The strict resolver currently captures one immutable startup shadow, while the older lifecycle helper independently reparses storage and warning-defaults malformed JSON. Consumers include memory/KV/cache/index/query/read/durability/shutdown owners, snapshot creation/restore/retention, GC/task/migration next-run capture, health/metrics/SSE/CLI/Dashboard, and docs. Stored publication must remain inside the shared namespace/durability path; LKG/diagnostics must use approved system-flagged v3 transition controls; no generic file API may become a second control writer.
- **P2b-4 first red proof:** effective CLI/environment overrides of `memory.hard_limit_bytes` do not currently recompute defaults that depend on `H`, so a legal process override can leave stale host-derived defaults and false blocking issues. Correct this precedence dependency before activating any public configuration authority.
- **P2b-4 derived-default evidence:** the new proof failed against `a9db742` because a 4 GiB environment hard limit retained the 6 GiB host-derived soft default. Defaults now resolve against the effective dependency map both while validating stored/LKG documents and after CLI/environment precedence. Stored, LKG, environment, and CLI hard limits recompute only default-sourced dependents; explicitly stored dependent values retain their value/source. All 16 resolver, 9 startup-shadow, and 16 memory-coordinator tests pass. An initial shadow run without the required `TMPDIR` override correctly rejected reserve defaults against the small `/tmp` filesystem; the prescribed home-backed rerun passed and no `/tmp` artifact was retained.
- **P2b-4 configuration-control codec evidence:** production now has a bounded CanonicalConfigValueV1 encoder/decoder and duplicate-aware strict JSON canonicalizer, plus typed runtime/lifecycle LKG and diagnostics body readers/writers. All six canonical all-tag/numeric-boundary/maximum-string fixtures and all eight BLAKE3/SHA-512 LKG/diagnostic controls round-trip byte-for-byte against the independent reference artifacts. Writer-side proofs reject zero schema/state, negative time, wrong hash width, malformed canonical payload, duplicate JSON keys, negative zero, trailing JSON, and oversized input. The 16-test native/control target, 72-test complete v4 format fixture target, deterministic mutation watchdog, and all library test-target compilation pass.
- **P2b-4 live-authority shell evidence:** `StorageEngine` now owns one configuration authority rather than an immutable report field. The authority retains the exact immutable startup report for compatibility/diagnostics and publishes a separate coherent generation snapshot containing desired state, active per-property values/sources/owners/activation classes, and pending restart/convergence sets. Active scalar consumers read through that snapshot. Sixteen concurrent readers proved pointer-coherent generation publication; all ten startup configuration and sixteen memory-coordinator regressions pass unchanged.
- **P2b-4 strict replacement evidence:** embedded runtime/lifecycle replacement now validates the complete bounded duplicate-aware candidate, serializes competing updates, rejects family blocking issues before mutation, checks authority generation before publication, persists through the shared namespace/durability path, and only then publishes a family-isolated active snapshot. Startup-bound changes remain desired-but-pending-restart; current dynamic and next-run values advance coherently. The legacy lifecycle reader/writer no longer forms a second storage-parsing authority: every snapshot/retention consumer reads one active generation, typed saves use strict replacement, missing policy retains registered defaults, and malformed unresolved lifecycle policy disables snapshot writes and retention rather than warning-enabling writes. Rejected input, shutdown-induced publication failure, and concurrent same-family replacement preserve exact stored-bytes/generation consistency across reopen. The 41-test resolver/shadow/lifecycle boundary, 117 adjacent HTTP/GC/version tests, formatting, diff hygiene, and complete library test-target check pass; only the historical unused test macro warning remains.
- **P2b-4 configuration recovery-control evidence:** when an existing v3 transition recovery identity is available, strict runtime and lifecycle replacement now publishes independently selected A/B LKG and diagnostics controls after the current JSON file is durable. LKG policy bytes are duplicate-aware canonical stored JSON and exclude process-only CLI/environment overrides; policy and effective fingerprints use separate fixed domains. Startup validates control framing, database identity, family/schema closure, policy fingerprint, bounded canonical decoding, and bounded JSON recovery before offering an LKG fallback. A valid current file remains primary even when controls are corrupt; malformed/unreadable current state may use only a validated LKG; a missing current file continues to use compiled defaults. Identityless v3 databases do not invent an ID and report the control capability unavailable. Post-file ControlStore/readback failures activate the durable current policy while surfacing bounded degraded status instead of returning a false rollback-style error. Lifecycle writers emit schema v1 while legacy v0 remains read-compatible. The transition writer cannot place the current namespace root and later A/B controls in one v3 atomic root publication; a crash between them is safe because valid current bytes win, and this sequencing deviation is explicit until v4 authority exists. Bounded append-history recovery remains open and is not represented as LKG support. The 85-test resolver/authority/lifecycle/control/codec/A-B boundary, 117 adjacent HTTP/GC/version tests, 287 library tests, Linux all-test compilation, Windows GNU all-test compilation, formatting, and diff hygiene pass. Only the historical unused test macro and Windows health warnings remain.
- **Next action:** add root-only runtime/lifecycle GET, replacement PUT, and RFC 7396 PATCH routes through the configuration authority, while rejecting both exact configuration paths through generic file mutation routes.
- **P2b-4 configuration HTTP entry gate:** the repository was reconciled after a host-side Codex crash at pushed commit `279dcdb`; no source or evidence changed and only the pre-existing untracked `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` remain. The route surface is one protected Axum router, existing `/system/lifecycle` legacy GET/PUT handlers, no `/system/runtime`, and a 1 MiB non-upload body limit equal to the frozen configuration-document cap. Both exact configuration paths already fall under the engine-wide `is_system_path` root guard, but this landing unit adds route-level regression proof so a future generic API cannot become a second writer. PATCH must select its valid current or validated LKG base, merge, validate, persist, and publish while holding the authority input lock; a route-level read/merge/write would lose disjoint concurrent updates. The focused falsifiers are concurrent disjoint lifecycle PATCH, duplicate-aware raw JSON rejection, and exact generic-path denial.
- **P2b-4 configuration HTTP evidence:** `/system/runtime` and `/system/lifecycle` now provide root-only GET, strict replacement PUT, and RFC 7396 PATCH through one authority. PATCH canonicalizes duplicate-aware bounded JSON, selects only a valid current or validated LKG base while holding the authority input lock, merges and forces schema v1, validates the complete result, and keeps the lock through durable publication and coherent generation activation. Missing bases instruct a complete PUT; malformed, trailing, duplicate, scalar-result, unknown-field, oversize, unauthorized, unsupported-media-type, and protected-path requests cannot mutate file bytes or generation. Root authorization precedes representation validation. Responses normalize all 41 family properties into complete active/desired documents with exact sources, stored state/schema, human-readable transition-control identity/age, degradation, disabled capability, family-filtered aliases/issues, and pending restart/convergence values; native non-UTF-8 paths use explicit platform byte encodings. Process overrides remain active/desired but are never copied into stored JSON. Ten focused HTTP tests, six transition-control/LKG tests, sixteen authority-shadow tests, and 61 adjacent lifecycle/GC/task/version tests pass, including concurrent disjoint PATCH and current-corrupt/LKG-base recovery. All 287 library tests, Linux and Windows GNU all-test compilation, formatting, diff hygiene, and mdBook generation pass with only the historical unused test macro and Windows health warnings.
- **P2b-4 configuration HTTP live evidence:** a rebuilt debug CLI started a fresh authenticated database plus separate identity store on `127.0.0.1:18646` from home-backed campaign scratch. A root API key was exchanged for a JWT; unauthenticated GET returned 401, authenticated runtime/lifecycle GET returned complete envelopes, sparse lifecycle PUT returned defaults with exact sources, PATCH changed auto retention from 2 to 3 while preserving manual retention 9, duplicate JSON returned 400, and generic access to `/.aeordb-config/lifecycle.json` returned 404. Runtime PUT staged a 4 GiB hard limit and its derived 3 GiB soft limit while the active generation remained unchanged; after graceful restart both became active and pending restart cleared. A second graceful shutdown and offline verify reported 97/97 valid entries, zero corrupt entries, zero directory/B-tree issues, zero stale/missing/invalid KV rows, and zero invalid Voids.
- **Next action:** converge the remaining runtime/lifecycle owners on authority snapshots and wire the complete 41-property CLI override surface with generated drift proof.
- **P2b-4 command-line override evidence:** `aeordb start` now derives all 41 runtime/lifecycle options directly from the frozen configuration registry rather than maintaining a parallel clap field list. Arguments retain raw `OsString` values, require one explicit value, reject missing/duplicate/unknown forms, and enter the shared resolver under their exact registered CLI names. A typed override container refuses names absent from the registry. The serving open/create path, cluster-join pre-open, and peer-registration pre-open all carry the same immutable override set into the configuration authority; existing embedded constructors remain source-compatible and use an empty set. Six parser/drift tests prove exact 41-property parity and failure behavior, while seventeen authority-shadow tests prove command-line precedence, source attribution, derived defaults, and absence of stored JSON mutation.
- **P2b-4 command-line live evidence:** the normal debug CLI started a fresh database on `127.0.0.1:18647` with command-line hard-memory, index-flush, and snapshot-write values. Root runtime/lifecycle responses reported the exact effective values and `command_line` sources, no runtime/lifecycle JSON was synthesized, health became ready, shutdown completed cleanly, and offline verification passed 79/79 entries with zero corruption, directory/B-tree, KV, snapshot, or Void issues. The home-backed disposable workspace was removed after evidence capture.
- **P2b-4 command-line landing gate:** the final focused pass completed 16 resolver, 17 authority-shadow, 10 configuration HTTP, 56 legacy CLI-configuration, and 6 generated CLI-contract tests. Linux workspace all-target and Windows GNU library/CLI test compilation, formatting, diff hygiene, and mdBook generation pass; warnings are limited to the existing Windows health/test cfg exclusions and historical unused test macro.
- **Next action:** converge each remaining owner on the shared authority and implement truthful dynamic convergence/next-run capture before publishing configuration observability.
- **P2b-4 bounded append-history evidence:** invalid or unreadable current runtime/lifecycle state now consults the validated LKG first and, only when that source is absent or unusable, scans a fixed newest 1 GiB of the selected WAL for at most 32 matching system `FileRecord` revisions. Candidate framing, flags, path hash/path, entry hash, declared file length, chunks, and v1 whole-file hash are verified before bounded materialization; candidates are ordered by logical update time and physical offset. Valid current state still wins over damaged recovery controls, and genuinely missing current state still uses defaults without resurrecting history. The same read-only scanner supplies pre-open emergency-spill discovery and reports scan/candidate truncation. Ten configuration-control, sixteen resolver, eight deployment-guard, and two direct scanner-bound proofs pass.
- **P2b-4 owner-convergence evidence:** one authority now publishes durable desired state as `pending_convergence`, serializes owner work under the family input lock, promotes only owner-acknowledged properties, and retains old active values plus bounded errors when an owner fails. Memory, directory/KV/index residency, index buffering, read streaming, query admission, durability grouping, recovery, shutdown, lifecycle, GC, maintenance, migration handoff, and snapshot retention consume authority generations according to their frozen dynamic, next-run, or startup-bound classes. Query work owns one request/global budget across nested execution and locator scanning; malformed query policy disables only queries. Malformed durability policy disables only group batching and executes immediate singleton hard commits, preserving every hard barrier/read-back invariant and ordinary writes. Query-plan caching remains explicitly unavailable and pending for P6 rather than being represented as active.
- **P2b-4 owner-convergence gate:** the complete 303-test library boundary passes, as do 113 resolver/control/shadow/deployment/durability tests and 323 affected configuration HTTP, file streaming, query, pagination, GC, lifecycle, and maintenance tests. `cargo check -j 4 --workspace --all-targets`, formatting, and diff hygiene are green; the only warning is the historical unused `require_wasm_parser` test macro.
- **P2b-4 bounded-observability evidence:** one shared producer now samples process ownership, the twenty-owner memory coordinator, durability frontier/group/latch/spill/repair state, and both complete configuration envelopes without a storage scan or metrics-induced cache eviction. `/system/stats`, root-only Prometheus, and root-only administrative metrics SSE consume that producer; non-root stats retain ordinary operational values while registry-marked host paths and unstructured durability diagnostics are redacted. Durability retains only its last bounded barrier observation, and Linux detailed memory uses bounded `smaps_rollup` collection while the fast one-second sampler remains unchanged; incomplete ownership field sets remain unavailable instead of publishing invented zeroes. Nine status CLI cases cover direct tokens, API-key exchange, reflected-key and reflected-bearer suppression, redirect refusal without credential forwarding, malformed/auth/unreachable responses, conflicting inputs, absent credentials, and bounded response bodies. Dashboard assets render runtime state, per-owner memory, and all 41 effective properties from the already-running metrics store; malformed SSE falls back to polling without squelching errors.
- **P2b-4 observability live evidence:** a fresh authenticated disk-backed server on `127.0.0.1:18648` exercised root API-key exchange, JSON and human `aeordb status`, non-root path redaction, non-root Prometheus denial, root Prometheus owner/durability/configuration families, root metrics SSE delivery, non-root metrics SSE suppression, and Dashboard asset delivery. A separate auth-disabled database on `127.0.0.1:18649` was exercised through a real Chrome tab at 1440x1000 and 390x844: runtime state, 20 memory-owner rows, and 41 configuration rows rendered with no console exception or horizontal overflow. Both servers shut down cleanly. Offline verification of the authenticated database reported 119/119 valid entries with zero corruption, directory/B-tree, KV, Void, or snapshot issues. The complete disposable 91 MiB workspace was removed afterward.
- **P2b-4 observability landing gate:** focused configuration-shadow, resolver, deployment-guard, transition-control, memory, durability, portal, metrics-pulse, Prometheus, SSE, configuration HTTP, and CLI status targets pass. Adversarial review reproduced reqwest replaying the root API-key POST body through a cross-origin `307`; the status client now rejects all redirects and a two-server regression proves the redirected endpoint receives zero requests. A second red proof reproduced a failed token endpoint reflecting the root key into CLI diagnostics; token-exchange failure bodies are now suppressed while retaining the HTTP status. A third red proof reproduced a failed stats endpoint reflecting its bearer authorization; the exact outbound token is now redacted while non-secret status detail remains visible. The same review removed duplicated recovery-state literals, required complete Linux ownership evidence, closed non-root path disclosure through free-form durability/spill/barrier errors, and removed duplicate Dashboard stylesheet loading. The complete workspace/all-target test command passes on the final behavior with 304 library tests plus every integration and CLI target; Linux all-target checking and Windows GNU workspace/all-target checking pass. JavaScript syntax, formatting, diff hygiene, and mdBook generation are green. The first complete-suite run exposed a constructor-failure destructor panic: contradictory persistent recovery identities correctly aborted `open`, but `Drop::shutdown` consulted configuration before authority initialization. The existing fail-closed regression was red, `resolved_unsigned_config` now honors its `Option` contract and lets pre-initialization cleanup use frozen fallbacks, the exact test and all 20 transition-control tests pass, and the complete workspace rerun exits successfully. Warnings remain limited to the historical unused WASM test macro and Windows-only health/test cfg exclusions.
- **P2b-4 verify-accounting evidence:** `verify` now computes current logical bytes from the selected HEAD directory tree rather than summing serialized FileRecord payload lengths. A separate retained logical total counts one canonical live FileRecord identity, with content/path fallbacks for legacy alias layouts; retained-outside-HEAD, unique retained identity count, and raw FileRecord WAL payload bytes remain distinct diagnostics. The legacy current-logical-minus-WAL-chunk delta remains available, explicitly labeled and clamped rather than presented as current-only deduplication. Red/green regressions cover long paths, multi-chunk history, snapshot-retained deletion, B-tree directories, and identity/content/path alias fallback without materializing file bodies.
- **P2b-4 verify-accounting live evidence:** a normal debug CLI served a disposable home-backed database, accepted a 300,123-byte HTTP upload, created a real snapshot, overwrote the path with seven bytes, shut down cleanly, and passed offline verification. The report separated 159.0 KiB current HEAD data (including normal startup-created configuration/index files), 9.8 MiB across 24 retained identities, 9.7 MiB retained outside HEAD, 19.0 KiB of FileRecord WAL payloads, and 9.8 MiB of chunk WAL payloads, with zero corruption, directory, B-tree, snapshot, KV, or Void issues. A restart returned the exact seven-byte current value and a second offline verification remained clean; the complete disposable workspace was removed.
- **P2b-4 verify-accounting landing gate:** the exact falsifier, all 27 resilience tests, seven adjacent storage/version suites, and CLI formatting proof pass. The final `cargo test -j 4 --workspace --all-targets -- --test-threads=4` run exits successfully across the complete workspace, Windows GNU workspace/all-target checking passes, formatting and diff hygiene are green, and mdBook builds successfully. Warnings remain limited to the historical unused WASM test macro and Windows-only health/test cfg exclusions.
- **Next action:** execute the P2 exit authenticated v3 route/embedded workload under the 8 GiB no-swap envelope, force clean-cache eviction, perform repeated seeded dirty restart/verify cycles, and record latency, RSS, I/O, operation-ledger, and deployment-compatibility evidence.
- **P2 exit recovery-guard falsifiers:** the first disposable dirty-kill cycle exposed two pre-open blockers before either startup or offline verification could reach authoritative recovery. Boundary-aligned online KV expansion legally stores a block length larger than its stage minimum, but the read-only transition inspector required exact equality even though `DiskKVStore` accepts the larger layout. After correcting that invariant, a foreign test artifact in the shared OS spill root exposed artifact validation before database-path isolation, allowing another database's location-class mismatch to block every database on the host. The scanner now identifies canonical native database ownership first, ignores foreign complete/pending incidents, and retains full fail-closed validation for matching incidents. Red/green proofs cover legal expansion slack, corrupt KV/hot-tail controls, matching location mismatch, and foreign manifest/pending mismatch; the complete deployment-guard, configuration-control, startup-shadow, and spill-hardening targets pass.
- **P2 exit dirty-cycle 1 partial evidence:** the preserved 825.9 MiB dirty copy initially reproduced both blockers and, after the fixes, passed normal offline verification with 57,614/57,614 valid entries, zero corrupt records, zero directory/B-tree/KV/Void/snapshot issues, and no mutation of the original. The untouched original reopened healthy under the fixed debug binary with stored 1 MiB index/directory caps and zero swap. A scoped `/p2-stress` reindex inherited the root glob config, completed instead of failing on a missing local config, forced 686 clean-index evictions, and retained 883,396 index bytes plus 1,047,069 directory bytes. Process RSS fell from roughly 585 MiB during active mutation to 153 MiB after eviction and returned to roughly 190 MiB after an exact root-index oracle query; 100 deterministic paths and the smoke file matched pre-kill bytes exactly. The query's 1.7 GiB cgroup charge is file page cache while process RSS is low, and the known v0 whole-index materialization remains explicitly assigned to P5.
- **P2 exit dirty-cycle 2 recovery-hardening evidence:** twelve concurrent writers acknowledged 176 JSON files before SIGKILL. The first untouched-copy recovery reproduced a replacement-store bootstrap-snapshot leak: every 512-record bulk page flush retained another historical generation until the 7,340,032-byte bootstrap ordinary limit rejected page 5,577. Recovery now atomically publishes an unavailable view, drains its own bootstrap lease while retaining provider-mediated prepare-before-overwrite safety, performs every bulk flush with zero historical readers, and activates the configured provider only after the final checkpoint. Timeout/error paths restore the prior readable publication before mutation. A second copy then exposed two adjacent frontier defects: rebuild had reduced the legal 33,710,718-byte boundary-aligned stage-3 span to the 32 MiB minimum, and EOF recovery could resynchronize inside stale hot-tail bytes after the selected durable frontier. Recovery now preserves the selected KV span in both `DiskKVStore` and `FileHeader`, fully verifies every post-frontier payload, accepts only one contiguous appended-entry run, and stops at its first discontinuity. The final fresh 2,438,607,194-byte copy recovered 73,094/73,094 valid WAL entries and 56,668 exact KV entries with zero corrupt/skipped regions, stale/missing/invalid KV rows, directory/B-tree issues, or broken snapshots. The untouched original reached HTTP ready in roughly three seconds under `MemoryMax=8GiB`, `MemorySwapMax=0`, peaked near 153 MiB cgroup memory during this reopen, returned every one of the 176 acknowledged files with exact cycle metadata, and matched the 125-byte smoke plus 100-path indexed-query oracles byte-for-byte. All three disposable 2.44 GB copies were deleted after retaining bounded text reports.
- **P2 exit dirty-cycle 2 automated gates:** the complete 63-test corruption-hardening target, 63 disk-KV tests, 13 provider-generation tests, 27 resilience tests, 58 system-store tests, and adjacent append-writer, header-repair, and single-file targets pass under the four-thread/home-backed scratch envelope. `cargo fmt --all -- --check`, Linux `cargo check --workspace --all-targets`, and Windows GNU `cargo check --workspace --all-targets --target x86_64-pc-windows-gnu` are green; only existing platform-specific unused warnings remain.
