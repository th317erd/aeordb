# Child 06 Progress: Query

- **Status:** active; P6-1, P6-2a, P6-2b, and durable scope-ordinal authority complete; P6-2c/P6-2d runtime convergence in progress
- **Current landing unit:** P6-2d-c2 concrete recovery store and bounded per-scope registry in progress
- **Entry commit:** `a337b08` (`test: qualify v1 shadow index generation`)
- **Last green commit:** `c99e16d` (`feat: own v4 index runtime lifecycle`)
- **Owner:** current Codex thread; serialized ownership of namespace fanout, v4 coverage runtime, and shared test manifest
- **Start gate:** satisfied: Children 03 and 05 are complete; Child 04 root-state APIs are complete for the later P7 gate
- **Plan:** [Child 06](../children/06-async-coverage-query-pagination-and-locators.md)
- **Owned files:** the active P6 v4 coordinator/producer/recovery/scope-ordinal modules and focused specs, `aeordb-lib/src/engine/v4/mod.rs`, `aeordb-lib/Cargo.toml`, this progress ledger, and the P6/P7 section of `TODO.md`
- **Forbidden/hotspot files:** v4 persistent format bytes, namespace/root authority semantics, live v3 index replacement, query/API/UI/SSE cutover before its P7 landing unit, deployment, production databases, unrelated user files
- **Hotspot handoff commit:** `a337b08` from Child 05
- **Narrow gate:** `timeout 15m cargo test -j 4 -p aeordb --test coverage_runtime_spec -- --test-threads=4`
- **Broad gate:** `cargo test -j4 --workspace --all-targets -- --test-threads=4` passes on the current P6-2c worker-composition tree
- **Drift/risks:** post-commit callbacks can arrive out of publication order; route-specific fanouts currently coexist with no-op constructors; acknowledgements may carry large batches; any blocking clone, lock, sync, parser, or index work would violate the acknowledgement contract; queue loss must become an explicit reconciliation requirement; coordinated API/client cutover remains deferred to P7
- **Ratification:** on 2026-08-13 the operator ratified the remaining campaign decisions and the documented recommendations; implementation remains authorized, while deployment and destructive production activation remain unauthorized
- **Territory:** the completed Child 03 `NamespaceMutationCoordinator` is the sole hard-authority commit owner. Directory and version producers attach caller-specific post-commit effect fanouts; import attaches an event fanout; task queue, backup locator/import apply, server promote, CLI promote, and root wrappers use the no-op fanout. The engine currently has no engine-owned recoverable-soft subscriber. Synchronous legacy indexing still exists after selected file writes and before their caller returns; its replacement belongs to P6-2/P6-4. P5 provides disconnected typed mutation/checkpoint/artifact codecs but no runtime owner.
- **P6-1 proof shape:** add the required target compile-red first; compare a storage-neutral production state machine to an independent ordered-map model; cover in-order, duplicate, reordered, missing, overflow, restart, identity mismatch, whole-root, cancellation, and malformed boundary behavior before connecting the engine seam
- **Evidence:** the P6 entry baseline passed 104 namespace/tree/index tests. The new `coverage_runtime_spec` first failed to compile because the runtime module and ratified coverage names did not exist. The storage-neutral tracker now requires exact namespace/control authority continuity, accepts unrelated global publication-sequence gaps, treats exact duplicate delivery as idempotent, and latches every loss, reorder, stale, conflicting-duplicate, or authority-discontinuity case for reconciliation. Manifest coverage fields now use portable epoch/publication terminology without changing bytes. The focused target passed 7 tests; the affected v4 format/index matrix passed 174 tests; `cargo check -j4 --workspace --all-targets`, formatting, `git diff --check`, and the complete all-target workspace test gauntlet all passed. One initial affected-matrix command named the nonexistent `index_v1_manifest_spec` target and was corrected before tests ran.

## P6-1b Engine-Owned Soft Handoff

- **Territory and authority:** all namespace mutation producers converge on `NamespaceMutationCoordinator::execute_with_namespace`; the engine-owned offer occurs exactly once after the hard publication receipt and before the pre-existing caller-specific fanout. Directory, version, import, task, backup, server/CLI promotion, and whole-root wrappers retain their established fanout behavior. An architecture gate proves there is one coordinator call, one storage wrapper, and no producer bypass.
- **Bounded admission:** `SoftMutationHubV1` preallocates a fixed 4,096-notice queue and caps retained notice payload at 8 MiB and each notice at 256 KiB. Producer admission uses `try_lock`, checked accounting, and fallible cloning. Contention, queue exhaustion, oversize input, allocation failure, poison, malformed sequence, and an unexpected offer panic never wait for or fail the already hard-acknowledged mutation; each atomically latches reconciliation and advances bounded loss evidence.
- **Failure-direction proof:** the injected offer-panic test first failed because the committed locator remained authoritative while coverage still claimed no loss. A lock-free emergency latch now records the publication sequence and `QueueUnavailable` before the coordinator returns success. Cache/counter, engine handoff, and caller fanout unwind boundaries are independent, so failure in one cannot suppress the others.
- **TDD and focused evidence:** the initial hub/API targets failed to compile before implementation. Queue pressure, byte pressure, malformed sequence, too-small drain, deterministic contention, concurrent producer accounting, no-op/failed publication, whole-root, caller-fanout panic, oversized acknowledgement, and engine-offer panic paths are covered. The final narrow matrix passed 508 library tests, 10 coverage-runtime tests, and 33 namespace-mutation integration tests. The upload/version adjacency matrix passed 22 and 49 tests respectively.
- **Architecture and static evidence:** the exact error-suppression inventory was regenerated and reviewed at 1,457 occurrences; its gate passed all 28 tests. `coverage_runtime.rs` is classified as runtime authority rather than persistent-format conversion. `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp timeout 20m cargo check -j4 --workspace --all-targets`, formatting, and `git diff --check` passed.
- **Broad evidence:** `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp timeout 30m cargo test -j4 --workspace --all-targets -- --test-threads=4` passed with exit code 0 across 258 passing targets and 6,046 standard passing test lines. The complete log is `/home/wyatt/.cache/codex/aeordb-tests/p6-1b-full.log`.
- **Deferred ownership:** the hub's fixed 8 MiB payload cap and preallocated slots are engine bounded but do not yet participate in `MemoryCoordinator`; the single worker, shared memory admission, draining, and latch-clear protocol belong to P6-2. Persistent journal/replay and authoritative reconciliation belong to P6-1c. No v4 writer, query activation, migration, deployment, or production operation was activated here.
- **P6-1c execution split:** (a) exact verified NamespaceRoot/SemanticState/SystemFamily authority derivation; (b) bounded postcommit reorder and frozen journal-segment encoding; (c) validated replay plus explicit diff/rebuild outcomes for absent, ambiguous, corrupt, or disconnected authority; and (d) generation-checked loss clearing with restart, cancellation, corruption, and concurrent-loss proof. Persistent publication remains a soft-worker responsibility and is not added to the hard acknowledgement path.

## P6-1c Recoverable Journal and Reconciliation

- **Exact authority:** coverage is derived from the verified NamespaceRoot, its matching SemanticState object, and the selected SystemFamily semantic projection. Missing, ambiguous, corrupt, or canceled selection remains distinguishable and never becomes an empty delta. A semantic-control change across an otherwise contiguous namespace notice window now requires authoritative reconciliation; replaying file notices alone cannot falsely claim exact coverage under new semantics.
- **Bounded ordering and encoding:** postcommit notices are bounded by count and retained bytes, reordered by publication sequence/root continuity, deduplicated exactly, and rejected to bounded diff/rebuild on conflicts, whole-root transitions, invalid identity, or pressure. Exact windows encode the frozen system mutation-journal bytes with domain-hashed UUID identities, explicit batch ordinals, frozen owner identity, and no synthetic move or whole-root record.
- **Recovery and loss clearing:** journal replay is bounded by segment count and encoded bytes, cancellation-aware, validates every frozen segment/record and chain edge, and preserves corruption evidence. The soft hub tracks every dropped notice with a monotonic loss epoch and in-flight publication guard. Loss can clear only through an opaque tracker reconciliation proof at or beyond all lost/queued evidence; stale tokens and concurrent drops cannot be erased.
- **Failure direction:** there is still no persistent sink, worker, v4 activation, parser/index work, or journal sync in the hard writer. P6-2 owns the single memory-coordinated drain/persist worker and coverage-epoch binding. A replay summary validates a chain but deliberately does not activate or apply it before that owner exists.
- **TDD and focused evidence:** the new target first failed to compile before the contracts existed. The semantic-control regression then failed by returning `Exact` and passed after the fail-closed guard. The final focused targets pass 12 coverage-journal, 10 coverage-runtime, and 28 error-squelch architecture tests. The exact suppression inventory remains 1,457 reviewed occurrences; formatting and `git diff --check` pass.
- **Broad evidence:** the first complete all-target gauntlet passed 6,057 tests with 7 ignored before the final semantic-control guard. The phase-boundary rerun after that guard passed 259 targets, 6,058 tests, and 7 ignored with zero failures. Its log is `/home/wyatt/.cache/codex/aeordb-tests/p6-1c-final-full.log`.

## P6-1d Qualification

- **Restart/failure matrix:** the coverage tracker, soft hub, journal, and namespace coordinator suites prove restart comparison, dropped/duplicate/out-of-order delivery, queue count/byte pressure, oversized acknowledgement, allocation/poison/panic failure direction, whole-root reconciliation, cancellation, malformed/corrupt authority and journal input, and concurrent loss-generation races.
- **Acknowledgement isolation:** one deterministic unit test retains the consumer queue lock while another thread offers the postcommit acknowledgement. The offer completes while the lock remains held and returns `QueueContended`, proving the producer uses nonblocking admission rather than waiting for downstream work. Existing namespace tests prove soft panic and oversize loss cannot change the already-durable hard success or suppress caller fanout. Source architecture gates retain exactly one engine-owned post-authority offer and no parser/index/journal sync in that path.
- **Bounds:** the hub preallocates fixed notice capacity, checks retained bytes before cloning, uses fallible allocations, and converts every inability to retain soft state into explicit reconciliation evidence. Journal ordering/replay is count/byte bounded and cancellation-aware. P6-2 now owns transferring these fixed bounds into the process-wide memory budget and persistent worker lifecycle.
- **Next action:** add the compile-red P6-2 coordinator target after mapping all legacy buffer, parser, task, timer, spill, checkpoint, cache, startup, and shutdown owners. The new owner must replace or adapt those paths rather than coexist as another independent index runtime.

## P6-2 Entry Territory and Split

- **Producers:** ordinary file writes currently invoke `IndexingPipeline` synchronously from two `DirectoryOps` paths; reindex owns a task-local `IndexWriteBuffer`; delete/index cleanup and manual index operations call `IndexManager`; all ultimately mutate the engine-wide `SharedIndexWriteBuffer`. The P6-1 engine handoff is the sole post-authority namespace notice producer and is not yet consumed by a worker.
- **Consumers:** query/search load through `IndexManager`; the server owns a separate periodic `flush_index_buffer_if_due` timer; task reindex polls its wrapper's count/time state; shutdown directly forces the shared buffer; emergency spill snapshots it; runtime metrics and the memory dashboard read its stats; configuration activation stages clean/dirty/publication/count/time limits.
- **Existing strengths to preserve:** the legacy shared buffer already applies the ratified 262,144-mutation/30-second defaults, process-wide `MemoryCoordinator` reservations, byte-bounded publication batches, clean LRU/TTL eviction, failed-flush restoration, shutdown flush, and emergency-spill capture. P6-2 must adapt these proven behaviors and tests, not discard them.
- **Architectural gap:** ownership remains split among `DirectoryOps`, `IndexingPipeline`, `IndexManager`, `IndexWriteBuffer`, `task_worker`, the server timer, shutdown, and emergency spill. Large `FieldIndex.entries`/`values` maps are still the mutable unit. A v1 coordinator added beside those paths would violate the single-owner contract.
- **Execution split:** P6-2a freezes the storage-neutral active/frozen memtable and memory state machine; P6-2b binds journals/checkpoints/recovery; P6-2c converges parser and all index mutation producers; P6-2d hands timer, pressure, cache, startup, shutdown, metrics, and spill to the same owner. P6-3 alone activates immutable page/pointer publication and query coverage.
- **P6-2a falsifier:** under a tiny process/local budget, admission must reserve before cloning, exact duplicate delivery must be idempotent, conflicting/stale identities must fail without mutation, active work may continue beside one frozen batch, failed completion must retain or explicitly spill every dirty byte, cancellation must not consume state, and shutdown must reject new work until the final batch is resolved.

## P6-2a Bounded Coordinator State Machine

- **TDD:** the dedicated `index_coordinator_spec` first failed to compile because no v4 coordinator module or contract existed. A tight exact-fit memory test then failed because provisional order-key headroom exceeded the true retained footprint; ordered records now expose a nonallocating canonical key-length check, and admission reserves the exact modeled bytes before materializing the key or cloning the record. A final abandoned-publication test failed on the absent retry contract before attempt-qualified frozen-batch reissue was added.
- **Single bounded state:** one storage-neutral coordinator owns a deterministically ordered active memtable and at most one frozen memtable. It validates hash-width identity, role, operation identity, publication sequence, canonical record framing, and monotonic time before changing state. Exact duplicates are no-ops; stale or same-sequence conflicting mutations fail closed; newer mutations replace by canonical index/role/order key.
- **Memory and batching:** every retained mutation owns an exact `IndexDirtyBuffers` reservation, local mutation bytes are checked across active plus frozen state, and publication snapshots use bounded critical durability reservations. Flush triggers cover mutation count, age, process pressure, explicit request, and shutdown. Deterministic prefix batches never exceed `publication_batch_max_bytes`; active work may continue while one batch is frozen.
- **Failure direction:** cancellation before freeze changes nothing. Failed completion restores every distinct frozen record and merges observation counts while newer active state wins. Foreign tokens cannot consume another coordinator's state. Abandoned handles can be reissued with a new attempt identity; stale attempts are refused while the exact frozen batch remains recoverable. Shutdown rejects new admissions, requires all active/frozen state to resolve, then enters `Stopped`.
- **Focused proof:** 13 coordinator tests cover invalid options/identity/bytes, malformed records, exact-fit global and local pressure, exact accounting, duplicate/replacement/stale/conflict semantics, count/age/process-pressure triggers, bounded partial batches, active-plus-frozen operation, failure restore, pre-freeze cancellation, foreign and stale attempts, retry, clock rollback, and draining. Both contiguous and posting nonallocating order-key lengths match materialized keys.
- **Affected/static proof:** 32 copy-on-write, 46 frozen index-reference, 18 memory-coordinator, and 28 error-squelch architecture tests pass. The exact reviewed suppression inventory remains 1,457 because suppression-shaped constructs found during review were removed rather than allowlisted. Workspace all-target checking, formatting, and `git diff --check` pass; the only check warning is the pre-existing unused `require_wasm_parser` test macro.
- **Broad proof:** the post-review all-target workspace gauntlet passed 260 targets, 6,073 tests, and 7 ignored with zero failures. Its complete log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2a-final-full.log`.
- **Activation boundary:** this module is deliberately storage-neutral and disconnected from live v3 writers, queries, tasks, timers, storage, and service routes. P6-2b owns durable journal/checkpoint recovery; P6-2c replaces all producer paths; P6-2d transfers runtime lifecycle ownership. P6-3 remains the sole immutable index publication/query activation phase.

## P6-2b Recoverable Checkpoint Binding

- **TDD and contract:** the dedicated recovery target first failed to compile before the storage-neutral durability boundary, selected-root, publication receipt, recovery outcome, and bounded options existed. A later read-count falsifier proved that checkpoint closure and replay loaded the journal head twice; the validated reservation-owning attachment is now passed directly into replay, so publication and restart each load the head once.
- **Publication and crash direction:** immutable dependencies are validated and written before the typed checkpoint, all immutable bytes are synced, and the complete attachment/journal graph is reopened under shared memory admission before the selected checkpoint advances. Initial selection requires sequence one, later selection requires the exact successor, stale expectations fail before writes, and exact retries revalidate bytes without republishing the selector. Failures before selector publication can leave unselected immutable artifacts for later collection but cannot replace the prior root. A selector-call error is commit-unknown: reopen must select either the expected root or the exact validated next root, and a dedicated fault test proves the exact-new case recovers and retries idempotently.
- **Bounded restart recovery:** checkpoint, attachment, and journal reads probe length before allocation and retain exact `IndexDirtyBuffers` reservations through validation. Attachment count/bytes and journal segment/bytes are independently bounded; chain cycles, gaps, wrong generations/owners/roots, missing or malformed artifacts, changed-after-probe bytes, cancellation, memory refusal, and every injected store boundary remain distinct from resumable success. Stable reconciliation reasons retain optional parser/read-integrity evidence instead of discarding the underlying error.
- **Portability and isolation:** publication/recovery round-trips all five database hash profiles. The runtime remains injected-storage-only and has no `StorageEngine`, `DirectoryOps`, control-store, first-authority publisher, filesystem, task, timer, query, or service caller. Live v3 bytes and writers remain unchanged; P6-2c owns producer convergence, P6-2d owns concrete storage/startup/shutdown, and P6-3 alone owns immutable page/query activation.
- **Focused and affected proof:** 13 recovery tests and the 111-test combined coordinator/coverage/task/shadow/memory/error matrix pass. All 28 error-squelch architecture tests pass with the exact reviewed inventory unchanged at 1,457; no new suppression was allowlisted. `cargo check -j4 --workspace --all-targets`, formatting, and `git diff --check` pass; the check retains only the pre-existing unused `require_wasm_parser` test-macro warning. Strict library Clippy remains blocked by 111 historical diagnostics and reports no diagnostic in `index_coordinator_recovery.rs`; its log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2b-clippy.log`.
- **Broad proof:** the final-tree command `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp timeout 30m cargo test -j4 --workspace --all-targets -- --test-threads=4` passed 261 targets, 6,086 tests, and 7 intentionally ignored tests with zero failures. The complete log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2b-final-full-r2.log` with SHA-256 `15184be768209c5c13da80d63827daff1f4d8234b630b4158694e1b46282ec83`.
- **Next action:** map every parser, mapper, converter, delete, manual-index, reindex, whole-root, and post-authority producer into one coordinator task contract before replacing any live path. P6-2c must prove there is no second mutable index route and must preserve the hard-write acknowledgement boundary.

## P6-2c Producer Convergence

- **Territory:** production mutation ownership is split between synchronous `DirectoryOps` parser calls, post-commit metadata fanout, the standalone cleanup worker, task-local reindex buffering/config retirement, public `IndexManager` mutations, the server flush timer, and storage shutdown/spill. Query/search remain legacy readers until P6-3/P7 activation; P6-2c must converge writers without pretending those readers have already moved.
- **P6-2c-a task boundary:** `IndexProducerCoordinatorV1` admits only exact operation/publication/root/semantic/journal identities or root-pinned maintenance scope. It cannot carry a file body. Count and retained bytes are bounded before cloning and charged to the process-wide `Task` owner. One opaque lease owns execution; lease metadata is not duplicated, cancellation consumes neither task nor retry, and exact duplicate admission is idempotent while conflicting reuse fails closed.
- **P6-2c-b result boundary:** one bounded report carries sorted-unique per-owner ready, frozen-unindexable, retryable, or degraded outcomes. Scope catalogs, value stores, and field indexes retain distinct owner IDs, so per-field-index degradation never forces another record class to lie about ownership. Every ordered record is hash-bound, ownership-checked, and fully decoded before the first mutation admission, then enters `IndexCoordinatorV1` with the source operation/publication identity. Mixed success/retry retains successful idempotent records while scheduling the source task; exhaustion spills through an injected durable boundary, and spill refusal retains recoverable work.
- **TDD evidence:** the dedicated target first failed to compile because the producer module did not exist. Ten tests now cover invalid options/source identity, canonical root-pinned scopes, exact memory accounting, duplicate/conflicting admission, canonical leasing, foreign/stale leases, cancellation, mixed outcomes, malformed later records and spill receipts, frozen/operational reason registries, retry delay/exhaustion, admission pressure, spill failure, and all nine producer task kinds through the same lease path. Foreign or stale leases are validated before observing their clocks, and cancellation clears a valid lease before time validation, so untrusted completion timestamps cannot poison or strand the coordinator.
- **Architecture evidence:** the initial error-squelch gate identified five suppression-shaped conversion/default constructs in the new module. They were removed rather than allowlisted; the exact reviewed inventory remains unchanged. The task contract remains storage-neutral and does not activate v1 pages, query coverage, runtime timers, or live writers.
- **Focused and broad proof:** the exact affected producer/coordinator/recovery/coverage/reference/memory/error matrix passed 150 tests. `cargo check -j4 --workspace --all-targets`, Rust formatting, and `git diff --check` pass; the check retains only the pre-existing unused `require_wasm_parser` test-macro warning. The final-tree command `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp timeout 30m cargo test -j4 --workspace --all-targets -- --test-threads=4` passed 262 result groups, 6,096 tests, and seven intentional ignores with zero failures. The retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-ab-final-full-r3.log` with SHA-256 `b2323ea8ffb3694520ad662e65e6d9e6c0350028e3f21781832fd660604a7b43`.
- **P6-2c-c collector slice:** the new storage-neutral `IndexProducerCollectorV1` accepts only exact before/after namespace-root and record-revision references plus decoded scope/value/field definitions; file bodies stay behind an injected parser executor. Scope, canonical value, posting, and per-owner document-state records are emitted through the frozen v1 codecs. Replacement semantics emit the current live value row and tombstone only removed value ordinals, while posting changes tombstone only keys absent from the new compiled set. Scope reverse removal remains a publisher responsibility because the frozen reverse record has no tombstone form.
- **Bounded/failure direction:** definition decoding, path/glob matching, parser output, source extraction, converter output, posting comparison, owner validation, dispositions, and retained reports all reserve through the shared `Task` memory owner before allocation and retain those reservations for the data lifetime. Parser cancellation is terminal; dependency and host failures are typed retryable outcomes; deterministic parser, selector, mapper, canonical-value, and converter failures emit frozen state; malformed configuration degrades only its applicable owner and surfaces typed diagnostics. SHA-512 proof caught and removed a hard-coded BLAKE3 evidence hash before landing. The collector contains no `expect`/`unwrap`, broad ignored error, result-status probe, or Result-to-Option suppression.
- **Collector proof:** the target first failed to compile on the missing collector module. Eleven focused tests now cover exact metadata and parsed transitions, record replacement/tombstones, both hash widths, independent field degradation, source-byte limits, deterministic/retryable/cancelled parser outcomes, malformed evidence, invalid identities/paths/ordinals/duplicate owners, out-of-scope no-op behavior, memory pressure, and complete reservation release. The affected collector/coordinator/recovery/reference/pipeline/error matrix passes 161 tests. All 28 suppression-architecture tests pass with the reviewed inventory still exactly 1,457 entries; its only fixture changes are five line-number updates caused by moving the unchanged glob matcher into shared path utilities. Package lib/all-test checking passes with only the historical unused test macro warning. The all-target workspace gauntlet passes 263 result groups, 6,107 tests, and seven intentional ignores with zero failures; its retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-collector-full.log`, SHA-256 `9657233da257c4e887ce9cb849de5a84b7394646b1ceef5df38eeb7b1f0b495b`. Strict Clippy remains blocked by 112 pre-existing crate-wide diagnostics.
- **Leased executor boundary:** `IndexProducerExecutorV1` now binds one collector invocation to the producer coordinator's sole opaque lease. Before parser or index work it revalidates the exact retained operation, semantic-state root, before/after namespace roots, and canonical maintenance scope. Validation, cancellation, collector, mutation-admission, and completion failures clear only the matching lease while retaining the task for an explicit retry; a successful completion consumes it. The collector report's shared-memory reservation remains live through mutation admission and is released on every return path.
- **Executor proof:** the target first failed to compile on the missing executor module. Four focused tests cover exact completion, semantic/before/after/scope mismatch before collection, pre-collection cancellation, mutation-buffer pressure, collector memory refusal, task retention, lease cleanup, and reservation release. The combined producer executor/collector/coordinator/recovery matrix passes 51 tests, and all 28 error-squelch architecture checks pass without expanding the reviewed inventory. `cargo check -j4 -p aeordb --lib --tests`, formatting, and `git diff --check` pass; the check retains only the historical unused `require_wasm_parser` test-macro warning. The final-tree all-target workspace gauntlet passed 264 result groups, 6,111 tests, and seven intentional ignores with zero failures. Its retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-executor-workspace.log`.
- **Activation boundary:** the executor remains storage-neutral and has no live engine, directory, task, timer, query, or service caller. It therefore does not alter the durable-write acknowledgement path or justify a live HTTP test yet. Concrete journal source resolution and runtime ownership must exist before any synchronous v3 fanout is removed.
- **Deterministic journal admission:** the new storage-neutral admission adapter validates the complete decoded segment and every inter-batch namespace-root edge before queueing its first task, then derives one stable body-free mutation task from the full mutation identity plus batch ordinal. Exact replay is idempotent; count/byte pressure uses the same injected spill boundary; cancellation is checked before validation and between records, and a partially admitted cancellation resumes through deterministic duplicate recognition rather than losing work. All five database hash profiles derive nonzero distinct operation identities.
- **Admission proof:** the target first failed to compile on the missing adapter. Six focused tests cover exact per-source task creation, retry idempotence, bounded spill, discontinuous-root rejection before side effects, every hash profile, invalid hash width, pre-work cancellation, partial admission, and deterministic resume. The 72-test journal/task/producer/executor/error matrix passes. The error-squelch gate initially found two conversion-shaped constructs; both conversions were removed rather than allowlisted. Package lib/test checking passes with only the historical unused `require_wasm_parser` test-macro warning; formatting and `git diff --check` pass.
- **Newly surfaced convergence requirement:** one source revision may match multiple semantic scopes. A producer task cannot be consumed after collecting only one scope, and journal admission cannot enumerate scopes before loading the exact semantic root. The executor must therefore aggregate every applicable bounded scope under the same lease and completion before concrete source resolution or live activation.
- **Multi-scope convergence:** `IndexProducerCollectorV1` now owns one report builder and one shared-memory reservation across every applicable scope for a source transition. `max_scopes` joins the existing value-store, field, definition-byte, mutation, and report-byte bounds. Single-scope collection delegates to this same path; malformed scopes degrade their complete owner bundle through one helper; cross-scope duplicate owners, pressure, and cancellation fail without a second aggregation implementation. The executor consumes its lease only after the complete multi-scope report enters the mutation coordinator.
- **Multi-scope proof:** the initial overlap test failed because the first fixture intentionally covered only direct root children; replacing it with a genuinely overlapping direct-child scope proved eight distinct retained records across two scopes. Scope-limit and cross-bundle duplicate-owner failures release all report memory. The 86-test producer/collector/executor/coordinator/recovery/error matrix passes, as do package lib/test checking, formatting, and `git diff --check`; only the historical unused test macro warning remains.
- **Scope-local ordinal correction:** the first multi-scope shape incorrectly carried one document ordinal beside the shared revision transition. Child 05 freezes ordinals as scope-local identities, so each `IndexCollectorScopeWorkV1` now pairs its definition bundle with its own ordinal while the before/after FileRecord references remain shared. The overlap proof decodes emitted ScopeOrdinal records and requires distinct ordinals `3` and `9`; the same 86-test affected matrix and package check pass after the correction.
- **Exact source resolution:** one storage-neutral resolver rebinds the opaque lease to exactly one record in the retained journal by operation identity, journal head, semantic root, publication sequence, and before/after namespace roots. A second injected boundary loads only the named FileRecord revisions and verifies missing state, revision hash, FileRecord path, FileKey, and cancellation before exposing an owned transition with a borrowed collector view. Reader cancellation is terminal; retryable/corrupt reader evidence remains typed rather than becoming absence.
- **Source proof:** the target first failed to compile on the missing resolver. Five tests cover exact lease-to-record selection, exact before/after loading, wrong journal, missing revision, revision/path drift, cooperative cancellation, reader-class cancellation, and preserved retryable error evidence. The complete 90-test journal/task/source/producer/collector/executor/coordinator/error matrix passes; the post-review source/error subset passes 33 tests. Package lib/test checking, formatting, and `git diff --check` pass with only the historical unused test macro warning.
- **Semantic scope source contract:** retained semantic authority now resolves to either an explicit content-only state or one exact bounded complete scope set. Every scope carries the retained semantic root, its own nonzero scope-local document ordinal, immutable Scope/ValueStore/FieldIndex definition bytes, and derived owner identities. The resolver validates root identity, definition and parent-child closure, globally unique owners, count/byte limits, cancellation, and typed retryable/corrupt source failures before collector work. An unavailable catalog cannot become a successful empty scope set; a genuinely empty complete catalog must be reported explicitly by the concrete semantic source.
- **Semantic source proof:** the compile-red target failed on the absent contract before implementation. Twelve source tests cover exact multi-scope ownership and ordinals, explicit content-only state, outer and per-scope semantic-root mismatch, duplicate owners, zero ordinals, every aggregate count/byte bound, exact-fit bytes, malformed definitions, closure mismatch, invalid transition/root/options, cancellation before and during validation, and retryable/corrupt/cancelled source classes. The affected coordinator/recovery/admission/collector/executor/source matrix passes 70 tests and all 28 error-squelch architecture tests pass without an allowlist expansion. Package lib/test checking passes with only the historical unused test macro warning; formatting and `git diff --check` pass. Narrow Clippy reports no diagnostic in `index_producer_source.rs`; crate-wide Clippy remains blocked by 111 historical warnings and the pre-existing denied `never_loop` in `range_extract.rs`, retained at `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-semantic-source-clippy.log`.
- **Activation boundary:** the semantic source remains injected and storage-neutral. It does not yet traverse the concrete semantic catalog, allocate persisted scope ordinals, call the leased executor, or activate a live writer. Those behaviors must converge through one composition/runtime owner before synchronous v3 fanout can be removed.
- **Leased mutation-worker composition:** `IndexProducerMutationWorkerV1` now resolves exactly one retained mutation-journal record, verifies its before/after FileRecord revisions, resolves the complete semantic scope set, and enters the existing leased executor through one failure-aware adapter. Explicit content-only authority completes with no derived mutations and never enters the collector. Retryable revision or semantic reads use the producer coordinator's shared exponential backoff/exhaustion/spill path; cancellation and terminal source failures release only the matching lease while retaining the task. Task-level retry deadline overflow, spill refusal, malformed spill receipts, and cancellation all retain recoverable work without a stale lease.
- **Worker proof:** seven focused worker tests cover full source/scope/executor composition, explicit content-only completion, revision and semantic retry, terminal corruption, cancellation, mutation pressure, invalid options, reservation release, the disconnected activation boundary, and preservation of both source and spill evidence when retry coordination fails. Thirteen coordinator tests cover the shared task-level retry path, including zero delay, deadline overflow, exhaustion, valid spill, spill refusal, and malformed receipts. The affected producer/coordinator/recovery/error matrix passes 108 tests after the final overflow and combined-error guards. The exact-tree all-target workspace gauntlet passed 267 result groups, 6,140 tests, and seven intentional ignores with zero failures; its retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-worker-final-full.log`, SHA-256 `c3adb6b806e8509b84048769630946f168f6c99dbe7f25cdcff472e3501b10a2`.
- **Live-activation constraints:** the injected semantic source currently returns owned catalog/definition bytes after its temporary read budget can be dropped. A concrete source must reserve all retained semantic bytes before allocation and hold that reservation through worker consumption. Terminal source corruption must latch/pause the live runtime rather than immediately re-leasing the retained task, and durable retry spills must retain the typed source evidence. Scope-local ordinal lookup/allocation must be recovered from the selected shadow generation and checkpointed without reuse; it cannot be guessed from paths or shared across scopes.
- **Compatibility boundary:** live legacy indexing remains query-visible until P6-3 has durably published and selected a compatible immutable shadow generation. P6-2d may own and recover the shadow runtime, but removing the synchronous v3 path before that cutover would make current queries stale. The final architecture gate therefore removes legacy producer bypasses at the coordinated P6-3 cutover rather than during disconnected worker construction.
- **Frozen semantic reader prerequisite:** borrowed typed views now expose exact SemanticDefinition class/identity/payload, catalog-leaf bindings, and catalog-internal radix children from the already frozen `SemanticObjectV1` bytes. Iteration does not materialize a second record or child vector and delegates complete framing, CRC, identity, order, count, and kind validation to the existing canonical decoder. The focused target first failed to compile on the absent API, then passed five both-width golden, wrong-kind, CRC-corrupt, and truncated-input tests. The 135-test format/writer/semantic-store/transfer/error matrix and package lib/test checking pass; the only inventory edits are five line-number updates for unchanged reviewed `namespace.rs` invariants.
- **Retained semantic-read admission:** `IndexSemanticScopeSourceV1` now returns one opaque read guard that requires `MemoryOwner::Task`, verifies a conservative lower bound for every retained semantic root, owner identity, and encoded definition, and keeps that reservation live through validation, parser/collector execution, mutation admission, and task completion. Wrong-owner and under-reserved reads fail closed and release accounting immediately; the worker cannot accidentally consume uncharged owned catalog bytes. Thirteen source tests and eight worker tests prove constructor rejection, complete-lifetime accounting, parser-time retention, and release after completion. The 102-test source/worker/executor/collector/coordinator/admission/memory/error matrix passes, including all 28 error-squelch architecture checks. Workspace all-target qualification passes 268 result groups, 6,147 tests, and seven intentional ignores with zero failures; the retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-reservation-full.log`, SHA-256 `faf5078f487029078d3d35439713003ce7ff296169512b63b1dcb56f18cf9c8d`.
- **Operation-bound semantic read contract:** every semantic source call now receives the exact leased producer operation ID, retained transition, aggregate limits, and the worker's cooperative cancellation callback in one request value. There is no compatibility wrapper that can fabricate an operation identity. A compile-red source test proved the contract was absent; fourteen source tests and nine worker tests now prove exact forwarding and callback visibility, including an end-to-end lease-to-source identity assertion. Workspace all-target checking passes with only the historical unused `require_wasm_parser` test-macro warning. The contract remains internal and disconnected from service/runtime activation.
- **Shared scope membership authority:** `scope_matches_path` beside `ScopeDefinitionV1` is now the sole corrected-v1 owner for direct-child and owner-relative-glob membership. Both future semantic traversal and the collector use that contract; the collector retains only a typed error adapter. Root and non-root children, nested descendants, glob recursion, sibling-prefix boundaries, owner self-paths, noncanonical paths, and fabricated missing-glob invariants are covered. The 76-test v4 format target and 11-test collector target pass.
- **Bounded concrete semantic catalog:** `CatalogIndexSemanticScopeSourceV1` now reads the exact retained SemanticState from `V4SemanticObjectStore`, distinguishes explicit content-only authority, and traverses immutable Patricia catalog nodes with an explicit bounded stack. Three validated passes retain only applicable scopes, then their value stores, then their field indexes; no whole-catalog record, node, or definition set is materialized. Every pass revalidates root/node identity, parent depth/prefix/subtree counts, state record/node/dependency counts, definition class/identity/owner closure, aggregate limits, and cancellation under one retained `MemoryOwner::Task` reservation. Canonical catalog decoding now owns class-specific owner-key validation for every consumer instead of duplicating it in the index reader.
- **Scope-ordinal prerequisite:** one storage-neutral planner freezes create allocation, exact operation retry, same-scope update/move/delete preservation, cross-scope source/destination behavior, recovery/reindex reuse, delete-then-recreate non-reuse, checked high-water advance, cancellation, and corrupt-state refusal. The concrete source requests one ordinal per applicable scope with the exact operation ID, semantic root, transition, membership, and cancellation callback. This does not close the durable authority item: P6-2d must atomically persist the operation claim and high-water in selected shadow state before returning an ordinal.
- **Concrete source proof:** 25 focused tests cover content-only and explicit empty-complete results, complete and overlapping graphs, exact before/after membership, each aggregate count/byte limit and exact fit, admission before first read, retained/released accounting, cancellation during traversal, missing/ambiguous nodes, substituted/missing definitions, count disagreement, typed ordinal failures, and the full planner matrix. A disposable file-backed `StorageEngine` publishes every independent graph object through `V4SemanticObjectStore`, resolves it through the production adapter, and shuts down cleanly. Seven canonical reader tests include checksum-valid owner-shape attacks at both hash widths. The affected 178-test matrix and all 28 error-squelch architecture checks pass; workspace all-target checking passes with only the historical unused `require_wasm_parser` test macro warning. The exact suppression inventory did not grow; ten reviewed line locations were refreshed after source movement. Strict library Clippy remains blocked by 112 historical diagnostics and reports none in either changed production module. The final-tree all-target workspace gauntlet passes 269 result groups, 6,177 tests, and seven intentional ignores with zero failures; its retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2c-semantic-catalog-full.log`, SHA-256 `c18bd039639df2bf41e4fc1cc4a834b6620f68e6d8576dc55cdc60d44edb2cfb`.
- **Activation boundary:** the concrete object reader is read-only and the architecture gate permits exactly that one disconnected `V4SemanticObjectStore` caller while forbidding publication. No service, live writer, query, timer, task runtime, or production activation reaches this source. Legacy query-visible indexing remains authoritative until the P6-3 shadow cutover.
- **Next action:** implement the selected-shadow durable scope-ordinal claim authority, then give one P6-2d runtime owner the soft queue, journal/checkpoint store, task coordinator, mutation coordinator, retry/degraded latch, timer, pressure, spill, startup, metrics, and shutdown lifecycle before any live producer activation.

## P6-2d Durable Scope-Ordinal Authority Entry

- **Ratification:** the owner ratified the recommended selected-shadow ordinal decisions on 2026-08-13. The authority must persist every operation claim before returning, bind retries to the exact transition, preserve nonreuse through a scope-local checked high-water, and serialize concurrent allocation through selector-last compare-and-swap. No in-memory production allocation map is permitted.
- **Landing split:** P6-2d-a freezes the storage-neutral selected-state/CAS contract and proves allocation, preservation, exact retry, operation-ID conflict, concurrent selector conflict, cancellation, stale authority, and store-failure behavior. P6-2d-b binds it to immutable checkpoint bytes, selected scope manifests, ordinal/reverse directory reads, and a concrete synced storage adapter. Only that complete binding closes the durable-authority TODO.
- **Entry commit:** `8884730`; branch `development`; legacy v3 indexing and every service/runtime route remain authoritative and unchanged.
- **Central falsifier:** two new operations that race on one selected scope generation must never receive the same ordinal; a crash or commit-unknown failure must never allow the caller to observe an unproven allocation.
- **P6-2d-a implementation:** `DurableIndexScopeOrdinalAuthorityV1` validates one exact semantic-root/scope/transition fingerprint, observes the selected shadow generation, delegates allocation semantics to the frozen planner, and publishes every new or preserved operation claim together with the checked scope high-water through one selector-last CAS contract before returning. Exact selected claims bypass publication only after their fingerprint, ordinal, high-water, and any present live reverse mapping agree. New claims are bounded, selector conflicts retry within a configured hard limit, and cancellation/store failures never become success.
- **P6-2d-a proof:** the target first failed to compile on the absent module. Fourteen focused tests cover durable-before-return allocation, exact retry, commit-unknown reopen, deterministic and true two-thread selector races, live-ordinal preservation, operation-ID/fingerprint conflict, claim/reverse disagreement, stale/malformed selected state, cancellation before and after observation, bounded conflict exhaustion, pending-claim pressure, every store error class, and malformed options/requests. The affected semantic source/producer/recovery/error-squelch matrix passes 114 tests. Workspace all-target checking passes with only the historical unused test macro warning. The final-tree all-target gauntlet passes 270 result groups, 6,191 tests, and seven intentional ignores with zero failures; retained log `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-ordinal-contract-full.log`, SHA-256 `aeaba219fe6fb67650b589508de55d36e3250f2405194a7f45d9f4199b4fda67`. Strict library Clippy remains blocked by the same 112 historical diagnostics and reports none in the new module.
- **Boundary:** the store is a contract only and has no production implementation or caller. It cannot allocate from an in-memory production map, mutate checkpoint bytes, read a selected manifest, or activate runtime/service behavior. P6-2d-b must freeze the bounded pending-claim payload, publish it through existing selector-last checkpoint durability, and resolve live mappings from that exact selected ScopeCatalog generation before this authority is usable.
- **Operator ratification (2026-08-13):** the selected `IndexTaskCheckpoint` remains the sole authority envelope; scope-ordinal pending claims use its typed `resume_key`, carry the source publication sequence needed for exact retirement, and add no parallel artifact or selector. The nested SORC v1 payload is capped at 8,192 strict operation-ID-ordered claims, relies on the enclosing immutable AIDX key/CRC instead of a duplicate checksum, and rejects any claim at or below its applied-through watermark.
- **P6-2d-b1 implementation:** the exact leased source publication sequence now reaches semantic resolution, scope-ordinal claim fingerprinting, durable claim observation, and publication. `ScopeOrdinalClaimResumeV1` freezes the ratified SORC v1 nested checkpoint payload for all five database hash profiles, including strict operation ordering, exact profile-dependent row width, a hard 8,192-claim bound, applied-through retirement validation, fallible allocation, and fail-closed malformed input behavior. Independent Blake3-256 and SHA-512 byte fixtures and the generated contract digest pin the persistent representation without adding a second checksum or artifact family.
- **P6-2d-b1 proof:** the focused producer/semantic/authority/codec matrix passes 67 tests; the error-squelch architecture gate passes 28 tests without allowlist growth; reference `check-contracts` and workspace all-target checking pass with only the historical unused test macro warning. The first cold all-target gauntlet reached its 30-minute command timeout after 81/82 `system_store_spec` cases had passed; the unfinished existing timer case then passed in an isolated 82/82 target run. A warm-cache uninterrupted all-target rerun passes 271 result groups, 6,196 tests, and seven intentional ignores with zero failures; retained log `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-scope-resume-full-rerun.log`, SHA-256 `739aa669b2e9cac85b31c78545686df3048c53b0dc6662e5e6584f303038c760`.
- **Boundary:** SORC bytes are frozen and carried end to end, but no production store yet reads them from or publishes them through the selected checkpoint. The next landing unit implements that composition adapter, validates the selected checkpoint/manifest/reverse closure under memory admission, and returns no ordinal until selector-last durability is proven.

## P6-2d-b2 Selected Checkpoint Store Adapter

- **Implementation:** `RecoveryIndexScopeOrdinalStateStoreV1` is one scope-specific composition over the existing `IndexRecoveryStoreV1`; it introduces no selector, task-ID derivation, or persistent format. Observation recovers and rechecks the exact selected checkpoint, validates the SORC watermark against the attached ScopeCatalog coverage, closes the exact ordinal/reverse roots and manifest aggregates, and resolves requested FileKeys through one bounded reverse-directory path. Publication preserves the checkpoint fields and attachments, appends one strict operation-ordered pending claim, advances the scope high-water in the exact successor checkpoint, and delegates dependency/checkpoint sync plus selector-last publication to the existing recovery owner.
- **Crash and failure direction:** stale selectors return `SelectionChanged`; an error after selector publication reopens the complete exact successor and reports committed only when its durable claim agrees byte-for-byte. Errors before selection leave the old root authoritative. Missing/corrupt artifacts, SORC/coverage disagreement, owner/generation/aggregate mismatch, duplicate claims, and malformed public requests fail closed. Cancellation, memory refusal, and store failures remain retryable. Root-to-leaf traversal follows one path, validates every parent descriptor exactly, relies on the frozen depth-15 directory cap, drops each directory before loading the next artifact, and releases all `IndexDirtyBuffers` reservations on every outcome.
- **TDD and focused proof:** the target first failed to compile because the adapter did not exist. Eight final tests use the independent Blake3-256 and SHA-512 scope directory/page fixtures plus a real frozen system journal/checkpoint graph. They prove exact live/missing FileKey lookup, selector-last order, durable claim/high-water bytes, restart observation, commit-unknown reopen without double publication, selector races, pre-selector failure, malformed SORC/watermarks, missing pages, exact child-generation closure, multi-level traversal, cancellation, memory pressure, store failure, constructor/request validation, and zero retained reservation bytes. A final adversarial falsifier proved a non-advancing requested high-water reached storage before rejection; boundary validation now rejects it before I/O.
- **Affected/static proof:** the adapter/authority/SORC/recovery/shadow matrix passes 45 tests, all 28 error-squelch architecture tests pass without allowlist expansion, workspace all-target checking passes with only the historical unused test macro warning, and the 436-fixture/95-route/38-document contract gate passes. That gate also caught two stale SHA fields left by the preceding SORC registry update; the report digests now bind the current format registry and generated contract again. Crate-wide Clippy remains blocked by the pre-existing denied `never_loop` in `range_extract.rs` and reports the same 112 historical warnings; after grouping snapshot inputs into one internal request value, it reports no diagnostic in the new adapter.
- **Broad proof:** the pre-adversarial exact-source all-target workspace run passed 272 result groups, 6,204 tests, and seven intentional ignores with zero failures; retained log `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-scope-store-full.log`, SHA-256 `7e69c9fedf3e2afc19d6a95d520c773474a63db0d75c06e8ee6ce0fea5602747`. After boundary-validation hardening, the first cold final run reached its 30-minute command timeout after 175 green result groups because all test binaries relinked and `disk_kv_store_spec` alone consumed 226.69 seconds; it produced no failure. The uninterrupted warm-cache rerun passed all 272 result groups, 6,204 tests, and seven intentional ignores with zero failures. A final exact-source run after the Clippy-driven internal request refactor also passed all 272 groups, 6,204 tests, and seven ignores with zero failures. The authoritative retained log is `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-scope-store-land-full.log`, SHA-256 `eafd74f53affacfd9629ed0350cb312ee6b4a59c708cdae757465b90209e2107`.
- **Activation boundary:** the adapter is a production implementation of the scope-ordinal state-store contract over an injected recovery store, but no service, live writer, query, timer, or production runtime constructs it yet. It cannot be exercised through the CLI/API without violating the ratified activation sequence. The next slice gives one recovered runtime owner the per-scope adapters, soft queue, coordinator, retry/degraded latch, timer, pressure, spill, metrics, startup, and shutdown lifecycle while legacy query-visible indexing remains authoritative until P6-3 cutover.

## P6-2d-c Runtime Owner Entry

- **Territory correction:** selected checkpoint state is scope-specific, while producer admission and ordered mutation buffering are process-wide. There is no concrete production `IndexRecoveryStoreV1` yet. Runtime activation is therefore split into: (1) one storage-neutral, fail-closed owner over the already proven soft hub, producer coordinator, mutation coordinator, worker, publication, degraded, and drain states; (2) one bounded concrete recovery store plus per-scope adapter registry; and (3) `StorageEngine` startup/timer/pressure/spill/metrics/shutdown installation. This preserves one selector and one runtime owner rather than allowing storage callbacks or per-scope workers to become competing authorities.
- **P6-2d-c1 central falsifier:** an unrecovered, degraded, draining, or stopped owner must not admit new producer work; a failed or abandoned immutable publication must retain every dirty mutation; and graceful stop must remain impossible while a task is queued/leased or a mutation batch is active/frozen.
- **Compatibility boundary:** P6-2d-c may build and recover only shadow state. Existing v3 indexing stays query-visible and its synchronous post-commit compatibility path remains until P6-3 proves immutable coverage and exact fallback at the coordinated cutover.

## P6-2d-c1 Fail-Closed Runtime Owner

- **Implementation:** `IndexRuntimeOwnerV1` is the single storage-neutral lifecycle owner for one soft mutation hub, producer coordinator, mutation coordinator, producer worker, immutable-batch publisher, retry state, degraded latch, and drain state. Recovery admits content-only authority explicitly; every other start requires recovered state before producer or journal work is accepted. `Recovering`, `Running`, `Degraded`, `Draining`, and `Stopped` transitions reject work outside their exact legal frontier.
- **Failure direction:** soft-queue loss, reconciliation evidence, worker/source/parser corruption, retry arithmetic overflow, publisher panic, malformed/dishonest/stale receipts, commit-unknown publication, local finalization failure, and failed exact-batch restoration all latch degradation. Retryable or canceled preselection publication restores the exact frozen batch. Publication I/O runs outside the owner state lock under one exact `(batch_id, attempt_id)` in-flight claim. Draining permanently closes soft admission before queue inspection, and stop is impossible while soft evidence, tasks, leases, active/frozen mutations, in-flight publication, or retry state remains.
- **TDD and focused proof:** two final falsifiers first failed with 16 passing tests: post-stop soft offers could be accepted into a queue with no consumer, and a worker retry-deadline overflow released its lease without degrading the owner. Linearized `SoftMutationHubV1::close_admission` plus fail-closed retry arithmetic corrected both. The final owner target passes 18 tests, including recovery, admission gating, soft loss, retry/cancellation, publisher error classes, dishonest receipts, panic containment, dirty-batch retention, concurrent publication exclusion, drain/stop, and memory accounting. The affected coverage/error/coordinator/worker/owner/namespace matrix passes 111 tests.
- **Static and architecture proof:** workspace all-target checking, formatting, and `git diff --check` pass; the only check warning is the historical unused `require_wasm_parser` test macro. The reviewed error-suppression inventory contains 1,469 occurrences, with all new runtime-owner authority catches classified `authority-state-failure`; its 28 architecture tests pass.
- **Broad proof:** `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp timeout 2400s cargo test -j 4 --workspace --all-targets -- --test-threads=4` passes 273 result groups, 6,222 tests, and seven intentional ignores with zero failures. Retained log `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-runtime-owner-final-hardened-full.log`, SHA-256 `f52265819691935bd0c90db7ba1072514abe090f8e9fc62a2aa1f1d56e654fd6`.
- **Activation boundary and next action:** the owner is disconnected and owns a private hub only for focused composition proof. P6-2d-c2 binds one concrete v4 recovery backend and bounded per-scope adapter registry. P6-2d-c3 must converge that owner with the existing `StorageEngine` hub rather than activate two hubs, and must expose cached/atomic health state so semantic/parser work under the current owner mutex cannot starve health or metrics. No live v3 writer, query, API, service, deployment, or persistent-format activation changed in c1.

## P6-2d-c2 Concrete Recovery Store Entry

- **Typed AIOP prerequisite:** the frozen IndexOperationControl body now has one typed, zero-copy reader/writer rather than offset parsing in each recovery caller. Operation kind/state and the shared `StableReasonV1`/`RetryClassV1` registries are closed enums; writer validation rejects wrong-width or zero required identities, noncanonical optional hashes, inverted time/counter closure, and allocation failure before common system-control framing. The common codec remains the sole header/CRC/body-identity owner.
- **Proof:** the target first failed to compile because the typed module was absent. Five focused tests round-trip every supported hash profile, exactly re-encode both independent-width golden fixtures, prove canonical optional hashes, reject wrong widths/times/counters, and reject another control family. The 153-test control/format/writer/platform/error matrix passes; the reviewed suppression inventory is exact at 1,473 entries. Workspace all-target checking passes with only the historical unused `require_wasm_parser` test macro warning.
- **Next action:** use typed AIOP A/B selection for `IndexRecoveryStoreV1`, then compose the bounded concrete store and per-scope registry. The store remains disconnected from live v3 and must not represent IndexArtifacts as namespace files or sidecars.

## P6-2d-c2a Native Immutable IndexArtifact Authority

- **Implementation:** `V4FirstAuthorityPublisher` now exposes one native v4 immutable IndexArtifact boundary: an allocation-free exact-length probe, a bounded exact loader, and an idempotent batch publisher. Artifacts are canonical `EntryTypeV4::IndexArtifact` system WholeEntities with KV tag 10; they are never namespace files or sidecars. One batch validates every AIDX identity first, flushes the exact KV baseline, reuses existing canonical entities, appends only missing entities, advances the header once, publishes all missing locators in one atomic KV visibility batch, and performs exact readback after authority publication.
- **Bounds and failure direction:** keys must equal the selected database hash width; locators must remain within the frozen 16 MiB artifact/entity cap, selected durable hot tail, and physical file. Length probes allocate no artifact bytes. Loads reserve fallibly and compact the validated WholeEntity buffer in place instead of retaining a second full-value copy. Publication accepts at most 4,097 artifacts and 64 MiB of encoded values, rejects duplicate/fabricated identities and wrong KV roles, and treats an all-existing batch as exact idempotence without consuming a header sequence. A mixed retry consumes one write sequence and one entry count only for each missing entity.
- **TDD and affected proof:** the target first failed to compile on the absent exact-length probe. Seven focused tests prove native publish/load/reopen, exact idempotence, mixed existing/new retry, malformed keys and expected lengths, absent/degraded authority, wrong database, count bounds, duplicate/fabricated identity, wrong-role collision, and no authority mutation on refusal. A six-point dependency fault matrix interrupts both entities before write, after write, and after hidden KV staging; every case reopens on the prior header with no visible locator and retries successfully across orphan bytes. All 89 first-authority crash/publication tests pass. Workspace all-target checking passes with only the historical unused `require_wasm_parser` test macro warning, and all 28 error-squelch architecture tests pass without expanding the exact reviewed inventory.
- **Broad proof:** the exact-source workspace/all-target run passes 274 result groups, 6,234 tests, and seven intentional ignores with zero failures. Retained log `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-native-index-artifact-full.log`, SHA-256 `fdb1972f461177f154f95bca9b4018061fa29b3fb168f36c52184f905688c03e`.
- **Activation boundary and next action:** no `StorageEngine`, service, route, live v3 writer, query, timer, or destructive caller can reach this physical boundary yet. P6-2d-c2b must publish selected checkpoint roots through typed IndexOperationControl A/B authority with durable replacement lineage before the concrete recovery store can implement selector-last publication.

## P6-2d-c2b Native IndexOperationControl Authority

- **Implementation:** `V4FirstAuthorityPublisher` now owns the one physical mutable selector for v4 index recovery checkpoints. Each operation identity is the canonical `index_id || operation_id`; its A/B controls are v1 system `FileRecord` entities backed by one canonical immutable system chunk. Selection delegates to the shared typed `discover_mutable_control` implementation, validates the selected `IndexOperationControlV1`, and exposes selector-last compare-and-swap over the exact prior control sequence plus checkpoint-artifact identity. Exact post-commit retries are idempotent and consume no new header sequence.
- **Crash and lineage direction:** the immutable chunk and target slot record publish in one native first-authority transaction. The common stable-entity dependency path checks each entity's exact prior locator, stages one atomic KV visibility batch, advances authority only after dependency durability, and surfaces post-selection failures as committed receipts. Replacing A or B prepares `PointerOrControlReplace` lineage immediately before activation; any failed activation discards its buffered soft record, while successful replacement hard-flushes retirement before returning. A new write sequence may be burned before failed preselection work, but neither the selector nor a partial locator becomes visible.
- **Validation and bounds:** physical reads verify KV role, system/no-compression WholeEntity framing, exact path and content type, empty metadata, one bounded chunk, canonical chunk identity, total size, and whole-file content hash before decoding the control. Publication rejects degraded or absent first authority, wrong database/index/operation identities, malformed or checkpoint-less bodies, stale expectations, non-successor sequences, timestamp overflow, locator collisions, and fallible-allocation failures. The shared dependency implementation now has truthful stable-entity names and diagnostics rather than leaking GC-specific vocabulary into index recovery.
- **TDD and fault proof:** the target first failed to compile on the absent AIOP publication types and methods. Five focused tests prove A/B/A selection, hard replacement lineage, restart selection, exact idempotence after reopen, malformed/wrong-owner/missing-checkpoint/stale-selector refusal without mutation, six dependency interruption prefixes across both entities, post-commit observer uncertainty, and failed third-slot replacement with zero retained soft lineage followed by same-owner retry.
- **Static and broad proof:** formatting and `git diff --check` pass; the error-squelch audit remains exact at 1,473 reviewed production occurrences. Workspace all-target checking passes with only the historical unused `require_wasm_parser` warning. The final exact-source workspace run passes 274 result groups, 6,239 tests, and seven intentional ignores with zero failures; retained log `/home/wyatt/.cache/codex/aeordb-tests/p6-2d-aiop-land-full.log`, SHA-256 `b2c2eaa9fe5e4c6d8d5da11f127c86244c9f79ef00ea08bab291aea299b6e2eb`.
- **Activation boundary and next action:** no `StorageEngine`, service, route, live v3 writer, query, timer, or destructive caller constructs this publisher path. The next slice implements the concrete `IndexRecoveryStoreV1` over native IndexArtifacts plus this AIOP selector and composes a bounded, evictable per-scope adapter registry under the shared `MemoryCoordinator`; only P6-2d-c3 may install that recovered owner into engine lifecycle.
