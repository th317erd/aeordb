# Child 05 Progress: Index

- **Status:** active
- **Current landing unit:** P5-7
- **Entry commit:** `5512b72`
- **Last green commit:** `8d58d0c`
- **Owner:** Codex, mutation/checkpoint codecs and unpublished attachment rooting
- **Start gate:** satisfied; P5-6 sparse NVT build, lookup, fallback, healing, and qualification are green
- **Plan:** [Child 05](../children/05-index-definitions-pages-and-nvt.md)
- **Owned files:** focused `engine/v4/index_task.rs` codec/closure ownership, a dedicated `index_v1_task_spec`, narrowly required independent task fixtures/reference helpers, `TODO.md`, and this progress ledger
- **Forbidden/hotspot files:** no v1 active-pointer publication, query/API activation, namespace/root authority, hard commit waiters, GC sweep/Void policy, v3 source mutation, or KV ordering/page-layout changes
- **Hotspot handoff commit:** none
- **Narrow gate:** a dedicated `index_v1_task_spec` will independently prove exact mutation/journal/checkpoint writers and unpublished attachment closure; `index_v1_reference_spec`, `v4_format_fixture_spec`, `v4_writer_fixture_spec`, and `v4_reader_hardening_spec` remain mandatory adjacent gates
- **Broad gate:** 6,015 tests across 255 result groups, zero failures, seven intentional ignores
- **Drift/risks:** P5-6 is closed at `8d58d0c`. P5-7 may add exact immutable mutation/journal/checkpoint writers and storage-neutral attachment-root closure, but it may not persist checkpoints, publish/select active pointers, connect task/query/API consumers, delete/reuse WAL extents, mutate v3 source state, change KV ordering/page layout, or cross the P4-9 activation gate. P5 remains shadow-only.
- **Evidence:** 2026-08-10 territory inventory retained at `/home/wyatt/.cache/codex/aeordb-tests/logs/p5-1-nvt-territory.txt`. Combined pre-edit characterization passed 176 tests across `nvt_spec`, `nvt_ops_spec`, `kv_store_spec`, `kv_snapshot_spec`, `disk_kv_store_spec`, and `index_store_spec`. An initial run without the required home-backed `TMPDIR` hit tmpfs `ENOSPC`; the exact home-backed rerun passed, proving an environmental false start rather than a code regression.
- **P5-1 proof:** `index_v1_reference_spec` first failed to compile because the three explicit owners did not exist, then passed all five independent ownership and complete-object byte fixtures. The affected NVT/KV/v0 field-index matrix passes 227 tests. Workspace all-target checking, Rust formatting, diff hygiene, the 436-fixture/95-route/38-document contract gate, and all 28 error-squelch architecture tests pass. The suppression inventory remains exactly 1,477 reviewed occurrences; the moved converter clone invariant retains its prior reviewed classification. Strict library Clippy remains exactly 110 historical diagnostics and reports none in the new ownership modules; retained log SHA-256 is `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`.
- **P5-1 broad and live proof:** the complete package/all-target gate passes 5,644 tests across 232 result groups with zero failures; retained log SHA-256 is `2edce8f3d9cb1d2b0459b7acb23de8e2ace205754df404c601b599ee9fb8b864`. A fresh auth-disabled CLI database at `/home/wyatt/.cache/codex/aeordb-tests/real-world/p5-1-20260812-1/live.aeordb` passed text/JSON writes, v0 `@filename` search, graceful shutdown, restart, exact file reads, repeated search, and offline verification. Verification found 198 valid entries with zero corrupt hashes/headers, missing/dangling/unlisted entries, B-tree issues, invalid offsets, or invalid voids. HTTP and verify log SHA-256 values are `08eb44aa4f70c1a926468740ba4dba6bf11155a085b8ee99fb3fc48eb6299eca` and `b0cedc386825edaf90fddbafc7fb5569443134afa8240d1490d0c0ecfc1e5916`. Production source contains no `NormalizedVectorTable` consumer outside the compatibility facade/re-export, no v1 field-index code uses a compatibility NVT, and no active v1 index pointer was introduced.
- **P5-1 landing:** committed and pushed as `3b4ad09`; all byte-identity, affected, broad, static, architecture, Clippy-baseline, and real-world gates recorded above remain green.
- **P5-2 territory:** corrected/v0 converter and strategy identities are generated from `spec/semantics/v1` into `v4::contract_generated`; ACNV/AFIX decoders currently keep their registry lookup private; SourceSelectorV1 and ValueStoreDefinitionV1 already enforce frozen source/dependency closure; legacy `scalar_converter`, `source_resolver`, `index_config`, and route-level literal byte conversion remain v0-only behavior and are forbidden as corrected v1 authority.
- **P5-2a runtime proof:** one shared permanent registry now closes all 25 converter rows, 12 strategy rows, source-selector identities, virtual metadata identities/types, capability masks, operation masks, and converter/strategy compatibility. Public runtime constructors re-decode complete ACNV/AFIX/ValueStore bytes. Corrected typed exact, bytes, UTF-8, `u64`, `i64`, finite `f64`, timestamp, and bool converters own canonicalization, comparison, fixed coordinates, exact recheck, and identical source/query compilation. Metadata reads the stored complete content hash at the selected database hash width. JSON-path traversal is iterative, bounded, cancellation-aware, order-preserving, duplicate-preserving, and all-or-nothing. Plugin mapper absence/host failure/cancellation remain operational while deterministic rejections remain document state. The executable definition closure binds exact ValueStore identity, semantic family, strategy operations, converter semantics, and aggregate limits. No v1 runtime has a production consumer or imports a legacy/KV/v0 NVT owner.
- **P5-2a focused and affected proof:** `index_v1_reference_spec` passes 26 tests covering registry closure, independent scalar boundaries/coordinates/comparators, malformed keys, exact collision recheck, coercion, hash-width selection, JSON traversal/regex/limits/cancellation, plugin outcomes, public-constructor corruption, definition identity, and aggregate limits. The 13-target affected matrix passes with zero failures; the only warning is the historical unused `require_wasm_parser` macro. Final focused and affected logs have SHA-256 `0883584cb4c886802b65f4c6aa2092cfb11b5f3cf4e33ca64111797c6f54d5e5` and `19092648fc11d2d8a39805cf382ef4bcfdb61ca3b0d2a65cb1d63f209c31930b`.
- **P5-2a broad/static proof:** the complete workspace/all-target suite passes 5,945 tests across 253 result groups with zero failures against the exact final source; retained log SHA-256 is `860308a10ade67869508a7620610dca179e19dee0d3d5a090d8f5cefaf21cfed`. Workspace/all-target checking passes. The 436-fixture/95-route/38-document contract gate passes. All 28 error-squelch architecture tests pass and the exact reviewed suppression ceiling shrank from 1,477 to 1,476 after removing a legacy `unreachable!`. Strict library Clippy remains byte-identical to the P5-1 baseline at exactly 110 historical diagnostics, with no changed-file finding; log SHA-256 remains `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`. Formatting, diff hygiene, and the shadow-only/legacy-isolation architecture grep pass.
- **P5-2a real-world boundary:** this landing unit is a disconnected pure shadow runtime with no CLI, API, storage writer, query path, or active-pointer consumer, so a server exercise cannot execute it without violating the activation boundary. The complete P5-2 real-world/native proof remains open until the ratified token/migration semantics and shadow harness exist; no live-test completion is claimed for P5-2.
- **P5-2a landing:** committed as `90d8b31`; exact-source verification and the recorded focused, affected, broad, static, architecture, suppression, and Clippy-baseline gates are green.
- **P5-2b semantic proof:** the ratified posting classes, shared domain-separated token coordinate, and exact legacy-scalar-to-fixed-point mapping are now frozen in independently generated semantic bundles and all dependent ACNV/AFIX fixtures. `AeorTextFoldV1` is backed by a checked-in Unicode 17 table containing 1,488 mappings over 844 maximally coalesced alphanumeric ranges; its 36,564 bytes have internal BLAKE3 `9f1bdd82a6142ddc3824e125c28ab941de2ac9b98fd7eaffaa5b85a3f6f884d2` and SHA-256 `e7821d3fe6b5d69e18b3c7b8eed56e4fc70adafe7834c5d455b2c8883a1ef25d`. The corrected runtime and isolated migration-v0 adapter prove source/query parity, malformed-key rejection, bounded allocations, shuffled determinism, phonetic ASCII filtering, and exact IEEE scalar conversion without a float-to-integer cast.
- **P5-2b Linux proof:** `index_v1_reference_spec` passes 35 tests and the error-squelch architecture target passes 28. The complete workspace/all-target gate passes 5,956 tests across 253 result groups with zero failures; retained log SHA-256 is `8f12a5f1b3d0998d2dfc8f3934d37f1fb26fc4d1b5aa72364b16686852f96e29`. Workspace check, both formatting checks, diff hygiene, the 147-test independent reference tool, Unicode-table verification, and the 436-fixture/95-route/38-document contract gate pass. Strict library Clippy is byte-identical to the P5-2a baseline at 110 historical diagnostics and no changed-file finding; log SHA-256 remains `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`. One broad rerun accidentally omitted the literal command-prefix `TMPDIR` and hit the 8 GiB GC scratch guard on the 7.7 GiB tmpfs; no source or test was changed, and the exact home-backed rerun passed all 5,956 tests.
- **P5-2b native and real-world proof:** an exact source archive with SHA-256 `2938c2d123fab45ee7b6314752b4ad3774ab05ea575607732d3898a5d7d33b9f` and lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de` passes all 35 focused tests, 147 independent-reference tests, and Unicode-table verification on native macOS arm64 and Windows x86_64 MSVC. Native log SHA-256 values are `f9c15cd61fa057cd4ef1fbd6e3ccde78006e551488b4553aecde5ac3ea848b9f` and `9d6779ef975c4aa7966829cf21f826e3866a88f12ba7d74deba7ee6f59de5876`. A disposable external shadow harness processed three real repository documents totaling 6,063 bytes and proved deterministic source/query equality for 3,834 Unicode postings, 345 Soundex postings, and 1,410 migration postings, including invalid binary behavior; retained log SHA-256 is `d68df7476993e01ee9bc1b6c830d6bc39b83cf266ba7ea520c2bcb35d5f9b31d`. All isolated native source/build caches and the local compiled harness were removed after proof.
- **P5-2b landing:** committed and pushed as `baaf789`. Architecture review finds no service, route, query, live v3 index, active-pointer, storage publication, or KV layout consumer for the new runtime.
- **P5-3 territory:** P0 readers already validate all four immutable manifest bodies and generic ordered-page records, while the generic immutable writer can reproduce an envelope from raw identity/body bytes. The missing P5-3 ownership is typed: `IndexManifestV1` exposes only raw body/definition plus a populated bit; scope ordinal/reverse, canonical-value, and deterministic-state records are parsed by private role-specific offset functions into a generic `OrderedRecordV1`; and no production typed body writer or exact manifest-chain closure validator exists. The independent reference generator already freezes empty/populated manifests and scope/value/state records at both hash widths. P5-3 will create one typed record codec owner and one typed manifest codec owner, make the existing generic reader delegate to them, and prove exact fixture bytes plus shallow correctness-reference closure. It will not encode page containers/directories, mutate COW generations, publish/select active pointers, or connect a query/service consumer.
- **P5-4 territory:** production owns a bounded zero-copy decoder for the common ArtifactDirectory and ordered-page envelopes, but only the independent reference generator can produce their typed bodies. The reader already validates role-specific identities, fences, record ordering, aggregate counts, physical-hint framing, and P5-3 record codecs; it does not expose PostingPage previous/next IDs, provide a typed page/directory writer, or provide streaming-safe adjacent-link validation. P5-4 will add typed fallible writers that derive outer fences and aggregates from validated entries/records, byte-match all 28 independent page/directory fixtures at both hash widths, expose posting links, and validate adjacent links without collecting a complete index. P5-4 will not follow directory children, mutate COW trees, allocate/retire PageIds, publish/select active pointers, implement NVT behavior, or connect query/API consumers.
- **P5-3 implementation:** `index_record` is now the sole typed owner for scope-ordinal, FileKey-reverse, canonical-value, and deterministic ValueStore/FieldIndex state records. Its bounded exact decoders reject trailing bytes; its fallible encoders validate nonzero ordinals and hash widths, canonical paths and derived FileKeys, unambiguous tombstone/payload state, canonical values/evidence, legal owner-specific stage/reason pairs, and the 1 MiB key / 4 KiB evidence caps. The generic ordered-page reader delegates those four roles to the same prefix decoders, deleting duplicate offset/semantic logic. `index_manifest` now owns borrowed typed ScopeCatalog, ValueStore, FieldIndex, and FieldNvt bodies, coverage versions, bounded exact readers, and bounded writers. `IndexManifestV1` exposes those typed details; one public typed manifest writer retains the existing immutable AIDX envelope as the sole physical owner and re-decodes its output. The generic pointer-or-manifest discriminator intentionally returns a compact manifest summary, while callers needing body semantics opt into `decode_index_manifest`, avoiding a fourfold enum-size regression. One shallow validator closes exact ScopeCatalog -> ValueStore -> FieldIndex references, byte-identical coverage, and embedded definition owner identities without following pages or selecting pointers.
- **P5-3 TDD and malformed proof:** the focused target first failed to compile on the absent typed record/manifest APIs and `details` model. The final focused target passes 41 tests. Every empty/populated manifest at Blake3-256 and SHA-512 round-trips to the independent bytes and immutable key; every scope/value/state record in both-width page fixtures round-trips exactly. Rejection proof covers zero generation/page/document identities, wrong hash widths, FileKey/path mismatch, unknown capabilities, root/count disagreement, trailing bytes, illegal reverse tombstones, ambiguous canonical-value tombstones, wrong state owner/stage/reason, oversized evidence, mixed manifest references, mixed coverage, and semantic owner mismatch. The 16-target adjacent legacy/current/v4 index matrix passes 453 tests with zero failures; retained log SHA-256 is `37beba1a8ecc26c8bd08170a1fc2df87568b9a39a0027a3e08e8ccb0c10d879f`.
- **P5-3 Linux and static proof:** the exact final production source passes 5,962 tests across 253 workspace/all-target result groups with zero failures and seven intentional ignores; retained log SHA-256 is `c0fc291b704131873e55f6354537d7ab532ca3f664adb1c3b6611704ca278332`. Workspace/all-target checking, both Rust formatting gates, diff hygiene, mdBook, the 436-fixture/95-route/38-document contract gate, and all 147 independent reference tests pass. The reference and contract log SHA-256 values are `93f819234f0a9372cd434c9a71ecc10b915ece173579b25b7276d91dd21f3a14` and `fc31ebde719c835acb310b9459e21dc8308f34b3e66d24575dad2a3f814f0260`. Removing two duplicate `Result -> Option` probes shrinks the exact reviewed error-squelch ceiling from 1,476 to 1,474; all 28 architecture tests pass with log SHA-256 `78e9e71083f4cdbac0859118e9c3023298b91404aa3c4baab04673fb900176ad`. Strict library Clippy remains byte-identical to the 110-diagnostic historical baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b` and reports no changed-file finding.
- **P5-3 native and real-world proof:** corrected exact source archive SHA-256 `5a7cc1f8928e986d34a0c4ff52e2333d63712728c3b0793f3e636d81d676ee80` with lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de` passes all 130 focused/fixture tests on native macOS arm64 and Windows x86_64 MSVC. Native log SHA-256 values are macOS `2ce90b648b5091b5dab96eb85dba6351d944877d4584db36947f7555405081a0`, Windows stdout `d8c50b52f8dc0c3900b370db243be4ebd8749b29cb78c42492ae7628a585f1ba`, and Windows stderr `bfa175ff00d961cbbafc378b1e8437749e8617b3bb01afa4793f5c36d0f87aa5`. The first macOS package attempt correctly failed only because `tar --exclude-vcs` removed the required `.gitattributes`; the corrected archive includes it, and isolated native trees were removed after proof. An external path-dependent shadow harness processed three real Markdown/TOML/JSON documents at both hash widths into 24 typed records, byte-matched all 16 manifests, closed four correctness chains, and rejected 24 malformed cases. It touched no database or active pointer; retained log SHA-256 is `4158504cc50d5315abbfa73bf9aa16f2c954b9a95e32333b5e8a4d784e7f95ee`.
- **P5-3 boundary review:** no service, route, `StorageEngine`, query, v3 live-index, active-pointer publication/selection, COW mutation, page-container writer, namespace/root authority, GC/Void, or KV layout caller references the new writers. No P5-3 codec imports the legacy/KV/v0 NVT owners. Multi-value source ordinals are exact fields and remain strictly page-ordered; whole-store/cross-page contiguity proof belongs to P5-4/P5-5 page and COW ownership rather than being falsely inferred from a standalone record.
- **P5-3 landing:** committed and pushed as `883b899`; the source, persistent fixtures, reviewed suppression inventory, focused/adjacent/broad/static/native/real-world proof, and shadow-only architecture boundary above are green.
- **P5-4 implementation:** `index_page` now owns typed fallible ArtifactDirectory leaf/internal writers, ordered-page writers, and the Posting record codec. Writers derive outer fences, counts, logical bytes, coordinate bounds, and directory PageId ranges from validated inputs; preflight the immutable AIDX 4 MiB cap before final allocation; and re-decode their output. The single public role registry closes owner class, child kind, key codec, and PageId policy. Posting readers expose previous/next PageIds, reject self-links, and validate one adjacent bidirectional link plus strict fence order without collecting a chain. Existing readers now preserve conversion evidence and return typed errors instead of reopening validated state with panics.
- **P5-4 TDD and malformed proof:** compile-red tests first named the absent writer/link/Posting ownership and public role registry. The final focused gate passes 163 tests across five targets. All 28 independent leaf/internal directory and page fixtures at Blake3-256 and SHA-512 round-trip byte-for-byte. Rejection proof covers wrong/zero owners, generations, roles, PageIds, links, record framing/order, duplicate and empty pages, all descriptor counts and generations, zero hashes, aggregate overflow, role/PageId disagreement, oversized artifacts, standalone Posting framing, and a middle descriptor whose oversized fence would otherwise allocate below the outer artifact cap. Retained focused log SHA-256 is `3463f7c3292d7e7eac30eed1a3dddeb22d71c42e94d896d589982ce9a67483b0`.
- **P5-4 Linux and static proof:** the exact final source passes 5,967 tests across 253 workspace/all-target result groups with zero failures and seven intentional ignores; retained log SHA-256 is `50859364fce70c3c001720ce2ac3728006cf5128bdb45d6cc34cf9d93d1cc083`. Workspace/all-target checking, both Rust formatting gates, diff hygiene, mdBook, all 147 independent-reference tests, and the 436-fixture/95-route/38-document contract gate pass. Contract log SHA-256 is `fc31ebde719c835acb310b9459e21dc8308f34b3e66d24575dad2a3f814f0260`. Removing duplicate conversion/panic suppression paths shrinks the exact reviewed error-squelch ceiling from 1,474 to 1,453; all 28 architecture tests pass. Strict library Clippy is again byte-identical to the 110-diagnostic historical baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b` and reports no P5-4 file.
- **P5-4 native and real-world proof:** exact source archive SHA-256 `90b28cc6e945ce1a76fa0d38d131ca152b3b0093cb85d6878fc9a022b4f66a16` with lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de` passes all 135 focused/fixture tests on native macOS arm64 and Windows x86_64 MSVC. Native log SHA-256 values are macOS `46493987bb972be3c8d4b63f51c557d5809af3a8950869a057aafcf8fd360fa6`, Windows stdout `cda2223b2062193915cce41566233d857e1d1be00fded6a95580d87cf21ae14f`, and Windows stderr `b6179f86b4fff0b939bd6fbc6f58805a0cd42084dca8b7bf95356a3e1661ffdd`. A disposable external shadow harness processed three real Markdown/TOML/JSON documents totaling 389,836 bytes into 96 Posting records, six linked pages, and two leaf directories across both hash widths; it also rejected corrupt pages, detached links, and oversized keys. Retained log SHA-256 is `f716835fa9ebadae3fe64b7eb0a1ac05dbf32aba0db3d9b3fc12d5ca3bcff466`. Isolated native source/build artifacts and the separate reference target were removed after proof.
- **P5-4 boundary and landing:** no service, route, `StorageEngine`, query, live v3 index, active-pointer publication/selection, COW mutation, namespace/root authority, GC/Void, KV layout, or legacy/KV/v0 NVT consumer references the new writers. Directory writers do not follow children, and adjacent-link validation consumes exactly two decoded pages. Source and tests were committed and pushed as `5512b72`.
- **P5-5 territory:** P5-4 is the sole typed page/directory codec owner. V0 `FieldIndex` and `FieldIndexV0Nvt` mutation remains a forbidden compatibility path. P6 will own storage publication, journals, manifests, retirement admission, and active-pointer selection; P7 will own query traversal. P5-5 therefore owns one pure, storage-neutral, shadow-only COW planner over caller-supplied immutable path artifacts. A mutation may materialize only the target page, its immediate posting neighbors when links change, and the bounded affected directory paths for those replacements; shared ancestors must be deduplicated. Outputs are dependency-ordered immutable artifacts plus root/count/PageId/retirement deltas; they are not durable or authoritative. Physical hints on rewritten descriptors are cleared. Same-owner unchanged children may retain older birth generations, while every rewritten page/directory uses the requested new generation. Page IDs never wrap or reuse: rewrite retains, split retains left and allocates fresh right, merge retains the lower and reports the upper retired. Local compaction preserves tombstones unless an explicit proof-bearing authority supplied by the later coverage owner permits dropping them. Missing/corrupt children fail the derived closure; the planner never skips them or reaches v0/KV/NVT compatibility APIs.
- **P5-5 execution slices:** P5-5a freezes the planner/model/allocator; P5-5b lands one-page mutation/split/neighbor rewrites; P5-5c lands recursive directory COW; P5-5d lands merge/compaction/tombstone proof; P5-5e lands closure/property/resource proof; P5-5f completes broad/native/real-world evidence.
- **P5-5a/b red-to-green proof:** `index_v1_cow_spec` first failed to compile because the storage-neutral planner did not exist. The initial five-case implementation exposed a page-size fixture mistake, then passed. Adversarial review added three independently falsifying tests: aggregate peak workspace accounting, partitioning an over-hard-cap candidate before writer validation, and PageId high-water coverage of the rewritten neighbor's outward link; all three failed before correction and now pass. The 10-case target covers both hash widths, deterministic immutable input, idempotent no-op, exact-key insertion/replacement/tombstone rejection, stable-left/fresh-right split identity, bidirectional next-neighbor relinking, nonwrapping allocation, malformed generation/neighbor authority, output amplification, and aggregate peak bounds. The planner retains v1 page identity on rewrite, allocates only fresh right PageIds, emits pages before neighbor replacements, and has no storage/publication/v0/KV/NVT dependency.
- **P5-5a/b broad/static proof:** the exact final source passes 5,977 tests across 254 workspace/all-target result groups with zero failures and seven intentional ignores; retained log SHA-256 is `ec6cfa8cac93a180728bebdc684e4f3277012f043a6f4509f1bc6a0cb1d20ef1`. The 145-test COW/reference/format/writer/reader matrix, workspace/all-target checking, Rust formatting, diff hygiene, all 28 error-squelch architecture tests, and the 436-fixture/95-route/38-document contract gate pass. Artifact preflight now shares a representable-length formula so the planner can partition before the immutable writer enforces its 4 MiB cap. The exact reviewed error-squelch inventory remains at its prior ceiling; five unchanged `index_artifact` entries received line-only refreshes, and the new fallible record reservation preserves its allocator failure evidence instead of suppressing it.
- **P5-5c implementation:** the page plan now retains an exact bounded source summary for each replacement. The storage-neutral directory planner consumes one explicit root-to-leaf path per replaced page, validates every source page/leaf and directory/parent descriptor exactly, deduplicates shared ancestors, and rewrites only affected nodes bottom-up. Rewritten descriptors clear all physical hints. Directories target 64 KiB, recursively split through level 15, grow a new root when required, retain dependency order, and cap the complete operation at 64 MiB with no more than three affected paths. Unrelated sibling artifacts are never loaded. The planner remains disconnected from storage, publication, manifests, active pointers, queries, v0 indexes, KV, and every NVT owner.
- **P5-5c contract correction:** ordered pages already permit nonempty tombstone-only pages and define logical bytes as exact live record bytes, but both directory validators incorrectly rejected their zero logical-byte summaries. Production and independent-reference codecs now require zero logical bytes exactly when live count is zero, preserving malformed-summary rejection while making retained tombstones representable.
- **P5-5c focused proof:** compile-red tests first named the absent directory planner. The final 15-case COW target covers both hash widths, exact source/path closure, cross-leaf neighbor replacement, shared-root deduplication, reversed-path determinism, cleared hints, recursive leaf and parent splits, level-2 root growth, synthetic unloaded siblings, tombstone-only summaries, and missing/corrupt/forged/cross-owner rejection. The independent reference oracle passes all 148 tests, including tombstone-only page/directory parity at both hash widths. The combined COW/reference/format/writer/reader matrix passes 150 tests, and all 28 error-squelch architecture tests remain green.
- **P5-5c broad/static proof:** the exact final source passes 5,982 tests across 254 workspace/all-target result groups with zero failures and seven intentional ignores; retained log SHA-256 is `8c3a59e0d49ad6377080474f3ae12a3fe8423e816db076bcb6bf8a6fa408e1a4`. Workspace/all-target checking, Rust formatting, diff hygiene, the 150-test COW/reference/format/writer/reader matrix, all 28 error-squelch architecture tests, all 148 independent-reference tests, and the 436-fixture/95-route/38-document contract gate pass. The final isolation grep finds no storage, service, query, active-pointer, legacy/v0, KV, or NVT dependency in the COW planner.
- **P5-5d implementation:** the pure planner now accepts exactly one or two immutable source pages plus only the outward Posting neighbors required by a link change. Eligible pairs merge into one target-sized page while retaining the numerically lower stable PageId, sorting every permanently retired PageId, allocating no identity, and rewriting only changed outward links. One-page compaction is a no-op unless exact tombstone removal changes it; a fully removed page/window is represented by an empty replacement and propagates through bounded directory COW to an explicit absent root. Empty middle windows can retire two pages and relink both outward neighbors, so the frozen affected-path cap is four rather than three.
- **P5-5d tombstone authority:** tombstones remain present by default. Removal requires an explicit proof bound to the exact ordered immutable source-page keys and owner, a nonzero coverage epoch and covered sequence, a journal contiguous at least through that sequence, and a pin-safe generation range covering every source while remaining below the new generation. Detached, reordered, incomplete, stale, or cross-owner proof fails closed before output construction.
- **P5-5d proof:** the 24-case focused COW target covers both lower-ID orientations, both outward relinks, unchanged-window eligibility, retained and fully removed tombstones, exact proof rejection, corrupt/detached/missing windows and neighbors, stale generations/PageId high water, deterministic repeated output, four-path directory retirement, absent-root propagation, and aggregate peak workspace accounting. The exact 159-test COW/reference/format/writer/reader matrix, all 148 independent-reference tests, all 28 error-squelch architecture tests, workspace/all-target checking, both formatting gates, diff hygiene, and the 436-fixture/95-route/38-document contract gate pass. The suppression audit reviews exactly 1,453 production occurrences. The complete workspace/all-target suite exits zero with seven intentional crash-injection ignores; its exact test inventory contains 5,995 tests across the unchanged 254 result groups, with inventory SHA-256 `8ffa78db08c147f5e2f32fba6f254d268c789870935c4ffd296729f92203e4c8`. The final isolation grep finds no storage, service, query, active-pointer, legacy/v0, KV, Void/GC, or NVT dependency in the compactor.
- **P5-5e closure implementation:** one storage-neutral `IndexCopyOnWriteClosureRequestV1` now binds the exact supplied immutable source pages, source-to-replacement order, PageId high-water and contiguous allocation range, permanent retirement set, stable-ID survival, posting links, directory paths, dependency-ordered immutable outputs, cleared physical hints, exact root aggregates, owner/role/generation, and complete retained encoded-byte accounting. Its success summary exposes the exact root/count/PageId/resource facts that P6 publication admission will need without granting storage or pointer authority. Mutated, duplicate, detached, out-of-order, stale, cross-owner, counter-inconsistent, and over-cap plans fail closed before they can be consumed.
- **P5-5e adversarial correction:** deterministic evolving-model tests exposed a real recursive directory-root defect: when every legal child descriptor individually exceeded the 64 KiB target, each parent level retained the same child count and recursed until the depth cap. The planner now treats 64 KiB as a packing target, retries a nonreducing level against the frozen 4 MiB artifact cap, and fails explicitly if even the hard-cap grouping cannot reduce. No page format, directory format, KV layout, or active authority changed.
- **P5-5e proof:** the focused target passes 31 tests, including whole-plan success at both hash widths, non-PageId scope-page closure, exact byte summaries, malformed plan mutations, recursive dependency order, outward PageId high-water, aggregate path amplification, full retirement, and deterministic randomized insert/replace/tombstone/split/compact/merge sequences checked after every operation against an independent `BTreeSet` model. The exact COW/reference/format/writer/reader matrix passes 166 tests and the independent reference implementation passes all 148 tests. Workspace/all-target checking, Rust formatting, diff hygiene, mdBook, all 28 error-squelch architecture tests, the exact 1,453-occurrence suppression audit, and the independent 436-fixture/95-route/38-document contract gate pass. The exact final-source workspace/all-target run passes 5,998 tests across 254 result groups with zero failures and seven intentional ignores; retained log SHA-256 is `0337a8eff45bec7b3c88a045495fdef9875686d16b11f5df94fd1497099f8139`. Source isolation finds no storage, service, query, active-pointer, legacy/v0, KV, GC/Void, or NVT dependency.
- **P5-5f exact-source proof:** strict Clippy exposed three new style diagnostics in the COW module; behavior-preserving corrections landed as `fc788c0`, after which strict library Clippy returned to the byte-identical 110-diagnostic historical baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b` with no COW finding. The final 166-test affected matrix passes with log SHA-256 `48d8d9d0508616c6f74e590f685dc650473caca7c6235297eac3bc07f9675d3c`. The complete workspace/all-target suite passes 5,998 tests across 254 result groups with zero failures and seven intentional ignores; retained log SHA-256 is `06f29920ffd0b2c6d7902bf5f7530e91a9d2027ea176c27a82c58a49d9dafd08`. Workspace/all-target checking, Rust formatting, diff hygiene, mdBook, all 148 independent-reference tests, all 28 error-squelch architecture tests, the exact 1,453-occurrence suppression audit, and the 436-fixture/95-route/38-document contract gate pass.
- **P5-5f native and real-world proof:** exact `fc788c0` source archive SHA-256 `8d1d40225ea580e6f8758788b9bd45999aaec67e560085d9c29a4536c3abc98d` with lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de` passes all 166 tests on native macOS arm64 and Windows x86_64 MSVC. Native log SHA-256 values are macOS `b5ee52256d6f0f0baa9f8cb71332f68add117309083b016905b07fbb02953aa2`, Windows stdout `447b9ddc4ac81f2f0c4cd0c763e4e1e70b3dedd9b67891fdab2c4dd0c2f25278`, and Windows stderr `085d4d32e20221f6c3db778cb4f7d7a53ed5a458080b7ba885c9cff46192806e`. A disposable external path-dependent harness processed three real Markdown/TOML/JSON documents totaling 389,836 bytes at both hash widths into 2,122 postings and ten replacement pages, closed exact root/count/allocation/resource authority, rejected forged root state, and produced byte-identical output on repeated runs; retained deterministic log SHA-256 is `d9427c8ad54b7b209a9dfd188f66446b2e9b771653463d89edf5e54b6fbd14f4`. Both native remote workspaces were removed after proof. P5-5 never acquired storage, publication, pointer, query, v0/KV/NVT, or destructive authority.
- **P5-6 territory:** P0 already owns a bounded zero-copy NVT tile decoder, fixed-point coordinate-to-cell mapping, fixture-level same-tile predecessor probe, typed FieldNvt manifests, and NvtTile ArtifactDirectory codecs. It does not own a production tile writer, sparse builder, cross-tile traversal, FieldNvt-to-FieldIndex basis closure, Posting-directory PageId/fence validation, exact directory predecessor fallback, or healing proposal. Corrected converters and Posting pages are the only valid producers. P6 storage/publication and P7 query traversal are future consumers; P5-6 therefore remains a pure caller-supplied planner. Missing/corrupt/stale NVT evidence degrades to the exact Posting directory, while corrupt correctness-path directories/pages fail closed. Legacy, KV, and v0 field-index NVT owners remain forbidden.
- **P5-6 execution slices:** P5-6a lands typed tile writing plus deterministic bounded sparse construction; P5-6b lands manifest/basis closure, cross-tile hint selection, pinned Posting-directory validation, and exact predecessor fallback; P5-6c lands nonblocking healing proposals and randomized equivalence/resource proof; P5-6d completes broad/static/native/real-world qualification.
- **P5-6a implementation and proof:** one typed writer now derives exact `NvtTileV1` bodies and immutable identities through the shared AIDX envelope owner, re-decodes every result, and byte-matches both independent hash-width fixtures. One deterministic builder consumes already ordered Posting-page summaries without sorting or PageId ordering, coalesces equal fixed-point cells, preserves sparse gaps, carries successor hints across tile boundaries, accepts the ratified 256/1,024/4,096-cell geometries, represents empty indexes without a fabricated tile, preflights exact encoded bytes, and enforces caller-supplied page/tile/output bounds with fallible allocation. Seven focused tests cover exact bytes, both hash widths, geometry, equal-cell/gap behavior, empty state, malformed identities/order/ranges, aggregate overflow, and every resource ceiling; the final five-target NVT/reference/format/writer/reader matrix passes 142 tests. The exact final source passes 6,005 workspace/all-target tests across 255 result groups with zero failures and seven intentional ignores; retained log SHA-256 is `4ad39be1f1119786279b2311a65c8f74ed397cfb138cc8586bd6ae6ce0ad5b14`. Workspace/all-target checking, Rust formatting, mdBook, all 148 independent-reference tests, all 28 suppression-architecture tests, the exact 1,453-occurrence reviewed inventory, and the 436-fixture/95-route/38-document contract gate pass. Strict library Clippy remains byte-identical to the frozen 110-diagnostic baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`; the two initially suppression-shaped optional-ID defaults were replaced with explicit serializer branches rather than admitted. P5-6a remains storage-neutral and introduces no publication, pointer selection, query, legacy/v0/KV NVT, GC/Void, or destructive authority.
- **P5-6b implementation:** a pinned FieldIndex now retains its exact immutable manifest key, owner, generation, source head, Posting-directory root, endpoint IDs, PageId high-water, and aggregate counts. A usable FieldNvt must close to that owner, Posting generation, and source head. Bounded sparse lookup validates complete NVT directory paths, scans the target tile and caller-supplied prior nonempty tiles by exact little-endian tile fence, and preserves corruption or resource diagnostics in an explicit fallback rather than treating them as authoritative query failure. Hinted PageIds are accepted only after a complete root-to-leaf walk of the pinned Posting directory and exact immutable page-hash, owner, role, generation, aggregate, endpoint, and lower-fence validation. Missing, stale, forward, corrupt, or resource-limited hints fall back to the exact Posting directory predecessor; corruption in that authoritative directory/page path fails closed.
- **P5-6b proof:** thirteen focused tests cover both hash widths, matching and stale basis closure, sparse cross-tile predecessor discovery, exact hint/fallback equivalence including page artifact identity, corrupt NVT diagnostic preservation, missing/stale/forward hints, empty indexes, two-level directory closure, detached paths, authoritative directory/page corruption, and every lookup ceiling. The final NVT/reference/format/writer/reader matrix passes 148 tests, and the independent reference implementation passes all 148 tests. Workspace/all-target checking, the 436-fixture/95-route/38-document contract gate, mdBook, Rust formatting, diff hygiene, all 28 error-squelch architecture tests, and the exact 1,453-occurrence reviewed suppression inventory pass. The exact-source workspace/all-target suite passes 6,011 tests across 255 result groups with zero failures and seven intentional ignores; retained log SHA-256 is `4cfc6505432e98b8c8c4ce1119497b395dc2bb7f0c4db8bc46157d1b28e7c043`. Strict library Clippy remains byte-identical to the frozen historical baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`, with no NVT finding. Source isolation finds no storage, service, query, active-pointer, legacy/v0, KV, GC/Void, WAL, filesystem, or asynchronous-runtime dependency; the exact Posting directory remains the sole correctness authority.
- **P5-6b landing:** committed and pushed as `3757260`; no active consumer or authority boundary changed.
- **P5-6c implementation:** one storage-neutral lookup completion request now makes the attempt state explicit: a validated NVT hint or an exact fallback with its preserved cause. A valid hinted page returns immediately without touching the exact path. Missing, incompatible, malformed, overflowing, or stale hints use the exact Posting directory. Only after exact fallback succeeds may the result carry one deterministic repair advisory binding the exact FieldIndex manifest, compatible observed FieldNvt manifest when available, owner, Posting generation, source head, target coordinate, exact page ID/generation/artifact hash, fallback reason, and bounded diagnostic class/code. Proposal copies are preflighted and fallible under a 512-byte default and 4 KiB hard ceiling. Empty indexes need no repair; query-local resource pressure is explicitly skipped as non-actionable. Proposal pressure and allocation failure remain visible but cannot replace the exact anchor. The result grants no queue, worker, storage, publication, retry, pointer, or query authority.
- **P5-6c proof:** the focused target passes 16 tests covering valid hint completion, exact fallback, bounded and deterministic proposal fields, absent/stale/corrupt evidence, bounded corrupt diagnostics, invalid/overflowing hints, empty indexes, proposal-budget refusal with identical exact anchors, nonrepairable query-local pressure, and fail-closed authoritative Posting corruption. The final NVT/reference/format/writer/reader matrix passes 151 tests, and the independent reference implementation passes all 148 tests. Workspace/all-target checking, the 436-fixture/95-route/38-document contract gate, mdBook, Rust formatting, diff hygiene, all 28 error-squelch architecture tests, the exact 1,453-occurrence reviewed inventory, and source-isolation checks pass. Strict library Clippy remains byte-identical to the frozen historical baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`, with no NVT finding. The exact-source workspace/all-target suite passes 6,014 tests across 255 result groups with zero failures and seven intentional ignores; retained log SHA-256 is `0b9a00077043cb73db53068a57537416b6bb951ca388b95601faa0472633163d`.
- **P5-6c landing:** committed and pushed as `8d58d0c`; the proposal remains a caller-owned value with no runtime queue or publication authority.
- **P5-6d randomized proof:** a deterministic independent model builds 64 logically ordered Posting pages whose PageIds are deliberately permuted, then exercises both hash widths, 65,536- and 131,072-cell resolutions, all three ratified tile geometries, sparse gaps, and 3,072 point lookups. Every exact directory result matches the model; every accepted NVT hint starts at or before that exact predecessor, and the complete forward-link sequence reaches it. Missing predecessors fall back exactly, repeated builds are byte-identical, and both hint and fallback branches are exercised. The focused target passes 17 tests; the final five-target affected matrix passes 152 tests with retained log SHA-256 `6aff74f36346de238b29adee2d6d6d624c3bebdc1610511f7622eaa75d80014a`.
- **P5-6d platform and real-world proof:** exact tracked source plus lockfile was packaged as archive SHA-256 `8d4f803d087eab2cd1c27603dae248aa30a5b1a63dd2133d6ff0b2366fa8e703` with lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`. Native macOS arm64 and Windows x86_64 MSVC each pass all 152 affected tests; retained log SHA-256 values are `fa142d61455e1971a4207e805384bac04bc488513db04ccb04e25ba31650d362` and `0e27458d525cfa9290b255f7d57485468909d479d07511ee0732dcd464ccf89e`. A disposable external path-dependent harness processed 497,391 bytes from three real AeorDB documents into 123 page samples, 12 sparse plans, 748 tiles, and 516 modeled lookups across both hash widths, both resolutions, and all tile geometries. It proved repeated byte identity, hint-not-after-exact behavior, corrupt-tile rejection, and one-byte output-budget refusal; repeated output SHA-256 is `36099271339881804de030d56dc2c8fe786825cd0c6b1cc97000596ae57f9c7d`. All remote workspaces, the 2.9 GiB local harness build cache, the 283 MiB reference cache, and transfer artifacts were removed after evidence capture.
- **P5-6d final gates:** the exact-source workspace/all-target suite passes 6,015 tests across 255 result groups with zero failures and seven intentional ignores; retained log SHA-256 is `3031d928ebf2b53245dd688a69b8c9e005ab94aede2393d5f901a4375fa69b27`. Workspace/all-target checking, Rust formatting, diff hygiene, mdBook, the 436-fixture/95-route/38-document contract gate, all 28 suppression architecture tests, the exact 1,453-occurrence inventory, and source isolation pass. Strict library Clippy remains byte-identical to the frozen baseline at SHA-256 `b10b6d2188637c384bc84f10d35b7d65f2500131e4225dfbc138ac116cdaf41b`. P5-6 is closed without a storage writer, active pointer, query/API consumer, legacy/v0/KV dependency, or destructive authority.
- **Next action:** inventory P5-7 mutation/checkpoint codec gaps and unpublished attachment-root closure against the frozen v1 fixtures before adding the first writer.
