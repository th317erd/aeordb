# Child 03 Progress: Namespace

- **Status:** blocked only by Child 01 native-platform execution
- **Current landing unit:** P2c-3 registry-governed import
- **Entry commit:** `08651a3`
- **Last green commit:** P2c peer/client sync convergence `08651a3`
- **Owner:** Codex, namespace/semantic authority and integration owner
- **Start gate:** Child 02 is green; Child 01 is code-complete and Linux-green, but native `wyatt-mac` and `win11vm` execution remains required
- **Plan:** [Child 03](../children/03-namespace-semantic-roots-and-system-families.md)
- **Owned files:** this ledger; `engine/v4/system_family.rs`; the embedded registry loader currently in `engine/v4/admission.rs`; strict directory/B-tree traversal result APIs; P2c consumers in backup, sync, peer, import, indexing, GC, permissions, plugin host, repair, verify, and generic data adapters; focused P2c specs and architecture gates
- **Forbidden/hotspot files:** P2d mutation/locator behavior before P2c lands; v4 persistent registry bytes or generated IDs; index page/NVT/query behavior; physical GC eligibility/sweep/Void allocation; route response schemas; migration cutover; evidence or production databases; unrelated user artifacts
- **Hotspot handoff commit:** none
- **Narrow gate:** P2c-focused registry/traversal target first; campaign root target remains `timeout 15m cargo test -j 4 -p aeordb --test v4_root_migration_spec -- --test-threads=4`
- **Broad gate:** green for P2c-2: 4,814 tests across 196 suites, zero failures and seven ignored; workspace/all-target check, formatting, docs, and v4 contract gate are green
- **Drift/risks:** the frozen registry has no persisted family for structural parent directories such as `/.aeordb-system`; runtime traversal must recognize only strict ancestors of known path descriptors without granting those containers a persisted family policy. Existing flat-directory parse failures can become empty/complete results, best-effort B-tree warnings are discarded by several wrappers, peer/system augmentation silently skips every error, and hard-coded path lists disagree with the 46-family registry. P2 retains the old v3 system-flag predicate only as an explicitly named v0 byte-layout evaluator.
- **Evidence:** exhaustive source searches found the old predicates in DirectoryOps/batch v3 flags, auth, backup, sync, indexing, task, plugin, download/fetch/symlink/version/engine routes, plus hard-coded protected roots in GC, backup, and tree walking. Traversal consumers include backup/import, peer and embedded sync, directory accounting, reindex, verify, and route sync. The current registry decoder exposes eighteen raw policy bytes and its five-algorithm embedded cache is private to admission. A 194-test P2c-adjacent characterization gate passed across B-tree directories, tree walking, backup/import, sync, permissions, and GC. Existing peer tests require node-local `/.aeordb-system/config/secret_key` replication and backup tests inspect secret email configuration, directly characterizing behavior the ratified registry intentionally corrects; `V4-SYSTEM-FAMILY-POLICY` now records that approved divergence. Both native hosts remained unavailable at `ff3f95c` (`wyatt-mac`: no route; `win11vm`: forwarded port refused).
- **Next action:** prove full-export and sparse-patch imports validate one registry-authorized path closure before target mutation, while continuing to retry the exact P1c native gates when both hosts are online

## 2026-08-06 Context Recovery

- The host crash changed assistant context, not repository state. `development` remained clean at pushed commit `9151f8d` apart from the acknowledged untracked `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` paths.
- Both native P1c hosts remain unavailable: `wyatt-mac` returns `No route to host`; `win11vm` returns `Connection refused` on its forwarded SSH port.
- The P2c registry proof can still advance without violating the production start gate: add the independent manifest-oracle and runtime-classification tests first, demonstrate their precise failure against the absent typed/runtime APIs, and keep production activation blocked pending native closure or an explicit dependency waiver.

## P2c-1 Registry Model Proof

- **Red proof:** the focused `v4_system_family_runtime_spec` failed because the runtime exposed all eighteen policy fields as raw `u8` values, had no shared embedded-registry accessor, and classified known absolute-family ancestors as unknown protected state.
- **Implementation:** the runtime now decodes every frozen policy byte into a closed enum or validated GC bitset, owns one `OnceLock` registry per hash algorithm in the SystemFamily module, and exposes that exact object to both header and peer admission. Strict ancestors of absolute registered paths are classified as runtime-only `StructuralContainer` values with no family ID or invented policy; unmatched protected descendants remain `UnknownProtected`.
- **Independent proof:** the new target compares all 46 family policies and all 63 descriptors to `system-family-registry-v1.manifest.json`, proves pointer reuse for all five hash algorithms and admission, and pins permissions, conflicts, controls, API-key, and email-secret policy corrections. It passes 4 tests. Existing admission passes 9 tests, the persistent-format target passes 72 tests, the 21,364-case reader-hardening target passes, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages.
- **Boundary:** no backup, sync, index, GC, generic route, traversal, or writer behavior is activated by this landing unit. Native P1c execution remains open and P2c consumer conversion remains blocked behind that recorded gate.

## P2c-1 Traversal Outcome Proof

- **Red proof:** `traversal_completeness_spec` failed because B-tree results exposed only entries/warnings, page-window visitor stops had no independent completion state, directory wrappers had no typed traversal result, and malformed flat directories still became empty results without retained evidence.
- **Implementation:** added shared `TraversalIntegrity::{Complete, DiagnosticallyPartial, Corrupt}` and `VisitorCompletion::{Exhausted, StoppedByVisitor}` contracts. Best-effort B-tree roots now classify missing, wrong-type, or malformed root authority as corrupt; damage below a valid root merges upward as diagnostically partial; operational I/O, pressure, durability, shutdown, and cancellation errors remain hard `Err` results. Directory traversal now retains flat and B-tree issues in one typed result, and bounded windows report integrity independently from intentional early stop.
- **Proof:** the focused target passes 5 tests covering a valid tree, missing child, missing root, malformed inline root, malformed flat directory, cycle, and early page completion. Existing strict/best-effort B-tree, B-tree directory, DirectoryOps, and recursive listing targets pass 163 tests unchanged; the complete P2c-adjacent boundary passes 199 tests. Package all-target checking passes with only the historical unused WASM-test macro warning, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages. The first adjacent command named a nonexistent `recursive_listing_spec`; the corrected registered target is `directory_listing_spec` and passes.
- **Boundary:** ordinary listing keeps its existing diagnostic best-effort response while no longer destroying flat-corruption evidence in the checked path. Consumer-specific fail-closed conversion for backup, sync, indexing, GC, repair conclusions, and system/plugin enumeration remains the next P2c unit.

## P2c-2 Shared Operation Resolver

- **Red proof:** the focused runtime target failed because no shared resolver, closed transfer-operation selector, or typed ordinary/structural/known operation result existed. Consumers would otherwise have to select raw policy columns independently and repeat unknown-protected handling.
- **Implementation:** `SystemFamilyPolicyResolverV1` now owns classification through the exact embedded registry and selects complete full, transfer, or index policy without collapsing structural containers into ordinary data. `SystemFamilyTransferOperationV1` closes the seven frozen transfer columns, and `SystemFamilyPolicyDecisionV1` keeps ordinary, structural-container, and known-family decisions distinct. Unknown protected state remains a typed `unknown_protected_system_family` failure.
- **Proof:** the runtime target passes 6 tests, including every transfer operation over all 46 independent manifest rows. Admission passes 9 tests, persistent formats pass 72, the bounded reader-hardening target passes its 21,364-case corpus, all-target checking is green apart from the historical unused WASM-test macro warning, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages.
- **Boundary:** this unit adds the single consumer-facing decision surface but deliberately changes no backup, replication, indexing, authorization, GC, or generic route behavior. Those consumers are converted in the following landing units.

## P2c-2 Index Scope Convergence

- **Red proof:** direct indexing accepted an unknown `.aeordb-future` path as ordinary data, while the reindex worker downgraded its typed pipeline failure and completed successfully. Wrapper-level predicates also made streaming, batch, full-parser, and reindex paths choose independently. The strengthened reindex fixture deletes the unknown file's body to prove classification must happen before body I/O.
- **Implementation:** added the engine-facing `SystemFamilyPolicyResolver`, which centralizes path normalization and typed runtime error translation. The indexing pipeline now selects the frozen `IndexPolicyV1`; permissions and ordinary data remain indexable, declared excluded/not-applicable/canonical-only families are skipped, and unknown protected state returns `EngineError::SystemFamilyPolicy`. Streamed, buffered, full-parser, and batch paths delegate to the pipeline. Reindex classifies before migration or body reads, preserves known internal FileRecord migration, and treats policy failure as a task-fatal result.
- **Proof:** the affected gate passes 171 tests across `pipeline_spec` (49), `reindex_spec` (28), `write_pipeline_spec` (21), `upload_commit_spec` (21), `directory_ops_spec` (46), and `v4_system_family_runtime_spec` (6). Package all-target checking passes with only the historical unused WASM-test macro warning.
- **Removal evidence:** `is_internal_path` has no remaining indexing or reindex consumer. Its only live consumer is the pending client-sync conversion; wrapper-level index guards and task-owned protected-name lists are gone.

## P2c-2 Logical Backup And Data Export Convergence

- **Red proof:** a logical backup retained `/docs/.aeordb-indexes/text.idx`, and an unknown protected child returned a bare directory parse error after its corrupt body had already been read. The focused tests also pinned that malformed redacted secrets must not be read while malformed required portable records must fail the operation.
- **Implementation:** `include_system=false` now selects `DataExport` and `include_system=true` selects `LogicalBackup`. One registry resolver filters HEAD and historical tree traversal, derives detached absolute candidates from embedded descriptors, and classifies children before reading their FileRecord, symlink, or directory bodies. The export writer rebuilds flat or B-tree directory closure bottom-up, prunes empty structural containers, preserves retained header flags and entry versions, and rejects unknown protected state. Hard-coded backup path arrays, credential predicates, and special system-tree writers were removed.
- **Focused and adjacent proof:** `backup_diff_spec` (23), `backup_export_spec` (31), `backup_http_spec` (20), `backup_import_spec` (22), `backup_task_spec` (11), `tree_walker_spec` (21), and `v4_system_family_runtime_spec` (6) pass, for 134 tests. `v4_admission_spec` (9), `v4_format_fixture_spec` (72), and `v4_reader_hardening_spec` (2 tests with 21,364 malformed cases) pass. `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all`, `mdbook build docs`, and the v4 contract gate are green; the contract gate reports 436 fixtures, 95 routes, and 38 documentation pages.
- **Live proof:** after explicitly rebuilding the CLI, a disposable server stored `ordinary-live`, a namespace permission record, and a derived index through real `PUT /files/...` requests (all HTTP 201), then produced a logical artifact through `POST /versions/export` (HTTP 200). Offline verification reported 10 valid entries, zero integrity/KV/directory issues, and exactly 26 current logical bytes: ordinary data plus permissions, with the 13-byte derived index omitted. Booting the artifact returned exact bodies for ordinary data and permissions (HTTP 200), concealed the derived index (HTTP 404), and shut down cleanly. The first live attempt used a stale `target/debug/aeordb` left by test-only relinking and was discarded; the explicit `cargo build -j 4 --workspace` made the executable proof current.
- **Boundary:** import, peer/client sync, GC, permission authority, plugin/system enumeration, repair/verify conclusions, and generic protected-path adapters remain unconverted. The old v0 system predicate and `augment_with_system_subtrees` therefore remain only until those consumers are moved and the architecture-removal gate can become authoritative.

## P2c-2 Peer And Client Sync Convergence

- **Red proof:** embedded and HTTP diff producers independently walked and filtered trees, peer augmentation silently skipped every failure, local sync copied chunks and raw FileRecords before selective merge resolution, and inbound apply accepted registry-omitted paths. Focused tests reproduced forbidden peer mutations, unknown protected state read before classification, same-HEAD detached portable changes, selective chunk leakage, and a malicious HTTP peer triggering forbidden-state handling.
- **Implementation:** embedded and HTTP diff production now delegate to `compute_sync_diff`, which selects peer or client registry policy and uses one strict, budgeted transfer walker. Detached current authorities are registry-derived; historical bases exclude detached current state. Chunk manifests are recomputed after registry, selective-path, API-key, and permission filtering. Local sync uses the same tree authority, transfers only chunks required by resolved `AddFile` operations, and reconstructs records through normal mutation APIs. Both embedded apply and remote HTTP receive reject omitted, node-local, unknown, or structural-leaf paths before chunk requests or mutation.
- **Pre-writer correction:** the live proof discovered that root `/.aeordb-indexes` and `/.aeordb-logs` producers were absent from the 61-descriptor registry. The independent source now adds absolute-prefix descriptors to existing families `0x0060` and `0x0061`, preserving their policies and descendant matchers. All 63 descriptors, fingerprints, dependent v4 headers, manifests, ledgers, readers, and admission fixtures were regenerated before any v4 writer exists.
- **Proof:** `sync_api_spec` (24), `sync_apply_spec` (11), `sync_engine_spec` (29), `client_sync_spec` (27), `replication_e2e_spec` (19), `sync_http_spec` (18), `backup_export_spec` (31), and `tree_walker_spec` (21) pass, for 180 tests. Runtime SystemFamily (6), persistent fixtures (72), v4 admission (9), and the 21,364-case malformed-reader target pass. The independent reference passes 145 tests and freshly verifies all 436 cases.
- **Live proof:** the original real-server request failed HTTP 500 with `unknown_protected_system_family` at `/.aeordb-indexes`. After explicit CLI rebuild, the same disposable database returns HTTP 200; visible paths contain ordinary data, permissions, namespace configuration, and portable plugin state while root/nested indexes, logs, and conflicts are absent. Advertised chunks exactly equal the unique chunks referenced by visible files. Graceful shutdown succeeds and offline verify reports 242 valid entries with zero corrupt headers/hashes, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid voids, or broken snapshots.
- **Removal evidence:** sync producer code contains no `is_system_path`, `is_internal_path`, protected path literal, `augment_with_system_subtrees`, duplicate `walk_version_tree`, or duplicate `diff_trees` policy path. The compatibility boolean remains only as an adapter selecting the closed peer/client transfer operation.
- **Deferred receiver hardening:** the legacy HTTP receiver still needs strict typed response/chunk decoding, chunk payload hash and completeness proof, and convergence on the shared apply authority. Embedded apply also still squelches non-`NotFound` delete errors. These are explicit P2e/Child 06 obligations and prevent claiming end-to-end sync apply closure here.
- **Broad proof:** `cargo test -j4 --workspace --all-targets -- --test-threads=4` passed 4,814 tests across 196 suites with zero failures and seven ignored. `cargo check -j4 --workspace --all-targets`, workspace and reference-tool formatting checks, `mdbook build docs`, `git diff --check`, and the v4 contract gate are green. The contract gate independently verifies 436 fixtures, 95 routes, and 38 documentation pages.

## P2c-3 Registry-Governed Full-Export Import

- **Red proof:** a full export augmented with an unknown `/.aeordb-*` path imported successfully and advanced HEAD, while a privileged import copied a node-local API-key payload. Clearing legacy `FLAG_SYSTEM` from a portable user record also made CLI inspection misclassify the artifact as user-data-only.
- **Implementation:** full-export import now validates HEAD and every named snapshot through one operation-specific registry walk before the first target mutation. A second bounded walk writes only selected closure, rebuilds directories when children are omitted, replaces current path aliases, remaps snapshot roots, preserves portable required state, and omits credentials, node-local controls, logs, GC state, and derived indexes. CLI inspection decodes FileRecord, Symlink, DeletionRecord, and Snapshot subjects through registry Import versus DataExport policy instead of trusting header flags. Import-mode tree writes preserve logical operation metrics and count newly copied chunk bytes once; artifact construction does not contaminate runtime metrics.
- **Proof:** `backup_import_spec` (27), `backup_export_spec` (31), `backup_http_spec` (20), `event_emission_spec` (34), `tree_walker_spec` (21), and `v4_system_family_runtime_spec` (6) pass, for 139 tests. Workspace/all-target checking, formatting, `git diff --check`, and `mdbook build docs` are green.
- **Boundary:** sparse patch artifacts still use the legacy raw-inventory application path. They require an overlay-aware registry validation/rebuild design so omitted patch families cannot be copied or left reachable through the advertised target root; that remains the next P2c-3 landing unit.

## P2c Preflight Test Protocol

1. **Registry authority:** decode the checked-in binary through the production reader while using the independently generated manifest as the policy oracle. Prove all 46 families, 63 descriptors, every typed policy value, all five hash-specific cached registry instances, and admission/runtime pointer identity. The production serializer or decoder may not generate its own expected values.
2. **Path classification:** prove ordinary paths, all matcher kinds, exact known families, unknown protected paths, and runtime-only structural ancestors such as `/.aeordb-system`, `/.aeordb-system/cluster`, and `/.aeordb-config`. Structural containers are traversal-only and never inherit or invent a persisted family policy.
3. **Operation policy:** table-drive logical backup, peer replication, client sync, import, generic data, indexing, verify, repair, migration, spill, and GC decisions over every family. Pin the required corrections for permissions, conflicts, controls, credentials/secrets, derived indexes, and logs.
4. **Traversal states:** independently reproduce a valid flat directory, malformed flat root, valid B-tree, malformed inline B-tree root, missing/corrupt child branch, cycle, warning-cap exhaustion, operational I/O/admission failure, cancellation, and visitor-requested early stop. Integrity is exactly `complete`, `diagnostically_partial`, or `corrupt`; visitor completion is a separate field.
5. **Consumer behavior:** destructive or authoritative backup, import, sync, reindex, GC, counters, repair conclusions, and system-store enumeration must reject incomplete traversal. Verify and explicitly diagnostic tooling may retain readable entries while recording bounded evidence. No caller may turn partial/corrupt into an empty or complete collection.
6. **Architecture removal:** fail while legacy policy predicates or protected path arrays remain outside the named v0 byte-layout evaluator. Permit route/SDK compatibility parameters only as adapters into registry operation policy, never as lower-level authority.
7. **Verification order:** focused red target; adjacent 194-test characterization set; route/plugin/transfer/index regressions; contract and architecture gates; workspace suite; native checks; then a live home-backed disposable server exercising authorized and concealed paths plus injected traversal damage. Every command uses four Cargo jobs/threads, the home-backed test temporary directory, and a hard timeout.
