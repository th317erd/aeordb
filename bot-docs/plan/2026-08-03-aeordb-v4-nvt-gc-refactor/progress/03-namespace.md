# Child 03 Progress: Namespace

- **Status:** P2e, Child 08 P2f, Child 01 P3a, and Child 03 P3b-1 through P3b-2c are complete and pushed; P3b-3 read-view/lifecycle preflight is active
- **Current landing unit:** P3b-3c disconnected authorization-first read-view resolver before service activation
- **Entry commit:** `9f3c6d0`
- **Last green commit:** P3b-3b bounded root request pins `9b1a7a8`
- **Owner:** Codex, namespace/semantic authority and integration owner
- **Start gate:** Children 01 and 02 plus Child 08 P2f are green and pushed; the complete P3b-2 writer family passes its exact native matrix on Linux, macOS arm64, and Windows x86_64 MSVC
- **Plan:** [Child 03](../children/03-namespace-semantic-roots-and-system-families.md)
- **Owned files:** this ledger; narrowly scoped v4 namespace/root publication modules; exact root/semantic/control encoders; `v4_root_migration_spec`; test-target registration; P3b-2 architecture evidence
- **Forbidden/hotspot files:** standalone backup artifact construction except where needed to distinguish it from active-database mutation; v4 persistent registry bytes or generated IDs; index page/NVT/query behavior; physical GC eligibility/sweep/Void allocation; Child 06 route response redesign; Child 07 migration cutover; evidence or production databases; unrelated user artifacts
- **Hotspot handoff commit:** `ba60b4d` from completed P3b-1 immutable authority readers into P3b-2 shadow publication
- **Narrow gate:** the 283-test Wave 5d affected matrix covers active full/sparse import, HTTP import/promotion, namespace architecture, configuration capture, configuration shadow, the v3 authority facade, and all 72 frozen v4 format fixtures. It proves active locator batches, historical content-only import, import snapshot/HEAD publication, memory pressure, malformed/corrupt input, restart behavior, and the absence of a P7 migration source writer. Every Cargo command uses at most four jobs and four test threads, a hard timeout, and the designated home-backed `TMPDIR`.
- **Broad gate:** workspace/all-target checking, mdBook, and the independent 436-fixture/95-route/38-doc contract gate pass. The complete Wave 5d workspace/all-target run passes 5,232 eligible tests across 198 groups with zero failures and 7 ignored tests. Its retained log has SHA-256 `13535fca81bdc18297953da9ae27f020fd73e3682e25557a77dfd99b1f3e5c86`; the only check diagnostic is the historical unused `require_wasm_parser` test macro.
- **Drift/risks:** active import locator/HEAD authority is already converged and must not be duplicated. Direct backup HEAD writes remain intentionally confined to standalone artifact construction; historical snapshot imports remain immutable-content-only. V4 migration source capture has frozen configuration and persistent-format readers but no runtime caller, module, lease writer, or progress writer. Its runtime activation, shared migration authority, and memory ownership remain explicit P7 obligations. Current-authority SSE cannot notify an ordinary subscriber after the acknowledged mutation removes that subscriber's grant; typed prior-audience/recipient evidence remains Child 06/P7 event-contract work. Physical v4 GC semantics remain Child 04.
- **Evidence:** Wave 5a landed as `89a0196`. Wave 5b strictly visits credential directories without materializing complete listings, retains at most 128 expected FileRecord identities plus one bounded decode workspace, combines token/link candidates where capacity permits, and rechecks each expected identity under `MaintenanceRepair` authority. Missing or changed candidates are safe skips. Every acknowledged batch owns its exact counters/events/metrics; a later failure returns bounded `PartialOperation` evidence with exact token/link counts rather than erasing committed work. The queued-task adapter now carries its event bus into the same request context instead of dropping acknowledged deletion SSE events. Before serialization, the SSE adapter projects each batch member through the SystemFamily registry, path-prefix filter, current user/group permissions, and active API-key rules; recipient-addressed and administrative events are kept off ordinary non-root global streams, malformed identities fail at connection, and mutable authority is rechecked for every event. A normal self-authenticated CLI instance on `127.0.0.1:16837` minted and rotated a real refresh token, cleaned exactly the one revoked token through `POST /system/tasks/cleanup`, and emitted one `entries_deleted` SSE event with the exact protected path, `maintenance_repair` kind, operation ID, and publication sequence to the root subscriber. Repeated cleanup returned zero before and after a clean restart. A fresh self-authenticated CLI instance on `127.0.0.1:16839` connected concurrent root and real user-key SSE clients after granting that user read authority over `/live/allowed`. Root received the allowed event, an ungranted `/private` event, and protected credential events; the user received only the granted path. After revoking the grant without reconnecting, a second allowed-path event was absent from the existing user stream. Inline cleanup acknowledged exactly one revoked token and returned zero after a clean restart. Offline verification reported 1,132/1,132 valid entries with zero corrupt hashes/headers, directory/B-tree issues, or stale/missing/invalid KV entries. Every server shut down cleanly, no listener remains, and all credential-bearing disposable artifacts are removed after evidence capture.
- **Next action:** add the smallest falsifying P3b-3c tests for authorization-first root resolution, one-shot HEAD capture, exact explicit-root behavior, selected-root restriction, content-only semantics, cancellation, and the complete lifecycle/error matrix. Capability bits 1, 2, 5, 17, and 23, ordinary HEAD/service callers, physical GC publication, and migration activation remain disabled.

## P3b-3 Entry Territory And Decomposition

- **Authority sources:** one selected v4 header supplies the logical database, physical instance, hash algorithm, capability floor, registry identity, write-sequence high water, and exactly one captured HEAD. `NamespaceRootV1` plus its committed `RootAdmissionCommit` proves a complete admitted root; internal B-tree and nested directory hashes do not. Root-candidate and root-expiry rows already freeze pending, retired, and reclaimed evidence, while durable `TaskPin` bytes already freeze task-owned roots. Those codecs currently validate rows/bodies without exposing the typed reader values needed by a common resolver.
- **Authorization boundary:** current credential, user/group, path, generic-data, and concealment authority remains owned by route/embedded adapters. P3b-3 freezes an authorization-first resolver interface: current authority executes before historical state or metadata is loaded; selected-root permissions may only restrict that authority; share credentials reject explicit non-live roots. Child 06/P7 owns concrete route conversion and public response/error envelopes.
- **Lifecycle race boundary:** classification followed by a separate pin call is invalid because sweep can retire between them. The lifecycle source API must admit a readable state and acquire its request pin under one guard, then return an owned RAII pin. The in-memory implementation is explicitly bounded by distinct-root and total-pin caps, charges retained registry bytes to the process memory coordinator, observes cancellation before admission, does not refresh retention, and exposes the same guard to future retirement rechecks. P4 owns durable candidate/expiry publication and will implement this source against selected lifecycle controls.
- **Lifecycle outcomes:** successful views are `live`, `retained`, or `pending_delete`; only pending has checked advisory expiry. Logically retired and physically reclaimed roots are deterministic gone. Unknown/unadmitted roots, corrupt lifecycle authority, unavailable lifecycle authority, and corrupt immutable authority remain distinct typed failures. A supplied root never falls through to HEAD. Current HEAD remains readable only when its exact immutable authority is valid; missing admission is corruption, not implicit provenance.
- **Read-view shape:** the disconnected resolver captures database/physical identity, selected root and optional legacy mapping, immutable namespace/semantic authority, selected registry/capability identity, lifecycle metadata, current-plus-restrictive authorization snapshot, concealment, cancellation, and one request pin. Content-only roots remain valid for structural/content consumers and carry their frozen semantic unavailability rather than failing every read.
- **Transfer closure:** namespace root, namespace tree, semantic state, and admission edges are typed authority dependencies. Protected path/type/control/workspace decisions delegate to the selected `SystemFamilyPolicyResolverV1`; unknown protected state fails closed. Derived index and GC history remain outside answer authority and are omitted/rebuilt by declared policy. The classifier is streaming and bounded rather than collecting a database-sized closure in memory; strict walkers in P4/P7 consume its decisions and publish complete, explicit data-only, or typed incomplete outcomes.
- **Central falsifiers:** no authorization denial may touch root/lifecycle sources; omitted selection captures HEAD exactly once; an explicit unknown, retired, or reclaimed root never reads HEAD; sweep cannot retire after request admission wins the shared guard; reading cannot change pending state or expiry; malformed lifecycle/task-pin bytes never become absence; content-only semantics remain content-readable; unknown protected transfer state never becomes ordinary or silently omitted; and no production route/service caller may activate the resolver in this unit.
- **Proof shape:** dedicated external specs fail first, then cover both hash widths, frozen fixtures, malformed and boundary inputs, cancellation, pressure, concurrent pin/retirement races, authorization ordering, selected-root restriction, lifecycle/error matrix, transfer operation matrix, bounded streaming counts, and source-level isolation. The landing then runs affected v4 roots/GC/SystemFamily/memory suites, workspace/all-target gates, documentation/contract checks, exact native-host qualification, and real native storage reopen without starting a service.

## P3b-3a Typed Lifecycle Reader Evidence

- **Reader surface:** RootCandidate rows now expose the exact pending timestamp, first/last unreachable generations, frozen grace, authority digest, and admission evidence. RootExpiry rows expose typed logically-retired versus physically-reclaimed state, retirement/proof edges, and optional evidence expiry. Mutable durable TaskPin controls expose exact database/task identities, task kind/state/times/fence, and bounded sorted root/artifact vectors only after the existing control validator closes every count and length.
- **Format correction:** both lifecycle row readers now enforce the frozen two-value `RootRetirementReasonV1` registry. The former validator accepted any nonzero reason even though no reader could safely interpret it; unknown values now fail with `root_candidate_reason` or `root_expiry_reason` rather than becoming valid lifecycle state.
- **Proof:** `v4_lifecycle_reader_spec` first failed on the absent typed APIs, then passed five tests across Blake3-256 and SHA-512 fixtures, pending/retired/reclaimed state, unknown reasons, malformed task state, and oversized counts. The adjacent `v4_format_fixture_spec` and `v4_writer_fixture_spec` pass all 75 and 12 tests respectively, proving the stricter enum remains byte-compatible with every frozen fixture and no writer contract drifted.

## P3b-3b Bounded Request-Pin Evidence

- **Landing commit:** `9b1a7a8` (`add bounded v4 root request pins`).
- **Coordinator contract:** `RootReadPinCoordinatorV1` retains only per-root gates for currently active or contending requests, not a database-sized lifecycle catalog. Each gate has one explicit process-memory reservation, distinct-root and total-pin caps, exact selected-algorithm hash validation, cancellation before and after lifecycle observation, and an owned RAII pin. Lifecycle observation and pin acquisition run under the same per-root guard used by future P4 retirement rechecks, while unrelated roots remain concurrent.
- **Lifecycle and failure direction:** live, retained, and pending-delete roots are readable; pending expiry is checked from `pending_since + max(frozen_grace, current_grace)` without refreshing lifecycle state. Retired, reclaimed, unknown, corrupt, and unavailable roots retain distinct stable errors. Pin-coordinator poison/accounting corruption is separately reported as `read_pin_corrupt`, rather than being mislabeled as lifecycle corruption. Cleanup errors propagate on ordinary paths; only `Drop` cannot return, so it latches and logs release failure.
- **Race/resource proof:** the external spec uses channel handshakes rather than sleeps to force both request-first and retirement-first orders. Twelve tests cover both hash widths, malformed/zero hashes, invalid caps, checked expiry overflow, cancellation before and after observation, lifecycle-source failure, hard memory pressure, root/pin caps, shared same-root accounting, retirement cancellation/action failure, unrelated-root concurrency, exact loser behavior, and zero retained reservations after the final pin.
- **Adjacent gates:** the pin, typed lifecycle-reader, and process memory-coordinator matrix passes 35 tests. All-target checking passes with only the historical unused `require_wasm_parser` test macro. The suppression architecture gate passes 28 tests against an exact 1,429-entry reviewed inventory: four typed lifecycle-reader additions from P3b-3a and five pin-coordinator additions. A new audit regression prevents v4 runtime authority failures from inheriting the persistent-format policy merely because of their module path.

## P3b-2 Entry Territory And Decomposition

- **Authority visibility finding:** the current v3 `NamespaceMutationCoordinator` gives one hard durability acknowledgement, but `DiskKVStore::insert` republishes the write buffer while the transaction is still open. It therefore cannot, without an additional visibility boundary, satisfy v4's frozen rule that a new NamespaceRoot and its first authority/admission witness become visible together. P3b-2 will not place v4 root bytes into a legacy `DirectoryIndex` dependency and call that atomic.
- **Producers:** the future semantic compiler, namespace-root builder, snapshot/fork/sync-base/migration authority transitions, Child 07 migration destination, exact v4 entity/control encoders, and the shared namespace/durability coordinator substrate.
- **Consumers:** P3b-3 root resolution and lifecycle, Child 04 GC/verify/repair closure, Child 05 index roots, Child 06 query/API reads, Child 07 migration/cutover, backup/replication, capability admission, and the independent fixture/architecture gates.
- **Landing shape:** P3b-2a lands exact root and prepare/commit encoders; P3b-2b lands deterministic write-once semantic-object storage; P3b-2c lands the prepared-root/first-authority visibility transaction. Every slice keeps capability bits 1, 2, and 5 and all service/HEAD callers disabled until the complete writer family is proven.
- **Central falsifiers:** production encoders must byte-match the independent Blake3-256 and SHA-512 fixtures; semantic paths must be deterministic and exact-byte idempotent; no reader may resolve a prepared root; every crash prefix around first admission must reopen to old authority or an unadmitted orphan; and successful retry must produce one root witness rather than parallel authority paths.
- **Immediate proof:** extend the independent `v4_root_migration_spec` so it fails on the absent production encoders before adding implementation. Every Cargo invocation remains home-backed, bounded to four jobs/four test threads, and protected by a hard timeout.

## P3b-2a Implementation And Linux Evidence

- **Exact codecs:** `NamespaceRootV1`, complete/content-only `SemanticStateRootV1`, immutable `RootPublicationPrepare`, and immutable `RootAdmissionCommit` now have typed production encoders. They use the shared checked framing, capability, CRC, and digest implementations and decode their own bounded output before returning it. The control framing helper is visible only to sibling v4 modules; no service, storage, HEAD, admission, or migration caller is connected.
- **Independent proof:** the focused target constructs its expected values from frozen fixture offsets and independent Blake3-256/SHA-512 digest oracles. Production output byte-matches both hash-width fixture families. The rejection matrix covers reserved capabilities; zero and wrong-width namespace/semantic edges; both complete-state count invariants; zero database and transaction identities; negative times; empty/oversized authority identities; every prepare/commit hash role; and both sequence fields. The architecture gate recursively proves each new encoder has exactly one production occurrence and forbids storage or service authority in the codec modules.
- **Linux gates:** the focused target passes 14 tests and the adjacent admission/ControlStore/fixture/reader/writer matrix passes 124 tests. The exact final-tree workspace/all-target gate passes 5,493 tests across 209 result groups with zero failures and seven intentional ignores; its retained log SHA-256 is `d2f3829d1c1d2ec11fb58f8ee2147564a46e768143757f56e61288bd6cb53230`. Workspace/all-target checking, both formatting gates, `git diff --check`, mdBook, the independent 436-fixture/95-route/38-document contract gate, the exact 1,420-entry suppression audit, and all 27 suppression architecture tests pass. The only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Native and landing proof:** exact commit `59648a4cefdba5e2a6efa8214c1c08920cdcf8e6` passes the same 124-test matrix on native macOS arm64 and Windows x86_64 MSVC under Rust 1.95.0. Both hosts used the exact local `Cargo.lock` with SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`. macOS also passes the complete 436-fixture/95-route/38-document contract gate. Native warnings are limited to known platform-only unused imports and the expected embedded-docs fallback when mdBook is absent. The isolated Git-backed trees, virtual environment, and build caches were removed after qualification; no user checkout was modified. P3b-2a is complete and pushed.

## P3b-2b Semantic Object Storage Closure

- **Storage contract:** `V4SemanticObjectStore` publishes state, leaf, internal, and definition objects as strict system FileRecord v1 entries at `/.aeordb-system/semantic-objects/<algorithm>/<kind>/<lowercase-id>`. Validation, kind-specific caps, memory admission, canonical decoding, expected identity, and existing-object collision checks all precede mutation. Exact retries perform no publication; successful writes use the shared namespace authority and hard durability path, perform bounded exact read-back, and never select HEAD.
- **Failure and architecture proof:** eight focused tests cover exact Blake3-256/SHA-512 paths, all four kinds, restart/load, sixteen concurrent attempts with one durability sequence, zero-write retry, identity mismatch, collision refusal, malformed wrappers, cap rejection before decode, memory exhaustion with no mutation, unchanged HEAD, and source-level caller isolation. The architecture gate permits exactly one typed `DirectoryOps` adapter and its store caller; service, root, HEAD, and control paths remain disconnected.
- **Linux qualification:** the focused target passes 8 tests and the twelve-target adjacent v4 matrix passes 267. The exact final-tree workspace/all-target gate passes 5,501 tests across 210 harness groups with zero failures, seven intentional ignores, and 126 filtered tests. Preserved log SHA-256: `e66b2627c53974c7dc7293100d4ee0fa5fdfbee9f6de20349dca3e769de6beab`. Workspace/all-target checking, both formatters, `git diff --check`, mdBook, the independent 436-fixture/95-route/38-document contract gate, the exact 1,420-entry suppression audit, and all 27 suppression architecture tests pass.
- **Lint and real-storage boundary:** strict focused Clippy remains blocked by the established 110-diagnostic crate baseline; none references the semantic-store implementation or spec. Preserved non-strict log SHA-256: `dde4e74b7b9ab0d5ac613bcd8d058eead4e4df9889abcffeb52a3b9335168b9d`. A live HTTP/startup test would falsely activate v4 authority, so the focused proof uses real `StorageEngine` files, hard publication, clean shutdown, reopen, and byte-exact load on every native host.
- **Native and landing proof:** exact commit `0b739c71b434101f35660c051bd657f73148681e` passes the same twelve-target, 267-test matrix on macOS arm64 and Windows x86_64 MSVC with exact lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de`. macOS also passes the 436-fixture/95-route/38-document contract gate. Native logs were retained locally with SHA-256 `8094691c6e3ba24ac552b03f71c764d290faa685b24444c212f31bdc53b255f4` for macOS and `6b33820ae59540f0b2ef00ed1ae49b9c4be18f27e3452a9026ab158b46453471` for Windows. Isolated source trees, build targets, logs, and the disposable macOS contract environment were removed after qualification; no user checkout was modified. P3b-2b is complete and pushed.

## P3b-2c Linux-Green First-Authority Candidate

- **Bounded visibility authority:** `DiskKVStore` now owns one exclusive eight-entry visibility batch that refuses dirty baselines, nested transactions, threshold/page flushes, ordinary insertion, duplicate keys, and wrong terminal tokens. Staged locators remain absent from every published snapshot. Abort restores the exact entry count, hot-tail frontier, write buffer, and hot buffer; publication requires the exact completed hard-authority receipt.
- **Single durability boundary:** `V4FirstAuthorityPublisher` deterministically prepares the namespace tree, semantic state, `NamespaceRoot`, root prepare, and root-admission control files as eight exact WholeEntity/FileRecord records. One root-state guard and one admitted inactive-header publication ticket cover raw entity append/readback, hot-tail publication, data barrier, inactive-slot write, full barrier, header readback, and terminal KV visibility. Existing identity collisions, malformed or oversized inputs, wrong databases, degraded headers, and memory/size bounds refuse before mutation. Exact retries reconstruct and verify all eight bytes without another hard publication.
- **Crash and acknowledgement direction:** eleven internal tests cover hidden staging, rollback, all 24 dependency-record failure prefixes, all five inactive-header I/O prefixes on real file bytes, clean restart, malformed and cap refusal, existing-identity collision, sixteen concurrent attempts, exact retry, locator bounds, different-transaction refusal, and post-linearization failure. Every restart selects either the old authority or one complete new root. A post-commit failure returns the exact committed receipt and retry observes the same root and publication sequence rather than blindly republishing. An impossible admission-guard cancellation failure latches subsequent hard writes instead of logging and continuing.
- **Format and isolation proof:** the public target passes four tests for exact root/witness closure, idempotence, frozen SHA-512 identity width, and source ownership. First-authority publication remains the sole caller of the private atomic KV/header dependency APIs; it has no service, route, migration, or ordinary HEAD caller. Writer capability bits 1, 2, and 5 remain disabled.
- **Linux qualification:** the final workspace/all-target run passes 5,523 tests across 211 harness groups with zero failures, seven intentional ignores, and 126 filtered tests. Retained log SHA-256 is `5027f3975f6f70ad60edacedad565e71ad8957d50d0df3c59f0c6ebdc646cfaf`. Workspace/all-target checking, both Rust formatters, `git diff --check`, mdBook, the independent 436-fixture/95-route/38-document contract gate, the exact 1,420-entry suppression check, and all 27 suppression architecture tests pass. The suppression inventory changed only 34 source line numbers; its normalized SHA-256 remains `5ad71e9d3533eda2d2fdd39779ba3380c1595f4fa9fd73a117c0df6c9e9a4792`. The only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Real-storage boundary:** fault and restart tests use ordinary host files, native positional writes/readback and data/full barriers, real in-file KV pages, serialized hot tails, clean close, and reopen from the selected v4 header. A live HTTP server would falsely activate the deliberately disconnected v4 service path, so no route-level claim is made; direct native storage proof is the intentional P3b-2c closure boundary.
- **Native closure:** exact commit `9f3c6d0eebf691ebe8f5e059f85efd72f313c0a1` and lockfile SHA-256 `b9fa3f1afdb6eac58ff877e942381bc92e45e81a3a2e51a20c671024333793de` pass the landing matrix on native macOS arm64 and Windows x86_64 MSVC. macOS passes 427 library tests plus 258 external tests and the complete 436-fixture/95-route/38-document contract gate; retained log SHA-256 values are `2124de9ad5d3eba4a0fec3795e99866252ae8ee17ee1ecbff658cdfb55fc80a4`, `648f1011742073b663d7afc9db7c1d4a45197677f4c3e443a5358ef7686bcfff`, and `4fe1a37347c3929b02de78a08b888878b03eb14404eb58f60e47881771e12ae1`. Windows passes all 22 landing-specific internal tests and 195 external tests; the external log SHA-256 is `d67a5d6e457fe51a12a0f4e51660aa6293aaa1306d6095162cf0f95dbc86bd40`. A broader Windows library probe passed 422 tests but retained three unrelated Unix-path fixture failures, and the legacy DiskKV target passed 61 tests but two maximum-stage allocation cases exhausted the VM drive; neither failure touches the first-authority unit, and the exact atomic-KV tests pass directly. Isolated source trees, build targets, logs, and the macOS contract environment were removed after evidence capture; local retained copies remain under the home-backed Codex cache. Windows cleanup recovered about 6.64 GB and left about 9.03 GB free. P3b-2c is complete and pushed; no service or migration caller is active.

## P3b-1 Entry Territory

- **Producers:** the independent fixture generator is currently the only producer of `NamespaceRootV1`, semantic-object, and `RootAdmissionCommit` bytes. P3a supplies disconnected WholeEntityV1 and SystemControlV1 encoders plus ControlStore publication machinery, but no root writer or selected-root service caller exists.
- **Current readers:** `decode_namespace_root` and `decode_semantic_object` validate individual `ADIR`/`ASEM` payloads. `decode_whole_entity` validates generic v4 framing. `decode_system_control` validates a root-admission body only as opaque control bytes. No reader currently proves exact outer DirectoryIndex identity, exposes the full semantic-state authority, or binds one committed admission witness to one decoded root.
- **Future consumers:** P3b-2 root publication, P3b-3 `ResolvedReadView`, Child 04 GC, Child 06 query/API, and Child 07 migration all consume this immutable contract. P3b-1 must remain storage- and route-disconnected so those later owners cannot be activated accidentally.
- **Proof boundary:** the independent test model owns expected byte offsets, identities, and edge relationships without calling production decoders to construct its oracle. Production code may only decode and validate already supplied immutable bytes; it may not publish a root, select HEAD, classify lifecycle, consult mutable aliases, or mutate a database.

## P3b-1 Implementation And Linux Evidence

- **Immutable authority:** `decode_immutable_namespace_authority` binds an exact system `DirectoryIndex` WholeEntityV1 key/value to its v0 namespace-tree edge, typed SemanticStateRootV1, immutable RootAdmissionCommit, and logical database ID. Missing and invalid references retain their exact role and expected identity. It never consults current aliases, storage, HEAD, lifecycle, routes, permissions, or ControlStore authority.
- **Typed closure:** the namespace-tree reader accepts canonical empty, flat, B-tree leaf, and B-tree internal v0 roots. It validates outer type/key/codec/flags, content identity, ordering, nonzero hash-width-correct edges, and legacy child entry types. The result preserves typed `Entry` versus `BTreeNode` edges so GC, verify, migration, and read-view consumers do not need a second decoder. Semantic state exposes exact capabilities, codec/profile versions, compiler and registry fingerprints, catalog root/counts, and stable content-only reason. Root-admission decoding exposes every frozen field through checked readers and adds no reviewed panic/suppression site.
- **Independent proof:** the test model computes Blake3-256 and SHA-512 identities itself and writes WholeEntityV1 framing, integrity hashes, header CRCs, flat directory entries, and leaf/internal B-tree payloads from frozen offsets without production encoders. A source gate prevents the reference model from importing those production encoders. Eleven focused tests cover both hash widths, complete/content-only semantics, every typed field, all four missing edges, valid cross-root substitutions, outer representation/key/content failures, invalid hash widths, flat/leaf/internal structures, malformed ordering/types/edges, wrong semantic kind, database mismatch, and source-level service disconnection.
- **Real storage:** a disposable home-backed database stores all four exact authority objects through `StorageEngine`, performs a clean shutdown and reopen, reads each value through verified storage, and resolves the same immutable root only after restart. The test shuts the reopened engine down and its temporary directory is removed.
- **Linux gates:** the focused target passes 11 tests. The adjacent v4 matrix remains green across root migration, format fixtures, reader hardening, admission, writers, and ControlStore. Workspace/all-target checking passes with only the historical unused `require_wasm_parser` test macro. The final workspace/all-target run passes 5,490 tests across 209 groups with zero failures and seven intentional ignores; retained log SHA-256 is `d5eff5ef36265578aa2eb3b95cdef6c93532e234c32cc2328c26fc4f95f8e13a`. The 436-fixture/95-route/38-doc contract gate, mdBook, exact 1,420-entry suppression check, and all 27 suppression architecture tests pass. Strict crate-wide Clippy remains blocked by the established 110-diagnostic historical baseline and reported no reason to broaden this unit into unrelated cleanup.
- **Native gates:** exact commit `ba60b4d9447e89576a0ce3665b0570c41566c34a` passes the same 121-test root/format/reader/admission/writer/ControlStore matrix on macOS arm64 and Windows x86_64 MSVC. macOS also passes the complete 436-fixture/95-route/38-document contract gate. Windows natively exercises all 436 fixture cases through the Rust fixture target; the POSIX contract wrapper is not claimed there because the host lacks its `jq` and Python/PyYAML command dependencies. Both isolated build caches were removed after qualification; no user checkout was modified.
- **P3b-1 landing:** implementation commit `ba60b4d` is pushed on `origin/development`. The tracked worktree is clean at closure; unrelated operator-owned `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` remain untracked and excluded.
- **Deferred service boundary:** P3b-3 must acquire these objects through bounded verified storage reads under request/task memory admission, compare required capabilities to the selected runtime, classify lifecycle and semantic availability, enforce authorization, and pin the resolved root. P3b-1 deliberately does none of those jobs. P3b-2 remains the only owner allowed to publish shadow root/admission bytes.

## P2e Wave 5c Preflight: Exact Repair And GC Follow-up Failure Direction

- **Producer/consumer map:** `verify_and_repair_checked` performs initial checked verification, KV rebuild, targeted B-tree repair, full directory rebuild fallback, stale directory-locator repair, Void snapshot staging, a forced hard hot-tail publication, and final checked verification. The legacy library wrapper and CLI consume its terminal result; the directory repair methods otherwise have only focused test callers. Mutating GC is entered by embedded callers, HTTP, queued tasks, CLI, soak/crash workers, and the benchmark binary. Its result feeds JSON, CLI/task summaries, and `gc_completed` SSE. Every storage writer can feed the recheck state; sweep consumes it through `take_gc_recheck` and `gc_recheck_contains`.
- **Frozen acknowledgement rule:** repair `completed` counts only maintenance actions that crossed their authority boundary: one completed KV rebuild, each targeted/full directory publication, each stale-locator publication, and one Void republication only after the forced hard hot-tail publication succeeds. A full rebuild that fails after publishing directories returns their exact count. `repair_verified_report` merges nested partial counts and bounded evidence; no later repair, final verification, or durability failure may erase earlier acknowledged work.
- **Stale-locator authority:** `repair_stale_dir_key` is the remaining repair writer that directly replaces a stable locator. It must re-read and conditionally replace the canonical target inside one shared `MaintenanceRepair` namespace mutation, preserving no-op behavior and one acknowledgement per actual replacement.
- **GC teardown classification:** normal completion must explicitly combine the primary GC result with `end_gc_recheck`; `Drop` remains an unwind fallback only. Recovered teardown failure is optional telemetry, not authority success: clear poisoned state so later GC remains usable, preserve an existing primary error unchanged, and expose a successful-primary incident as a bounded result/event warning plus a monotonic metric. Pre-GC snapshot-list/delete/retention warning paths remain classified for the P2f repository-wide squelch audit rather than being silently broadened into this landing unit.
- **Baseline:** before behavior edits, `gc_spec`, `corruption_hardening_spec`, `namespace_mutation_spec`, and `resilience_features_spec` passed 168 tests with zero failures under four Cargo jobs, four test threads, a hard timeout, and the designated home-backed `TMPDIR`.
- **Red proof plan:** construct two stale locators where root repair acknowledges and a later canonical walk encounters corrupt directory bytes; inject a post-first-publication full-rebuild failure; poison recheck teardown after a successful primary result and after a primary error. Require exact counts/evidence, primary-error precedence, lock recovery, result/event warning visibility, one shared stale-locator mutation, and no false Void acknowledgement.

## P2e Wave 5c Implementation And Evidence

- **Exact repair outcomes:** a bounded `RepairProgress` counts successful KV rebuilds, targeted directory publications, full-rebuild directory publications, stale-locator publications, and hard-published Void snapshots. Collectable targeted failures and nested `PartialOperation` counts are merged rather than flattened. Later repair, final verification, and durability failures produce one bounded `verify and repair` partial outcome whenever prior work or failed attempts exist; zero-acknowledgement failures retain their original typed error.
- **Directory authority:** full rebuild wraps every post-publication workspace/planning failure with the exact number of directories already acknowledged. Group cleanup cannot erase its primary publication failure. `repair_stale_dir_key` now re-reads current and canonical targets and conditionally replaces the stable locator inside one `MaintenanceRepair` namespace mutation; missing, non-link, canonical, and unreachable cases remain zero-write no-ops.
- **GC teardown:** normal mutating GC explicitly combines its primary result with recheck teardown before emitting `gc_completed`; Drop is unwind-only. A poisoned recheck lock is cleared after state retirement so later runs remain usable. Recovered teardown after success returns one bounded optional `cleanup_warnings` item, emits the same warning, prints it in the CLI, and increments `aeordb_gc_recheck_teardown_failures_total`. Empty warnings remain absent from normal HTTP/SSE JSON. If a primary error already exists, it is returned unchanged while the cleanup incident remains logged and metered.
- **Automated proof:** the red compile gate first demonstrated all missing contracts. Focused units then proved exact one-acknowledgement full-rebuild and stale-locator failures, nested 5-completed/3-failed cardinality, no pre-flush Void completion, and both teardown precedence directions. The 324-unit, 258-affected, all-target check, formatting, mdBook, and 5,231-test workspace gauntlets are green with zero failures.
- **Live proof:** a freshly built debug CLI served a new auth-disabled, snapshot-write-disabled database on `127.0.0.1:16841`. Real HTTP writes created and replaced `/live/a.txt`; mutating `POST /system/gc` reclaimed 11 entries and 2,008 bytes with the unchanged normal response shape. An immediate dry-run GC succeeded, proving recheck state was released. The replacement body survived clean shutdown, offline verification, restart, and fetch. Verification reported 198/198 valid entries with zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots. The restarted server shut down cleanly and no listener remains.
- **Boundary:** pre-GC snapshot-list/delete/retention warning paths remain explicitly classified for Child 08 P2f rather than being silently broadened here. Wave 5d owns only the import/migration-source exit audit; v4 physical GC semantics remain Child 04 authority.

## P2e Wave 5d Import And Migration-Source Exit Audit

- **Import authority:** active full and sparse imports route current file, directory, symlink, and deletion locators through bounded `ImportLocatorBatch` maintenance acknowledgements. Imported snapshot locators use conditional `NamespaceMutationCoordinator` publication, and promoted roots use the shared compare-and-publish `Import` transition. Historical snapshot imports return false from alias selection and therefore copy immutable content without publishing current path locators. The three direct backup `update_head` calls are all confined to standalone export/patch artifact construction before the active-import entrypoint.
- **Migration boundary:** `capture_migration_run_configuration` has exactly one source occurrence: its dead-code definition reserved for P7. No v4 migration module exists, and migration lease/progress control kinds have no runtime caller outside strict format validation. Configuration registry rows, `MemoryOwner::Migration`, migration-only format readers, and forced v3 FileRecord reindex migration are prerequisites or existing behavior, not a v4 source-capture writer.
- **Architecture proof:** the strengthened import gate freezes active destination-mode selection, bounded maintenance batches, shared coordinator execution, historical content-only behavior, snapshot/HEAD authority, deletion identity evidence, and absence of raw active-import publication. A separate source-tree gate fails if migration configuration gains a caller, a migration runtime module appears, or lease/progress controls gain a writer before P7 review.
- **Automated proof:** 283 affected tests pass across backup import/HTTP, namespace authority, v3 facade, configuration resolver/shadow, and persistent v4 formats. Workspace/all-target checking, formatting, mdBook, and the independent 436-fixture/95-route/38-doc contract gate pass. The complete workspace passes 5,232 tests across 198 groups with zero failures and seven intentional ignores; retained log SHA-256 is `13535fca81bdc18297953da9ae27f020fd73e3682e25557a77dfd99b1f3e5c86`.
- **Live boundary:** Wave 5d changes no production behavior, wire schema, or persistent bytes. The current production source is the exact Wave 5c code already proven through disposable HTTP write/replace, mutating and dry-run GC, clean shutdown, offline verification, reopen, and byte-exact fetch. Repeating that live run for source-only architecture assertions would add no behavioral coverage.
- **Wave 5 exit:** reindex migration, credential cleanup, repair, stale-locator/Void publication, and GC follow-up now have exact failure and acknowledgement direction under shared authority. Existing backup import is source-gated as converged. The absent v4 migration source runtime remains a named P7 dependency rather than a false completion claim.

## P2e Wave 5 Preflight: Maintenance And Repair

- **Producer/consumer map:** forced reindex invokes the already-converged `DirectoryOps` FileRecord migration mutation but owns task-level continuation, checkpoints, and terminal evidence. Verify/repair invokes KV rebuild, targeted directory repair, full tree rebuild, stale directory-locator repair, Void republish, and a final durability barrier. Credential cleanup scans two protected families and calls ordinary delete once per expired row. Mutating GC owns a recheck guard whose teardown currently runs from `Drop`. Backup import already publishes selected locators and HEAD through the shared import mutation adapters. The P7 migration runtime writer does not yet exist; only frozen configuration capture and persistent format readers are present.
- **Observed defects:** reindex warned and skipped ordinary migration/body/index failures, advanced its checkpoint past them, and returned `Completed` unless ten failures were consecutive. Cleanup can delete an exact prefix and then return only the later error. Repair prose can describe a failed targeted attempt followed by successful fallback, but exact acknowledgement/cardinality still needs proof across every repair family. GC teardown logs and discards `end_gc_recheck` failure. Reimplementing import or migration capture in Wave 5 would create duplicate authority or activate P7 prematurely.
- **Landing decomposition:** Wave 5a makes reindex outcomes and checkpoints truthful. Wave 5b converts credential cleanup to one bounded maintenance decision/publication or exact typed partial result. Wave 5c proves and converges repair and GC follow-up failure direction. Wave 5d audits existing import authority, freezes architecture gates, and records migration capture as unavailable until its P7 owner exists. Each unit keeps serious engine failures immediate and treats collectable corruption as bounded exact evidence.
- **Proof direction:** reproduce false success/advanced checkpoints first; then partial cleanup, concurrent expiry/revocation changes, targeted-repair fallback and later failure, stale locator/Void publication failure, GC primary-versus-teardown error precedence, import bypass absence, and migration-writer absence. Run focused tests before architecture, affected suites, full workspace, and disposable live maintenance/restart/verify proof.

## P2e Wave 5a Implementation: Exact Reindex Partial Outcomes

- **Failure contract:** shared `EngineError::PartialOperation` carries an operation name, exact completed/failed counts, and bounded evidence. Reindex retains at most eight samples with bounded path and error text and reports the exact omitted count. Nonconsecutive failures no longer disappear behind later successes; ten consecutive failures return the same exact evidence with a circuit-breaker marker.
- **Checkpoint contract:** only the contiguous successful prefix can advance the durable checkpoint, and only after its buffered index mutations are flushed. Later valid work may still publish independently after a damaged file, but cancellation, pressure, shutdown, crash recovery, or operator retry cannot skip the earliest incomplete path.
- **Failure direction:** resource pressure, cancellation, and shutdown preserve retry/requeue semantics. Low-level I/O, database identity, nested partial-operation, SystemFamily, and durability failures abort immediately instead of entering the collectable per-file path. Corrupt/missing/unsupported individual FileRecords and document/parser failures remain collectable and diagnosable.
- **Proof:** the new behavioral tests reproduced and then eliminated false completion, checkpoint overrun, unbounded/incorrect alternating-failure evidence, discarded circuit-breaker history, and later authority failures erasing earlier partial evidence. All 31 reindex tests, 22 error-display tests, the internal serious-error classification and mixed-authority proofs, the architecture guard, 238 adjacent tests, and all 5,194 eligible workspace/all-target tests pass. Documentation, formatting, all-target checking, real CLI HTTP/restart behavior, and offline integrity verification are green.

## P2e Wave 5b Preflight: Bounded Credential Cleanup Authority

- **Authority map:** embedded cleanup, the root-only direct HTTP endpoint, and queued cleanup tasks all call `system_store::cleanup_expired_tokens`. Refresh-token and magic-link writers already use typed `JsonStore` transforms under namespace authority. Cleanup alone scans complete directory vectors, buffers bodies without operation admission, and calls ordinary `delete_file` once per row. Each delete independently updates detached path locators, directory parents, HEAD, counters, index removals, SSE, and durability.
- **Race and bound contract:** scan one strict key-ordered directory view without collecting every child. Read one current FileRecord body through a 1 MiB bound, validate its identifier, and retain only qualifying path plus immutable FileRecord identity. Before each deletion, recheck that identity under the same namespace authority as the batch; absent or changed records are skipped. Retain no more than 128 candidates, account the decode and candidate workspace to maintenance memory, and combine both credential families into one batch when capacity allows.
- **Outcome contract:** validate the entire pending batch before publishing it. Empty and all-changed batches publish nothing. A successful batch is one `MaintenanceRepair` acknowledgement and records exact token/link metrics after acknowledgement. If a later batch, scan, read, or decode fails, preserve prior acknowledged counts in bounded `PartialOperation` evidence. No completed batch can disappear behind a later generic error, and no rejected batch can inflate counters.
- **Red proof:** existing code consumes three hard sequences for four mixed deletions, deletes an expired token before discovering malformed authority in the same intended batch, succeeds under deliberately exhausted maintenance memory, and reports a generic parse error after 128 individually acknowledged deletes. The target tests require one sequence, zero first-batch mutation, typed pressure refusal, and exact `completed=128`, `failed=1`, token/link/path evidence with one batch sequence.

## P2e Wave 4i Preflight: Remaining System And Plugin Authority

- **Territory refresh:** the pushed Wave 4h commit `d3e82ea` is the green handoff. Remaining live stable system producers are legacy config/JWT initialization, node identity, the singleton peer-config document, per-peer sync state, plugin records, email configuration, startup path migration, and join-audit evidence. Conflict resolution/dismissal already uses the shared sync-merge batch; standalone conflict creation has no production caller. Cron, tasks, credentials, permissions, shares, and ControlStore publication are already converged.
- **Observed defects:** peer routes read and replace the singleton document outside one authority window, so concurrent adds/removes lose updates. Add and join mutate `PeerManager` before persistence. Startup logs and continues after node-ID, peer-list, or bundled-plugin failures, allowing a nominally ready server with disabled or divergent system authority. JWT first initialization also performs a split read/create, plugin replacement policy is decided before its write authority, and legacy path migration performs separate copy/delete writes while silently retaining divergent old/new collisions.
- **Landing decomposition:** Wave 4i-1 owns peer/node/startup authority; Wave 4i-2 owns plugin decision/cache identity; Wave 4i-3 owns legacy config/email/path migration and the final failure-direction/source gate. Each slice preserves v3 bytes and public schemas, lands independently green, and treats recoverable derived logging separately from acknowledged authority.
- **Proof direction:** deterministic concurrent peer mutations must retain every accepted change; malformed or oversized peer authority must consume no durability sequence and mutate no runtime peer; node identity must be create-once; server construction must refuse unreadable required authority before serving. Focused proofs precede production edits, followed by cluster/sync/health/startup adjacency, static gates, broad tests, restart, and a disposable live HTTP run.

## P2e Wave 4i-2 Preflight: Plugin Decision And Cache Identity Authority

- **Territory:** `PluginManager` is the sole production writer over raw `system_store` plugin files. Startup installs bundled `extract` and `jq`; HTTP deploys, lists, invokes, and removes records; rules, indexing parsers/mappers, task execution, and query plugins consume the same manager; peer sync transports the acknowledged system-family file. Persisted `PluginRecord` JSON and slash-to-`::` filenames remain v3-compatible.
- **Observed defects:** deploy invalidates one manager-local cache before persistence, reads the current record outside write authority, and writes later. Concurrent first deploys can mint different IDs, bundled replacement can decide against stale state, failed persistence discards a still-valid cache, and another manager can execute a path-keyed stale compiled runtime after replacement. Reads are not capped by a record contract, nonempty checksum values are trusted without recomputation, record path/key identity is unchecked, and literal `::` keys alias slash-containing keys.
- **Landing design:** validate deployment input before mutation; hold an admitted workspace through WASM validation and namespace publication; decode and validate the current bounded record inside one `transform_file_buffered` authority window; decide regular ID reuse and bundled version/identity policy there; preserve canonical bundle timestamps and the legacy first-party-ID repair exception. Key compiled runtimes by exact path plus verified content checksum. Cache cleanup follows acknowledgement and is derived-only because content identity, not invalidation timing, guarantees correctness. Removal uses the converged file-delete authority and cannot publish cache state ahead of storage.
- **Proof direction:** first reproduce stale cross-manager invocation, divergent concurrent first-deploy IDs, accepted checksum corruption, and key aliasing. Then prove exact one-sequence deploys, zero-sequence rejected/no-op bundle decisions, malformed/oversized/pressure refusal before publication, deploy/remove races, canonical bundle idempotence, sync compatibility, startup refusal, restart, architecture, docs, broad tests, and disposable live HTTP behavior.

## P2e Wave 4i-2 Implementation And Evidence

- **Authority and compatibility:** regular deployment reuses the current stable plugin ID and `created_at` while one bounded `transform_file_buffered` authority window holds the strict current-record decision through publication. Bundled installation first classifies canonical no-op, newer-version, different-ID, or needs-store under authority; only needs-store reserves and validates WASM, then the policy is re-evaluated under the writing authority. Canonical first-party IDs, release timestamps, version comparison, and the legacy independently-installed bundle-ID repair exception are unchanged. PluginRecord JSON and v3 filename encoding are unchanged.
- **Bounds and failure direction:** plugin binaries are capped at 8 MiB, persisted JSON records at 48 MiB, and each ID/name/path/version/author field at 4 KiB. Empty, control-bearing, reserved, and literal-`::` paths fail before publication. Deployment reserves the larger of WASM compile amplification and the complete bounded replacement workspace. Stored nonempty checksums, record paths, metadata, and bodies are verified; legacy empty checksums are derived read-only. Malformed current authority, size, validation, and pressure failures consume no mutation sequence.
- **Cache and acknowledgement:** compiled runtimes are keyed by exact conceptual path plus verified BLAKE3 checksum. An invocation that starts after replacement therefore cannot find stale path-only bytes in another manager. Post-ack path invalidation uses reusable clean-cache predicate removal to retire every local content generation; cleanup failure is logged and metered as derived-state evidence rather than changing an already-acknowledged mutation into a false failure. Remove uses the converged file-delete authority and publishes cache cleanup only after success.
- **Focused and live proof:** the 25-test manager suite and 26-test HTTP suite cover deterministic stale execution, concurrent identity, deploy/remove linearization, checksum/path corruption, legacy compatibility, exact limits, pressure, cache accounting, canonical bundles, startup refusal, and response-memory behavior. The 389-test affected boundary additionally covers default `extract`/`jq`, parser/index/query/rule/task consumers, system storage, sync portability/conflicts, startup, and architecture. A fresh live database completed HTTP success/failure, restart, and offline integrity verification; no process or scratch artifact remains.

## P2e Wave 4i-3 Preflight: Legacy Config, Email, Path Migration, And Failure Direction

- **Producer/consumer map:** `FileAuthProvider` creates or loads the local JWT seed; CLI cluster join replaces it with the admitted cluster seed; join, readiness, health, sync-token minting, and every authenticated route consume it. Root-only email settings write one node-local secret document; settings reads, test mail, magic-link mail, and share notifications consume it. Engine construction always runs the two legacy system-directory migrations before returning an engine. Plugin HTTP framing, parser/index diagnostic logs, and automatic reindex scheduling are the remaining touched-system soft-failure sites.
- **Observed defects:** JWT first initialization performs an unlocked read followed by a later overwrite, so concurrent providers can return different in-memory keys while only one remains persisted. Generic config keys and values are unbounded and permit path-shaping characters; readiness accepts any key of at least 32 bytes although the JWT reader requires exactly 32. Email reads are unbounded, fields are not validated, embedded saves can amplify arbitrarily, and secret masking squelches serialization failure into an empty success object. Startup migration performs copy and delete as separate acknowledged writes, changes content metadata, leaves identical aliases behind, and silently accepts divergent canonical collisions.
- **Landing decomposition:** first add one bounded legacy config adapter with exact JWT get/store/initialize operations and a single-winner namespace transform, then bound and validate the existing email JSON without changing its persisted schema, then add one per-entry migration transition that preserves content metadata and atomically retires the old alias. Identical old/new records converge by retiring the legacy alias; divergent records fail startup without changing either. Touched derived logging and scheduling failures become explicit, metered/logged soft evidence; correctness-bearing failures remain fatal.
- **Proof order:** deterministic concurrent JWT initialization and malformed/boundary config tests fail first; email oversized/invalid/masked serialization behavior follows; migration tests then pin one-sequence moves, zero-sequence divergent refusal, exact metadata, identical convergence, retry, and startup refusal. Focused auth/system/email/startup/cluster/plugin/index/task tests precede architecture, docs, broad workspace, restart, disposable live HTTP, and offline verify gates.

## P2e Wave 4i-3a Implementation: Legacy And JWT Config Authority

- **Red proof:** 32 simultaneous `FileAuthProvider` initializers returned different Ed25519 seeds even though only the last write remained persisted. Cluster readiness also accepted a 33-byte value that `JwtManager` rejects, while empty/path-shaped keys and values above 1 MiB reached the ordinary file path.
- **Authority and bounds:** legacy config keys are nonempty, at most 255 bytes, and reject control, separator, and storage-escape characters; values are capped at 1 MiB before write and bounded before read. The reserved JWT key cannot pass through the generic writer unless it is exactly 32 bytes. Typed read/store operations share that contract. First initialization performs one authority-held transform and returns either its candidate or the already-persisted winner; malformed current authority fails closed without a write.
- **Consumers and proof:** `FileAuthProvider`, cluster readiness, join response, and CLI join now use the typed JWT adapter. The deterministic concurrent test consumes exactly one durability sequence and every provider returns the persisted winner. Malformed candidate/current, boundary, restart, HTTP join, health, generic config, CLI configuration/startup, and source-architecture tests pass: 277 library tests plus 59 CLI tests. Formatting, `git diff --check`, and `mdbook build docs` pass.

## P2e Wave 4i-3b Implementation: Bounded Email Configuration Authority

- **Bounds and compatibility:** the singleton email document retains its existing SMTP/OAuth JSON schema and path. Reads are capped at 128 KiB before materialization; every string field is capped at 16 KiB. Required identity, nonzero SMTP port, control-character, optional endpoint, and required OAuth-secret validation runs before persistence. Invalid persisted semantic authority is corruption, malformed JSON remains an explicit parse failure, and oversized stored authority fails before deserialization.
- **Failure direction:** secret masking now returns an explicit `EngineResult` instead of converting serialization failure into an empty successful object. Root HTTP writes return `400` for invalid authority and `413` for resource-limit violations; both consume zero durability sequence and create no config file. Root reads surface corrupt authority as `500` rather than reporting an unconfigured system. Notification consumers retain their existing post-authority failure behavior.
- **Proof:** 7 adapter tests cover round trip, exact and exceeded limits, malformed/invalid persisted state, zero-write refusal, SMTP and OAuth constraints, and masking. The 22-test HTTP suite proves root authorization, masking, explicit corrupt-state failure, and zero-write `400`/`413` responses. The 168-test adjacent gate covers magic links, share links, sharing/notification behavior, and source architecture. Workspace/all-target checking passes with only the historical unused WASM-test macro warning; formatting, `git diff --check`, and `mdbook build docs` pass.

## P2e Wave 4i-3c Implementation: Atomic Legacy System-Path Migration

- **Red proof:** one migrated row crossed two durability sequences, five rows crossed ten, divergent old/canonical aliases returned success while retaining both, and identical aliases were skipped without retiring the legacy path. The four exact target regressions failed before production changes while the other 82 system-store tests remained green.
- **Authority and compatibility:** `migrate_system_file_alias` plans one optional namespace mutation that either moves the source FileRecord to its canonical locator and retires the legacy locator, retires an exact duplicate legacy alias, or performs no mutation when the source is absent. Divergent canonical authority fails as corruption before publication and leaves both aliases unchanged. The move preserves content type, total size, creation/update timestamps, opaque metadata, content hash, and ordered chunks while retaining v3 bytes and path families.
- **Bounds and concurrency:** source and destination FileRecords use bounded verified reads with a 1 MiB encoded-record ceiling. One 8 MiB process-memory reservation covers bounded decoding and all planned FileRecord copies; pressure and oversized authority fail before mutation. Eight concurrent migration attempts produce one winner and exactly one durability sequence. Fresh writes initialize empty opaque metadata, while restore, sync, copy, and rename preserve it so migration cannot silently erase caller-owned header state.
- **Proof:** 503 affected tests pass across system storage, DirectoryOps, buffered batch, sync apply/engine, pipeline/event/blob publication, v3 facade, versioning, and write-pipeline targets. A disposable home-cache database seeded both legacy families, started through the normal CLI, migrated both rows, preserved MIME and opaque metadata, returned healthy, and shut down cleanly. Offline inspection proved old aliases absent and canonical bytes/metadata exact; `aeordb verify` reported 134/134 valid entries with zero hash/header/KV/directory/B-tree/Void/snapshot issues. A second normal startup emitted no migration operations and shut down cleanly.

## P2e Wave 4i-3d Implementation: Explicit Derived-Failure Direction

- **Red proof:** plugin request-envelope serialization retained an empty-body fallback; a corrupt parser/index diagnostic log was silently replaced and advanced the durability sequence; malformed automatic-reindex task/config authority returned a durable config acknowledgement without any durable-follow-up failure evidence. A source gate also demonstrated that obsolete permission, node, peer, and raw plugin compatibility writers remained callable after their typed authorities had taken ownership.
- **Failure direction:** correctness-bearing plugin request framing now returns explicit `500` and increments the existing plugin-error metric. Parser/index diagnostics use one memory-admitted bounded atomic append, preserve corrupt authority, cap retained complete records at 1 MiB, and meter derived append failures. Every post-config-commit automatic-reindex list, cancel, config read/decode, enqueue, unavailable-queue, and worker-join failure is structured and counted through `aeordb_system_soft_failures_total`; these failures do not falsify the already-durable config-file acknowledgement, and malformed config conservatively requests a full reindex.
- **Bypass removal:** raw permission, node, whole-peer-list, and plugin record write/read/remove compatibility helpers were deleted. Tests requiring malformed or legacy bytes seed the exact stable paths explicitly; behavioral tests use `PermissionStore`, `initialize_node_id`, `PeerConfigStore`, and `PluginManager`. The architecture gate prohibits their reintroduction and prohibits low-level entry/transaction authority in system/plugin owners; the existing task gate continues to enforce one coordinator-owned batch.
- **Focused and live proof:** 53 pipeline, 20 task-HTTP, 26 plugin-HTTP, 63 facade, and 169 adjacent tests pass for the failure-direction slice. The bypass-removal boundary passes 348 tests across system storage, cluster join/health/HTTP, sync, plugin manager/HTTP, and facade architecture. A fresh normal CLI server proved a valid automatic reindex, a protected parser diagnostic, durable malformed-config `201`, structured `automatic_reindex/config_decode` evidence, exactly one matching Prometheus series increment, a failed conservative full task, successful retry after repair, clean restart, and offline 155/155 verification with zero integrity findings. No server or scratch artifact remains.
- **Wave exit:** workspace/all-target checking, explicit all-target compilation, format, documentation, and diff checks pass. The full suite executes all 5,184 eligible tests successfully from 5,191 registered tests; seven remain intentionally ignored. The 63-test disk-KV group, recurring sync, restart, fault/corruption, architecture, metrics, plugin, task, and system-authority groups all pass under the four-job/four-thread and home-backed scratch limits.

## P2e Wave 1 Preflight: Core DirectoryOps

- **Entry commit:** `1a13f18`; P2d is green and pushed, and no P2e production edits existed before the host crash.
- **Operation ledger:** buffered and streamed file writes pre-store immutable chunks, then independently publish FileRecord content, identity, and path keys; parent propagation writes flat/B-tree dependencies, directory path locators, and HEAD; user counters/events run after the legacy transaction. File/directory/symlink deletion separately writes deletion evidence, retires the path locator, rewrites ancestors, and publishes HEAD. FileRecord migration and directory rebuild bypass the exact-receipt facade entirely.
- **Split-authority defects:** `btree_insert_batched`, `btree_from_entries`, and `btree_delete` persist nodes while parent state is still being planned. Directory cache fills and implicit-directory counters also occur before hard acknowledgement. `delete_file_with_indexing` opens an outer namespace transaction and then calls the independently guarded `delete_file`, preserving a nested producer path that cannot enter the exclusive P2d top-level coordinator.
- **Landing design:** the coordinator will plan under one held namespace guard, validate and pre-admit before the first write, and preserve exact FileRecord entry versions. B-tree construction/insertion/deletion will produce mutation-free dependency plans shared by current legacy adapters and the new coordinator path. Wave 1 producers will contribute immutable dependencies, stable path-locator replacements/retirements, one HEAD transition, exact source identities, and post-commit cache/counter/event effects to one facade call.
- **Wave boundary:** restore/version, copy/rename, blob/batch/merge, sync/import, system/plugin, and maintenance repair callers remain on their explicitly assigned later waves. Shared legacy helpers may remain only for those named consumers and must be source-gated so converted Wave 1 methods cannot call them.
- **Proof order:** green pre-change facade/DirectoryOps/B-tree/event/migration/rebuild baseline; failing coordinator-planning, version, operation-ledger, no-precommit-side-effect, B-tree, nested-wrapper, and architecture tests; focused implementation gates; adjacent producer/consumer suites; full workspace; live create/overwrite/delete/directory/symlink/reopen/verify; then progress evidence and a pushed coherent commit.

## 2026-08-06 Context Recovery

- The host crash changed assistant context, not repository state. `development` remained clean at pushed commit `9151f8d` apart from the acknowledged untracked `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` paths.
- Both native P1c hosts remain unavailable: `wyatt-mac` returns `No route to host`; `win11vm` returns `Connection refused` on its forwarded SSH port.
- The P2c registry proof can still advance without violating the production start gate: add the independent manifest-oracle and runtime-classification tests first, demonstrate their precise failure against the absent typed/runtime APIs, and keep production activation blocked pending native closure or an explicit dependency waiver.

## P2c-1 Registry Model Proof

- **Red proof:** the focused `v4_system_family_runtime_spec` failed because the runtime exposed all eighteen policy fields as raw `u8` values, had no shared embedded-registry accessor, and classified known absolute-family ancestors as unknown protected state.
- **Implementation:** the runtime now decodes every frozen policy byte into a closed enum or validated GC bitset, owns one `OnceLock` registry per hash algorithm in the SystemFamily module, and exposes that exact object to both header and peer admission. Strict ancestors of absolute registered paths are classified as runtime-only `StructuralContainer` values with no family ID or invented policy; unmatched protected descendants remain `UnknownProtected`.
- **Independent proof:** the new target compares all 46 family policies and all 63 descriptors to `system-family-registry-v1.manifest.json`, proves pointer reuse for all five hash algorithms and admission, and pins permissions, conflicts, controls, API-key, and email-secret policy corrections. It passes 4 tests. Existing admission passes 9 tests, the persistent-format target passes 72 tests, the 21,364-case reader-hardening target passes, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages.
- **Boundary:** no backup, sync, index, GC, generic route, traversal, or writer behavior is activated by this landing unit. Native P1c execution remains open and P2c consumer conversion remains blocked behind that recorded gate.

## P2c-1 Traversal Outcome Proof

- **Red proof:** `traversal_completeness_spec` failed because B-tree results exposed only entries/warnings, page-window visitor stops had no independent completion state, directory wrappers had no typed traversal result, and malformed flat directories still became empty results without retained evidence.
- **Implementation:** added shared `TraversalIntegrity::{Complete, DiagnosticallyPartial, Corrupt}` and `VisitorCompletion::{Exhausted, StoppedByVisitor}` contracts. Best-effort B-tree roots now classify missing, wrong-type, or malformed root authority as corrupt; damage below a valid root merges upward as diagnostically partial; operational I/O, pressure, durability, shutdown, and cancellation errors remain hard `Err` results. Directory traversal now retains flat and B-tree issues in one typed result, and bounded windows report integrity independently from intentional early stop.
- **Proof:** the focused target passes 5 tests covering a valid tree, missing child, missing root, malformed inline root, malformed flat directory, cycle, and early page completion. Existing strict/best-effort B-tree, B-tree directory, DirectoryOps, and recursive listing targets pass 163 tests unchanged; the complete P2c-adjacent boundary passes 199 tests. Package all-target checking passes with only the historical unused WASM-test macro warning, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages. The first adjacent command named a nonexistent `recursive_listing_spec`; the corrected registered target is `directory_listing_spec` and passes.
- **Boundary:** ordinary listing keeps its existing diagnostic best-effort response while no longer destroying flat-corruption evidence in the checked path. Consumer-specific fail-closed conversion for backup, sync, indexing, GC, repair conclusions, and system/plugin enumeration remains the next P2c unit.

## P2c-2 Shared Operation Resolver

- **Red proof:** the focused runtime target failed because no shared resolver, closed transfer-operation selector, or typed ordinary/structural/known operation result existed. Consumers would otherwise have to select raw policy columns independently and repeat unknown-protected handling.
- **Implementation:** `SystemFamilyPolicyResolverV1` now owns classification through the exact embedded registry and selects complete full, transfer, or index policy without collapsing structural containers into ordinary data. `SystemFamilyTransferOperationV1` closes the seven frozen transfer columns, and `SystemFamilyPolicyDecisionV1` keeps ordinary, structural-container, and known-family decisions distinct. Unknown protected state remains a typed `unknown_protected_system_family` failure.
- **Proof:** the runtime target passes 6 tests, including every transfer operation over all 46 independent manifest rows. Admission passes 9 tests, persistent formats pass 72, the bounded reader-hardening target passes its 21,364-case corpus, all-target checking is green apart from the historical unused WASM-test macro warning, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages.
- **Boundary:** this unit adds the single consumer-facing decision surface but deliberately changes no backup, replication, indexing, authorization, GC, or generic route behavior. Those consumers are converted in the following landing units.

## P2c-2 Index Scope Convergence

- **Red proof:** direct indexing accepted an unknown `.aeordb-future` path as ordinary data, while the reindex worker downgraded its typed pipeline failure and completed successfully. Wrapper-level predicates also made streaming, batch, full-parser, and reindex paths choose independently. The strengthened reindex fixture deletes the unknown file's body to prove classification must happen before body I/O.
- **Implementation:** added the engine-facing `SystemFamilyPolicyResolver`, which centralizes path normalization and typed runtime error translation. The indexing pipeline now selects the frozen `IndexPolicyV1`; permissions and ordinary data remain indexable, declared excluded/not-applicable/canonical-only families are skipped, and unknown protected state returns `EngineError::SystemFamilyPolicy`. Streamed, buffered, full-parser, and batch paths delegate to the pipeline. Reindex classifies before migration or body reads, preserves known internal FileRecord migration, and treats policy failure as a task-fatal result.
- **Proof:** the affected gate passes 171 tests across `pipeline_spec` (49), `reindex_spec` (28), `write_pipeline_spec` (21), `upload_commit_spec` (21), `directory_ops_spec` (46), and `v4_system_family_runtime_spec` (6). Package all-target checking passes with only the historical unused WASM-test macro warning.
- **Removal evidence:** `is_internal_path` has no remaining indexing or reindex consumer. Its only live consumer is the pending client-sync conversion; wrapper-level index guards and task-owned protected-name lists are gone.

## P2c-2 Logical Backup And Data Export Convergence

- **Red proof:** a logical backup retained `/docs/.aeordb-indexes/text.idx`, and an unknown protected child returned a bare directory parse error after its corrupt body had already been read. The focused tests also pinned that malformed redacted secrets must not be read while malformed required portable records must fail the operation.
- **Implementation:** `include_system=false` now selects `DataExport` and `include_system=true` selects `LogicalBackup`. One registry resolver filters HEAD and historical tree traversal, derives detached absolute candidates from embedded descriptors, and classifies children before reading their FileRecord, symlink, or directory bodies. The export writer rebuilds flat or B-tree directory closure bottom-up, prunes empty structural containers, preserves retained header flags and entry versions, and rejects unknown protected state. Hard-coded backup path arrays, credential predicates, and special system-tree writers were removed.
- **Focused and adjacent proof:** `backup_diff_spec` (23), `backup_export_spec` (31), `backup_http_spec` (20), `backup_import_spec` (22), `backup_task_spec` (11), `tree_walker_spec` (21), and `v4_system_family_runtime_spec` (6) pass, for 134 tests. `v4_admission_spec` (9), `v4_format_fixture_spec` (72), and `v4_reader_hardening_spec` (2 tests with 21,364 malformed cases) pass. `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all`, `mdbook build docs`, and the v4 contract gate are green; the contract gate reports 436 fixtures, 95 routes, and 38 documentation pages.
- **Live proof:** after explicitly rebuilding the CLI, a disposable server stored `ordinary-live`, a namespace permission record, and a derived index through real `PUT /files/...` requests (all HTTP 201), then produced a logical artifact through `POST /versions/export` (HTTP 200). Offline verification reported 10 valid entries, zero integrity/KV/directory issues, and exactly 26 current logical bytes: ordinary data plus permissions, with the 13-byte derived index omitted. Booting the artifact returned exact bodies for ordinary data and permissions (HTTP 200), concealed the derived index (HTTP 404), and shut down cleanly. The first live attempt used a stale `target/debug/aeordb` left by test-only relinking and was discarded; the explicit `cargo build -j 4 --workspace` made the executable proof current.
- **Boundary:** import, peer/client sync, GC, permission authority, plugin/system enumeration, repair/verify conclusions, and generic protected-path adapters remain unconverted. The old v0 system predicate and `augment_with_system_subtrees` therefore remain only until those consumers are moved and the architecture-removal gate can become authoritative.

## P2c-2 Peer And Client Sync Convergence

- **Red proof:** embedded and HTTP diff producers independently walked and filtered trees, peer augmentation silently skipped every failure, local sync copied chunks and raw FileRecords before selective merge resolution, and inbound apply accepted registry-omitted paths. Focused tests reproduced forbidden peer mutations, unknown protected state read before classification, same-HEAD detached portable changes, selective chunk leakage, and a malicious HTTP peer triggering forbidden-state handling.
- **Implementation:** embedded and HTTP diff production now delegate to `compute_sync_diff`, which selects peer or client registry policy and uses one strict, budgeted transfer walker. Detached current authorities are registry-derived; historical bases exclude detached current state. Chunk manifests are recomputed after registry, selective-path, API-key, and permission filtering. Local sync uses the same tree authority, transfers only chunks required by resolved `AddFile` operations, and reconstructs records through normal mutation APIs. Both embedded apply and remote HTTP receive reject omitted, node-local, unknown, or structural-leaf paths before chunk requests or mutation.
- **Pre-writer correction:** the live proof discovered that root `/.aeordb-indexes` and `/.aeordb-logs` producers were absent from the 61-descriptor registry. The independent source now adds absolute-prefix descriptors to existing families `0x0060` and `0x0061`, preserving their policies and descendant matchers. All 63 descriptors, fingerprints, dependent v4 headers, manifests, ledgers, readers, and admission fixtures were regenerated before any v4 writer exists.
- **Proof:** `sync_api_spec` (24), `sync_apply_spec` (11), `sync_engine_spec` (29), `client_sync_spec` (27), `replication_e2e_spec` (19), `sync_http_spec` (18), `backup_export_spec` (31), and `tree_walker_spec` (21) pass, for 180 tests. Runtime SystemFamily (6), persistent fixtures (72), v4 admission (9), and the 21,364-case malformed-reader target pass. The independent reference passes 145 tests and freshly verifies all 436 cases.
- **Live proof:** the original real-server request failed HTTP 500 with `unknown_protected_system_family` at `/.aeordb-indexes`. After explicit CLI rebuild, the same disposable database returns HTTP 200; visible paths contain ordinary data, permissions, namespace configuration, and portable plugin state while root/nested indexes, logs, and conflicts are absent. Advertised chunks exactly equal the unique chunks referenced by visible files. Graceful shutdown succeeds and offline verify reports 242 valid entries with zero corrupt headers/hashes, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid voids, or broken snapshots.
- **Removal evidence:** sync producer code contains no `is_system_path`, `is_internal_path`, protected path literal, `augment_with_system_subtrees`, duplicate `walk_version_tree`, or duplicate `diff_trees` policy path. The compatibility boolean remains only as an adapter selecting the closed peer/client transfer operation.
- **Deferred receiver hardening:** the legacy HTTP receiver still needs strict typed response/chunk decoding, chunk payload hash and completeness proof, and convergence on the shared apply authority. Embedded apply also still squelches non-`NotFound` delete errors. These are explicit P2e/Child 06 obligations and prevent claiming end-to-end sync apply closure here.
- **Broad proof:** `cargo test -j4 --workspace --all-targets -- --test-threads=4` passed 4,814 tests across 196 suites with zero failures and seven ignored. `cargo check -j4 --workspace --all-targets`, workspace and reference-tool formatting checks, `mdbook build docs`, `git diff --check`, and the v4 contract gate are green. The contract gate independently verifies 436 fixtures, 95 routes, and 38 documentation pages.

## P2c-3 Registry-Governed Full-Export Import

- **Red proof:** a full export augmented with an unknown `/.aeordb-*` path imported successfully and advanced HEAD, while a privileged import copied a node-local API-key payload. Clearing legacy `FLAG_SYSTEM` from a portable user record also made CLI inspection misclassify the artifact as user-data-only.
- **Implementation:** full-export import now validates HEAD and every named snapshot through one operation-specific registry walk before the first target mutation. A second bounded walk writes only selected closure, rebuilds directories when children are omitted, replaces current path aliases, remaps snapshot roots, preserves portable required state, and omits credentials, node-local controls, logs, GC state, and derived indexes. CLI inspection decodes FileRecord, Symlink, DeletionRecord, and Snapshot subjects through registry Import versus DataExport policy instead of trusting header flags. Import-mode tree writes preserve logical operation metrics and count newly copied chunk bytes once; artifact construction does not contaminate runtime metrics.
- **Proof:** `backup_import_spec` (27), `backup_export_spec` (31), `backup_http_spec` (20), `event_emission_spec` (34), `tree_walker_spec` (21), and `v4_system_family_runtime_spec` (6) pass, for 139 tests. Workspace/all-target checking, formatting, `git diff --check`, and `mdbook build docs` are green.
- **Boundary:** sparse patch artifacts still use the legacy raw-inventory application path. They require an overlay-aware registry validation/rebuild design so omitted patch families cannot be copied or left reachable through the advertised target root; that remains the next P2c-3 landing unit.

## P2c Preflight Test Protocol

1. **Registry authority:** decode the checked-in binary through the production reader while using the independently generated manifest as the policy oracle. Prove all 46 families, 63 descriptors, every typed policy value, all five hash-specific cached registry instances, and admission/runtime pointer identity. The production serializer or decoder may not generate its own expected values.
2. **Path classification:** prove ordinary paths, all matcher kinds, exact known families, unknown protected paths, and runtime-only structural ancestors such as `/.aeordb-system`, `/.aeordb-system/cluster`, and `/.aeordb-config`. Structural containers are traversal-only and never inherit or invent a persisted family policy.
3. **Operation policy:** table-drive logical backup, peer replication, client sync, import, generic data, indexing, verify, repair, migration, spill, and GC decisions over every family. Pin the required corrections for permissions, conflicts, controls, credentials/secrets, derived indexes, and logs.
4. **Traversal states:** independently reproduce a valid flat directory, malformed flat root, valid B-tree, malformed inline B-tree root, missing/corrupt child branch, cycle, warning-cap exhaustion, operational I/O/admission failure, cancellation, and visitor-requested early stop. Integrity is exactly `complete`, `diagnostically_partial`, or `corrupt`; visitor completion is a separate field.
5. **Consumer behavior:** destructive or authoritative backup, import, sync, reindex, GC, counters, repair conclusions, and system-store enumeration must reject incomplete traversal. Verify and explicitly diagnostic tooling may retain readable entries while recording bounded evidence. No caller may turn partial/corrupt into an empty or complete collection.
6. **Architecture removal:** fail while legacy policy predicates or protected path arrays remain outside the named v0 byte-layout evaluator. Permit route/SDK compatibility parameters only as adapters into registry operation policy, never as lower-level authority.
7. **Verification order:** focused red target; adjacent 194-test characterization set; route/plugin/transfer/index regressions; contract and architecture gates; workspace suite; native checks; then a live home-backed disposable server exercising authorized and concealed paths plus injected traversal damage. Every command uses four Cargo jobs/threads, the home-backed test temporary directory, and a hard timeout.

## P2c-3 Registry-Governed Sparse Patches

- **Red proof:** sparse patch artifacts copied protected leaves without one logical-backup decision, trusted embedded FileRecord/Symlink paths, applied deletion inventories directly, and published the artifact's unfiltered target root. A filtered full export/import also changed the base root, so the next valid patch was rejected despite an equivalent selected namespace. After the first implementation pass, a live restart exposed one further invariant violation: sparse deletion tombstoned the target path without writing the durable `DeletionRecord` used by WAL rebuild and verification.
- **Implementation:** patch generation now strictly walks both source roots under `LogicalBackup`, rejects missing/corrupt roots and unknown protected state before artifact creation, stores complete selected base and target directory closures, and keeps unchanged FileRecords/chunks sparse. Import validates every leaf and the complete patch-first/target-fallback overlay before mutation, rejects embedded-path/key/retained-delete contradictions, accepts exact or selected-semantic base equivalence, rebuilds only selected closure, avoids rewriting identical target aliases, and returns the rebuilt selected target root. Applied deletion records retain their validated version/flags/payload, are written under the ordinary durable deletion key before the path alias is tombstoned, and survive restart/KV rebuild. Directory routing scratch is coordinator-admitted and released.
- **Focused proof:** `backup_diff_spec` passes 26 tests, `backup_export_spec` 31, `backup_import_spec` 41, `tree_walker_spec` 21, and `corruption_hardening_spec` 64. The restart regression first failed with exactly one missing KV entry and passes after durable deletion evidence was restored. Pressure, malformed body/key/path, structural-leaf, unknown-family, node-local omission, namespace-permission, restore-mode, force, unchanged-overlay, and logical-base cases are all covered.
- **Live proof:** a fresh disk-backed source and target under the home-backed scratch root completed CLI full export/import followed by a sparse add/change/delete patch. A half-truncated patch failed with `Unexpected end of file` and left the target byte-for-byte unchanged. The valid patch survived restart; HTTP returned `keep-v1`, `change-v2`, `add-v2`, and `404` for the deletion. Offline `aeordb verify` reported 103 valid entries, zero corrupt headers/hashes, zero directory issues, and zero stale/missing/invalid KV entries.
- **Broad proof:** `cargo test -j4 --workspace --all-targets -- --test-threads=4` passes 4,836 tests across 196 suites with zero failures and seven intentional ignores. Workspace/all-target checking, formatting, `mdbook build docs`, `git diff --check`, and the independent v4 contract gate are green; the contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages. The only check diagnostic is the historical unused `require_wasm_parser` test macro warning.
- **Boundary:** current patches use selected logical roots and carry both directory closures; legacy exact-root patches remain compatible when their overlay is complete. Sparse import still mutates path aliases before optional HEAD promotion under the existing v3 publication model; P2d owns the frozen mutation/locator publication replacement. Remaining P2c work is consumer convergence and removal of competing legacy policy lists.

## P2c-4 Generic Data And Plugin Consumer Convergence

- **Red proof:** generic routes and WASM hosts recognized only the two v0 detached system roots, so nested conflicts/indexes and other registry-concealed families could be read, shared, downloaded, linked, versioned, or written. Query rows were filtered only after pagination, while aggregate and analyze cardinality included concealed rows.
- **Implementation:** one generic-data resolver now governs direct file/blob/fetch/download/share/share-link/version/symlink operations, path-aware middleware, recursive listings, search results, and WASM host access. QueryEngine exposes authority-filtered paginated, aggregate, and explain execution; filtering occurs before sorting, totals, groups, offsets, limits, and cursors. Unknown protected state remains a typed failure, permissions remain visible, structural containers remain traversal-only, and system plugin identity no longer bypasses generic family policy.
- **Proof:** full `aggregation_spec` (41), `query_pagination_spec` (45), `download_spec` (25), `share_link_spec` (18), `sharing_spec` (44), `system_access_spec` (34), `upload_commit_spec` (22), `v4_system_family_runtime_spec` (7), and `wasm_query_e2e_spec` (27) pass, for 263 tests. Workspace/all-target checking and formatting are green; the only diagnostic is the historical unused `require_wasm_parser` test macro.
- **Boundary:** operation-owner repair/verify/GC policy, backup target inspection, deleted-record and repair traversal, v0 flag/topology compatibility naming, and the architecture-removal gate remain open. Global search index discovery already omits concealed index scopes; its result-layer registry filter remains defense in depth rather than the pagination authority defect reproduced in direct query execution.

## P2c-5 Strict Authority And Consumer Exit

- **Red proof:** authoritative listing consumers could reinterpret malformed flat directories as empty, accept diagnostically partial B-tree walks, skip missing aliases, or collapse stored user/group/key/permission/peer decoding failures into absence. Generic HTTP, share, permission, cluster, sync, index-discovery, GC, verify, repair, backup-target, plugin, and live-counter paths still had independent fail-open or best-effort wrappers. Permission middleware additionally broadened non-path access when an API-key authority lookup failed.
- **Implementation:** explicit strict flat, B-tree, windowed, visitor, and recursive DirectoryOps APIs now preserve diagnostic listing separately while rejecting corrupt, partial, unexplained-missing, over-budget, and unknown-protected authority. Backup inspection, GC, verify, repair, counters, system stores, conflicts, grants, index discovery, query fallback, reindex, WASM, ZIP, symlink, generic routes, sharing, cluster state, and sync state use that strict authority. Stored permission/user/group/API-key/index/peer records retain corruption taxonomy. One active-key helper, result-bearing permission checks, and strict share metadata classification remove broadening and whole-file buffering. The old path predicate is gone; only the explicitly named v0 on-disk flag evaluator remains, guarded by source architecture tests.
- **Failure-direction proof:** malformed permission authority returns `500` without overwrite; missing or invalid API-key identity returns `401` while authority read failure returns `500`; unknown protected paths fail typed/`500`, known concealed paths remain `404`; malformed peer/sync/system authority disables or rejects the affected operation instead of producing partial success; destructive GC and repair conclusions reject incomplete traversal.
- **Automated proof:** the final workspace/all-target gate passes 4,887 tests across 196 suites with zero failures and seven intentional ignores. `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all -- --check`, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate pass. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages. The only check diagnostic is the historical unused `require_wasm_parser` test macro.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh home-backed database through the normal server path. Ordinary PUT/GET/list returned `201/200/200`; known concealed and unknown protected paths returned `404/500`; malformed `.aeordb-permissions` made share return `500` while byte-for-byte authority remained unchanged; cleanup delete returned `200`; graceful SIGTERM exited zero. Offline verification reported 328 valid entries, zero corrupt hashes/headers, zero missing/dangling/B-tree/unlisted directory issues, zero stale/missing/invalid KV entries, zero invalid Voids, and zero broken snapshots.
- **Platform boundary:** native P1c execution is not claimed. The final retry still found `wyatt-mac` unreachable with `No route to host` and `win11vm` refusing its forwarded SSH port. Exact native fixture, admission, reader-hardening, contract, and legacy gates remain mandatory when those hosts return.
- **Deferred boundaries:** P2d owns mutation/locator publication and the existing diagnostic stale-child deletion compatibility. P2e/Child 06 owns path/diff-bound chunk capabilities, strict remote response/chunk decoding, shared receive/apply authority, and non-`NotFound` delete propagation. The broader P8 audit still owns optional cron/webhook/configuration error-squelch review; none is represented as P2c closure.

## P2d-1 Preflight: Shared Mutation And Locator Facade

- **Entry commit:** `e9330d8`; P2c hotspot handoff is complete and pushed.
- **Territory:** namespace authority currently enters through `namespace_write_guard`, `direct_hard_authority_guard`, `TransactionGuard`, direct HEAD/header publishers, and batch flush helpers. DirectoryOps, batch/blob, backup/import, sync, version, system/plugin/task, repair, GC, and CLI adapters then publish stable path/content/identity keys, HEAD, counters, events, cache invalidations, and index wakeups independently. P2e owns their conversion; P2d owns the shared service contract they will target.
- **Existing substrate:** Child 02 already provides one process-wide durability sequence, grouped hard-frontier proof, fallible transaction completion, operation admission, serious-failure latching, and shutdown visibility. P2d must return the exact existing `DurabilityCommitReceipt`; it must not invent a competing sequence or journal.
- **Facade contract:** top-level namespace admission occurs before mutation; one nonzero operation ID names the whole logical mutation; stable-locator replacement records ordered old/new KV physical incarnations while namespace authority is held; HEAD before/after is captured before acknowledgement; successful completion exposes the exact hard-publication sequence; one soft fanout runs after hard proof and cannot turn committed success into an apparent failure.
- **Red-test protocol:** unique IDs and monotonic sequences; exact replacement offsets/types/lengths; duplicate and mismatched stable keys; nested top-level refusal before writes; injected mutation and durability failures; no pre-commit fanout; exactly one post-commit fanout; panic containment; transaction/namespace reuse after every failure. Persistent bytes and all existing producer behavior remain unchanged until P2e.
- **Narrow gate:** `timeout 3m env TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp cargo test -j 4 -p aeordb --test namespace_mutation_spec -- --test-threads=4`.
- **Red proof:** the five-test target fails to compile at the intentionally absent `engine::namespace_mutation` module. The tests independently require exact durability-frontier reuse, ordered old/new KV incarnation evidence, pre-mutation malformed/nested rejection, reusable transaction authority, and panic-contained one-shot post-commit fanout.

## P2d-2 Shared Facade Implementation Evidence

- **Service boundary:** `NamespaceMutationBatch` is a mutation-free in-memory plan with one closed operation kind, canonical identity-bearing source paths, dependency-first entries, stable-locator replacements/retirements, and an optional v3 HEAD transition. Empty, dependency-only, duplicate/aliased-key, malformed-width, identity-free, invalid-entry-type, unchanged-HEAD, immutable-chunk-locator, and physical-Void-locator plans refuse before hard admission or storage mutation.
- **Durability boundary:** the new top-level transaction path reserves its bounded hard waiter before the first dependency append, reuses that exact `DurabilityCommitReceipt.sequence`, and excludes late legacy transaction joiners under the existing KV transaction mutex. Existing `TransactionGuard` callers retain their current nested behavior and unit-returning completion API. Failed preflight/admission consumes no sequence, writes no locator, emits no fanout, and leaves authority reusable.
- **Locator and acknowledgement proof:** stable replacements capture ordered old/new physical KV incarnations without assuming monotonic offsets; retirement requires an existing mutable namespace locator; successful acknowledgement carries one nonzero UUID, exact hard sequence, previous/new HEAD, exact source identities, and locator evidence. Twelve focused tests include concurrent UUID/sequence uniqueness, memory-pressure refusal, nested refusal, alias/type/no-op rejection, exact physical evidence, one-shot post-commit fanout, panic containment, partial-dependency failure, and hard-publication failure.
- **Non-activation boundary:** the architecture gate permits the two facade types only in `namespace_mutation.rs` and its engine re-export. No current DirectoryOps, blob, batch, version, sync, system, plugin, maintenance, or repair producer imports the facade; producer conversion remains explicitly owned by the characterized P2e waves.
- **Adjacent proof:** all 36 storage-engine transaction/durability tests pass. Eight adjacent suites pass 257 tests: `directory_ops_spec` 48, `disk_kv_store_spec` 63, `event_emission_spec` 34, `hot_file_transaction_spec` 18, `sdk_bulk_write_spec` 15, `upload_commit_spec` 22, `v3_contract_facade_spec` 48, and `version_access_spec` 9.
- **Static proof:** `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all -- --check`, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate pass. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages; the only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Adversarial completion corrections:** final review reproduced a post-hard-commit KV expansion preflight failure that escaped as `InvalidMagic`, making a durable exact-receipt mutation appear retryable. The exact-receipt path now preserves the committed acknowledgement, logs the maintenance failure, and leaves retryable expansion queued; legacy transaction callers retain their existing synchronous expansion-error contract. Review also found that ordinary errors after a successful dependency append could escape without a serious latch. Once any dependency, locator, or HEAD mutation may have started, the facade now classifies every later failure as post-mutation, durably contains the partial evidence, emits no acknowledgement, and latches write authority for repair. Test-only fault seams prove both post-dependency and pre-hard-publication failures without changing production bytes or interfaces.
- **Broad proof:** the final `cargo test -j 4 --workspace --all-targets -- --test-threads=4` gate passes 4,901 tests across 197 result groups with zero failures and seven intentional ignores under a 30-minute hard ceiling. An earlier 15-minute attempt timed out while actively progressing with every completed group green and was not counted as a pass. The focused facade targets pass 12 tests and the storage-engine transaction/durability target passes 36 tests.
- **Live proof:** a freshly built debug CLI started a new home-backed disposable database through the normal server path. JSON and CRLF text PUTs returned metadata for `/p2d/live/a.json` and `/p2d/live/b.txt`; GET/list returned both exact paths. After graceful shutdown and reopen, the JSON remained 26 bytes with SHA-256 `721cadc9d1a7f9a3e3a6ad762df0cfb787a6780002ccb484317d026e78785416`, and the 13-byte text remained `alpha\r\nbeta\r\n` with SHA-256 `98ab4d3aeab1e120560e942e2df6a0db1147bf94bafcf1590000ffb3c2b6fc80`. Offline verification reported 203 valid entries, zero corrupt hashes/headers, zero missing/dangling/B-tree/unlisted directory issues, zero stale/missing/invalid KV entries, zero invalid Voids, and zero broken snapshots. Both server runs shut down cleanly and the disposable workspace was removed.
- **P2d exit:** persistent bytes and existing producer behavior remain unchanged, no producer imports the facade, and P2e owns staged activation. Native P1c execution remains a separate open platform prerequisite and is not represented as P2d closure.

## P2e Wave 1 Core DirectoryOps Convergence

- **Planning and authority:** `NamespaceMutationCoordinator` now supports mutation-free planning while holding namespace authority, exact no-op completion without consuming a ticket or fanout, and version-aware dependency and stable-locator writes. FileRecord and parent-directory planners build complete dependency, locator, and HEAD transitions before execution. One coordinator-owned durability receipt is the acknowledgement authority for each converted logical operation.
- **B-tree planning:** `BTreeMutationPlan` separates node construction, insertion, and deletion from persistence. The existing B-tree APIs remain plan-then-flush adapters for later-wave callers, while converted DirectoryOps producers add planned nodes to their one namespace mutation. Focused tests prove planning performs no storage writes and preserve all flat/B-tree thresholds and traversal behavior.
- **Converted producers:** streamed, buffered, and direct file finalization; file deletion; directory creation/deletion; symlink creation/update/deletion; root initialization; FileRecord v0 migration; full directory rebuild; and targeted directory repair now publish through the shared facade. Stable dependencies and path locators precede one HEAD transition; cache changes, live counters, SSE events, and acknowledgement metrics run once after hard durability.
- **Acknowledgement contract:** successful entry events carry a UUID `operation_id`, exact hard-frontier `publication_sequence`, and closed `mutation_kind`. The new `aeordb_namespace_mutation_acknowledgements_total{mutation_kind}` metric counts the same post-commit authority. Operation-ledger tests prove exactly one event, metric increment, operation ID, and sequence for file, directory, symlink, migration, root-initialization, rebuild, and repair successes; true no-ops emit and consume nothing.
- **Legacy transaction boundary:** a converted top-level namespace write deliberately rejects an already-active legacy `TransactionGuard`. Returning success there would be false because the outer guard, rather than the coordinator, would own the eventual durability result. Transaction tests continue to prove raw dependency batching and reusable authority; the legacy buffered batch remains deferred to Wave 2, where its entire logical operation can move under one coordinator receipt. A rejected nested write may leave only unreachable immutable dependencies, never a stable path or HEAD publication, and normal GC can reclaim those append-only orphans.
- **Failure and architecture proof:** preflight, nested-authority, dependency, locator, hard-publication, and fanout failures retain the P2d failure direction. Source gates forbid converted producers from direct stable-key storage, direct flush/HEAD publication, legacy parent helpers, or legacy transaction ownership while allowing explicitly assigned later-wave callers. Rebuild remains memory-bounded to one planned directory mutation at a time under the outer scan-consistency guard.
- **Automated proof:** focused and adjacent gates passed 474 tests across twelve suites, with another 34 transaction and SDK-batch tests covering the legacy boundary. `cargo check -j4 --workspace --all-targets`, `cargo build -j4 --workspace`, formatting, `mdbook build docs`, `git diff --check`, and the v4 contract gate are green. The contract gate independently verifies 436 fixtures, 95 routes, and 38 documentation pages. The final full workspace/all-target gate passed 4,910 tests across 197 suites with zero failures and seven intentional ignores; its captured log SHA-256 is `b574d20da36b0d5d9ec0985b4c23498b2624026161d9986abd63e2a598e6f55f`.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh home-backed database through the normal server path with auth and snapshots disabled. Real HTTP operations created and overwrote an exact CRLF file, created/read/deleted a symlink, created/deleted directories, and wrote 260 files to cross the B-tree threshold. Deleting one middle file left a correctly ordered 259-entry listing. Two SSE captures covered 269 entry mutations across file, directory, and symlink create/update/delete families; every event carried all acknowledgement fields. After graceful shutdown and reopen, the listing remained 259 entries, surviving content was exact, deleted paths returned `404`, and root contained only the expected tree. Both pre-reopen and post-reopen offline verification reported 2,661 valid entries with zero corrupt headers/hashes, directory inconsistencies, B-tree issues, stale/missing/invalid KV entries, invalid Voids, or broken snapshots.
- **Boundary:** restore/version, copy/rename, blob/batch/merge, sync/import, system/plugin, and remaining maintenance producers retain their named later-wave adapters. Native P1c execution on `wyatt-mac` and `win11vm` remains open and is not claimed by this Linux-green handoff.
- **Landing:** committed as `272b12d`; this is the required `directory_ops.rs` hotspot handoff into Wave 2.

## P2e Wave 2 Preflight: Blob, Batch, Copy, and Rename

- **Entry commit:** `b78363e`; Wave 1 and its hotspot handoff are green and pushed.
- **Producer map:** `/blobs/commit` calls `batch_commit::commit_files`; embedded `store_files_buffered_batch` and JSON merge batch call `commit_buffered_files`; both use a separate namespace guard plus legacy `TransactionGuard`, direct FileRecord materialization, direct flat/B-tree/path-locator writes, direct HEAD publication, and their own counter/event/index fanout. Streamed PUT/finalize already uses the Wave 1 coordinator, but metadata indexing remains a separate post-return wrapper. File and symlink rename directly create destination locators, rewrite two parent chains, retire the source, and emit two events inside a legacy transaction. Recursive copy nests legacy transactions around newly converted Wave 1 producers, and the HTTP multi-source wrapper currently turns failures into partial success.
- **Reproduced boundary:** all 268 pre-change tests across SDK batch, blob commit/e2e, merge HTTP, rename HTTP, sharing, symlink, pipeline, event, and durability-contract suites pass. They characterize current MIME/hash/size validation, system-family filtering, stale-directory recovery, B-tree conversion, auth, created-time preservation, hard-tail publication, and the legacy outer-transaction batch behavior. No existing success test exercises recursive copy; source inspection proves it now enters a legacy outer transaction before calling coordinator-owned `create_directory`, which must reject nested top-level authority.
- **Ratified behavior correction:** Wave 2's multi-file atomic-publication requirement supersedes `/files/copy`'s documented per-source partial-success behavior. The complete requested source closure must validate before mutation and publish under one operation ID/hard sequence, or none of its path locators/HEAD changes may become authoritative. The response keeps its successful `copied` list; failures become the route's normal typed error rather than HTTP 200 with an `errors` array.
- **Planning design:** one directory-delta planner collects upserts, removals, and explicit empty-directory creation, then finalizes deepest paths first so each ancestor is planned once. Large-directory updates use a planned-node B-tree overlay with deterministic lookup/deduplication and reachable-node output; they may not persist intermediate roots or rebuild a whole large tree. File/symlink and directory dependencies, stable locator replacements/retirements, and one final HEAD transition enter one `NamespaceMutationBatch`.
- **Fanout design:** one hard acknowledgement increments the namespace metric once. Blob/buffered/merge/copy operations emit one aggregate created event. Rename keeps the established created/deleted relationship event types for client compatibility, but both derive from one soft fanout and carry the same operation ID/publication sequence. Metadata indexing moves behind hard acknowledgement and shares one buffered index writer per logical operation.
- **Proof order:** red recursive-copy and multi-source atomicity tests; red batch/rename acknowledgement cardinality and outer-transaction refusal; red B-tree overlay/no-write tests; implementation; source-removal gates; focused and adjacent suites; full workspace; API/docs/contracts; live blob/buffered/merge/copy/rename across B-tree threshold; restart and offline verify.

## P2e Wave 2 Implementation And Evidence

- **Shared publication:** chunk-backed `/blobs/commit`, embedded buffered batches, and embedded JSON merge batches now prepare one set of FileRecord dependencies, stable locators, directory deltas, and one HEAD transition for `NamespaceMutationCoordinator`. The old batch-local `TransactionGuard`, direct FileRecord/directory/path/HEAD writers, repeated parent propagation, and pre-acknowledgement indexing are removed. Streamed finalize, batch, merge, copy, and rename metadata indexing now enters the same engine-wide buffered index path after hard publication.
- **Directory and B-tree planning:** one deterministic deepest-first directory-delta planner handles explicit directory creation, child upserts, removals, stale locator recovery, type conflicts, and shared ancestors. Flat directories cross directly into B-tree form at the existing threshold. Existing B-trees apply all removals/upserts through an unpublished overlay and contribute only reachable planned nodes to the namespace batch; planning performs no intermediate node writes.
- **Merge and copy authority:** JSON merge reads and parses the authoritative current document, applies RFC 7396, prepares chunks, and publishes while holding one namespace authority, eliminating the reproduced concurrent lost-update race. Recursive and multi-source copy validates the complete file/directory/symlink closure, destinations, chunk locators, legacy FileRecord migration, depth, and bounded planning memory before publication. Copy preserves empty directories and symlink targets and rejects self-descendants, collisions, missing sources, malformed trees, and non-directory ancestors without exposing partial destinations.
- **Rename and fanout:** file and symlink rename publish destination creation, source retirement, both parent changes, and HEAD under one hard receipt. Their compatibility delete/create events share one operation ID and publication sequence. Batch, merge, and copy emit one aggregate created event; counters, directory-cache fills, metadata-index cleanup/addition, SSE, and acknowledgement metrics run only after hard success.
- **Adversarial corrections:** strict recursive copy traversal no longer heals stale source locators while namespace authority is held, avoiding a hidden nested mutation/deadlock. Legacy v0 copy/rename computes the missing whole-file hash under the same bounded planning admission while current v1 copies remain content-read-free. Copy validates chunk locators through bounded metadata rather than forcing one random body/header read per chunk. `copy_file` returns the FileRecord carried by its acknowledged publication instead of issuing a fallible post-commit metadata read that could falsely report failure after durable success. File publication revalidates every staged chunk while namespace authority is held and refuses missing, malformed, or wrong-type locators before path/HEAD publication. Direct, compressed, buffered, JSON-merge, and HTTP chunk staging all reject wrong-type immutable-key collisions. `/blobs/check` uses the same typed metadata validator and propagates corruption/operational errors instead of reporting an arbitrary KV row as `have` or squelching an error into `needed`. Corrupt symlink classification errors now propagate through rename and delete dispatch.
- **Focused and adjacent proof:** `sdk_bulk_write_spec` passes 27 tests, `v3_contract_facade_spec` passes 52, the upload protocol passes 21, and the two private authority-injection tests pass. Ten adjacent suites pass 334 tests: B-tree 73, DirectoryOps 50, events 36, merge HTTP 17, namespace mutation 13, pipeline 50, rename HTTP 19, sharing 50, blob commit 22, and blob end-to-end 4. These cover 300-child B-tree batch/reopen, exact acknowledgement cardinality, nested legacy-transaction refusal, full recursive copy, multi-source atomic failure, memory pressure, stale locator behavior, legacy v0 migration, metadata-index visibility, concurrent SDK/HTTP merge, route delegation, upload semantics, missing/malformed/wrong-type chunks, and corrupt symlink dispatch.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh home-backed database with auth and snapshot writes disabled. A real chunk upload and 300-file `/blobs/commit` used caller-supplied whole-file hash/size metadata, published a B-tree directory in 278 ms end to end, listed exactly 300 files, and returned exact bytes. Sixteen concurrent HTTP merge patches retained every disjoint field. Multi-source copy preserved a file, empty directory, and symlink target; a request containing a missing second source returned `404` and exposed no first destination; file rename retained exact content. A live filtered SSE stream captured batch, copy, merge, and rename acknowledgement fields, with both rename events sharing one operation ID and sequence. After graceful shutdown/reopen, every count, field, target, absence, and renamed body remained exact. Offline verification reported 1,667 valid entries and zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots.
- **Large-manifest live proof:** an explicitly rebuilt debug CLI started a second fresh home-backed database and accepted a 1,520,255-byte `/blobs/commit` manifest containing 20,000 references to one stored chunk. Chunk upload took 1.5 ms and commit took 167 ms end to end; server diagnostics recorded 21,185 microseconds of KV metadata lookup, zero chunk-body bytes read for validation, one one-byte MIME sniff, 98 ms total chunk validation, and 40 ms namespace publication. The exact 20,000-byte file and one-entry listing survived graceful shutdown and reopen. Offline verification reported 191 valid entries and zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots. The 263 MiB disposable workspace was removed after evidence capture.
- **Final automated proof:** the post-review workspace/all-target gate passes 4,941 tests across 197 result groups with zero failures and seven intentional ignores. Its captured log SHA-256 is `7df7e7bcf66be8260f88cd28c869e267861723aba52035bc04587456d98944b3`. `cargo check -j4 --workspace --all-targets`, `cargo build -j4 --workspace`, formatting, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate are green. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages; the only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Wave 2 exit:** the converted producers have one namespace authority, one hard acknowledgement, and one post-commit fanout; source gates exclude the removed direct/nested paths. Native P1c execution remains independently open until `wyatt-mac` and `win11vm` are reachable. Wave 3 owns restore/version, backup/import promotion, sync apply/conflict resolution, and remaining whole-HEAD transitions.

## P2e Wave 3a Version And Whole-HEAD Transition Convergence

- **Reproduced split authority:** the pre-change version-event proof failed on the first snapshot event because no operation ID existed. Fork promotion directly published HEAD and emitted `versions_promoted`, then separately retired the fork, persisted its deletion record, and emitted `versions_deleted`; a failure between those operations could expose a promoted HEAD plus a surviving fork while returning failure.
- **Shared bounded transition:** `NamespaceMutationBatch` now accepts a whole-HEAD transition as one constant-memory root selection with exact previous/new root source identity. Preflight requires HEAD to select either an existing `DirectoryIndex` root or a directory dependency in the same batch. It never expands the restored tree into per-file mutations; later derived-state workers can reconcile from the acknowledgement's old/new roots, and restart recovery can compare selected authority with derived source roots.
- **Converted producers:** snapshot create/delete/rename/restore and fork create/update/abandon/promote now plan stable locator writes, durable deletion records, optional HEAD publication, counters, and version events under one `NamespaceMutationCoordinator` receipt. Promotion retires the fork and publishes HEAD atomically. Restoring the already-selected root and updating a fork to its current root are true no-ops.
- **Fail-closed readers:** all direct version-locator reads validate the physical entry type and exact stored key. Version listings use a strict type scan that propagates malformed rows instead of silently omitting them. Fork updates and every HEAD transition reject missing or non-directory roots before durability admission.
- **Shared acknowledgement ownership:** acknowledgement metrics and event payload annotation moved from DirectoryOps into the common facade. Version relationship events now carry one UUID, exact hard publication sequence, and mutation kind; promotion's compatibility `versions_promoted` and `versions_deleted` events share that identity.
- **Proof:** the falsifying event and missing-root tests failed against the prior behavior. Focused version/event/namespace/restart proof passes 118 tests. The final expanded adjacent matrix passes 397 tests across version access, versioning, events, namespace authority, restart, lifecycle, counters, GC, content history, sharing, version HTTP, and backup export/diff suites; its log SHA-256 is `db48c4d481e76d083dfe636715d04827879f0b19745f6a1a17285a49aeb9a9a2`. Rename deletion survives reopen, corrupt locator types and invalid roots fail without authority or counter movement, and source gates forbid every converted version entrypoint from direct HEAD, locator, transaction, or event authority. Workspace/all-target check, formatting, docs, diff, and the independent 436-fixture/95-route/38-doc contract gate are green.
- **Boundary:** this landing unit does not alter standalone backup artifact construction. Active backup import/promotion, sync receive/apply, conflict resolution, and their acknowledgement/checkpoint paths remain the next Wave 3 units.

## P2e Wave 3b Active Backup Import And Promotion Convergence

- **Reproduced split authority:** explicit HTTP/CLI root promotion accepted any existing entry hash and wrote HEAD directly; active import wrote path aliases, sparse deletion tombstones, and snapshot locators outside the shared coordinator. Full privileged import also walked current HEAD first and then reused the same alias-writing mode for historical snapshots, allowing an older FileRecord to overwrite the current path locator. Ordinary reads and `exists` trusted those mutable locators, so restoring HEAD did not reliably restore current file/symlink/existence behavior.
- **Root and read authority:** explicit/imported roots must exist as exact `DirectoryIndex` entries and publish through the constant-memory whole-HEAD coordinator. Non-forced promoted imports compare the root captured at import entry while namespace authority is held and return a conflict if another acknowledged write changed HEAD. Ordinary file/symlink reads, metadata, existence, and directory-child filtering now follow the selected root; registry-known/structural SystemFamily state remains deliberately locator-authoritative because those families are detached from HEAD.
- **Bounded locator convergence:** active import path locators use hard-acknowledged maintenance batches capped at 256 locators or 8 MiB retained state, with an oversized single record isolated in one batch. Sparse deletion evidence and locator retirement share the same bounded transaction. Imported historical snapshot trees copy immutable content only, while snapshot-name locators use conditional namespace transactions and never overwrite a concurrent name. Standalone export/patch artifact construction retains its destination writer and direct artifact HEAD publication.
- **Adversarial corrections:** requested roots now pass verified physical reads, flat/B-tree parsing, and canonical-key recomputation before publication, including roots supplied as dependencies in the publishing batch. Ordinary mutation source/destination checks and nested directory loads use the selected root, while explicit repair alone decodes derived locators. Historical and deleted-file restore share one root-authoritative `Restore` receipt with zero payload-byte accounting, exact path/chunk validation, and post-ack event/index fanout. Sparse deletion carries the root-selected prior identity, separately validates any stale live locator for retirement, and propagates every batch cleanup failure. Whole-root transitions reconcile live file/directory/symlink/logical-byte counters from HEAD only after hard acknowledgement, while unpromoted imports leave them unchanged. Live-tree accounting consumes the generic-data SystemFamily decision so concealed indexes/system state and structural containers cannot inflate counters after restart. Import dedup now requires exact verified entries of the expected immutable type for chunks, FileRecords, symlinks, directories, B-tree nodes, and snapshots; source lookups must return the exact requested key. Rebuilt B-tree publication preflights every node and permits only byte-exact existing authority before writing any missing node. Stable-locator plans cannot replace or retire physical Chunk or Void authority. HTTP corruption details remain sanitized as `INTERNAL_ERROR` while the server logs the exact promotion failure.
- **Focused and adjacent proof:** the final focused boundary passes 201 tests across backup HTTP/import, DirectoryOps, events, namespace architecture, and version-file restore; an additional 68-test live-tree/startup-counter gate covers SystemFamily-filtered restart accounting. The adjacent B-tree, cache/hard-link, content-addressed/hash, directory listing/ops, GC, symlink, and version-history matrix passes 305 tests across eleven suites, plus a 216-test B-tree/backup matrix for rebuilt-node collision handling. Falsifying tests cover noncanonical and malformed existing/same-batch roots, restored file/symlink/existence and mutation authority, stale destination/source locators, historical alias isolation, bounded 300-file import, sparse deletion with an already-absent HEAD path, malformed deleted records, exact restore acknowledgements, stale-versus-selected corruption, root-transition counter ordering, concealed/unknown/structural SystemFamily accounting, malformed and valid pre-existing snapshot locators, wrong-type chunk/FileRecord/symlink/directory/internal-B-tree target collisions, same-type altered rebuilt-node bytes, all-node preflight, and immutable-payload locator replacement. Source gates forbid direct active-import HEAD/snapshot/deletion publication, old FileRecord materializers, restore transactions, and release-result squelching while positively requiring the shared coordinator.
- **Broad and static proof:** the final no-fail-fast workspace/all-target gate passes 4,990 tests across 197 result groups with zero failures and seven intentional ignores. Its captured log SHA-256 is `6555de7a2cd16402d3f6438eafcfca9010b45fbbbeba23eac6493ab237a7a6f9`. Workspace/all-target check and build, formatting, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate are green. The contract gate independently verifies 436 fixtures, 95 routes, and 38 documentation pages; the only check diagnostic is the historical unused `require_wasm_parser` test macro. The broad pass also replaced three stale path-locator fixtures with stronger paired proofs: selected-record corruption fails closed, derived-locator corruption does not control ordinary operations, reads do not heal, and explicit repair does.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh authenticated home-backed database. Real HTTP requests stored text/JSON and a symlink, created a snapshot, promoted its exact root, streamed a snapshot export, imported it without promotion while preserving current bytes and all four live metrics, promoted the imported root back to `version-one`, restored that historical file without buffering its body, and deleted/restored it. The first clean-reopen attempt exposed concealed derived/system state inflating startup counts from 2 files/31 bytes to 15 files/164,766 bytes; the new generic-data-policy walker reproduced red and corrected that defect. The fresh rerun returned exact files, symlink behavior, and 2 files/1 directory/1 symlink/31 logical bytes both before shutdown and after persisted-JWT reopen. Offline verification reported 294/294 valid entries, zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots.
- **Boundary:** ordinary namespace visibility is selected-root atomic; imported ordinary path locators are derived reconciliation and cannot change reads before promotion. Bounded locator batches are independently acknowledged before the final root transition. For privileged backups, registry-detached SystemFamily locators are therefore batch-visible because v3 has no single detached-family root to flip; this transition implementation does not claim global atomicity for that specialized system-state restore. Standalone export/patch artifact construction remains a separate destination writer by design.
- **Landing:** committed and pushed as `604aa1c` (`refactor: converge backup namespace producers`).

## P2e Wave 3c Sync, Conflict, And Checkpoint Convergence

- **Entry commit and territory:** `604aa1c` was the green backup-producer handoff. The sync boundary included embedded and HTTP diff/chunk exchange, staged immutable chunks, file/symlink/deletion merge, conflict evidence, peer checkpoints, remote response buffering, GC conflict reachability, SSE publication, and conflict resolution/dismissal. Existing v0 peer-state bytes, public response fields, selective-path filtering, honeymoon policy, and standalone immutable staging remained compatibility constraints.
- **Strict receive contract:** remote diff and chunk responses use exact closed schemas, collection and body limits, canonical hash widths, duplicate/extra/omitted reference rejection, decoded-size accounting, and whole-file content-hash proof. Existing immutable keys must resolve to the exact expected entry type and bytes. Chunk staging is fully prevalidated and atomic; recoverable unreferenced chunks may exist before namespace publication, but malformed, missing, wrong-type, colliding, or incomplete remote state cannot move namespace or checkpoint authority.
- **Shared merge and acknowledgement:** `DirectoryOps::apply_sync_merge` prepares files, symlinks, deletions, conflict metadata, parent closure, counters, derived indexing, and one SSE fanout under one memory-admitted `NamespaceMutationCoordinator` receipt. Embedded and HTTP sync invoke that same merge. Both prior local and prior remote roots persist in `PeerSyncState` v1, while the v0 reader supplies the legacy common base. Checkpoints advance only after the exact hard receipt and conflict evidence succeed; a lost checkpoint acknowledgement can retry the already-published namespace without duplicating ordinary mutations.
- **Conflict correctness:** merge is a true three-way comparison against both prior roots. Conflict evidence is created in the same receipt as the selected winner and retains exact immutable file, symlink, or deletion dependencies. GC treats unresolved evidence as live and fails closed on malformed evidence. Resolution verifies canonical identity and every selected chunk before publishing a file, symlink, or tombstone; resolution/dismissal and evidence cleanup share one receipt. Namespace ancestor type collisions, FileRecord-history corruption, and every delete error except `NotFound` remain observable and unacknowledged.
- **Bounded HTTP execution:** `/sync/diff` and `/sync/chunks` validate, build, and serialize responses on blocking workers under request cancellation and operation-memory admission. Responses are written to engine-local temporary artifacts and streamed through admitted frames rather than retained as one async-handler body. The built-in receiver caps diff reads at 128 MiB, chunk requests at 256 hashes, JSON responses at 96 MiB, and decoded chunks at 64 MiB; the server caps chunk requests at 10,000 hashes and 512 MiB decoded content.
- **Live-discovered SSE correction:** the first real two-node run proved namespace/checkpoint success but received no sync acknowledgement event. The orchestrator had constructed `RequestContext::system()` without the shared `EventBus`, even though lower-level tests supplied one. A failing orchestrator regression now requires the bus; `SyncEngine::with_event_bus` supplies it to both embedded and HTTP merge calls, and server assembly wires the shared bus. The repeated live capture received one `entries_created` event containing `/event-proof.txt` plus conflict evidence under one `sync_apply` mutation kind, operation ID, and publication sequence.
- **Adversarial path-binding correction:** final review found that embedded merge operations could name path A while carrying a valid FileRecord or symlink identity for path B, and conflict metadata could likewise transplant an unrelated valid version into the contested path. Three falsifying tests reproduced successful publication before the fix. The shared sync planner now requires each record path to equal its operation path, while the conflict loader requires every retained/chosen immutable record path to equal its evidence path. All three tests pass and preserve target/evidence authority on rejection.
- **Focused and route proof:** the engine matrix passes 315 tests across B-tree, conflict store, events, namespace mutation, SDK batch, sync API/apply/engine, and source architecture targets. The HTTP matrix passes 161 tests: 98 client-sync/conflict/replication/sync/version-history tests plus 63 cluster and SSE tests. These include malformed remote data, bounded responses, cancellation, memory pressure, chunk collisions, checkpoint-loss retry, delete failure, immutable loser reachability, corrupt chosen data, cleanup failure, path-identity transplantation, event cardinality, authorization, and restart behavior. Captured log SHA-256 values are `2a5742fbdb390454fdbbcda2b4795f37ff9f664cf9191728041ca21bfcc00f28` and `6def369838be10d53f09f2445612309f6efffb913079f3c248373414f6565dbd`.
- **Live and restart proof:** two fresh auth-disabled, snapshot-disabled servers exchanged exact CRLF content through `/sync/diff`, `/sync/chunks`, and `/sync/trigger`. Concurrent file, symlink, and modify-delete divergence produced typed retained evidence. Selecting the loser restored exact remote file bytes and symlink target; selecting the deletion removed the modified path; all three evidence records disappeared. A repeated sync applied zero ordinary operations. After graceful shutdown and reopen, selected content, deletion, and `/event-proof.txt` remained exact. The post-review binary then synced a new valid path-bound file byte-exact through the same live peer route, proving the stricter check does not reject valid replication. Offline verification reported 546 valid entries on node A in the initial run and 1,718 on node B after the final replay, with zero corrupt hashes/headers, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots.
- **Broad and static proof:** the final workspace/all-target gate passes 5,061 tests across 197 result groups with zero failures and seven intentional ignores. Its log SHA-256 is `ea464c88e042109cd6a8c41a49c628f809c2ccd51dde8c017fea81e85d7f9bc3`. Workspace/all-target check, formatting, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate pass. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages; the only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Wave 3 exit and deferred boundary:** version, backup/import, sync, conflict, and whole-HEAD producers now share namespace and hard-acknowledgement authority, with architecture gates excluding their removed direct paths. Independently installed detached portable plugins still produce recurring concurrent-create evidence because their current system-family state is excluded from historical roots; Wave 4 now explicitly owns plugin/system convergence and this repeat-conflict defect. Native P1c execution remains open until `wyatt-mac` and `win11vm` are reachable and is not claimed by this Linux-green handoff.

## P2e Wave 4 Preflight: System, Plugin, And Detached Control Producers

- **Entry commit:** `63a329e` is the pushed, workspace-green Wave 3 handoff. The tracked tree is clean; `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` are unrelated user-owned untracked paths and remain excluded.
- **Preserved contracts:** P2 keeps current v3 bytes, stable keys, task JSON, public system/plugin/task schemas, SystemFamily policy, and ordinary DirectoryOps authorization. This wave changes authority and acknowledgement ownership, not persisted format activation. Native P1c execution remains independently open while `wyatt-mac` and `win11vm` are unreachable.
- **Reproduced detached-task split authority:** `TaskQueue::enqueue` appends the task record and rewrites the registry as separate raw `store_entry` calls. Pruning tombstones task rows before separately replacing the registry. The in-process state mutex prevents stale concurrent transitions but cannot prevent a crash or hard-publication failure from leaving an unlisted task or a registry that names deleted rows. Single-record lifecycle updates also return without one explicit namespace hard acknowledgement.
- **System compound risks:** `store_user` publishes the user before its automatic group, while `delete_user` removes the user and then explicitly discards automatic-group deletion errors. Startup path migration performs each copy and delete as separate acknowledged mutations. Cleanup performs one deletion receipt per row. These paths require one planned authority boundary or an explicitly bounded sequence whose partial state is recoverable and observable.
- **Control and scheduling risks:** the v3 transition ControlStore re-enters DirectoryOps through a path-shaped special writer while already holding namespace authority; Wave 4 must replace that string-path exception with a typed control-publication context. Cron reads currently collapse all storage and parse failures to an empty schedule, treats task-list failures as no duplicate, and discards enqueue failures; those failure directions are part of this wave's error-squelch and authority proof.
- **Plugin/sync defect:** portable plugin state is detached from historical namespace roots. Independent current plugin rows therefore lack a checkpointed common base and can recreate concurrent-create evidence on every sync cycle even after both peer checkpoints advance. Wave 4 must make repeated sync idempotent without pretending detached mutable state is represented by the ordinary HEAD root.
- **First landing unit:** route task record and registry replacements/retirements through one `NamespaceMutationBatch` while retaining deterministic task hashes, JSON bytes, and FileRecord tag compatibility. The falsifying proof requires enqueue and lifecycle success to cross a hard frontier once, pruning to commit row retirements and registry replacement together, restart to retain registry closure, and source architecture to reject raw task storage afterward.
- **Focused baseline:** `task_queue_spec`, `task_worker_memory_spec`, `backup_task_spec`, `reindex_spec`, `cron_spec`, `namespace_mutation_spec`, `resilience_features_spec`, and task HTTP coverage establish the task producer and consumer boundary. Every Cargo command uses at most four jobs and four harness threads, a hard timeout, and `TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp`.

## P2e Wave 4a: Detached Task Authority

- **Falsifying proof:** the pre-change task acknowledgement test observed `next_sequence` remain at 6 when enqueue success required 7, proving that returning from two raw appends was not a hard publication acknowledgement. The source gate independently failed because `task_queue.rs` contained neither `NamespaceMutationCoordinator` nor `NamespaceMutationBatch` and directly invoked both `store_entry` and `mark_entry_deleted`.
- **Implementation:** deterministic task rows retain the exact `::aeordb:task:{id}` BLAKE3 key, registry key, JSON bytes, unflagged `FileRecord` tag, and entry version 0. Enqueue now replaces the task and registry in one `SystemWrite` locator batch. Every dequeue/checkpoint/status/cancel/requeue/defer/finish transition plans from the latest record while the engine-wide namespace guard is held and crosses one hard frontier. This replaces the per-`TaskQueue` mutex as cross-wrapper serialization authority while retaining it for local in-memory cancellation/progress ordering.
- **Pruning and bounds:** pruning compares a constant-size ordered registry digest under namespace authority, retires at most 256 task locators, and replaces the retained registry in the same receipt. A zero-result prune performs no write. Hard-waiter pressure refuses before either row or registry changes, and a 260-row legacy fixture proves 256-plus-4 bounded completion with complete registry closure after each receipt.
- **Strict reads:** every registry and task read now verifies exact stored key and the sanctioned `FileRecord` tag before JSON decoding. Missing, wrong-type, wrong-key, duplicate-ID, malformed, and identity-mismatched authority remains typed corruption rather than absence or partial listing.
- **Automated proof:** the focused `task_queue_spec`, `task_worker_memory_spec`, and `namespace_mutation_spec` boundary passes 73 tests, including independent queue wrappers concurrently checkpointing and cancelling one task without losing either state. The adjacent backup-task, cleanup HTTP, cron, clean-restart, GC, reindex, verification, and task HTTP matrix passes 172 tests. Workspace/all-target checking is green; the only warning is the historical unused `require_wasm_parser` test macro. Formatting and diff hygiene pass.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh auth-disabled, snapshot-disabled, home-backed database. A real `POST /system/tasks/reindex` created task `0f3b9257-4c37-446d-be8f-db7af195b55b`; the worker persisted its expected `Not found: /wave4-live` terminal state. After graceful shutdown and reopen, direct task GET and registry listing returned exact arguments, timestamps, error, and status. Offline verification reported 82 valid entries and zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots. Both servers exited cleanly and the disposable workspace was removed.
- **Boundary:** this landing converts only detached task/registry authority. Cron error direction and producer acknowledgement, compound users/groups, ordinary system files, plugins, peer/config/conflict records, legacy path migration, and typed ControlStore recursion remain in subsequent Wave 4 units.

## P2e Wave 4b Preflight: Compound User And Automatic-Group Authority

- **Entry and baseline:** pushed commit `0bc591b` is the green Wave 4a handoff. The existing `user_spec` and `system_store_spec` boundary passes 93 tests before behavior changes. The tracked tree was otherwise clean; the acknowledged user-owned `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` paths remain excluded.
- **Complete producer map:** `store_user` is invoked by embedded callers, auth fixtures, sharing/permission setup, and the root-only admin create route. It writes one versioned user JSON document and one deterministic `user:{uuid}` Group document. `delete_user` is an embedded system-store operation; the HTTP delete route currently deactivates through `update_user`. Direct group CRUD remains independently valid for named groups. JSON serialization is owned by `JsonVersioned`, while each current `JsonStore::put`/`delete_file` call independently owns directory propagation, HEAD, hard acknowledgement, counters, indexing, cache invalidation through events, and SSE fanout.
- **Complete consumer map:** user/group rows feed authentication identity lookups, permission-cache loading, query-based group membership, shares and share display, admin enumeration, peer/logical-backup transfer, strict restart enumeration, SystemFamily traversal, counters, indexing exclusion, and cache invalidation. A user without its automatic group loses ownership semantics; a deleted user with a retained or malformed companion can leave grants and repair conclusions detached from account authority.
- **Reproduced design defect:** current creation necessarily crosses two hard frontiers because `USER_STORE.put` returns before `store_group`; deletion removes the user first and then discards every automatic-group deletion error. Separate wrappers cannot provide a pair invariant, and a wrong-type/corrupt group can therefore turn deletion into acknowledged partial success.
- **Frozen behavior:** retain exact `/.aeordb-system/users/{uuid}` and `/.aeordb-system/groups/user:{uuid}` paths, v0 versioned JSON bytes, current overwrite semantics, one deterministic automatic-group definition, `false` for an absent user, and successful deletion when only the optional automatic group is already absent. No public HTTP schema or authorization policy changes in this unit.
- **Proof plan:** failing tests require exactly one hard sequence for create and delete; hard-waiter pressure must leave both pair members unchanged; malformed/wrong-type companion authority must fail before deleting the user; absent optional companion remains accepted; stored bytes must still decode through the existing v0 readers. A source gate rejects sequential `USER_STORE.put` plus `store_group`, direct pairwise `delete_file`, and `let _` squelching. Focused proof expands through namespace, auth, permissions, sharing, restart, and a real disposable server/reopen/offline-verify workflow.

## P2e Wave 4b: Compound User And Automatic-Group Authority

- **Red proof:** pre-change create and delete each advanced `next_sequence` twice. A wrong-type automatic-group path let `delete_user` remove the user, discard the group deletion failure, and return success. The source gate found sequential `USER_STORE.put`/`store_group` and `let _ = delete_group` paths. The v0 byte characterization remained green.
- **Shared implementation:** `store_user` serializes the existing User and deterministic Group through their unchanged v0 `JsonVersioned` writers, then publishes both existing paths in one `SystemWrite` buffered batch. `DirectoryOps::delete_files_batch_with_kind` is now the one plural file-retirement planner for ordinary single-file deletion and compound internal deletion. Explicit primary, required, and optional presence policies preserve `delete_user`'s absent-user `false` behavior without deleting an orphan group, while accepting an already-absent optional group. Every present file contributes deletion evidence, locator retirement, parent propagation, counters, index cleanup, exact source identity, and event data to one receipt.
- **Failure and concurrency proof:** hard-waiter pressure refuses before either pair member changes on create or delete. A wrong-type group prevents both overwrite and deletion. Six concurrent wrappers performing 72 create/delete operations leave either both pair members present or both absent. Duplicate user overwrite, nil UUID rejection, missing-user no-op, missing optional group, ordinary file deletion, counter reconciliation, and exact v0 payload bytes remain green.
- **Acknowledgement proof:** compound create and delete each advance one hard sequence, increment `writes_total` once, and emit one `system_write` event containing both exact paths, one operation ID, and the hard publication sequence. Source architecture proves `store_user` and `delete_user` cannot call their sequential wrappers and proves the shared deletion method owns `execute_optional_namespace_mutation`.
- **Automated proof:** focused user/system coverage passes 103 tests after adding the new invariants. The adjacent admin/auth, magic-link/refresh, permission scenarios, sharing, restart, backup, sync, directory, counter, event, and namespace matrix passes 520 tests across 15 targets with no warnings or failures. Workspace/all-target checking, formatting, `git diff --check`, and `mdbook build docs` pass; only the historical unused `require_wasm_parser` check warning remains.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh auth-disabled, snapshot-disabled database in the designated home cache. Real `POST /system/users` created `wave4-live`; direct user and deterministic group GETs returned exact fields. Both survived a graceful shutdown/restart. Offline verification reported 92/92 valid entries with zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots. Both servers stopped cleanly and the disposable database was deleted.
- **Documentation and boundary:** the embedded library guide now freezes the pair's one-acknowledgement, absent-primary, optional-companion, event, and logical-write behavior. This unit changes no public HTTP schema, authorization policy, SystemFamily policy, or persisted bytes. Other system rows, plugins, cron, legacy migration, and typed ControlStore recursion remain for subsequent Wave 4 units.

## P2e Wave 4c Preflight: Portable-Plugin Sync Idempotence

- **Entry and preserved format:** pushed commit `7826526` is the green Wave 4b handoff. P2 may not add a detached checkpoint schema or change `PeerSyncState`/`PluginRecord` wire shape. Existing plugin IDs, versions, authors, checksums, public paths, unresolved-conflict metadata paths, ordinary HEAD roots, and SystemFamily policy remain intact.
- **Complete producer/consumer map:** bundled startup installation and user plugin deployment write `PluginRecord` JSON through `system_store`; plugin list/invoke/cache, backup, peer transfer, conflict evidence, GC retention, embedded sync, HTTP sync, and peer checkpoints consume those records. Current peer trees augment ordinary HEAD with detached portable families, while historical ordinary roots omit them. `three_way_merge` therefore sees independently divergent detached rows as concurrent additions on every cycle. `apply_merge_operations_with_conflicts` is the shared embedded/HTTP evidence boundary; `/.aeordb-conflicts/{path}/.meta` is the durable local authority for one unresolved winner/loser pair.
- **Root cause and landing design:** bundled records currently use each node's installation clock for both `created_at` and `updated_at`, so identical embedded WASM, plugin ID, version, author, and checksum still serialize to different chunks and file identities. Each bundle will carry one immutable release timestamp, and startup will canonicalize existing same-version first-party rows once. For genuinely different custom rows, the shared receipt will strictly parse and validate an existing exact conflict pair, retain it without a second namespace write, and report only newly observed conflicts. Missing, malformed, wrong-type, wrong-path, or incomplete retained evidence remains a hard error rather than an idempotent match.
- **Proof plan:** first reproduce two independent bundle installs producing different bytes and recurring concurrent-create evidence after both peer checkpoints advance. Add a separate divergent custom-plugin case proving first-cycle evidence and second-cycle no-op/count stability, plus changed-version replacement, corrupt evidence refusal, restart, embedded/HTTP shared-path, and source-architecture coverage. Focused plugin/sync/conflict tests run under four jobs/threads, a hard timeout, and the designated home-backed `TMPDIR` before adjacent, documentation, broad, and live two-node proof.

## P2e Wave 4c: Portable-Plugin Sync Idempotence

- **Red proof and root cause:** independently installed first-party bundles originally serialized different node-local `created_at` and `updated_at` values, creating different chunk and file identities despite equal WASM, IDs, versions, authors, and checksums. A detached custom-plugin pair then produced a new `ConcurrentCreate` count and rewrote `/.aeordb-conflicts/.../.meta` on each cycle because historical ordinary roots do not contain detached portable rows. Malformed retained evidence was overwritten instead of failing closed.
- **Canonical bundle authority:** every embedded bundle now declares one release timestamp. Startup uses that value for both persistent timestamps, so independent nodes produce byte-identical `PluginRecord` JSON. A same-ID/current-version legacy record is canonicalized once when the existing version gate permits replacement; a newer same-ID plugin and a different-ID user-managed plugin remain untouched. No `PluginRecord`, peer-checkpoint, path, SystemFamily, or public response schema changed.
- **Exact evidence authority:** retained metadata is reusable only when type, normalized path, complete winner/loser identity, virtual time, node ID, size, and content type match and both immutable versions validate by exact type, key, canonical path-bound identity, and metadata. Missing, malformed, wrong-type, transplanted, or incomplete evidence remains an error. `conflicts_detected` now counts newly retained pairs rather than rediscovered exact pairs.
- **Adversarial concurrency correction:** final review found the first reuse check happened before namespace admission. Per-peer locking allows different peers to race, so both could observe no evidence and then serialize duplicate receipts. The check now executes inside `DirectoryOps::apply_sync_receipt` while namespace authority is held; immutable loser staging, evidence publication, and the returned count derive from that one decision. An eight-peer contention test reports an aggregate count of one and leaves one evidence record. Source architecture requires both embedded and HTTP orchestrators to use this receipt and forbids pre-authority deduplication.
- **Focused and adjacent proof:** the final focused matrix passes 192 tests across plugin deployment, conflict storage, sync apply/engine, and the complete v3 architecture target; log SHA-256 is `c050bdc77d07f29ee60b921de69778ba5d97dad5e17a2ee1414bb6b2942fa781`. The adjacent plugin-manager, HTTP sync/client/conflict, replication, GC, and backup export/import matrix passes 227 tests; log SHA-256 is `1e5a406237a21903bf842ac9fc17afc11d5815822a63661fd20b63899e42ee48`. The two initially failing GC-coupled sync cases pass unchanged with the required home-backed `TMPDIR`; their prior unresolved filesystem-derived reserve was caused by accidentally using the constrained `/tmp` tmpfs, not a production or fixture defect.
- **Broad and static proof:** after the final strict identity/path review correction, the complete workspace/all-target gate passes 5,085 tests across 197 result groups with zero failures and seven intentional ignores; log SHA-256 is `98d6a22af271bc760536ffcae2fad4fd7b60c40086f9055b1e4d49d5019868af`. Workspace/all-target checking, formatting, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate pass. The contract gate verifies 436 fixtures, 95 routes, and 38 docs; the only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Corrected live proof:** an explicitly rebuilt debug CLI from the final source started two fresh auth-disabled, snapshot-disabled databases in the designated home cache. Their startup `/plugins` responses were byte-identical (`a0c5dd417c309c6bb596f6c9cc111f70d5d002caab6991ca84a58bbb7edfb3aa`). Before peer registration, node B received a custom plugin and node A received a later divergent record, making A the stable winner. The first real `/sync/trigger` reported one conflict and zero operations; the next trigger and a forced trigger after graceful A restart each reported zero conflicts and zero operations. The conflict response remained byte-identical (`b6aa2b9176789d2d3e16c409828b23d0b7a1317721a3506ee83ccc6979d241a0`) and contained only `/.aeordb-system/plugins/custom`. Both processes stopped cleanly; offline verification reported 146/146 and 88/88 valid entries with zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots.
- **Documentation and boundary:** default-plugin, replication, and cluster-operations docs now define canonical release timestamps, one-time canonicalization, exact evidence reuse, concurrent serialization, fail-closed corruption behavior, and newly-retained conflict counts. This unit deliberately does not invent detached checkpoint state, change ordinary HEAD semantics, or activate a v4 writer.

## P2e Wave 4d Preflight: Cron Authority And Error Direction

- **Entry commit:** pushed Wave 4c commit `d213ec0` is the green handoff. The acknowledged untracked `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` paths remain excluded.
- **Producer and consumer map:** `/.aeordb-config/cron.json` is seeded at startup, replaced by the embedded save API, and read-modify-written by root HTTP create, update, and delete routes. The scheduler reloads it every minute, lists detached task authority, and enqueues through the Wave 4a `TaskQueue` receipt. Generic file routes cannot mutate this protected family. File publication owns namespace/root/locator authority and SSE/counter fanout; task enqueue independently owns its task/registry acknowledgement, as required because a due-time observation is not durable compound state with the schedule document.
- **Reproduced authority defects:** malformed JSON and every storage read failure currently become an empty schedule; scheduler task-list failure becomes "no duplicate" and can create duplicate work; enqueue failure is discarded. HTTP CRUD computes a replacement outside namespace authority, so concurrent create/update/delete requests can overwrite one another even though each final file write is individually durable.
- **Landing design:** make config loading strict except for true absence, validate complete documents before publication, add one reusable buffered-file transform that reads and plans the replacement while namespace authority is held, and route every HTTP cron mutation through it. Extract one synchronous scheduler-tick authority that returns failures; the background loop logs a failed tick and retries on the next cadence without claiming success or losing TaskQueue durability semantics.
- **Proof order:** preserve valid and missing-config behavior; first add failing malformed-config, concurrent mutation, scheduler-list/enqueue, acknowledgement, and source-architecture tests; implement the smallest shared path; run cron/task/namespace/HTTP/startup adjacency, broad and static gates, then a real disposable HTTP create/concurrent-update/restart workflow before landing.

## P2e Wave 4d: Cron Authority And Error Direction

- **Strict stored authority:** `load_cron_config` now treats only a truly absent file as an empty schedule. Malformed JSON, duplicate IDs, invalid expressions, wrong storage authority, and declared bodies above 1 MiB remain observable errors. Startup seeding is an atomic create-if-absent operation; an existing empty or malformed document is preserved without a write so diagnostic evidence is never overwritten.
- **Atomic mutation:** root HTTP create, update, and delete now call the typed embedded `create_cron_schedule`, `update_cron_schedule`, and `delete_cron_schedule` operations. Their private mutation authority reads the exact current body while namespace authority is held, validates the proposed complete document, reuses the existing chunk/FileRecord publication planner, and publishes root, locator, counters, SSE, and read metrics through one optional receipt. The generic callback is deliberately private so callers cannot run arbitrary work while the global namespace lock is held. Twelve simultaneous mutations retain twelve unique schedules and advance exactly twelve hard sequences.
- **Scheduler error direction:** one testable `run_cron_tick` loads configuration strictly, lists task authority once when work is due, deduplicates pending/running tasks including earlier schedules from the same tick, and propagates list/enqueue failures. A durability-pressure regression proves failed enqueue creates no phantom task or sequence and succeeds on retry after pressure is released. The background loop logs a failed tick and retries at the next cadence instead of converting it to success.
- **Automated proof:** the final focused cron/task-HTTP/architecture matrix passes 98 tests; SHA-256 `b3230f602aee790f6b66ac9999c1b0bbd4ce5dae577d9bae8fea30b9bb810b0e`. The task, namespace, system, event, SDK, HTTP, restart, and architecture adjacency passes 372 tests; SHA-256 `784135422ca34a4fdbb7b6181d3a74384acc6c3d7f8267010500f38e0ec28f08`. The final workspace/all-target gate passes 5,098 tests across 197 groups with zero failures and seven intentional ignores; SHA-256 `47a6b517ad56b39057c2289ee2c33fcb099262dc25967bba7a75350fe9cd56b7`.
- **Static and documentation proof:** workspace/all-target checking, formatting, `git diff --check`, `mdbook build docs`, stale-route searches, explicit CLI construction, and the independent contract gate are green. The all-target check log SHA-256 is `ac6b2f7c653c4f022388b8a24b45990b1c921bc1df607e2240de438cdc84fba5`; the CLI build log SHA-256 is `4ff6bff2872a4fb2c6eb86136bcef4ec92b00f132b17f7823bab8e4de6cc5b3d`. The contract gate proves 436 fixtures, 95 routes, and 38 docs; its log SHA-256 is `fc31ebde719c835acb310b9459e21dc8308f34b3e66d24575dad2a3f814f0260`. Public docs now use only `/system/cron`, describe the protected 1 MiB authority and atomic concurrency semantics, and document scheduler failure/retry behavior for HTTP and embedded callers.
- **Live proof and boundary:** the explicitly rebuilt final debug CLI started a fresh auth-disabled, snapshot-disabled database in the designated home cache. Defaults seeded once; one ordinary create returned `201`; an exact duplicate returned `409`; twelve concurrent creates all returned `201` and all survived; a concurrent patch and delete both survived graceful restart with the expected 14 schedules. Offline verification reported 168/168 valid entries with zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots; verifier log SHA-256 is `0335253cdb914cf74d023f78105206bac8874889657399c765fa7352f60f2ce9`. This landing changes no persisted schema and leaves keys, permissions, shares, peer/config adapters, legacy migration, ControlStore recursion, and the system-only empty-root warning for subsequent Wave 4 units.

## P2e Wave 4e Preflight: Credential Authority And Cache Fanout

- **Entry and baseline:** pushed commit `1f324b5` is the green Wave 4d handoff. The credential/cache baseline passes 251 tests across system-store, API-key-cache, provider, middleware, self-service/scoped HTTP, magic-link, refresh, and bounded-cache targets; log SHA-256 is `cf77736487eeea113eeedc5e9ec0879ed8d67fbe5825f0eb898e76481691996c`. The tracked tree is otherwise clean and the acknowledged `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` paths remain excluded.
- **Complete authority map:** API keys, magic links, and refresh tokens are versioned JSON rows below `/.aeordb-system/`; ordinary creation already publishes through one `DirectoryOps` receipt, while revoke/use/label transitions still read and replace through separate authority windows. API-key, group, permission, grants, and index-config caches affect HTTP middleware, SSE, sync, share discovery, listing decoration, and WASM host calls. Route-local invalidation covers only selected HTTP producers and can return `500` after the durable mutation already committed; embedded writes can leave cached authorization stale.
- **Separate-auth constraint:** `--auth file://...` stores signing material and API-key rows in a separate AeorDB identity engine while users, groups, permissions, and data remain in the main engine. The current API-key cache is nevertheless loaded with the main engine at several consumers. Cache convergence must retain the selected auth engine explicitly and must not silently redirect external identity authority into the data database.
- **Landing design:** make each `StorageEngine` own its bounded authority caches, have `AppState` reference the main engine's group cache and the selected auth engine's API-key cache, and pass the auth engine through middleware/SSE/sync/plugin consumers. Every ordinary namespace acknowledgement invalidates caches from canonical source paths; whole-root publication invalidates complete main-engine derived authority. Cache failure remains observable and fail-closed on subsequent access but cannot turn an already committed mutation into retryable HTTP failure.
- **Typed transition design:** add one bounded `JsonStore` transform used only behind typed service operations. API-key revocation accepts an explicit `Any`, `OwnedBy`, or `ShareLink` policy and returns a typed result from the same authority window; label update, magic-link use, and refresh-token revocation similarly read, validate, mutate, and publish once. Missing/policy-mismatch/already-applied operations are true no-write outcomes. Routes may call typed provider/service operations but may not list, manually deserialize, or evict after mutation.
- **Proof plan:** first make stale API-key/group/permission caches, separate-auth-engine lookup, route split authority, and concurrent typed-transition tests fail. Add malformed/wrong-type/oversized/no-op/hard-pressure cases, exact sequence/event assertions, whole-root invalidation, source architecture gates, auth and plugin adjacency, restart, and a live self-contained plus separate-identity HTTP workflow before broad/static/documentation gates.

## P2e Wave 4e: Credential Authority And Cache Fanout

- **Engine-owned authority:** each `StorageEngine` now owns its bounded permission, group, grants, index-configuration, and API-key caches. `AppState` uses the main engine for namespace authorization caches and the selected authentication engine for API-key authority. Middleware, SSE, sync, plugin manager, and WASM host calls retain that distinction, including `--auth=file://...`; same-engine compatibility wrappers remain explicit adapters.
- **Coordinator-owned fanout:** every acknowledged locator replacement or retirement derives targeted authority-cache invalidation from canonical source paths inside the namespace coordinator. Whole-root publication is separately declared, validates an exact canonical root `DirectoryIndex`, invalidates all authority and directory caches, reconciles the live namespace, and records one logical write only after hard acknowledgement. Route-local eviction and duplicate version/import fanout accounting were removed, and architecture tests prove custom fanouts cannot bypass either targeted or whole-root behavior.
- **Typed credential transitions:** bounded `JsonStore::transform` operations serialize API-key policy revocation and label changes, magic-link consumption, refresh-token revocation, and refresh-token rotation claims under one authority window. Missing, mismatched, already-applied, and expired outcomes are typed no-write results. Credential rows are capped at 1 MiB on creation and transition; malformed, wrong-type, transplanted, corrupt, and oversized authority fails closed.
- **Single-winner correction:** adversarial review found that magic-link verification and refresh rotation could previously let concurrent callers validate before separately marking the record. `consume_magic_link` and `claim_refresh_token_rotation` now return the credential record only to the caller that atomically changes its state. Concurrent HTTP and storage tests prove exactly one success; losers receive `401`, while corrupt authority remains `500` and never masquerades as an authentication miss.
- **Root and issuing-key policy:** every `AuthProvider` must explicitly implement authenticated root-owned key creation; HTTP root rotation no longer reuses the initial bootstrap escape hatch. An enabled provider that does not expose its authority engine is rejected during router construction before bundled-plugin side effects; only disabled auth receives the inert same-engine fallback. Refresh records revalidate their issuing key through the selected authentication engine before and after rotation, preserve the issuing `key_id` in JWTs, and fail after key revocation in a separate identity database.
- **Automated proof:** the final credential/namespace adjacency passes 685 tests across 23 targets with no failures; log SHA-256 is `c8f9d277a735055a107da36f8a4edc69dce81c59f31932fb23941fa751454730`. After the final enabled-provider fail-fast correction, the complete workspace passes 5,124 tests across 197 result groups with zero failures and seven intentional ignores; log SHA-256 is `62c15f5294ed995368fa54a20d21703b5d51590a7781225d820a790a41dcf04f`. Workspace/all-target checking is green with only the historical unused `require_wasm_parser` test macro warning; log SHA-256 is `1fe441ace47491a1f9beb9b860f96b12fe058b0d22db9229a277179a02acf03c`. Formatting, docs, and diff hygiene pass. The independent contract gate remains at 436 fixtures, 95 routes, and 38 docs; log SHA-256 is `fc31ebde719c835acb310b9459e21dc8308f34b3e66d24575dad2a3f814f0260`.
- **Live proof:** an explicitly rebuilt debug CLI started fresh snapshot-disabled main and separate identity databases. Real HTTP covered root-key creation and exchange, user labels, file access, refresh rotation, API-key revocation, rejected old refresh/access/key exchange, list/stats/health, and concurrent reuse of one magic link and one refresh token with exact `200,401` outcomes. After graceful restart, the retained root key still exchanged, the revoked key remained rejected, and the file remained readable. Offline verification reported 298/298 main and 56/56 identity entries valid with zero corrupt headers/hashes, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid Voids, or snapshot issues.
- **Preserved boundary:** no persistent format or public response schema changed. Primary-engine memory statistics intentionally do not double-count an external identity engine; explicit separate-auth memory/cache observability remains tracked for P8. Remaining permissions/share/config/peer/conflict adapters, legacy migration, typed ControlStore recursion, and the system-only empty-root diagnostic remain subsequent Wave 4 work.

## P2e Wave 4f: Startup Root Diagnostic

- **Entry and producer map:** pushed Wave 4e commit `94c5798` is the green handoff. `DirectoryOps::ensure_root_directory` is the only producer of the startup repair warning and is called by normal server engine construction before system-path migration. Existing valid root state must remain a no-write operation; startup repair remains the sole owner of root replacement.
- **Reproduced defect:** the diagnostic called the generic root listing and interpreted zero returned children as possible data loss. Detached SystemFamily rows intentionally do not propagate into ordinary HEAD, so a valid database containing only retained system configuration produced `Root directory exists but appears empty` on every restart. A valid genuinely empty database had the same false positive. Malformed flat-root bytes also collapsed to that ambiguous warning after best-effort listing discarded the corrupt child result.
- **Landing design:** use a strict root listing window with `offset=0` and `limit=1`. Success proves enough root authority to suppress the warning without materializing a large root; an empty complete window is valid. Missing, malformed, wrong-type, stale, or otherwise unreadable authority remains an error and emits one explicit `aeordb verify --repair` warning. A missing root locator beside a nonzero HEAD is preserved for explicit repair rather than replaced with an empty namespace. No KV scan, user-count heuristic, schema change, or detached-system traversal is introduced.
- **Red/green proof:** log-capture regressions first reproduced both false-positive system-only output and the ambiguous malformed-root output. Adversarial review then reproduced the adjacent destructive case: deleting the root locator while retaining a nonempty HEAD caused startup initialization to overwrite HEAD with the empty-root hash. The final four behavior tests prove first initialization, existing-root no-op, valid system-only silence, malformed-root warning, and missing-locator preservation with no durability-ticket movement.
- **Automated proof:** root/listing/counter/system-store/restart/shutdown adjacency passes 243 tests across six targets; log SHA-256 is `c3d842ac2c813b51165f2aec2975240449b4a5ac9d275260ecd554a27edc12fc`. The complete workspace passes 5,128 tests across 197 result groups with zero failures and seven intentional ignores; log SHA-256 is `2b15cfd36bc49087bc3c4250e061658127d918c327e9359f7401ffe525abde1b`. Workspace/all-target checking, explicit CLI construction, formatting, `git diff --check`, and `mdbook build docs` pass. The independent contract gate remains green at 436 fixtures, 95 routes, and 38 docs.
- **Live proof:** the final debug CLI created a fresh snapshot-disabled, auth-disabled system-only database in the designated home cache. Real health and root-listing requests returned `healthy` and an exact empty ordinary listing. After graceful shutdown and restart, the listing remained empty and the startup log contained neither repair warning. Offline verification reported 78 valid records and 72 KV entries with zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots. Restart-log SHA-256 is `cc58257c69838f693b1ab26ce4a90cb93b28c65937ab485ac78f1d262983f436`; verification-log SHA-256 is `efddbc6b27022c6827823e5aeac2f518cd645700993c3fd607b3eb36f4f7f4a4`.
- **Boundary:** Wave 4f changes no public schema or persistent bytes. Startup still creates the first root only when HEAD is uninitialized, never repairs an initialized namespace, and performs one bounded strict read for an existing locator. Remaining system-family adapters and typed ControlStore recursion stay in subsequent Wave 4 landing units.

## P2e Wave 4g Preflight: Typed ControlStore Publication Context

- **Entry and authority map:** pushed Wave 4f commit `d1d4bbc` is the green handoff. Mutable v3 transition controls are produced only by `V3TransitionControlStore`; configuration LKG/diagnostics, durability recovery, and emergency-spill recovery are its indirect consumers. Publication re-enters the shared DirectoryOps namespace coordinator while an outer namespace guard serializes A/B selection and selected-state readback.
- **Reproduced boundary defect:** the private transition writer accepted a caller-supplied string and granted its special recursion path by checking the `/.aeordb-system/controls/v1/` prefix. Although a source architecture test restricted known callers, authority still depended on path shape rather than a typed capability owned by ControlStore.
- **Landing design:** a private ControlStore constructor validates mutable kind and bounded canonical identity, then owns the outer namespace guard. Its crate-visible opaque context derives canonical A/B target paths from typed kind, identity, and slot. The DirectoryOps adapter accepts only that context, defensively requires the same engine, and runs the unchanged system-flagged FileRecord v0 writer through one ordinary namespace coordinator transaction. The context remains alive through hard acknowledgement, byte-for-byte readback, and selected sequence verification.
- **Proof design:** a source architecture gate requires the private constructor, held authority, same-engine check, typed slot derivation, and sole ControlStore call site while forbidding string-prefix authority. Existing runtime tests prove exact persisted v0 bytes, malformed and mismatched inputs, bounded sizes, sequence exhaustion, 16-way concurrent publication, restart, recovery, spill, and repair behavior. Adjacent configuration, durability, namespace, shutdown, and deployment tests plus a disposable live restart/verify run complete the landing boundary.

## P2e Wave 4g: Typed ControlStore Publication Context

- **Authority convergence:** transition publication no longer accepts a caller-supplied path or recognizes authority by prefix. Only the private ControlStore constructor can mint `V3ControlPublicationContextV0`; the opaque context owns the outer namespace guard, typed kind/identity, canonical A/B path derivation, and engine identity. DirectoryOps defensively rejects a context from another engine and remains the one ordinary namespace coordinator transaction.
- **Preserved crash direction and bytes:** the authority lifetime still covers current-sequence selection, inactive-slot hard publication, byte-exact readback, and selected-state verification, so concurrent publishers cannot race between commit and verification. The adapter writes the same system-flagged FileRecord v0 and changes no public or persisted schema.
- **Automated proof:** the exact ControlStore target passes 20 tests and the adjacent configuration-control, configuration-HTTP, emergency-spill, deployment-guard, namespace-mutation, shutdown, configuration-shadow, resilience, and hot-file matrix passes 187 tests. The complete workspace passes 5,128 tests across 197 groups with zero failures and seven intentional ignores. Workspace/all-target checking, explicit CLI construction, formatting, `git diff --check`, `mdbook build docs`, and the independent 436-fixture/95-route/38-doc contract gate pass. The first broad attempt reached 137 green groups before its 15-minute hard timeout; the complete cached rerun used a 30-minute cap and is the recorded gate.
- **Live proof:** the explicitly built debug CLI started a fresh auth-disabled v3 database in the designated home cache. A lifecycle v1 replacement persisted exact snapshot, retention, and GC policy; the server shut down gracefully, reopened healthy, returned the exact stored policy, and round-tripped an ordinary file byte-for-byte before a second graceful shutdown. As specified for a fresh identityless v3 database, configuration recovery controls reported unavailable rather than inventing a database identity; actual mutable ControlStore publication is therefore covered by the runtime and adjacent tests rather than overstated as live coverage. Offline verification reported 194/194 valid entries and 161 KV rows with zero corrupt headers/hashes, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid Voids, or snapshot issues. Restart-log SHA-256 is `a085c297cbea867b4de43225be743a3b4c4d6ecaa78809cd77bc0e4eca9d6bda`; verifier SHA-256 is `00ada85497e3ec45b4bb772d6509920ac7bfba1412f305d2aed4a056859fad8a`.
- **Boundary:** this landing closes typed ControlStore recursion only. Remaining Wave 4 permissions/share/config/peer/conflict adapters, legacy system-path migration, direct/raw producer and error-squelch gates, and complete Wave 4 exit proof remain open.

## P2e Wave 4h Preflight: Compound Share Permission Authority

- **Entry and producer map:** pushed Wave 4g commit `ccecc94` is the green handoff. `POST /files/share` is the only multi-path grant producer; `DELETE /files/shares` revokes one typed link. Both persist user-visible `.aeordb-permissions` FileRecords. Permission, group, and global grants caches consume acknowledged paths; middleware, listing, share discovery, plugin hosts, SSE, and sync consume those caches. Recipient `files_shared` events and best-effort email are post-authority notification fanout.
- **Reproduced split authority:** the route reads and rewrites each requested permission file independently. A valid two-path request advances two hard sequences. If a later permission file is malformed, the endpoint returns `500` after the first grant is already durable and advances one sequence. Single-path unshare likewise reads outside the writer's authority window and can overwrite a concurrent grant or revoke.
- **Landing design:** add one typed `PermissionStore` that validates strict crudlify flags and bounded request expansion, rejects protected SystemFamily paths, resolves all target path kinds from selected-root authority, groups multiple file patterns by canonical permission file, strictly decodes every current document before mutation, and holds one outer namespace guard through one existing buffered batch publication. Identical results are no-write outcomes. Routes own HTTP authorization and post-ack recipient notifications only.
- **Proof order:** preserve existing file-with-missing-content classification and corrupt-state error direction; turn the two deterministic sequence/partial-publication regressions green; add invalid flags, protected paths, bounded expansion, identical no-op, concurrent grants/revokes, cache/event ordering, architecture, restart, broad, and disposable live HTTP verification before landing.

## P2e Wave 4h: Compound Share Permission Authority

- **Single mutation authority:** public `PermissionStore` now owns grant and revoke read-modify-write authority from selected-root classification through one hard-acknowledged namespace publication. Multi-path grants normalize and deduplicate paths and groups, reject protected SystemFamily state, classify files without loading their bodies, group file patterns by canonical `.aeordb-permissions` document, strictly decode every current document, and publish every changed document in one existing buffered batch. Revoke uses the same held authority and can derive a file-specific permission document after the content file has been deleted.
- **Bounded and pressure-aware behavior:** flags are positionally strict; requests are limited to 65,536 unique path/group updates and 16 MiB of path/group/flag metadata; each permission document is bounded to 1 MiB; aggregate serialized output is bounded to 64 MiB. A conservative two-copy request workspace and rewritten output remain charged to `KvWriteBuffers` under one workload reservation through publication. A constrained-memory regression returns `ResourceExhausted`, consumes no sequence, and creates no permission file. Final review found and closed both request- and output-retention gaps before landing.
- **Acknowledgement and notifications:** identical grants are zero-write successes. Mixed retries return the accepted paths and exact changed-path subset. The HTTP route emits recipient SSE and best-effort email only for that acknowledged subset and deduplicates repeated direct recipients before fanout. Root authorization runs before notification identity work, and the former squelched user lookup is gone. Source gates forbid route-owned permission links, direct file publication, error squelching, and false retry notification paths.
- **Red-to-green and focused proof:** the original two-path regression advanced two sequences, and malformed later authority returned failure after publishing the first path and advancing one sequence. Both now pass with exact one-sequence success and zero-sequence failure. The final sharing suite passes 68 tests; log SHA-256 is `9fcbbd8f5959edaf55603b34264df86d516f7aac88de3656c82b03f54238a464`. Eight permission unit tests independently pin positional parsing plus exact request/output bounds, over-limit, and overflow behavior; log SHA-256 is `f8512f31035206b5b17d5b482eba0ce88d4377c5fafb8615772b1c0a6f33ed4c`. The ten-target permission/cache/namespace/system/SSE/sync architecture adjacency passes 375 tests; log SHA-256 is `5ea4fe0deca6fa1ddaaa9aa5e0b227639e6ea326ad99269203bb66f48bf73759`.
- **Broad and static proof:** the complete workspace/all-target gate passes 5,148 tests across 197 result groups with zero failures and seven intentional ignores; log SHA-256 is `b6ac82fca43f3caa8a9b352433141899b62e3644abb2d7a4e76789cbc6ee2251`. Workspace/all-target checking, explicit CLI construction, formatting, `git diff --check`, `mdbook build docs`, and the independent contract gate are green. Check log SHA-256 is `664348bf21d3363318caa3ea87ef6c34c304f93a7e74985a0110c4307583da27`; CLI build SHA-256 is `30db14b39654127b8813e3c3c18897d6b627400aede11112c2a9aadb1a85a89d`; contract SHA-256 is `fc31ebde719c835acb310b9459e21dc8308f34b3e66d24575dad2a3f814f0260`. The only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Live proof:** the explicitly rebuilt final debug CLI started a fresh auth-disabled database in the designated home cache. Four real file PUTs returned `201`; a two-path share and identical retry returned `200`; both permission documents contained the exact v0 link. After injecting malformed later authority through the ordinary file API, a gamma/delta compound share returned `500` and gamma authority remained `404`. Deleting alpha followed by file-specific unshare returned `200` and retained exact empty v0 links. After graceful restart, beta remained granted, alpha remained revoked, gamma remained absent, delta retained exact malformed evidence, and ordinary content was byte-exact. The final post-review binary reopened the same database, reported healthy, accepted another two-path grant and retry, and shut down gracefully. Offline verification reported 592/592 valid entries and 424 KV rows with zero corrupt headers/hashes, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid Voids, or snapshot issues; verifier SHA-256 is `85be99e0bc418a0cd9351ba04955ac2956a819613e65a5d897fa21bf1d7533ee`.
- **Boundary:** persisted permission JSON remains v0 and no public request/response schema changed. Immutable chunk staging can leave only recoverable unreferenced content if pre-publication work fails; FileRecord, locator, root, cache, sequence, and notification authority remain unchanged. This unit closes compound share/unshare only; remaining direct/raw system and plugin producers, peer/config/conflict adapters, legacy path migration, swallowed failures, and complete Wave 4 exit proof remain open.
