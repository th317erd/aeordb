# Child 03 Progress: Namespace

- **Status:** P2e Wave 1 Core DirectoryOps producer convergence is Linux-green; Child 01 native-platform execution remains open
- **Current landing unit:** P2e Wave 2 Blob and Embedded Batch producer convergence
- **Entry commit:** `6a3ec86`
- **Last green commit:** P2e Wave 1 Core DirectoryOps producer convergence `272b12d`
- **Owner:** Codex, namespace/semantic authority and integration owner
- **Start gate:** Child 02 is green; Child 01 is code-complete and Linux-green, but native `wyatt-mac` and `win11vm` execution remains required
- **Plan:** [Child 03](../children/03-namespace-semantic-roots-and-system-families.md)
- **Owned files:** this ledger; `engine/v4/system_family.rs`; the embedded registry loader currently in `engine/v4/admission.rs`; strict directory/B-tree traversal result APIs; P2c consumers in backup, sync, peer, import, indexing, GC, permissions, plugin host, repair, verify, and generic data adapters; focused P2c specs and architecture gates
- **Forbidden/hotspot files:** P2d mutation/locator behavior before P2c lands; v4 persistent registry bytes or generated IDs; index page/NVT/query behavior; physical GC eligibility/sweep/Void allocation; route response schemas; migration cutover; evidence or production databases; unrelated user artifacts
- **Hotspot handoff commit:** `272b12d` from P2e Wave 1 into P2e Wave 2
- **Narrow gate:** P2c-focused registry/traversal target first; campaign root target remains `timeout 15m cargo test -j 4 -p aeordb --test v4_root_migration_spec -- --test-threads=4`
- **Broad gate:** green for P2c exit: 4,887 tests across 196 suites, zero failures and seven ignored; workspace/all-target check, formatting, docs, and v4 contract gate are green
- **Drift/risks:** the frozen registry has no persisted family for structural parent directories such as `/.aeordb-system`; runtime traversal must recognize only strict ancestors of known path descriptors without granting those containers a persisted family policy. Existing flat-directory parse failures can become empty/complete results, best-effort B-tree warnings are discarded by several wrappers, peer/system augmentation silently skips every error, and hard-coded path lists disagree with the 46-family registry. P2 retains the old v3 system-flag predicate only as an explicitly named v0 byte-layout evaluator.
- **Evidence:** exhaustive source searches found the old predicates in DirectoryOps/batch v3 flags, auth, backup, sync, indexing, task, plugin, download/fetch/symlink/version/engine routes, plus hard-coded protected roots in GC, backup, and tree walking. Traversal consumers include backup/import, peer and embedded sync, directory accounting, reindex, verify, and route sync. The current registry decoder exposes eighteen raw policy bytes and its five-algorithm embedded cache is private to admission. A 194-test P2c-adjacent characterization gate passed across B-tree directories, tree walking, backup/import, sync, permissions, and GC. Existing peer tests require node-local `/.aeordb-system/config/secret_key` replication and backup tests inspect secret email configuration, directly characterizing behavior the ratified registry intentionally corrects; `V4-SYSTEM-FAMILY-POLICY` now records that approved divergence. Both native hosts remained unavailable at `ff3f95c` (`wyatt-mac`: no route; `win11vm`: forwarded port refused).
- **Next action:** map and characterize every streamed/blob/batch/merge/copy/rename producer and its multi-file acknowledgement ledger before the first P2e Wave 2 production edit, while continuing to retry the exact P1c native gates when both hosts are online

## P2e Wave 1 Preflight: Core DirectoryOps

- **Entry commit:** `1a13f18`; P2d is green and pushed, and no P2e production edits existed before the host crash.
- **Operation ledger:** buffered and streamed file writes pre-store immutable chunks, then independently publish FileRecord content, identity, and path keys; parent propagation writes flat/B-tree dependencies, directory path locators, and HEAD; user counters/events run after the legacy transaction. File/directory/symlink deletion separately writes deletion evidence, retires the path locator, rewrites ancestors, and publishes HEAD. FileRecord migration and directory rebuild bypass the exact-receipt facade entirely.
- **Split-authority defects:** `btree_insert_batched`, `btree_from_entries`, and `btree_delete` persist nodes while parent state is still being planned. Directory cache fills and implicit-directory counters also occur before hard acknowledgement. `delete_file_with_indexing` opens an outer namespace transaction and then calls the independently guarded `delete_file`, preserving a nested producer path that cannot enter the exclusive P2d top-level coordinator.
- **Landing design:** the coordinator will plan under one held namespace guard, validate and pre-admit before the first write, and preserve exact FileRecord entry versions. B-tree construction/insertion/deletion will produce mutation-free dependency plans shared by current legacy adapters and the new coordinator path. Wave 1 producers will contribute immutable dependencies, stable path-locator replacements/retirements, one HEAD transition, exact source identities, and post-commit cache/counter/event effects to one facade call.
- **Wave boundary:** restore/version, copy/rename, blob/batch/merge, sync/import, system/plugin, and maintenance repair callers remain on their explicitly assigned later waves. Shared legacy helpers may remain only for those named consumers and must be source-gated so converted Wave 1 methods cannot call them.
- **Proof order:** green pre-change facade/DirectoryOps/B-tree/event/migration/rebuild baseline; failing coordinator-planning, version, operation-ledger, no-precommit-side-effect, B-tree, nested-wrapper, and architecture tests; focused implementation gates; adjacent producer/consumer suites; full workspace; live create/overwrite/delete/directory/symlink/reopen/verify; then progress evidence and a pushed coherent commit.

## 2026-08-06 Context Recovery

- The host crash changed assistant context, not repository state. `development` remained clean at pushed commit `9151f8d` apart from the acknowledged untracked `.codex/DETAILS.md`, `.codex/wip.md`, and `downloads/` paths.
- Both native P1c hosts remain unavailable: `wyatt-mac` returns `No route to host`; `win11vm` returns `Connection refused` on its forwarded SSH port.
- The P2c registry proof can still advance without violating the production start gate: add the independent manifest-oracle and runtime-classification tests first, demonstrate their precise failure against the absent typed/runtime APIs, and keep production activation blocked pending native closure or an explicit dependency waiver.

## P2c-1 Registry Model Proof

- **Red proof:** the focused `v4_system_family_runtime_spec` failed because the runtime exposed all eighteen policy fields as raw `u8` values, had no shared embedded-registry accessor, and classified known absolute-family ancestors as unknown protected state.
- **Implementation:** the runtime now decodes every frozen policy byte into a closed enum or validated GC bitset, owns one `OnceLock` registry per hash algorithm in the SystemFamily module, and exposes that exact object to both header and peer admission. Strict ancestors of absolute registered paths are classified as runtime-only `StructuralContainer` values with no family ID or invented policy; unmatched protected descendants remain `UnknownProtected`.
- **Independent proof:** the new target compares all 46 family policies and all 63 descriptors to `system-family-registry-v1.manifest.json`, proves pointer reuse for all five hash algorithms and admission, and pins permissions, conflicts, controls, API-key, and email-secret policy corrections. It passes 4 tests. Existing admission passes 9 tests, the persistent-format target passes 72 tests, the 21,364-case reader-hardening target passes, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages.
- **Boundary:** no backup, sync, index, GC, generic route, traversal, or writer behavior is activated by this landing unit. Native P1c execution remains open and P2c consumer conversion remains blocked behind that recorded gate.

## P2c-1 Traversal Outcome Proof

- **Red proof:** `traversal_completeness_spec` failed because B-tree results exposed only entries/warnings, page-window visitor stops had no independent completion state, directory wrappers had no typed traversal result, and malformed flat directories still became empty results without retained evidence.
- **Implementation:** added shared `TraversalIntegrity::{Complete, DiagnosticallyPartial, Corrupt}` and `VisitorCompletion::{Exhausted, StoppedByVisitor}` contracts. Best-effort B-tree roots now classify missing, wrong-type, or malformed root authority as corrupt; damage below a valid root merges upward as diagnostically partial; operational I/O, pressure, durability, shutdown, and cancellation errors remain hard `Err` results. Directory traversal now retains flat and B-tree issues in one typed result, and bounded windows report integrity independently from intentional early stop.
- **Proof:** the focused target passes 5 tests covering a valid tree, missing child, missing root, malformed inline root, malformed flat directory, cycle, and early page completion. Existing strict/best-effort B-tree, B-tree directory, DirectoryOps, and recursive listing targets pass 163 tests unchanged; the complete P2c-adjacent boundary passes 199 tests. Package all-target checking passes with only the historical unused WASM-test macro warning, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages. The first adjacent command named a nonexistent `recursive_listing_spec`; the corrected registered target is `directory_listing_spec` and passes.
- **Boundary:** ordinary listing keeps its existing diagnostic best-effort response while no longer destroying flat-corruption evidence in the checked path. Consumer-specific fail-closed conversion for backup, sync, indexing, GC, repair conclusions, and system/plugin enumeration remains the next P2c unit.

## P2c-2 Shared Operation Resolver

- **Red proof:** the focused runtime target failed because no shared resolver, closed transfer-operation selector, or typed ordinary/structural/known operation result existed. Consumers would otherwise have to select raw policy columns independently and repeat unknown-protected handling.
- **Implementation:** `SystemFamilyPolicyResolverV1` now owns classification through the exact embedded registry and selects complete full, transfer, or index policy without collapsing structural containers into ordinary data. `SystemFamilyTransferOperationV1` closes the seven frozen transfer columns, and `SystemFamilyPolicyDecisionV1` keeps ordinary, structural-container, and known-family decisions distinct. Unknown protected state remains a typed `unknown_protected_system_family` failure.
- **Proof:** the runtime target passes 6 tests, including every transfer operation over all 46 independent manifest rows. Admission passes 9 tests, persistent formats pass 72, the bounded reader-hardening target passes its 21,364-case corpus, all-target checking is green apart from the historical unused WASM-test macro warning, and the contract gate remains green at 436 fixtures, 95 routes, and 38 documentation pages.
- **Boundary:** this unit adds the single consumer-facing decision surface but deliberately changes no backup, replication, indexing, authorization, GC, or generic route behavior. Those consumers are converted in the following landing units.

## P2c-2 Index Scope Convergence

- **Red proof:** direct indexing accepted an unknown `.aeordb-future` path as ordinary data, while the reindex worker downgraded its typed pipeline failure and completed successfully. Wrapper-level predicates also made streaming, batch, full-parser, and reindex paths choose independently. The strengthened reindex fixture deletes the unknown file's body to prove classification must happen before body I/O.
- **Implementation:** added the engine-facing `SystemFamilyPolicyResolver`, which centralizes path normalization and typed runtime error translation. The indexing pipeline now selects the frozen `IndexPolicyV1`; permissions and ordinary data remain indexable, declared excluded/not-applicable/canonical-only families are skipped, and unknown protected state returns `EngineError::SystemFamilyPolicy`. Streamed, buffered, full-parser, and batch paths delegate to the pipeline. Reindex classifies before migration or body reads, preserves known internal FileRecord migration, and treats policy failure as a task-fatal result.
- **Proof:** the affected gate passes 171 tests across `pipeline_spec` (49), `reindex_spec` (28), `write_pipeline_spec` (21), `upload_commit_spec` (21), `directory_ops_spec` (46), and `v4_system_family_runtime_spec` (6). Package all-target checking passes with only the historical unused WASM-test macro warning.
- **Removal evidence:** `is_internal_path` has no remaining indexing or reindex consumer. Its only live consumer is the pending client-sync conversion; wrapper-level index guards and task-owned protected-name lists are gone.

## P2c-2 Logical Backup And Data Export Convergence

- **Red proof:** a logical backup retained `/docs/.aeordb-indexes/text.idx`, and an unknown protected child returned a bare directory parse error after its corrupt body had already been read. The focused tests also pinned that malformed redacted secrets must not be read while malformed required portable records must fail the operation.
- **Implementation:** `include_system=false` now selects `DataExport` and `include_system=true` selects `LogicalBackup`. One registry resolver filters HEAD and historical tree traversal, derives detached absolute candidates from embedded descriptors, and classifies children before reading their FileRecord, symlink, or directory bodies. The export writer rebuilds flat or B-tree directory closure bottom-up, prunes empty structural containers, preserves retained header flags and entry versions, and rejects unknown protected state. Hard-coded backup path arrays, credential predicates, and special system-tree writers were removed.
- **Focused and adjacent proof:** `backup_diff_spec` (23), `backup_export_spec` (31), `backup_http_spec` (20), `backup_import_spec` (22), `backup_task_spec` (11), `tree_walker_spec` (21), and `v4_system_family_runtime_spec` (6) pass, for 134 tests. `v4_admission_spec` (9), `v4_format_fixture_spec` (72), and `v4_reader_hardening_spec` (2 tests with 21,364 malformed cases) pass. `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all`, `mdbook build docs`, and the v4 contract gate are green; the contract gate reports 436 fixtures, 95 routes, and 38 documentation pages.
- **Live proof:** after explicitly rebuilding the CLI, a disposable server stored `ordinary-live`, a namespace permission record, and a derived index through real `PUT /files/...` requests (all HTTP 201), then produced a logical artifact through `POST /versions/export` (HTTP 200). Offline verification reported 10 valid entries, zero integrity/KV/directory issues, and exactly 26 current logical bytes: ordinary data plus permissions, with the 13-byte derived index omitted. Booting the artifact returned exact bodies for ordinary data and permissions (HTTP 200), concealed the derived index (HTTP 404), and shut down cleanly. The first live attempt used a stale `target/debug/aeordb` left by test-only relinking and was discarded; the explicit `cargo build -j 4 --workspace` made the executable proof current.
- **Boundary:** import, peer/client sync, GC, permission authority, plugin/system enumeration, repair/verify conclusions, and generic protected-path adapters remain unconverted. The old v0 system predicate and `augment_with_system_subtrees` therefore remain only until those consumers are moved and the architecture-removal gate can become authoritative.

## P2c-2 Peer And Client Sync Convergence

- **Red proof:** embedded and HTTP diff producers independently walked and filtered trees, peer augmentation silently skipped every failure, local sync copied chunks and raw FileRecords before selective merge resolution, and inbound apply accepted registry-omitted paths. Focused tests reproduced forbidden peer mutations, unknown protected state read before classification, same-HEAD detached portable changes, selective chunk leakage, and a malicious HTTP peer triggering forbidden-state handling.
- **Implementation:** embedded and HTTP diff production now delegate to `compute_sync_diff`, which selects peer or client registry policy and uses one strict, budgeted transfer walker. Detached current authorities are registry-derived; historical bases exclude detached current state. Chunk manifests are recomputed after registry, selective-path, API-key, and permission filtering. Local sync uses the same tree authority, transfers only chunks required by resolved `AddFile` operations, and reconstructs records through normal mutation APIs. Both embedded apply and remote HTTP receive reject omitted, node-local, unknown, or structural-leaf paths before chunk requests or mutation.
- **Pre-writer correction:** the live proof discovered that root `/.aeordb-indexes` and `/.aeordb-logs` producers were absent from the 61-descriptor registry. The independent source now adds absolute-prefix descriptors to existing families `0x0060` and `0x0061`, preserving their policies and descendant matchers. All 63 descriptors, fingerprints, dependent v4 headers, manifests, ledgers, readers, and admission fixtures were regenerated before any v4 writer exists.
- **Proof:** `sync_api_spec` (24), `sync_apply_spec` (11), `sync_engine_spec` (29), `client_sync_spec` (27), `replication_e2e_spec` (19), `sync_http_spec` (18), `backup_export_spec` (31), and `tree_walker_spec` (21) pass, for 180 tests. Runtime SystemFamily (6), persistent fixtures (72), v4 admission (9), and the 21,364-case malformed-reader target pass. The independent reference passes 145 tests and freshly verifies all 436 cases.
- **Live proof:** the original real-server request failed HTTP 500 with `unknown_protected_system_family` at `/.aeordb-indexes`. After explicit CLI rebuild, the same disposable database returns HTTP 200; visible paths contain ordinary data, permissions, namespace configuration, and portable plugin state while root/nested indexes, logs, and conflicts are absent. Advertised chunks exactly equal the unique chunks referenced by visible files. Graceful shutdown succeeds and offline verify reports 242 valid entries with zero corrupt headers/hashes, missing/dangling children, B-tree issues, stale/missing/invalid KV rows, invalid voids, or broken snapshots.
- **Removal evidence:** sync producer code contains no `is_system_path`, `is_internal_path`, protected path literal, `augment_with_system_subtrees`, duplicate `walk_version_tree`, or duplicate `diff_trees` policy path. The compatibility boolean remains only as an adapter selecting the closed peer/client transfer operation.
- **Deferred receiver hardening:** the legacy HTTP receiver still needs strict typed response/chunk decoding, chunk payload hash and completeness proof, and convergence on the shared apply authority. Embedded apply also still squelches non-`NotFound` delete errors. These are explicit P2e/Child 06 obligations and prevent claiming end-to-end sync apply closure here.
- **Broad proof:** `cargo test -j4 --workspace --all-targets -- --test-threads=4` passed 4,814 tests across 196 suites with zero failures and seven ignored. `cargo check -j4 --workspace --all-targets`, workspace and reference-tool formatting checks, `mdbook build docs`, `git diff --check`, and the v4 contract gate are green. The contract gate independently verifies 436 fixtures, 95 routes, and 38 documentation pages.

## P2c-3 Registry-Governed Full-Export Import

- **Red proof:** a full export augmented with an unknown `/.aeordb-*` path imported successfully and advanced HEAD, while a privileged import copied a node-local API-key payload. Clearing legacy `FLAG_SYSTEM` from a portable user record also made CLI inspection misclassify the artifact as user-data-only.
- **Implementation:** full-export import now validates HEAD and every named snapshot through one operation-specific registry walk before the first target mutation. A second bounded walk writes only selected closure, rebuilds directories when children are omitted, replaces current path aliases, remaps snapshot roots, preserves portable required state, and omits credentials, node-local controls, logs, GC state, and derived indexes. CLI inspection decodes FileRecord, Symlink, DeletionRecord, and Snapshot subjects through registry Import versus DataExport policy instead of trusting header flags. Import-mode tree writes preserve logical operation metrics and count newly copied chunk bytes once; artifact construction does not contaminate runtime metrics.
- **Proof:** `backup_import_spec` (27), `backup_export_spec` (31), `backup_http_spec` (20), `event_emission_spec` (34), `tree_walker_spec` (21), and `v4_system_family_runtime_spec` (6) pass, for 139 tests. Workspace/all-target checking, formatting, `git diff --check`, and `mdbook build docs` are green.
- **Boundary:** sparse patch artifacts still use the legacy raw-inventory application path. They require an overlay-aware registry validation/rebuild design so omitted patch families cannot be copied or left reachable through the advertised target root; that remains the next P2c-3 landing unit.

## P2c Preflight Test Protocol

1. **Registry authority:** decode the checked-in binary through the production reader while using the independently generated manifest as the policy oracle. Prove all 46 families, 63 descriptors, every typed policy value, all five hash-specific cached registry instances, and admission/runtime pointer identity. The production serializer or decoder may not generate its own expected values.
2. **Path classification:** prove ordinary paths, all matcher kinds, exact known families, unknown protected paths, and runtime-only structural ancestors such as `/.aeordb-system`, `/.aeordb-system/cluster`, and `/.aeordb-config`. Structural containers are traversal-only and never inherit or invent a persisted family policy.
3. **Operation policy:** table-drive logical backup, peer replication, client sync, import, generic data, indexing, verify, repair, migration, spill, and GC decisions over every family. Pin the required corrections for permissions, conflicts, controls, credentials/secrets, derived indexes, and logs.
4. **Traversal states:** independently reproduce a valid flat directory, malformed flat root, valid B-tree, malformed inline B-tree root, missing/corrupt child branch, cycle, warning-cap exhaustion, operational I/O/admission failure, cancellation, and visitor-requested early stop. Integrity is exactly `complete`, `diagnostically_partial`, or `corrupt`; visitor completion is a separate field.
5. **Consumer behavior:** destructive or authoritative backup, import, sync, reindex, GC, counters, repair conclusions, and system-store enumeration must reject incomplete traversal. Verify and explicitly diagnostic tooling may retain readable entries while recording bounded evidence. No caller may turn partial/corrupt into an empty or complete collection.
6. **Architecture removal:** fail while legacy policy predicates or protected path arrays remain outside the named v0 byte-layout evaluator. Permit route/SDK compatibility parameters only as adapters into registry operation policy, never as lower-level authority.
7. **Verification order:** focused red target; adjacent 194-test characterization set; route/plugin/transfer/index regressions; contract and architecture gates; workspace suite; native checks; then a live home-backed disposable server exercising authorized and concealed paths plus injected traversal damage. Every command uses four Cargo jobs/threads, the home-backed test temporary directory, and a hard timeout.

## P2c-3 Registry-Governed Sparse Patches

- **Red proof:** sparse patch artifacts copied protected leaves without one logical-backup decision, trusted embedded FileRecord/Symlink paths, applied deletion inventories directly, and published the artifact's unfiltered target root. A filtered full export/import also changed the base root, so the next valid patch was rejected despite an equivalent selected namespace. After the first implementation pass, a live restart exposed one further invariant violation: sparse deletion tombstoned the target path without writing the durable `DeletionRecord` used by WAL rebuild and verification.
- **Implementation:** patch generation now strictly walks both source roots under `LogicalBackup`, rejects missing/corrupt roots and unknown protected state before artifact creation, stores complete selected base and target directory closures, and keeps unchanged FileRecords/chunks sparse. Import validates every leaf and the complete patch-first/target-fallback overlay before mutation, rejects embedded-path/key/retained-delete contradictions, accepts exact or selected-semantic base equivalence, rebuilds only selected closure, avoids rewriting identical target aliases, and returns the rebuilt selected target root. Applied deletion records retain their validated version/flags/payload, are written under the ordinary durable deletion key before the path alias is tombstoned, and survive restart/KV rebuild. Directory routing scratch is coordinator-admitted and released.
- **Focused proof:** `backup_diff_spec` passes 26 tests, `backup_export_spec` 31, `backup_import_spec` 41, `tree_walker_spec` 21, and `corruption_hardening_spec` 64. The restart regression first failed with exactly one missing KV entry and passes after durable deletion evidence was restored. Pressure, malformed body/key/path, structural-leaf, unknown-family, node-local omission, namespace-permission, restore-mode, force, unchanged-overlay, and logical-base cases are all covered.
- **Live proof:** a fresh disk-backed source and target under the home-backed scratch root completed CLI full export/import followed by a sparse add/change/delete patch. A half-truncated patch failed with `Unexpected end of file` and left the target byte-for-byte unchanged. The valid patch survived restart; HTTP returned `keep-v1`, `change-v2`, `add-v2`, and `404` for the deletion. Offline `aeordb verify` reported 103 valid entries, zero corrupt headers/hashes, zero directory issues, and zero stale/missing/invalid KV entries.
- **Broad proof:** `cargo test -j4 --workspace --all-targets -- --test-threads=4` passes 4,836 tests across 196 suites with zero failures and seven intentional ignores. Workspace/all-target checking, formatting, `mdbook build docs`, `git diff --check`, and the independent v4 contract gate are green; the contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages. The only check diagnostic is the historical unused `require_wasm_parser` test macro warning.
- **Boundary:** current patches use selected logical roots and carry both directory closures; legacy exact-root patches remain compatible when their overlay is complete. Sparse import still mutates path aliases before optional HEAD promotion under the existing v3 publication model; P2d owns the frozen mutation/locator publication replacement. Remaining P2c work is consumer convergence and removal of competing legacy policy lists.

## P2c-4 Generic Data And Plugin Consumer Convergence

- **Red proof:** generic routes and WASM hosts recognized only the two v0 detached system roots, so nested conflicts/indexes and other registry-concealed families could be read, shared, downloaded, linked, versioned, or written. Query rows were filtered only after pagination, while aggregate and analyze cardinality included concealed rows.
- **Implementation:** one generic-data resolver now governs direct file/blob/fetch/download/share/share-link/version/symlink operations, path-aware middleware, recursive listings, search results, and WASM host access. QueryEngine exposes authority-filtered paginated, aggregate, and explain execution; filtering occurs before sorting, totals, groups, offsets, limits, and cursors. Unknown protected state remains a typed failure, permissions remain visible, structural containers remain traversal-only, and system plugin identity no longer bypasses generic family policy.
- **Proof:** full `aggregation_spec` (41), `query_pagination_spec` (45), `download_spec` (25), `share_link_spec` (18), `sharing_spec` (44), `system_access_spec` (34), `upload_commit_spec` (22), `v4_system_family_runtime_spec` (7), and `wasm_query_e2e_spec` (27) pass, for 263 tests. Workspace/all-target checking and formatting are green; the only diagnostic is the historical unused `require_wasm_parser` test macro.
- **Boundary:** operation-owner repair/verify/GC policy, backup target inspection, deleted-record and repair traversal, v0 flag/topology compatibility naming, and the architecture-removal gate remain open. Global search index discovery already omits concealed index scopes; its result-layer registry filter remains defense in depth rather than the pagination authority defect reproduced in direct query execution.

## P2c-5 Strict Authority And Consumer Exit

- **Red proof:** authoritative listing consumers could reinterpret malformed flat directories as empty, accept diagnostically partial B-tree walks, skip missing aliases, or collapse stored user/group/key/permission/peer decoding failures into absence. Generic HTTP, share, permission, cluster, sync, index-discovery, GC, verify, repair, backup-target, plugin, and live-counter paths still had independent fail-open or best-effort wrappers. Permission middleware additionally broadened non-path access when an API-key authority lookup failed.
- **Implementation:** explicit strict flat, B-tree, windowed, visitor, and recursive DirectoryOps APIs now preserve diagnostic listing separately while rejecting corrupt, partial, unexplained-missing, over-budget, and unknown-protected authority. Backup inspection, GC, verify, repair, counters, system stores, conflicts, grants, index discovery, query fallback, reindex, WASM, ZIP, symlink, generic routes, sharing, cluster state, and sync state use that strict authority. Stored permission/user/group/API-key/index/peer records retain corruption taxonomy. One active-key helper, result-bearing permission checks, and strict share metadata classification remove broadening and whole-file buffering. The old path predicate is gone; only the explicitly named v0 on-disk flag evaluator remains, guarded by source architecture tests.
- **Failure-direction proof:** malformed permission authority returns `500` without overwrite; missing or invalid API-key identity returns `401` while authority read failure returns `500`; unknown protected paths fail typed/`500`, known concealed paths remain `404`; malformed peer/sync/system authority disables or rejects the affected operation instead of producing partial success; destructive GC and repair conclusions reject incomplete traversal.
- **Automated proof:** the final workspace/all-target gate passes 4,887 tests across 196 suites with zero failures and seven intentional ignores. `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all -- --check`, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate pass. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages. The only check diagnostic is the historical unused `require_wasm_parser` test macro.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh home-backed database through the normal server path. Ordinary PUT/GET/list returned `201/200/200`; known concealed and unknown protected paths returned `404/500`; malformed `.aeordb-permissions` made share return `500` while byte-for-byte authority remained unchanged; cleanup delete returned `200`; graceful SIGTERM exited zero. Offline verification reported 328 valid entries, zero corrupt hashes/headers, zero missing/dangling/B-tree/unlisted directory issues, zero stale/missing/invalid KV entries, zero invalid Voids, and zero broken snapshots.
- **Platform boundary:** native P1c execution is not claimed. The final retry still found `wyatt-mac` unreachable with `No route to host` and `win11vm` refusing its forwarded SSH port. Exact native fixture, admission, reader-hardening, contract, and legacy gates remain mandatory when those hosts return.
- **Deferred boundaries:** P2d owns mutation/locator publication and the existing diagnostic stale-child deletion compatibility. P2e/Child 06 owns path/diff-bound chunk capabilities, strict remote response/chunk decoding, shared receive/apply authority, and non-`NotFound` delete propagation. The broader P8 audit still owns optional cron/webhook/configuration error-squelch review; none is represented as P2c closure.

## P2d-1 Preflight: Shared Mutation And Locator Facade

- **Entry commit:** `e9330d8`; P2c hotspot handoff is complete and pushed.
- **Territory:** namespace authority currently enters through `namespace_write_guard`, `direct_hard_authority_guard`, `TransactionGuard`, direct HEAD/header publishers, and batch flush helpers. DirectoryOps, batch/blob, backup/import, sync, version, system/plugin/task, repair, GC, and CLI adapters then publish stable path/content/identity keys, HEAD, counters, events, cache invalidations, and index wakeups independently. P2e owns their conversion; P2d owns the shared service contract they will target.
- **Existing substrate:** Child 02 already provides one process-wide durability sequence, grouped hard-frontier proof, fallible transaction completion, operation admission, serious-failure latching, and shutdown visibility. P2d must return the exact existing `DurabilityCommitReceipt`; it must not invent a competing sequence or journal.
- **Facade contract:** top-level namespace admission occurs before mutation; one nonzero operation ID names the whole logical mutation; stable-locator replacement records ordered old/new KV physical incarnations while namespace authority is held; HEAD before/after is captured before acknowledgement; successful completion exposes the exact hard-publication sequence; one soft fanout runs after hard proof and cannot turn committed success into an apparent failure.
- **Red-test protocol:** unique IDs and monotonic sequences; exact replacement offsets/types/lengths; duplicate and mismatched stable keys; nested top-level refusal before writes; injected mutation and durability failures; no pre-commit fanout; exactly one post-commit fanout; panic containment; transaction/namespace reuse after every failure. Persistent bytes and all existing producer behavior remain unchanged until P2e.
- **Narrow gate:** `timeout 3m env TMPDIR=/home/wyatt/.cache/codex/aeordb-tests/tmp cargo test -j 4 -p aeordb --test namespace_mutation_spec -- --test-threads=4`.
- **Red proof:** the five-test target fails to compile at the intentionally absent `engine::namespace_mutation` module. The tests independently require exact durability-frontier reuse, ordered old/new KV incarnation evidence, pre-mutation malformed/nested rejection, reusable transaction authority, and panic-contained one-shot post-commit fanout.

## P2d-2 Shared Facade Implementation Evidence

- **Service boundary:** `NamespaceMutationBatch` is a mutation-free in-memory plan with one closed operation kind, canonical identity-bearing source paths, dependency-first entries, stable-locator replacements/retirements, and an optional v3 HEAD transition. Empty, dependency-only, duplicate/aliased-key, malformed-width, identity-free, invalid-entry-type, unchanged-HEAD, immutable-chunk-locator, and physical-Void-locator plans refuse before hard admission or storage mutation.
- **Durability boundary:** the new top-level transaction path reserves its bounded hard waiter before the first dependency append, reuses that exact `DurabilityCommitReceipt.sequence`, and excludes late legacy transaction joiners under the existing KV transaction mutex. Existing `TransactionGuard` callers retain their current nested behavior and unit-returning completion API. Failed preflight/admission consumes no sequence, writes no locator, emits no fanout, and leaves authority reusable.
- **Locator and acknowledgement proof:** stable replacements capture ordered old/new physical KV incarnations without assuming monotonic offsets; retirement requires an existing mutable namespace locator; successful acknowledgement carries one nonzero UUID, exact hard sequence, previous/new HEAD, exact source identities, and locator evidence. Twelve focused tests include concurrent UUID/sequence uniqueness, memory-pressure refusal, nested refusal, alias/type/no-op rejection, exact physical evidence, one-shot post-commit fanout, panic containment, partial-dependency failure, and hard-publication failure.
- **Non-activation boundary:** the architecture gate permits the two facade types only in `namespace_mutation.rs` and its engine re-export. No current DirectoryOps, blob, batch, version, sync, system, plugin, maintenance, or repair producer imports the facade; producer conversion remains explicitly owned by the characterized P2e waves.
- **Adjacent proof:** all 36 storage-engine transaction/durability tests pass. Eight adjacent suites pass 257 tests: `directory_ops_spec` 48, `disk_kv_store_spec` 63, `event_emission_spec` 34, `hot_file_transaction_spec` 18, `sdk_bulk_write_spec` 15, `upload_commit_spec` 22, `v3_contract_facade_spec` 48, and `version_access_spec` 9.
- **Static proof:** `cargo check -j 4 --workspace --all-targets`, `cargo fmt --all -- --check`, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate pass. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages; the only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Adversarial completion corrections:** final review reproduced a post-hard-commit KV expansion preflight failure that escaped as `InvalidMagic`, making a durable exact-receipt mutation appear retryable. The exact-receipt path now preserves the committed acknowledgement, logs the maintenance failure, and leaves retryable expansion queued; legacy transaction callers retain their existing synchronous expansion-error contract. Review also found that ordinary errors after a successful dependency append could escape without a serious latch. Once any dependency, locator, or HEAD mutation may have started, the facade now classifies every later failure as post-mutation, durably contains the partial evidence, emits no acknowledgement, and latches write authority for repair. Test-only fault seams prove both post-dependency and pre-hard-publication failures without changing production bytes or interfaces.
- **Broad proof:** the final `cargo test -j 4 --workspace --all-targets -- --test-threads=4` gate passes 4,901 tests across 197 result groups with zero failures and seven intentional ignores under a 30-minute hard ceiling. An earlier 15-minute attempt timed out while actively progressing with every completed group green and was not counted as a pass. The focused facade targets pass 12 tests and the storage-engine transaction/durability target passes 36 tests.
- **Live proof:** a freshly built debug CLI started a new home-backed disposable database through the normal server path. JSON and CRLF text PUTs returned metadata for `/p2d/live/a.json` and `/p2d/live/b.txt`; GET/list returned both exact paths. After graceful shutdown and reopen, the JSON remained 26 bytes with SHA-256 `721cadc9d1a7f9a3e3a6ad762df0cfb787a6780002ccb484317d026e78785416`, and the 13-byte text remained `alpha\r\nbeta\r\n` with SHA-256 `98ab4d3aeab1e120560e942e2df6a0db1147bf94bafcf1590000ffb3c2b6fc80`. Offline verification reported 203 valid entries, zero corrupt hashes/headers, zero missing/dangling/B-tree/unlisted directory issues, zero stale/missing/invalid KV entries, zero invalid Voids, and zero broken snapshots. Both server runs shut down cleanly and the disposable workspace was removed.
- **P2d exit:** persistent bytes and existing producer behavior remain unchanged, no producer imports the facade, and P2e owns staged activation. Native P1c execution remains a separate open platform prerequisite and is not represented as P2d closure.

## P2e Wave 1 Core DirectoryOps Convergence

- **Planning and authority:** `NamespaceMutationCoordinator` now supports mutation-free planning while holding namespace authority, exact no-op completion without consuming a ticket or fanout, and version-aware dependency and stable-locator writes. FileRecord and parent-directory planners build complete dependency, locator, and HEAD transitions before execution. One coordinator-owned durability receipt is the acknowledgement authority for each converted logical operation.
- **B-tree planning:** `BTreeMutationPlan` separates node construction, insertion, and deletion from persistence. The existing B-tree APIs remain plan-then-flush adapters for later-wave callers, while converted DirectoryOps producers add planned nodes to their one namespace mutation. Focused tests prove planning performs no storage writes and preserve all flat/B-tree thresholds and traversal behavior.
- **Converted producers:** streamed, buffered, and direct file finalization; file deletion; directory creation/deletion; symlink creation/update/deletion; root initialization; FileRecord v0 migration; full directory rebuild; and targeted directory repair now publish through the shared facade. Stable dependencies and path locators precede one HEAD transition; cache changes, live counters, SSE events, and acknowledgement metrics run once after hard durability.
- **Acknowledgement contract:** successful entry events carry a UUID `operation_id`, exact hard-frontier `publication_sequence`, and closed `mutation_kind`. The new `aeordb_namespace_mutation_acknowledgements_total{mutation_kind}` metric counts the same post-commit authority. Operation-ledger tests prove exactly one event, metric increment, operation ID, and sequence for file, directory, symlink, migration, root-initialization, rebuild, and repair successes; true no-ops emit and consume nothing.
- **Legacy transaction boundary:** a converted top-level namespace write deliberately rejects an already-active legacy `TransactionGuard`. Returning success there would be false because the outer guard, rather than the coordinator, would own the eventual durability result. Transaction tests continue to prove raw dependency batching and reusable authority; the legacy buffered batch remains deferred to Wave 2, where its entire logical operation can move under one coordinator receipt. A rejected nested write may leave only unreachable immutable dependencies, never a stable path or HEAD publication, and normal GC can reclaim those append-only orphans.
- **Failure and architecture proof:** preflight, nested-authority, dependency, locator, hard-publication, and fanout failures retain the P2d failure direction. Source gates forbid converted producers from direct stable-key storage, direct flush/HEAD publication, legacy parent helpers, or legacy transaction ownership while allowing explicitly assigned later-wave callers. Rebuild remains memory-bounded to one planned directory mutation at a time under the outer scan-consistency guard.
- **Automated proof:** focused and adjacent gates passed 474 tests across twelve suites, with another 34 transaction and SDK-batch tests covering the legacy boundary. `cargo check -j4 --workspace --all-targets`, `cargo build -j4 --workspace`, formatting, `mdbook build docs`, `git diff --check`, and the v4 contract gate are green. The contract gate independently verifies 436 fixtures, 95 routes, and 38 documentation pages. The final full workspace/all-target gate passed 4,910 tests across 197 suites with zero failures and seven intentional ignores; its captured log SHA-256 is `b574d20da36b0d5d9ec0985b4c23498b2624026161d9986abd63e2a598e6f55f`.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh home-backed database through the normal server path with auth and snapshots disabled. Real HTTP operations created and overwrote an exact CRLF file, created/read/deleted a symlink, created/deleted directories, and wrote 260 files to cross the B-tree threshold. Deleting one middle file left a correctly ordered 259-entry listing. Two SSE captures covered 269 entry mutations across file, directory, and symlink create/update/delete families; every event carried all acknowledgement fields. After graceful shutdown and reopen, the listing remained 259 entries, surviving content was exact, deleted paths returned `404`, and root contained only the expected tree. Both pre-reopen and post-reopen offline verification reported 2,661 valid entries with zero corrupt headers/hashes, directory inconsistencies, B-tree issues, stale/missing/invalid KV entries, invalid Voids, or broken snapshots.
- **Boundary:** restore/version, copy/rename, blob/batch/merge, sync/import, system/plugin, and remaining maintenance producers retain their named later-wave adapters. Native P1c execution on `wyatt-mac` and `win11vm` remains open and is not claimed by this Linux-green handoff.
- **Landing:** committed as `272b12d`; this is the required `directory_ops.rs` hotspot handoff into Wave 2.

## P2e Wave 2 Preflight: Blob, Batch, Copy, and Rename

- **Entry commit:** `b78363e`; Wave 1 and its hotspot handoff are green and pushed.
- **Producer map:** `/blobs/commit` calls `batch_commit::commit_files`; embedded `store_files_buffered_batch` and JSON merge batch call `commit_buffered_files`; both use a separate namespace guard plus legacy `TransactionGuard`, direct FileRecord materialization, direct flat/B-tree/path-locator writes, direct HEAD publication, and their own counter/event/index fanout. Streamed PUT/finalize already uses the Wave 1 coordinator, but metadata indexing remains a separate post-return wrapper. File and symlink rename directly create destination locators, rewrite two parent chains, retire the source, and emit two events inside a legacy transaction. Recursive copy nests legacy transactions around newly converted Wave 1 producers, and the HTTP multi-source wrapper currently turns failures into partial success.
- **Reproduced boundary:** all 268 pre-change tests across SDK batch, blob commit/e2e, merge HTTP, rename HTTP, sharing, symlink, pipeline, event, and durability-contract suites pass. They characterize current MIME/hash/size validation, system-family filtering, stale-directory recovery, B-tree conversion, auth, created-time preservation, hard-tail publication, and the legacy outer-transaction batch behavior. No existing success test exercises recursive copy; source inspection proves it now enters a legacy outer transaction before calling coordinator-owned `create_directory`, which must reject nested top-level authority.
- **Ratified behavior correction:** Wave 2's multi-file atomic-publication requirement supersedes `/files/copy`'s documented per-source partial-success behavior. The complete requested source closure must validate before mutation and publish under one operation ID/hard sequence, or none of its path locators/HEAD changes may become authoritative. The response keeps its successful `copied` list; failures become the route's normal typed error rather than HTTP 200 with an `errors` array.
- **Planning design:** one directory-delta planner collects upserts, removals, and explicit empty-directory creation, then finalizes deepest paths first so each ancestor is planned once. Large-directory updates use a planned-node B-tree overlay with deterministic lookup/deduplication and reachable-node output; they may not persist intermediate roots or rebuild a whole large tree. File/symlink and directory dependencies, stable locator replacements/retirements, and one final HEAD transition enter one `NamespaceMutationBatch`.
- **Fanout design:** one hard acknowledgement increments the namespace metric once. Blob/buffered/merge/copy operations emit one aggregate created event. Rename keeps the established created/deleted relationship event types for client compatibility, but both derive from one soft fanout and carry the same operation ID/publication sequence. Metadata indexing moves behind hard acknowledgement and shares one buffered index writer per logical operation.
- **Proof order:** red recursive-copy and multi-source atomicity tests; red batch/rename acknowledgement cardinality and outer-transaction refusal; red B-tree overlay/no-write tests; implementation; source-removal gates; focused and adjacent suites; full workspace; API/docs/contracts; live blob/buffered/merge/copy/rename across B-tree threshold; restart and offline verify.

## P2e Wave 2 Implementation And Evidence

- **Shared publication:** chunk-backed `/blobs/commit`, embedded buffered batches, and embedded JSON merge batches now prepare one set of FileRecord dependencies, stable locators, directory deltas, and one HEAD transition for `NamespaceMutationCoordinator`. The old batch-local `TransactionGuard`, direct FileRecord/directory/path/HEAD writers, repeated parent propagation, and pre-acknowledgement indexing are removed. Streamed finalize, batch, merge, copy, and rename metadata indexing now enters the same engine-wide buffered index path after hard publication.
- **Directory and B-tree planning:** one deterministic deepest-first directory-delta planner handles explicit directory creation, child upserts, removals, stale locator recovery, type conflicts, and shared ancestors. Flat directories cross directly into B-tree form at the existing threshold. Existing B-trees apply all removals/upserts through an unpublished overlay and contribute only reachable planned nodes to the namespace batch; planning performs no intermediate node writes.
- **Merge and copy authority:** JSON merge reads and parses the authoritative current document, applies RFC 7396, prepares chunks, and publishes while holding one namespace authority, eliminating the reproduced concurrent lost-update race. Recursive and multi-source copy validates the complete file/directory/symlink closure, destinations, chunk locators, legacy FileRecord migration, depth, and bounded planning memory before publication. Copy preserves empty directories and symlink targets and rejects self-descendants, collisions, missing sources, malformed trees, and non-directory ancestors without exposing partial destinations.
- **Rename and fanout:** file and symlink rename publish destination creation, source retirement, both parent changes, and HEAD under one hard receipt. Their compatibility delete/create events share one operation ID and publication sequence. Batch, merge, and copy emit one aggregate created event; counters, directory-cache fills, metadata-index cleanup/addition, SSE, and acknowledgement metrics run only after hard success.
- **Adversarial corrections:** strict recursive copy traversal no longer heals stale source locators while namespace authority is held, avoiding a hidden nested mutation/deadlock. Legacy v0 copy/rename computes the missing whole-file hash under the same bounded planning admission while current v1 copies remain content-read-free. Copy validates chunk locators through bounded metadata rather than forcing one random body/header read per chunk. `copy_file` returns the FileRecord carried by its acknowledged publication instead of issuing a fallible post-commit metadata read that could falsely report failure after durable success. File publication revalidates every staged chunk while namespace authority is held and refuses missing, malformed, or wrong-type locators before path/HEAD publication. Direct, compressed, buffered, JSON-merge, and HTTP chunk staging all reject wrong-type immutable-key collisions. `/blobs/check` uses the same typed metadata validator and propagates corruption/operational errors instead of reporting an arbitrary KV row as `have` or squelching an error into `needed`. Corrupt symlink classification errors now propagate through rename and delete dispatch.
- **Focused and adjacent proof:** `sdk_bulk_write_spec` passes 27 tests, `v3_contract_facade_spec` passes 52, the upload protocol passes 21, and the two private authority-injection tests pass. Ten adjacent suites pass 334 tests: B-tree 73, DirectoryOps 50, events 36, merge HTTP 17, namespace mutation 13, pipeline 50, rename HTTP 19, sharing 50, blob commit 22, and blob end-to-end 4. These cover 300-child B-tree batch/reopen, exact acknowledgement cardinality, nested legacy-transaction refusal, full recursive copy, multi-source atomic failure, memory pressure, stale locator behavior, legacy v0 migration, metadata-index visibility, concurrent SDK/HTTP merge, route delegation, upload semantics, missing/malformed/wrong-type chunks, and corrupt symlink dispatch.
- **Live proof:** an explicitly rebuilt debug CLI started a fresh home-backed database with auth and snapshot writes disabled. A real chunk upload and 300-file `/blobs/commit` used caller-supplied whole-file hash/size metadata, published a B-tree directory in 278 ms end to end, listed exactly 300 files, and returned exact bytes. Sixteen concurrent HTTP merge patches retained every disjoint field. Multi-source copy preserved a file, empty directory, and symlink target; a request containing a missing second source returned `404` and exposed no first destination; file rename retained exact content. A live filtered SSE stream captured batch, copy, merge, and rename acknowledgement fields, with both rename events sharing one operation ID and sequence. After graceful shutdown/reopen, every count, field, target, absence, and renamed body remained exact. Offline verification reported 1,667 valid entries and zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots.
- **Large-manifest live proof:** an explicitly rebuilt debug CLI started a second fresh home-backed database and accepted a 1,520,255-byte `/blobs/commit` manifest containing 20,000 references to one stored chunk. Chunk upload took 1.5 ms and commit took 167 ms end to end; server diagnostics recorded 21,185 microseconds of KV metadata lookup, zero chunk-body bytes read for validation, one one-byte MIME sniff, 98 ms total chunk validation, and 40 ms namespace publication. The exact 20,000-byte file and one-entry listing survived graceful shutdown and reopen. Offline verification reported 191 valid entries and zero corrupt hashes/headers, missing/dangling/B-tree/unlisted-directory issues, stale/missing/invalid KV rows, invalid Voids, or broken snapshots. The 263 MiB disposable workspace was removed after evidence capture.
- **Final automated proof:** the post-review workspace/all-target gate passes 4,941 tests across 197 result groups with zero failures and seven intentional ignores. Its captured log SHA-256 is `7df7e7bcf66be8260f88cd28c869e267861723aba52035bc04587456d98944b3`. `cargo check -j4 --workspace --all-targets`, `cargo build -j4 --workspace`, formatting, `git diff --check`, `mdbook build docs`, and the independent v4 contract gate are green. The contract gate verifies 436 fixtures, 95 routes, and 38 documentation pages; the only check diagnostic remains the historical unused `require_wasm_parser` test macro.
- **Wave 2 exit:** the converted producers have one namespace authority, one hard acknowledgement, and one post-commit fanout; source gates exclude the removed direct/nested paths. Native P1c execution remains independently open until `wyatt-mac` and `win11vm` are reachable. Wave 3 owns restore/version, backup/import promotion, sync apply/conflict resolution, and remaining whole-HEAD transitions.
