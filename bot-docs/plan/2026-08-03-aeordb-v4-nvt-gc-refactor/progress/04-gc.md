# Child 04 Progress: GC

- **Status:** active
- **Current landing unit:** P4-6b
- **Entry commit:** `2f1b58d`
- **Last green commit:** `2f1b58d`
- **Owner:** Codex, physical-lifecycle/GC owner
- **Start gate:** Child 03 selected-root/lifecycle reader contracts green
- **Plan:** [Child 04](../children/04-physical-inventory-gc-and-void.md)
- **Owned files:** `aeordb-lib/src/engine/v4/gc_state.rs`, `aeordb-lib/src/engine/v4/gc_audit.rs` as a reused typed evidence contract, disconnected v4 retirement-lineage modules, the generic immutable-artifact append portion of `aeordb-lib/src/engine/v4/first_authority.rs`, dedicated P4 lineage specs, `aeordb-lib/Cargo.toml`, this ledger, and the matching `TODO.md` P4 section
- **Forbidden/hotspot files:** live `engine/gc.rs`, `void_manager.rs`, allocator/sweep routes, capability activation, root-admission semantics, migration cutover, and production service callers without a later recorded handoff
- **Hotspot handoff commit:** `5d9fe76` closed Child 03 root authority; P4-2b2 added only a disconnected immutable-GC-artifact append through the same writer/header/coordinator authority and did not alter root admission
- **Narrow gate:** P4-6b exact eligible-intent qualification and restricted hard publication from the physically selected quarantine manifest, with no locator-removal authority
- **Broad gate:** the complete 25-target v4 GC matrix passes 221 integration tests and all 471 library tests pass with four threads; all-target checking, formatting, mdBook, contract, and suppression-architecture gates pass
- **Drift/risks:** destructive activation remains forbidden; the current v3 GC materializes full KV/live/frontier sets and is characterization territory only; physical WAL enumeration, replacement lineage, and corruption continuation remain separate concerns. There is deliberately no independent mutable retirement-journal head: durable segments are discovered through the selected physical-inventory checkpoint and its monotonic write-sequence interval, while only the reconciled inventory manifest may advance `retirement_journal_through_sequence`. Repository-wide strict Clippy currently exits on 110 historical library diagnostics and reports no diagnostic in the changed P4-5 source or spec files.
- **Evidence:** Child 03 closed on Linux/macOS/Windows at `5d9fe76`; P4 territory refresh found the frozen both-width inventory fixtures/readers, `EntryScanner` WAL verification path, `ReadSnapshot::visit_all`, `LocatorReplacementCoordinator`, v3 monolithic `GcLiveSet`, verify/rebuild scanners, hot-tail Void evidence, and no production v4 inventory consumer or writer. The P4-1 compile-red boundaries proved the typed manifest/row/model and multi-child directory APIs were absent. Production now exposes borrowed exact inventory rows, the complete typed inventory checkpoint, a constant-memory cross-page reference model with cancellation/limit/failure latching, bounded depth-16 leaf/internal GC directory descriptors, and exact manifest-to-directory-to-page closure validation. Architecture search confirms these APIs have no live service, writer, route, allocator, capability, or v3-GC caller.
- **P4-1 proof:** `gc_v4_model_spec` passes 13 both-width happy/failure tests. The combined `gc_v4_model_spec`, `v4_format_fixture_spec`, `v4_lifecycle_reader_spec`, `gc_spec`, `void_manager_spec`, `corruption_hardening_spec`, `header_repair_spec`, `tree_walker_spec`, and `btree_spec` matrix passes 341 tests with four threads. `cargo check -p aeordb --all-targets -j4` passes with one unrelated unused-test-macro warning. Existing immutable fixtures and canonical keys remain unchanged.
- **P4-2 territory:** the live v3 `NamespaceMutationCoordinator` is the sole stable-locator mutation facade and its acknowledgements carry ordered old/new KV offsets, lengths, tags, and the hard publication sequence. They deliberately lack the integrity digest and v4 entity write sequence required by `PhysicalIncarnationIdV1`, so they are characterization input rather than fabricated v4 lineage. The first-authority v4 writer creates only a fresh disconnected database and has no replacement path. `gc_state.rs` validates retirement segments but exposes only a count, while `gc_audit.rs` already exposes the complete bounded `CorruptGcEvidenceV1`; P4-2 will reuse that evidence contract. Immutable GcArtifact encoding already supports retirement segments. No production v4 stable-key replacement, retirement journal owner, audited-through control, recovery synthesizer, service caller, or destructive consumer exists.
- **P4-2 landing decomposition:** P4-2a adds typed zero-copy segment/record readers and a conservative chain/reference model. P4-2b adds bounded append/flush/publication ownership. P4-2c makes the future v4 replacement boundary classify all five frozen reasons through that one owner. P4-2d adds post-watermark recovery synthesis and typed corruption evidence. P4-2e closes crash, cancellation, corruption, resource, architecture, and retained-or-leaked proof before activation.
- **P4-2a proof:** `gc_v4_lineage_spec` passes six both-width tests covering exact independent fixture fields, borrowed record iteration, reason typing, canonical multi-segment closure, reset/ordinal/predecessor/order failures, cancellation, record bounds, public-shape revalidation, terminal failure latching, and reuse of the existing typed corrupt-evidence reader. The combined lineage/model/format/lifecycle/v3-GC/Void/corruption/header/B-tree matrix passes 347 tests with four threads. All-target library checking passes with one unrelated historical unused test-macro warning. No production writer, service caller, capability, candidate, sweep, or Void path references the new APIs.
- **P4-2b1 territory correction:** neither the frozen dynamic-record registry nor current v4 code defines a separate retirement-journal head/watermark control. The selected `PhysicalInventoryManifestV1` is the sole persisted audited-through authority after reconciliation. Existing live v3 mutation acknowledgements lack complete `PhysicalIncarnationIdV1` fields and cannot safely seed v4 lineage; the new owner therefore remains disconnected until the future v4 replacement boundary exists.
- **P4-2b1 red proof:** the dedicated writer target initially failed because `engine::v4::gc_retirement` did not exist. The target now proves exact both-width fixture bytes, count/byte/time flush thresholds, immutable predecessor chaining, retry-identical sink failure behavior, pre-append versus retained-record failure direction, restart/database identity closure, sequence exhaustion, malformed/order/cancellation behavior, receipt binding, memory-pressure refusal/release, and absence of a live service/control caller.
- **P4-2b1 proof:** `gc_v4_lineage_writer_spec` passes 12 tests and `gc_v4_lineage_spec` passes six tests. The combined writer/lineage/model/format/lifecycle/v3-GC/Void/corruption/header/B-tree matrix passes 359 tests with four threads. `cargo check -j4 -p aeordb --all-targets` passes with one unrelated historical unused-test-macro warning. The owner reserves bounded GC memory before allocation, buffers at most the configured 16 MiB hard cap, validates its own immutable bytes through the independent reader, accepts only an exact hard-durability receipt, retains exact retry evidence on sink failure, and latches on dishonest receipts. No concrete storage sink, selected-inventory watermark handoff, service caller, replacement producer, capability, sweep, or Void path is active yet.
- **P4-2b2 red proof:** the dedicated storage target initially failed because `V4FirstAuthorityPublisher` did not implement `RetirementJournalDurableSinkV1`. Crash-prefix expansion then reproduced an old-authority restart failure: overwriting the selected hot tail before inactive-header selection left restart parsing a WholeEntity as hot-tail framing. A later-header regression separately proved that comparing exact retries against the current header timestamp incorrectly rejected an older immutable segment.
- **P4-2b2 proof:** commit `6d2e396` publishes one system/uncompressed `GcArtifact` WholeEntity through the existing KV/header/coordinator authority, leaves HEAD unchanged, and returns the entity's selected v4 write sequence as the persistent retry receipt. Baseline KV state is explicitly flushed, while the new entity begins after the still-selected hot-tail extent so every pre-header crash leaks unreachable evidence rather than damaging old authority. Exact key/value retries survive restart, post-commit uncertainty, and later header timestamps without another hard publication. Dependency, header-I/O, collision, malformed, missing/degraded-authority, database-mismatch, and sequence-exhaustion tests all fail closed. The external sink/owner tests pass at both hash widths; 18 internal crash tests and 458 adjacent integration tests pass with four threads. All-target checking passes with only the pre-existing unused test macro warning. Architecture search confirms no production service, replacement, GC, capability, sweep, Void, or root-admission caller was activated.
- **P4-2b3 red proof:** the dedicated inventory-retirement target first failed because no checkpoint reconciler, typed physical audit boundary, or manifest handoff existed. The first implementation run then rejected two invalid test scenarios whose target WAL boundaries regressed the selected prior inventory, proving the prior-authority guard before the fixtures were corrected.
- **P4-2b3 proof:** commit `ad172b6` adds one constant-memory physical-scan reconciler that accounts for every contiguous entity extent through the captured WAL/write-sequence boundary, rebuilds the complete current retirement chain from its reset, ignores noncurrent crash-prefix copies as authority, and requires rediscovery of the selected prior watermark. Cancellation, gaps, malformed/overflowing incarnations, record/entity limits, classification disagreement, corrupt chains, missing prior evidence, boundary regression, and candidate-manifest mismatch all fail without a handoff. The successful handoff is non-constructible outside its owner module and binds the exact database, audit offsets, write sequence, retirement watermark, and physical record count to the future immutable manifest. Both frozen hash profiles and the independent populated manifest fixtures agree exactly; zero-journal and legacy-incarnation scans remain valid. Eight narrow tests, 370 adjacent storage/GC/corruption tests, 30 cleanup/HTTP tests, and all 434 library tests pass with four threads. All-target checking passes with only the historical unused test macro warning. Strict Clippy remains blocked by the recorded 113-diagnostic repository baseline and reports no new checkpoint diagnostic. Architecture search confirms no live scanner, inventory writer, service, capability, v3 GC, sweep, Void, or root-admission caller was activated.
- **P4-2c territory:** live v3 locator acknowledgements remain unusable because they omit the integrity digest and v4 entity write sequence. The disconnected `V4ControlStore` still writes v3 `FileRecord`/chunk entities through `DirectoryOps`, so its name does not make it a truthful WholeEntity-v1 replacement producer. `V4FirstAuthorityPublisher` refuses pre-existing first-authority entities and only appends immutable retirement artifacts. There is therefore no current true v4 stable-key replacement writer to wire; this unit owns the single plural admission/activation contract that all later stable-key, relocation, repair, migration, and pointer/control producers must consume.
- **P4-2c test contract:** preflight a complete mixed-reason batch before owner mutation; require one reserved hard-publication sequence, strictly ordered unique stable keys, exact same-key/type old/new identities, forward write sequence, and non-overlapping extents; admit every retirement before one activation callback; expose whether evidence remains buffered, hard-published, or buffered after a final retryable sink failure; return a non-fabricable permit after activation failure so retry does not duplicate lineage; and never activate after partial admission, cancellation, malformed input, or a dishonest receipt.
- **P4-2c red proof:** the dedicated replacement target fails to compile on the absent plural batch, coordinator, journal-state, error, and non-constructible activation-permit contracts. The independent retirement-record iterator remains the oracle for inspecting the emitted segment.
- **P4-2c proof:** commit `5cb3719` adds one disconnected plural boundary that preflights every replacement before mutation, binds all five frozen reasons and exact old/new incarnation bytes into a bounded rolling permit digest, admits every retirement before one activation callback, and returns the same non-constructible permit after activation failure so retry cannot append duplicate lineage. Soft evidence may remain buffered; a retained final sink failure is surfaced in a `must_use` outcome, while partial admission, dishonest receipts, cancellation, malformed identity/order/extent input, and hard memory pressure never invoke activation. The dedicated target passes 11 tests across both hash widths. The adjacent B-tree/corruption/GC/inventory/lineage/model/fixture/lifecycle/Void matrix passes 381 tests, all 434 library tests pass, all-target checking and formatting pass, and strict production Clippy remains at the recorded 113-error historical baseline with no diagnostic in the new production or spec files. Architecture search confirms there is no live v3 mutation, service, route, control-store, capability, sweep, or Void caller; later true v4 writers must supply a reserved hard-publication sequence and complete v4 incarnation identities rather than fabricating them from v3 acknowledgements.
- **P4-2d red proof:** the dedicated recovery target initially failed to compile because the exact corrupt-evidence writer, durable evidence sink, compound recovery sink, and bounded lineage reconciler did not exist. The first strict-Clippy pass then found three new diagnostics in the generalized immutable-artifact publisher/evidence writer; all three were corrected before landing instead of being absorbed into the historical baseline.
- **P4-2d proof:** commit `ef127ee` bounds one externally grouped stable-key family to 64 physical incarnations and 4,096 pairwise extent checks. A unique selected highest v4 sequence may synthesize only missing lower-incarnation `Repair` retirements; malformed identity, wrong family/type, noncanonical order, selected absence/retirement/mismatch, equal highest sequence, overlap, and amplification hard-publish the existing `CorruptGcEvidenceV1` contract and protect the family. Evidence and journal segments share one compound sink and the same generalized immutable-GC WholeEntity/KV/header/coordinator authority. Evidence publishes before retirement append, every outcome explicitly refuses reclaim authority, dishonest evidence receipts latch recovery, and journal failure retains diagnosis plus retryable owner state. A real disposable v4 publisher hard-publishes both artifacts, reopens both locators, and performs exact evidence retry without republishing. Nine recovery tests, six shared-storage tests, 23 writer/replacement tests, the 15-target adjacent matrix, and all 434 library tests pass with four threads. All-target checking and formatting pass. Strict Clippy returns to the exact 113-error historical baseline with no diagnostic in the changed files. Architecture search confirms no service, live v3 GC, capability, candidate, sweep, Void, control-store, or root-admission caller is active.
- **P4-2e test contract:** invalid context or owner identity mutates neither sink; exact 64-incarnation groups remain bounded while 65 protects; stable-key families are strictly increasing and invalid groups do not advance order; dishonest journal receipts retain bytes and fail closed; a retryable journal failure can flush the exact retained segment but stale observations cannot duplicate it, while a refreshed observation closes as already complete; a real shared publisher must select evidence before an injected journal failure, reopen diagnosis without a journal locator, and complete exact retry with only one new hard publication; existing segment/checkpoint corruption, cancellation, crash-prefix, resource, and architecture gates remain green.
- **P4-2e red proof:** a dishonest journal sink correctly latched the retirement owner, but a second recovery attempt with refreshed physical observations bypassed owner health when no lineage remained missing and incorrectly returned `AlreadyComplete`. The regression failed before the fix and now requires every recovery operation, including an apparent no-op, to preflight the owner's latched state and monotonic clock before reporting success.
- **P4-2e proof:** commit `2a9637c` makes recovery refuse invalid run/database/hash/time/generation context and owner identity before mutation; admits exactly 64 incarnations at both frozen hash widths without an additional GC memory reservation; protects amplified, duplicate, regressing, cross-family, malformed, overlapping, and ambiguous groups without advancing invalid ordering state; latches dishonest evidence or journal receipts; retains exact pending journal bytes across retryable failure; requires refreshed observations before idempotent completion; and keeps cancellation between groups side-effect free. A real disposable v4 publisher hard-publishes evidence before an injected journal failure, reopens with the diagnosis selected and no journal locator, then exact-reuses that evidence while adding only the retirement publication. The dedicated recovery and storage targets pass 15 and seven tests; the 15-target adjacent matrix passes 400 tests; all 434 library tests and all-target checking pass with four threads. Strict Clippy remains at the exact 113-diagnostic historical baseline with no diagnostic in either changed production file. Architecture search again finds no live service, v3 GC, capability, candidate, sweep, Void, control-store, allocator, route, or root-admission caller. P4-2 is complete without activating destructive behavior.
- **P4-3 territory:** the frozen `gc_mark.rs` module is reader/validator-only: exact checkpoint, mutation-journal, AGCW manifest, and all six AGWO object contracts exist, but no runtime encoder, workspace, selector, or task consumes them. `ReadSnapshot::visit_all` streams bounded pages but discards bucket/slot identity and includes buffered overrides; a physical mark bitmap therefore needs a new captured-layout visitor that requires the write buffer to be empty and exposes exact bucket/slot positions without materializing KV. `MAX_ENTRIES_PER_PAGE` is the frozen slot count. Existing KV rebuild and directory-repair workspaces provide cancellation, memory admission, checksummed-run, bounded-open-file, and suspicious-lookalike precedents, but are temporary and intentionally delete on drop; resumable GC needs a separate private identity-bound lifecycle. P4-3 decomposes into bitmap/visitor, workspace writer, selected checkpoint/resume, mutation convergence, and closure proof. Destructive GC and live v3 callers remain forbidden.
- **P4-3a red proof:** the dedicated target first failed to compile because `engine::v4::gc_mark_runtime`, `ReadSnapshot::visit_captured_slots`, and `ReadSnapshot::find_captured_slot` did not exist. The prior visitor could not identify a physical slot and would merge unstable buffered overrides into a supposedly captured layout.
- **P4-3a proof:** commit `06e120b` adds a dense bitmap that admits and retains its complete fixed KV-slot geometry through `MemoryOwner::GarbageCollection`, rejects overflow, wrong geometry, pressure, bounds, and cancellation without leaked accounting, and mutates bits idempotently. The captured visitor refuses buffered state and NVT/layout disagreement; streams resident or file-backed pages with exact bucket/slot identity; validates every key's bucket, page-local duplicates, hash width, and final live count; preserves callback stop/error/cancellation semantics; and never materializes database-scale KV state. A file-backed proof scans four buckets while the page provider remains within a one-page cache cap. The dedicated target passes 8 tests; the complete adjacent inventory/lineage/model/KV matrix passed 127 tests before the final duplicate guard, and the final shared KV/snapshot perimeter passes 91 tests. All 434 library tests and all-target checking pass with four threads. Strict Clippy remains at the exact 113-diagnostic historical baseline with no diagnostic in either changed production file. Architecture proof finds no live v3 GC, service, route, control-store, Void, sweep, or reclaim caller. P4-3a remains disconnected and non-destructive; a live HTTP run is intentionally deferred until a runtime owner exists.
- **P4-3b red proof:** the dedicated workspace target first failed to compile because no durable workspace owner, identity/basis/options contracts, AGWO encoder, AGCW encoder, or immutable closure existed. Native Windows proof then reproduced `ERROR_ACCESS_DENIED` when the shared file barrier attempted `FlushFileBuffers` through a read-only validation handle; the barrier never became warning-success or a skipped durability step.
- **P4-3b proof:** commit `bcb2a8f` adds one disconnected workspace owner that creates identity-bound private run/checkpoint directories, reserves descriptor and manifest memory before allocation, streams bounded AGWO bytes with exact CRC32 and BLAKE3 readback, enforces the configured run cap and free-space reserve, and atomically installs one immutable AGCW closure without replacing an existing manifest. File mutation failures latch the owner; cancellation, malformed bodies, collisions, symlinks, crash prefixes, capacity and memory pressure, tampered objects, and post-install uncertainty cannot select a checkpoint. Windows uses a protected current-user-plus-SYSTEM DACL, and the shared native barrier retries `ERROR_ACCESS_DENIED` only through a write-capable handle to the same file object, preserving the required flush. Linux passes the 155-test workspace/mark/memory/format/platform/reader matrix, all 434 library tests, all-target checking, formatting, the exact 1,485-occurrence error-squelch audit, and its 28 architecture tests. Native macOS passes 31 workspace/platform tests; native Windows passes 29. Strict Clippy reports the 120-diagnostic historical lib-test baseline with no diagnostic in the new workspace, durability, or spec code. Architecture search confirms there is no service, live v3 GC, selected-checkpoint, control-store, sweep, Void, allocator, route, or reclaim caller. Exact Windows ACL readback and owner verification remains an explicit P4-3e native closure item.
- **P4-3c territory:** the frozen checkpoint reader validates but does not expose its capability bitmap, timestamps, authority/semantic/layout/policy/SystemFamily fingerprints, capture/reconciliation watermarks, bitmap geometry, workspace ID, or work counters. The AGCW reader likewise hides timestamps, hash algorithm, and its three resume fingerprints, so the existing closure check proves only database/run/generation/sequence/state and manifest BLAKE3. Immutable checkpoint fixtures and all six A/B GC controls already have independent both-width reference bytes, but production has no checkpoint/control encoder, selected-checkpoint owner, or restart loader. `V4FirstAuthorityPublisher` can append immutable WholeEntity-v1 GC artifacts; mutable controls require the P4-2 replacement coordinator because the similarly named `V4ControlStore` writes current v3 FileRecords and cannot produce truthful v4 incarnation lineage. P4-3c therefore lands as: (1) complete reader/context contracts and exact encoders, (2) immutable checkpoint plus mutable A/B WholeEntity publication with prior-control retirement before activation, and (3) full workspace/object readback and fail-closed restart selection. All remain disconnected from the service and live v3 GC.
- **P4-3c1 red proof:** the dedicated target failed to compile on the absent complete checkpoint fields, AGCW resume fields, checkpoint/control encoders, and exact resume-context validator. The adjacent writer architecture gate then rejected the intended fourth immutable-GC codec owner until it was changed from a stale global count to an exact four-module allowlist.
- **P4-3c1 proof:** production now exposes every frozen checkpoint and AGCW resume field, emits both checkpoint variants and both MarkRun A/B controls byte-for-byte equal to the independent Blake3-256/SHA-512 fixtures, shares one AGCA envelope encoder, and rejects nonresumable state or any database/run/generation/sequence/path/workspace/fingerprint/watermark/geometry mismatch before storage access. The larger validated checkpoint variant is boxed only after bounded decode so the artifact enum stays compact without weakening allocation-first defenses. The dedicated target passes five tests; the complete 18-target GC/lineage/corruption matrix passes 413 tests; the format/allocation watchdog passes 77 tests; and all 434 library tests pass with four threads. All-target checking and formatting pass. The reviewed error-squelch inventory remains exactly 1,485 occurrences and all 28 architecture tests pass. Strict Clippy returns to the exact 113-production-diagnostic historical baseline with no diagnostic in the changed production or spec files. Architecture proof confirms no service, live v3 GC, task, storage-engine, v3 control-store, route, sweep, Void, or reclaim caller uses the new codecs or validator.
- **P4-3c2 proof:** commit `2623d0c` publishes the complete immutable checkpoint before one canonical A/B MarkRun control and binds both to a durable workspace closure. Replacement reserves a durable v4 write sequence, prewrites and reads back the exact WholeEntity, admits the prior physical incarnation through the shared retirement owner before control activation, discards only its exact soft record if activation fails, and flushes retained lineage immediately after authority release. Canonical selection is A first, then an absent slot, then the older inactive slot; equal contradictory controls fail closed. Both hash widths complete A/B/A publication, exact retry, real close/reopen, and old/new physical-locator inspection. Private fault tests prove activation failure leaves the old control selected and does not reuse the reserved sequence, post-commit failure returns the exact committed receipt with hard lineage, and a failed immediate lineage flush retains explicit buffered evidence. The two storage tests, 15 replacement tests, 21 internal authority tests, the 104-test adjacent matrix, all 437 library tests, all-target checking, formatting, and `git diff --check` pass with four threads and the home-backed scratch path. The error-squelch inventory remains exactly 1,485 reviewed occurrences and all 28 architecture tests pass. Strict Clippy remains historical debt with no diagnostic in a changed production or spec file. Architecture gates keep the publisher disconnected from service, live v3 GC, task, v3 control-store, sweep, Void, route, and reclaim paths.
- **P4-3c3 red proof and design correction:** the dedicated workspace target first failed to compile because no bounded reopener or owned reopened-workspace handle existed. Restart selection also proved that one exact resume context cannot select A/B controls whose checkpoints legitimately have different run, sequence, and workspace identities; the request therefore accepts at most two exact contexts and matches each checkpoint before external path I/O rather than weakening any context field.
- **P4-3c3 proof:** production now reads both MarkRun controls independently under first authority, charges control and checkpoint WholeEntity bytes before allocation, validates each checkpoint against one of at most two exact captured contexts, and opens only complete private AGCW/AGWO closures. The workspace reopener charges the manifest at two times stored bytes, streams one charged object at a time, requires canonical `<kind>/<ordinal:016x>.agwo` names, rejects symlink/path substitution, truncation, growth, malformed identity/checksum/body, capacity, cancellation, and memory pressure, and revalidates an object against the retained immutable manifest on every point-of-use read. After external validation, the selector rechecks both control locators under authority, applies the frozen closure-aware A/B selector, preserves deterministic degraded-slot diagnostics, falls back from an invalid newer closure to a complete peer, and returns an owned bounded handle. No-control absence, both hash widths, close/reopen, missing checkpoint, corrupt control, tampered workspace, both-invalid refusal, equal-sequence ambiguity, cancellation, pressure, path substitution, and architecture isolation are covered. The final 17-target P4 matrix passes 243 tests; all 437 library tests, all 28 error-squelch architecture tests, formatting, `git diff --check`, and all-target checking pass with four threads and home-backed scratch. The reviewed suppression ceiling remains exactly 1,485. Strict Clippy remains at the historical 120-diagnostic lib-test baseline; P4-3c3 adds no diagnostic, and the sole touched-module diagnostic is the pre-existing P4-3c2 committed-failure fallback. Selection remains disconnected from service, live v3 GC, task, route, sweep, Void, reclaim, and destructive activation. Exact Windows DACL readback remains assigned to P4-3e.
- **P4-3d territory:** the frozen journal reader validates only segment summaries and hides record bytes, so no bounded consumer can reconcile roots, logical keys, or new physical incarnations. No encoder, buffered owner, first-authority sink, forward drain source, or final-guard state machine exists. Current v3 namespace acknowledgements carry root and locator offsets but not the v4 integrity digest/write sequence required by `PhysicalIncarnationIdV1`; they remain characterization rather than a fabricated producer. Journal records order by `(publication_sequence, MutationId)`. A single publication may exceed one segment, so segment closure must permit an equal boundary sequence only when the next MutationId advances; sequence-only continuity would either reject a valid bounded split or silently lose a batch tail. P4-3d lands the typed codec/iterator, bounded recoverable-soft owner, immutable sink, and guarded streamed reconciler while remaining disconnected from live service/v3 GC. The final guard must observe an unchanged layout, reconcile every sequence through its captured boundary, prove an empty second drain, and invoke final publication before releasing exclusion. Any gap, layout drift, starvation, cancellation, soft-journal loss, malformed chain, or publication uncertainty retains prior authority and requests restart rather than authorizing candidate state.
- **P4-3d1 red and proof:** the dedicated convergence target first failed to compile on the absent mutation operation, borrowed record iterator, write request, and segment encoder contracts. Production now exposes all ten frozen operations and every mutation field without copying record payloads, validates the exact physical incarnation, emits immutable journal segments byte-for-byte equal to both independent hash-width fixtures, and read-validates its own output. Segment closure now uses the complete `(publication_sequence, MutationId)` cursor, allowing one large publication to cross a bounded segment boundary while rejecting duplicates/regressions. Empty, wrong-width, zero, malformed physical, noncanonical-order, cap, and predecessor inputs fail before a usable artifact is returned. The focused target passes four tests, the checkpoint/format perimeter passes 80 tests, and all-target checking passes with only the historical unused test macro warning. Source isolation confirms no service, v3 namespace/GC, task, storage-engine, route, candidate, sweep, or Void caller is active.
- **P4-3d2 red and proof:** a committed writer now offers each exact mutation to one pre-reserved 16 MiB-bounded recoverable-soft owner without invoking a storage sink. The background owner flushes by count, byte target, or monotonic time; permits one publication to split across bounded segments; retains exact bytes after retryable sink failure; and permanently invalidates candidate completeness after any gap, malformed record, cancellation, clock regression, memory pressure, overflow, or publication uncertainty. Every hard receipt binds the artifact key, stored length, and a strictly advancing WholeEntity-v1 write sequence. A dedicated falsifier first proved that a stale nonzero receipt sequence was accepted, then passed after the owner rejected replayed durability tokens. `V4FirstAuthorityPublisher` validates and hard-publishes the exact immutable mark-mutation artifact through the shared KV/header/coordinator authority, supports exact reopen/retry without republication, and refuses wrong kind, key, database, generation, ordinal, sequence range, count, malformed bytes, and dishonest receipts before advancing owner state. The complete 17-target P4 matrix passes 288 tests, all 437 library tests pass, all-target checking and formatting pass, and all 28 error-squelch architecture tests pass with the reviewed ceiling reduced from 1,485 to 1,484. Strict Clippy remains blocked by the historical 122-line aggregate and has no diagnostic in the new convergence module, codec changes, or specs; the touched `first_authority.rs` hit is the already-recorded P4-3c2 test-helper debt. Architecture proof confirms no service, live v3 namespace/GC, task, storage-engine, route, candidate, sweep, Void, reclaim, or destructive caller is active.
- **P4-3d3 red and proof:** the dedicated target first failed to compile on the absent boundary/cursor, streamed source/visitor, catch-up, exclusive final-guard, and exact publication request/receipt contracts. One bounded reconciler now consumes borrowed AGWO mutation objects and immutable journal segments, orders every record by `(publication_sequence, MutationId)`, validates run/checkpoint/journal context and predecessor closure, applies before advancing state, and latches restart on gaps, root mismatch, malformed input, record caps, cancellation, memory pressure, source failure, layout drift, apply failure, starvation, a nonempty guarded second drain, or publication uncertainty. Catch-up captures cursor-relative authority boundaries for a bounded number of rounds; finalization captures and drains under caller-supplied exclusion, requires an unchanged boundary and empty second drain, captures it again, and invokes publication before the guard returns. The final request and receipt bind hash profile, database, run, generation, checkpoint, KV layout, authority root, terminal `(publication_sequence, MutationId)` cursor, journal head, applied count, and a later hard publication sequence. Adversarial review found two sequence-only failures: a boundary without the terminal MutationId could accept an omitted tail, and catch-up rejected a later terminal MutationId within the same publication. The boundary now requires the complete cursor, explicit falsifiers reject omitted tails, and same-publication cursor advances continue through another bounded round. A guard that fails after the inner operation cannot leave the reconciler falsely finalized. Both hash widths, cross-publication and same-publication moving boundaries, exact final publication, gaps, malformed input, cancellation, pressure, source exhaustion, starvation, apply failure, layout drift, nonempty second drain, dishonest receipts, and post-operation uncertainty are covered. The complete 18-target P4 matrix passes 296 tests, all 437 library tests pass, all-target checking and formatting pass, and the reviewed error-squelch inventory remains exactly 1,484 with all 28 architecture tests green. Strict Clippy remains at the exact 122-line historical aggregate with no diagnostic in a changed production or spec file. Source search confirms no service, live v3 namespace/GC, task, storage-engine, route, sweep, Void, reclaim, or destructive caller is active.
- **P4-3d4 red and proof:** the cancellation falsifier first showed that cancellation after one applied record allowed the rest of the segment to continue. The one reconciler path now rechecks cancellation and memory admission before every record, preserving the exact applied prefix and latching restart before the next mutation. The expanded 14-test target proves malformed predecessor/reset context, ignored visitor errors, soft-publication failure with byte-exact retry, permanent incompleteness of the failed owner, successful fresh-run consumption of the durable retry, authority bypass, double execution, guard-entry cancellation, dishonest and post-operation receipts, and two concurrent finalizers contending on one real mutex-backed exclusion boundary. A recursive architecture gate confines owner/reconciler/final-guard symbols to the disconnected v4 mark modules and the existing immutable first-authority sink. The complete 18-target P4 matrix passes 302 tests, all 437 library tests pass, all-target checking and formatting pass, the reviewed error-squelch inventory remains exactly 1,484 with all 28 architecture tests green, and strict Clippy remains at the exact 122-line historical aggregate with no changed-file diagnostic. No service, live v3 GC, task, storage-engine, route, sweep, Void, reclaim, or destructive caller is active.
- **P4-3e red and proof:** native Windows first exposed that a valid drive-letter workspace path was serialized with backslashes and rejected by the frozen portable checkpoint contract. After portable path normalization isolated that defect, a deliberate protected `Everyone: Full Control` workspace/manifest DACL reopened successfully, proving that the prior Windows implementation set permissions but never read them back. The closure now serializes one validated forward-slash absolute checkpoint path, creates directories with an explicit current-user owner and protected current-user-plus-SYSTEM DACL, sets the corresponding exact regular-file owner/DACL before content writes, and validates owner plus canonical DACL from the already-open no-follow handle with `GetKernelObjectSecurity`. Directory validation opens the exact directory object with `FILE_FLAG_BACKUP_SEMANTICS | FILE_FLAG_OPEN_REPARSE_POINT` and rejects reparse points. Broadening either the workspace directory or a regular manifest now fails closed before resume. Linux passes the complete 18-target P4 matrix, all 437 library tests, all-target checking, formatting, mdBook, `git diff --check`, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests. The four new fail-closed Windows conversion branches raise the exact reviewed suppression inventory from 1,484 to 1,488. Strict Clippy remains at the exact 122-line historical aggregate (120 reported diagnostics) with no new changed-code diagnostic; the existing `first_authority.rs` test-helper diagnostic remains unchanged. Native macOS arm64 passes 13 workspace, eight checkpoint-storage, and three internal mark-control tests. Native Windows x86_64 MSVC passes 11 workspace, eight checkpoint-storage, and the same three internal mark-control tests, including exact directory and regular-file DACL falsifiers. These are real private filesystem create, sync, close, reopen, tamper, and restart paths on all three platforms; no HTTP test is fabricated because P4-3 remains deliberately disconnected from service, live v3 GC, sweep, Void, reclaim, and destructive activation.
- **P4-4 territory:** Root candidate/expiry rows and summarized manifest/retirement/reclaim-proof readers already validate the frozen fixtures. `RootReadPinCoordinatorV1` supplies the shared per-root admission/retirement exclusion gate and computes advisory expiry with checked `pending_since + max(grace_at_pending, current configured grace)`. No complete lifecycle manifest body, retirement evidence, or reclaim-proof model is exposed; no lifecycle encoder, closure validator, transition owner, publisher, service source, or destructive caller exists. Root admission commit, NamespaceRoot authority, task pins, snapshot/fork/migration roots, selected lifecycle controls, and later quarantine manifests are graph inputs or consumers and may not independently classify lifecycle. P4-4 lands as P4-4a exact reader/writer closure, P4-4b bounded pure transition model, P4-4c hard retirement publication, P4-4d reclaim-proof/optional-expiry retention, and P4-4e adversarial qualification. P4-5 remains the owner of physical-incarnation quarantine and final physical reachability guards.
- **P4-4a red and proof:** the dedicated target first failed to compile because complete borrowed root-lifecycle/expiry/retirement/reclaim-proof bodies, exact encoders, graph closure, and a bounded catalog model did not exist. Production now exposes every frozen field and immutable edge, exact candidate/expiry row and immutable manifest/evidence encoders for Blake3-256 and SHA-512, strict self-validation through the existing readers, and one constant-memory model that validates candidate/expiry catalogs across ordered pages with record caps, cancellation, aggregate closure, and failure latching. Populated and empty bytes match all independent fixtures exactly at both hash widths. The focused target proves malformed state, graph mismatch, duplicate cross-page ordering, pre-start and mid-traversal cancellation, and failed-model reuse. The 25-target adjacent matrix passes 515 tests, all 437 library tests pass, all targets check, formatting, mdBook, `git diff --check`, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests pass. The reviewed suppression inventory remains exactly 1,488. Strict Clippy remains at the exact historical 122-error lib-test aggregate with no diagnostic in a changed production or spec file. Recursive source search confirms the new APIs have no service, live v3 GC, task, storage-engine, route, sweep, Void, reclaim, allocator, or destructive caller; a fabricated HTTP test would not exercise this disconnected format/model unit.
- **P4-4b red and proof:** the focused target first failed to compile on the absent `gc_root_transition` boundary. Production now exposes one constant-memory pure merge model over strictly root-ordered observations and optional prior candidate rows. It requires a complete, destructive-enabled mark with healthy lifecycle/physical authority, a strictly later lifecycle and mark generation, exact prior manifest/expiry aggregate closure, and bounded root count. The first unreachable complete mark freezes time, configured grace, reason, authority-set digest, and admission payload evidence; only a later complete mark may confirm or produce a retirement intent. Eligibility uses checked `pending_since + max(grace_at_pending, current configured grace)`, so zero grace still requires two marks, increases extend pending state, decreases never shorten frozen grace, and overflow fails closed. Reachability clears candidates; indeterminate evidence retains them. Candidate admission and retirement capacity are deterministic root order: the first failed growth latches capacity refusal for all later growth in that pass while clearing remains allowed. The hard cap counts candidate rows and mandatory logically-retired rows only; optional post-reclaim evidence remains governed by its separate retention budget, and mandatory evidence is never evicted even when already over cap. Retirement output is only an owned intent tied to the prior lifecycle hash and frozen evidence; it cannot publish, generate retirement IDs, mutate controls, or remove data. Eight focused tests cover both hash widths, byte-valid candidate output, zero/frozen/current grace, time overflow, mandatory/optional cap accounting, deterministic refusal, reachability/uncertainty, stale/mutated evidence, incomplete/disabled/unhealthy authority, pre-start/mid-pass cancellation with failure latching, ordering, bounds, prior-candidate coverage, and source isolation. The 26-target adjacent matrix passes 523 tests, all 437 library tests pass, all targets check, formatting, mdBook, `git diff --check`, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests pass. The reviewed suppression inventory remains exactly 1,488 after preserving integer conversion sources. Strict Clippy remains at the exact historical 122-error aggregate with no changed-file diagnostic. No service, live v3 GC, task, storage-engine, route, sweep, Void, reclaim, allocator, publication, or destructive caller references the model.
- **P4-4c execution shape:** P4-4c1 lands reusable exact GC page/directory writers with independent both-width fixture proof. P4-4c2 extracts one generic A/B GC-control replacement publisher shared by existing mark controls and new lifecycle controls. P4-4c3 publishes retirement commit, candidate/expiry catalog closure, immutable manifests, and finally the selected lifecycle control while holding `RootReadPinCoordinatorV1` retirement exclusion and rechecking current authority/evidence. P4-4c4 closes crash prefixes, exact retry, restart selection, pin/authority races, committed-failure reporting, and architecture isolation. The selected control remains the sole logical-retirement instant.
- **P4-4c1 red and proof:** the dedicated target first failed to compile because no production writer could emit the four frozen GC page/directory roles. Production now provides bounded exact page and arbitrary-depth directory writers for candidate, root-expiry, physical-inventory, and root-candidate state. Writers validate identity, fixed row widths, role semantics, strict ordering, non-overlapping ranges, generation/count closure, arithmetic overflow, and the 4 MiB persistent caps before allocation; every emitted artifact is self-decoded through the independent reader. Both Blake3-256 and SHA-512 outputs match all eight independent fixture page/directory pairs byte-for-byte. A synthetic two-leaf internal tree proves scalable directory composition and child closure without flattening pages. Malformed tests cover empty, unsorted, cross-role, overlapping, and invalid child-hash input. The six-target adjacent matrix passes 109 tests, all 437 library tests pass, all targets check, formatting and mdBook pass, the 436-fixture/95-route/38-doc contract gate passes, and all 28 suppression architecture tests pass with the inventory still exactly 1,488 after mechanical line refresh. Strict Clippy reports 114 historical library findings and no diagnostic in the changed production or spec files. Recursive source review confirms no service, task, allocator, live v3 GC, route, storage-engine, or destructive caller can reach these disconnected writers.
- **P4-4c2 red and proof:** the internal target first failed to compile on the absent shared GC-control publication method and neutral outcome. The former mark-only transaction is now one kind-scoped publisher that owns prepared-byte decoding, database/owner identity, A/B keys and slot selection, monotonic control sequence, durable write-sequence reservation, exact old/new physical-incarnation lineage admission, inactive-header dependency publication, atomic KV visibility, readback, and committed-failure classification. A neutral internal failure taxonomy has one mark-run adapter, preserving existing `mark_checkpoint_*` codes and receipt behavior without duplicating the transaction. A real root-lifecycle sequence hard-publishes three immutable manifests and controls A, B, then replacement A through the same path; it proves exact retry, one buffered replacement record before caller flush, mark/lifecycle key isolation, and wrong-family refusal without authority mutation. All 22 first-authority crash/restart/lineage tests pass, the eight-target adjacent matrix passes 127 tests, all 438 library tests pass, all targets check, formatting and mdBook pass, and the 436-fixture/95-route/38-doc contract gate passes. All 28 suppression architecture tests pass with exactly 1,488 reviewed occurrences; an initially introduced default-on-error helper was removed rather than increasing the ceiling. Strict Clippy remains at 113 historical library findings with no diagnostic in either changed source or spec file. The shared publisher remains private to the disconnected v4 first-authority unit and has no service, task, allocator, v3 GC, route, storage-engine, or destructive caller.
- **P4-4c3 territory and test contract:** lifecycle pages and directory nodes may approach the 1 GiB hard cap and therefore cannot be retained in one request or published while holding the root exclusion window. They are immutable and non-authoritative until a lifecycle control selects their manifest, so P4-4c3 first streams and hard-publishes exact support artifacts, validates their complete closure, and returns a non-constructible in-process closure permit. The short final transaction acquires the shared per-root retirement exclusion and first-authority state guard, independently rechecks selected HEAD/database identity plus the prior lifecycle control/hash, invokes a typed caller-owned verifier for snapshot/fork/migration/task authority not resident in first-authority storage, verifies the frozen retirement intent and closure permit, publishes the immutable retirement commit and exact expiry/lifecycle manifests, and selects the lifecycle control last. The selected control is the sole logical-retirement instant. A failure before it leaves the prior pending lifecycle readable; a committed failure after it reports the root retired while retaining physical bytes. Replacement lineage is prepared before control activation and hard-flushed only after exclusion is released, preserving the existing non-reentrant retirement sink.
- **P4-4c3a proof:** production now hard-publishes only exact root-candidate/root-expiry pages and directory nodes through the existing immutable GC WholeEntity/header/KV authority; exact retries reuse the prior entity and wrong database, key, role, or artifact kind cannot mutate authority. One bounded child-before-parent closure builder retains only unfinished descriptor summaries under `MemoryOwner::GarbageCollection`, validates every page through the constant-memory lifecycle model, matches every directory descriptor while treating physical hints as advisory, and requires exact manifest roots. A retirement-specific permit additionally proves that the target root is absent from the candidate catalog and appears exactly once as mandatory logically-retired expiry evidence linked to the exact retirement commit, timestamps, and final mark. Six focused tests cover both frozen hash widths, exact retry, malformed ordering, descriptor substitution, unsupported roles, artifact/record limits, cancellation with failure latching, incomplete closure, retirement-evidence mismatch, and hard memory pressure. Lifecycle support remains immutable and non-authoritative; no selector, service, task, live v3 GC, allocator, sweep, Void, reclaim, or destructive caller is active.
- **P4-4c3 red and proof:** the public target first failed to compile on the absent guarded root-retirement orchestrator and typed caller-owned authority verifier. A second deliberate falsifier then proved that a valid in-memory closure permit could select lifecycle authority even when its immutable support pages and directory were never hard-published. Production now reopens the exact candidate/expiry graph from the KV-backed WholeEntity bytes before exclusion, traverses it child-before-parent under the frozen depth and artifact caps, reserves every loaded entity through `MemoryOwner::GarbageCollection`, and requires the rebuilt closure to equal the non-constructible request permit. The short final transaction then holds the target's shared read-pin exclusion and the first-authority state guard from selected HEAD/database/prior-control/admission rechecks through a typed full-authority callback, immutable retirement commit, expiry manifest, lifecycle manifest, and lifecycle-control publication. The lifecycle control is last and is the sole logical-retirement instant; replacement lineage is prepared before activation and hard-flushed only after both exclusions release. Exact selected retry skips stale caller-owned authority while revalidating immutable closure and republishes no authority artifact. Focused fault tests prove missing support, cancellation, active pins, current HEAD, advanced lifecycle authority, admission/authority digest drift, target reauthorization, external verifier failure, pre-selector dependency failure with restart, post-selector committed failure with restart, exact retry, hard lineage, retained target/admission bytes, and no physical removal. All 31 internal first-authority tests, seven public closure/finalizer tests, the 39-test lifecycle/state/pin/first-authority integration matrix, all 28 suppression architecture tests, the exact 1,488-occurrence reviewed inventory, and all-target checking pass with four threads. Static source search confirms the finalizer, verifier, support publisher, and control fault seam remain confined to the disconnected v4 first-authority unit and specs; no service, live v3 GC, task, allocator, route, sweep, Void, reclaim, or destructive caller is active.
- **P4-4c4 red and proof:** the final-selector header matrix first reproduced a late-failure bug: after the exact new lifecycle header had been physically selected, generic error unwinding aborted the staged KV view, discarded prepared replacement lineage, and returned no committed retirement receipt. The shared GC-control publisher now captures the exact admitted successor, reconciles the physical A/B header after every publication failure, classifies only an exact selected successor as committed uncertainty, retains its buffered replacement lineage, and leaves earlier data-barrier/header-write failures pending. The process remains durability-latched; restart reconstructs the selected authority before exact retry. Every dependency prefix and all five native header phases prove restart selects exactly the prior or requested lifecycle. A real concurrent pin blocks behind retirement exclusion and observes `root_expired` only after selector publication. Post-selector lineage failure and pin-cleanup failure retain exact committed receipts. Adversarial review added a combined selector-uncertainty plus pin-cleanup falsifier; it initially proved cleanup evidence was squelched by the earlier committed failure, and the wrapper now preserves the receipt while reporting both failures. Corrupt prior control, prior manifest, or support locator cannot publish retirement authority. Recursive architecture proof confines the finalizer to disconnected first-authority code. The focused internal/public targets pass 37 and eight tests; the complete 18-target v4 matrix passes 168 tests; all 453 library tests, the legacy storage matrix, all-target checking, formatting, `git diff --check`, mdBook, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests pass. The reviewed suppression inventory remains exactly 1,488. Strict Clippy's historical aggregate is 118 errors with no diagnostic in a changed source or spec file. P4-4c is complete without a service, live v3 GC, task, allocator, route, sweep, Void, reclaim, or destructive caller.
- **P4-4d territory and execution shape:** the frozen reader/encoder already exposes the immutable `RootObjectReclaimProof`, both expiry-row states, proof/retirement closure checks, and mandatory/optional manifest aggregates. No producer qualifies physical inventory absence plus sweep receipts, no transition applies a proof to a mandatory row, and no compactor enforces the optional age/byte policy. The ratified policy keeps the newest evidence and evicts the oldest optional records; catalog persistence remains NamespaceRoot-hash ordered. P4-4d1 therefore adds a caller-verifier-backed, non-constructible qualification permit and checked proof/row transition. P4-4d2 streams root-ordered rows through a constant-memory model: expired rows drop first, a proposed `(expires_at, root_hash)` cutoff selects the newest nonexpired records, and final accounting rejects both budget overflow and needless eviction. P4-4d3 reuses the selector-last first-authority boundary to publish proof and replacement lifecycle state. P4-5/P4-6 remain the only future owners of physical absence and receipt production; this unit stays disconnected from live v3 GC, service, routes, allocator, Void, and destructive reclaim.
- **P4-4d1 red and proof:** the dedicated target first failed to compile on the absent reclaim-qualification module. Production now validates exact mandatory retirement evidence, retirement timestamps, database/hash identities, nonzero canonical incarnation and receipt evidence, a final physical inventory generation strictly newer than every included receipt, checked inventory/reclaim/retention times, and cancellation before and after the caller-owned verifier. The verifier cannot construct a permit; after it accepts the physical absence and receipt sets, production encodes and self-decodes the immutable proof and mandatory-to-optional replacement row, then verifies their exact graph closure. Both hash widths pass. Falsifiers cover verifier refusal, stale/replayed inventory, zero receipt evidence, already-reclaimed state, mismatched retirement time, retention overflow, pre-callback cancellation, and cancellation inside the callback. The focused and adjacent lifecycle matrix passes 19 tests, all 453 library tests and all-target checking pass, and strict Clippy reports only the historical library aggregate with no diagnostic in the new source or spec. Source isolation leaves the qualification boundary disconnected from service, live v3 GC, task workers, sweep, Void, allocator, and physical deletion.
- **P4-4d2 red and proof:** the dedicated target first failed to compile on the absent cutoff, selection, action, context, and retention-model contracts. Production now streams the NamespaceRoot-ordered prior expiry catalog in constant memory, applies the exact qualified mandatory-to-optional transition once, drops expired optional evidence before budget selection, preserves every mandatory row, and verifies a proposed `(evidence_expires_at_ms, namespace_root_hash)` cutoff describes the unique maximal newest optional set fitting the byte budget. `KeepAll` is required when no budget eviction is necessary; a cutoff must equal the oldest retained optional row, so alternate gap values cannot describe the same authority. Exact prior manifest counts, bytes, state classes, time bounds, target identity, root order, record limits, checked arithmetic, cancellation, and terminal failure all fail closed. Integer-conversion sources are preserved rather than collapsed, and no suppression was added. The four-test focused target covers both hash widths plus budget overflow, needless/ambiguous eviction, target exclusion/omission, aggregate lies, ordering, limits, malformed cutoff, and cancellation. The exact 18-target P4 matrix passes 168 tests, all 453 library tests pass, workspace all-target checking passes, all 28 suppression architecture tests preserve the exact 1,488-item inventory, and the independent contract gate passes 436 fixtures, 95 routes, and 38 documents. Strict Clippy remains blocked by 110 historical library diagnostics and reports none in either changed file. Recursive source search confirms the retention boundary remains disconnected from service, live v3 GC, task workers, sweep, Void, allocator, and physical deletion.
- **P4-4d3 red and proof:** the public boundary first failed to compile on the absent reclaim publication request, receipt, error, and first-authority method. Production now accepts only the non-constructible retention permit, reopens the exact bounded KV-backed expiry support graph, flushes preexisting replacement lineage, acquires the target root's retirement exclusion and the one first-authority state guard, rechecks selected HEAD and exact prior lifecycle/expiry authority, hard-publishes the immutable reclaim proof and replacement manifests, and selects the lifecycle control last through the existing shared GC-control publisher. Exact selected retry is idempotent, while selector uncertainty and post-commit pin/lineage failures preserve an exact committed receipt. No locator or physical byte is removed. Adversarial review found that aggregate closure alone allowed a byte-different expiry row with identical counts and bytes to replace the retention model's exact output; a deliberate falsifier published that substitution before the fix. The permit and durable closure now independently compute the same constant-memory, hash-profile-aware rolling digest over every retained canonical expiry row, and the selector refuses any mismatch before publishing authority. The review also removed a source-discarding format conversion and replaced an over-arity private constructor with one typed support-purpose enum. Four internal reclaim tests, the six-test focused target, the complete 18-target matrix with 170 tests, and all 457 library tests pass. All-target checking, formatting, `git diff --check`, mdBook, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests pass with the reviewed inventory still exactly 1,488. Strict Clippy reports the 118-diagnostic historical aggregate and no changed-file diagnostic. Recursive source search confirms reclaim publication remains disconnected from service, live v3 GC, tasks, routes, allocator, sweep, Void, physical deletion, and destructive activation.
- **P4-4e adversarial proof:** the reclaim selector now runs the same five native header-failure phases as retirement. Data-barrier and pre-header failures reopen on the prior logically-retired lifecycle; post-header, full-barrier, and readback uncertainty return exact committed receipts, retain buffered replacement lineage, and reopen on the requested reclaimed lifecycle. Every case retries to the same selected state and preserves the target's exact physical locator. Pre-publication cancellation and an active read pin emit no proof or selector; releasing the pin permits exact retry. A true concurrent read remains blocked while reclaim holds exclusion through inactive-control staging and receives `root_expired` only after selector commit. Post-selector lineage-flush and pin-cleanup failures preserve committed receipts, reopen as reclaimed, and retain target bytes. Corrupt selected control, selected prior manifest, or durable reclaim support cannot publish new authority. A tiny memory budget refuses during durable support revalidation before proof publication. Existing transition/model tests cover two-mark authority, deterministic hard-cap refusal, malformed rows, cancellation, optional retention capacity, and both hash widths. Nine focused internal reclaim tests, all 462 library tests, and the complete 170-test P4 matrix pass. All-target checking and the exact 1,488-item suppression architecture gate pass. The added unit is test-only and keeps root lifecycle disconnected from service, live v3 GC, tasks, routes, allocator, sweep, Void, physical deletion, and destructive activation. P4-4 is complete with all failures resolving to prior readable authority, selected retired/reclaimed authority, retained physical bytes, or leaked immutable support.
- **P4-5 territory:** the frozen quarantine family already has both-width controls/manifests, candidate pages, and candidate deltas, while `gc_state.rs` validates their bytes only through summarized manifest/delta variants and has no borrowed candidate or delta-record API. The seven permanent classes cover unreachable current locators, retired lower incarnations, uncommitted orphans, expired derived/audit artifacts, expired root-closure objects, and unexplained inventory gaps. Complete guarded mark convergence exposes only an abstract final-publication callback; no production quarantine transition model, closure validator, encoder, selector, final reachability/pin verifier, service caller, sweep caller, or destructive consumer exists. Inputs are the prior selected quarantine closure, complete mark output, selected physical inventory and lineage, selected root lifecycle basis, effective grace, and final current-authority/task/audit/request-pin observations. Consumers are P4-6 sweep proposals, later inventory states and root-object reclaim proofs, P4-8 verify/repair/observability, migration reset, and P5 derived-artifact retirement. P4-5 lands as exact codecs/closure, a bounded two-mark transition model, selector-last publication under global request-pin plus first-authority exclusion, and adversarial crash/race qualification. Physical locator removal remains owned by P4-6 and disabled.
- **P4-5a red and proof:** the dedicated target first failed to compile because the frozen quarantine family exposed only summary readers and no borrowed candidate rows, delta records, exact writers, or typed graph closure. A second falsifier proved raw little-endian fence comparison misordered physical offsets across the `255 -> 256` boundary, and a third proved the generic candidate-delta summary reader incorrectly treated its nonzero predecessor hash as reserved padding. Production now exposes all seven permanent candidate classes, exact borrowed candidate/delta/manifest readers and writers for Blake3-256 and SHA-512, and one bounded closure validator for compacted page/directory graphs plus ordered predecessor-linked deltas and the exact captured root-lifecycle basis. The validator compares typed physical incarnations, supports nested child-before-parent directories through the frozen depth and fanout, reserves child summaries through `MemoryOwner::GarbageCollection` before allocation, checks cancellation within records and children, caps support artifacts and aggregate delta bytes, latches terminal failure, and releases every reservation on success and failure. Base graph totals remain distinct from the final base-plus-delta manifest totals for P4-5b's overlay proof. Ten focused tests cover byte-equal fixtures, both hash widths, every class, set/clear state, wrong widths, reserved bytes, typed ordering, nested graphs, empty and delta-only closures, missing/extra/wrong-predecessor deltas, wrong lifecycle/role/child closure, cancellation, memory pressure, artifact limits, failure latching, and exact reservation release. The complete 19-target P4 matrix passes 180 tests, all 462 library tests pass, and workspace all-target checking is green. All 28 suppression architecture tests preserve the exact 1,488-item inventory; no new suppression was added. The independent contract gate verifies 436 fixtures, 95 routes, and 38 docs; formatting, mdBook, and diff hygiene pass. Strict Clippy remains blocked by 110 historical library diagnostics and 116 in the lib-test aggregate, with no diagnostic in a changed P4-5a source or spec file. Recursive source search confirms the new unit has no service, live v3 GC, task, allocator, storage-engine, route, sweep, Void, physical deletion, or destructive caller.
- **P4-5b red and proof:** the dedicated target first failed only because no physical-quarantine transition boundary existed. Production now exposes one constant-memory, failure-latching merge model over a strictly typed-incarnation-ordered union of current inventory evidence and prior candidates. A first confirmed-unreachable complete mark freezes exact incarnation, class, time, generation, grace, and hash profile; only a later complete generation with the same incarnation and class can emit a bounded non-authoritative sweep intent. Eligibility uses checked `pending_since + max(grace_at_pending, current configured grace)`, so zero grace still requires two complete marks, increases extend pending state, decreases cannot shorten it, equal millisecond timestamps remain valid, and overflow fails closed. Reachable evidence emits an exact clear, indeterminate evidence retains state, class changes restart frozen evidence, and missing prior rows fail aggregate closure. Candidate admission is deterministic physical order: the first failed growth latches refusal for later additions while clear/reclassification continues. The previous-incarnation buffers are reused, and all output allocations are bounded by one physical identity plus captured hash evidence. Nine focused tests cover both hash widths, byte-valid set/clear output, two marks, frozen/current/zero grace, equal timestamps, overflow, all reachability branches, class restart, deterministic capacity, invalid context/manifest/incarnation/candidate state, ordering, stream limits, cancellation, failure latching, and architecture isolation. The complete 20-target P4 matrix passes 189 tests, all 462 library tests pass, workspace all-target checking is green, and all 28 suppression architecture tests preserve the exact 1,488-item inventory. The independent contract gate verifies 436 fixtures, 95 routes, and 38 docs; formatting, mdBook, and diff hygiene pass. Strict Clippy remains blocked by 110 historical library diagnostics and reports no diagnostic in the new source or spec. No live server test is fabricated because the transition model has no service, task, allocator, storage, publication, sweep, Void, physical-deletion, or destructive caller.
- **P4-5c1 red and proof:** the focused target first failed to compile on the absent physical-quarantine support request and publisher method. Production now accepts only candidate pages, candidate-role directory nodes, and candidate deltas; revalidates their canonical kind, database, and key; and hard-publishes them through the existing immutable GC WholeEntity/KV/header/coordinator authority without selecting quarantine authority. Candidate pages, directory nodes, and deltas publish and exact-retry idempotently; wrong databases, lifecycle manifests, and root-candidate pages fail before a locator appears. The non-constructible publication permit now retains the exact validated closure summary needed for final durable revalidation. Three focused publication tests, ten closure tests, and nine transition tests pass; workspace all-target checking remains green with only the historical unused test macro warning. No second storage path or service, live v3 GC, task, sweep, Void, locator removal, or destructive caller was introduced.
- **P4-5c2/3 red and proof:** the public boundary first failed on the absent guarded physical-quarantine finalizer and caller-owned complete-authority verifier. Production now reopens the exact bounded candidate graph and ordered deltas from KV-backed WholeEntity bytes while holding one database-wide request-pin exclusion and the first-authority state guard, and requires the rebuilt closure to equal the non-constructible publication permit before any selector is written. The final verifier rechecks selected database/prior quarantine authority, complete mark/inventory/lineage/lifecycle bases, exact candidate incarnations and permanent classes, final unreachability, and absence of task/audit pins. The quarantine manifest is hard-published before the quarantine control, and that control is the sole selector and last authority write. Exact selected retry revalidates durable support without trusting stale external authority; every pre-selector dependency or early header failure restarts on the prior authority, while selector uncertainty and post-selector lineage or pin-cleanup failures preserve the exact committed receipt and restart on the requested quarantine authority. A racing read admission waits behind the global exclusion and observes the post-recheck state. Cancellation, corrupt or absent support, memory pressure, stale prior authority, every authority-basis drift, all five native header failure phases, exact retry, and replacement-lineage failure are covered. All 469 library tests and the complete 23-target P4 matrix with 211 integration tests pass with four threads. All-target checking, formatting, `git diff --check`, mdBook, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests pass; the reviewed inventory remains exactly 1,488 after five accidental new conversion sites were removed rather than admitted. Strict Clippy remains blocked by 110 historical library diagnostics and reports no diagnostic in the changed quarantine source/spec files. Recursive architecture proof confines the finalizer to disconnected v4 first-authority code and finds no service, live v3 GC, task, route, allocator, sweep, Void, locator removal, physical deletion, or destructive caller. P4-5c is complete without activating quarantine or reclaim.
- **P4-5d adversarial proof:** a final corruption matrix explicitly damages the selected quarantine control, its selected prior manifest, and the captured lifecycle support independently. Every case fails before the caller-owned authority verifier, emits no candidate manifest or selector, returns no committed receipt, and leaves physical bytes retained. This closes the transitive prior-manifest validation question rather than assuming the selected control alone was sufficient. Existing transition tests cover both hash widths, malformed rows, all permanent classes, two complete marks, grace overflow, cancellation, deterministic capacity refusal, and failure latching. Finalizer tests cover active and racing pins, every final authority drift, support corruption and memory pressure, selected-prior advancement, all five selector durability boundaries, restart to exactly prior or requested authority, exact retry, and post-selector lineage failure with an exact committed receipt. The complete 23-target P4 matrix passes 211 integration tests and all 470 library tests pass with four threads. No production code changed in this qualification unit, and physical locator/Void/removal paths remain absent. P4-5 is complete with every failure retaining prior authority, selecting one complete requested authority, retaining physical bytes, or leaking immutable support.
- **P4-6 territory:** frozen SweepProposal, commit/recovered receipt, and receipt-backed Void fixtures already define both hash widths, exact physical-incarnation ordering, the corrected `32 + 2H + count * (24 + 2H)` proposal body, a 4,096-candidate cap, all seven outcome classes, exact counters, and proposal/catalog closure. Before this landing the reader exposed raw candidate/outcome byte slices and omitted both persisted timestamps; no production writer, borrowed iterator, proposal qualifier, publisher, removal boundary, recovery reconciler, selected Void authority, allocator, service, task, or destructive caller existed. P4-6 consumes exact selected quarantine authority and later final locator/pin/lineage observations; it feeds P4-7 selected Void publication and later root-reclaim receipt evidence. The landing sequence remains codec, non-authoritative proposal qualification/publication, one plural caller-owned locator-removal boundary, receipt/recovery after independently verified Void selection, then adversarial closure.
- **P4-6a red and proof:** the dedicated target first failed to compile on the absent proposal/outcome/receipt writer and iterator contracts and on the persisted timestamps missing from typed reader output. Production now exposes failure-latching, typed-incarnation-ordered borrowed iterators, all seven frozen outcome classes, exact both-width proposal and commit/recovered receipt writers, and the persisted creation/commit timestamps. Proposal and receipt writers validate database/batch identity, generation, time, hash widths and nonzero roots, the 4,096-row cap, every physical incarnation, strict order, reclaimed versus skipped/failed result shape, exact totals, and self-decode before returning bytes. The physical-incarnation encoder is shared with quarantine instead of creating a third copy. Seven focused tests match every independent Blake3-256/SHA-512 fixture byte and key, cover every outcome class, zero/wrong-width/oversized/malformed inputs, duplicate/reversed rows, all inconsistent result shapes, terminal iterator failure, and source-level isolation. The complete 24-target v4 GC matrix passes 218 integration tests and all 470 library tests pass with four threads. Workspace all-target checking, formatting, `git diff --check`, mdBook, the 436-fixture/95-route/38-doc contract gate, the exact 1,488-item suppression audit, and all 28 suppression architecture tests pass. Strict Clippy remains blocked by the same 110 historical library diagnostics and reports no diagnostic in the changed production files. No service, live v3 GC, task, storage engine, allocator, selected Void, locator removal, physical deletion, or destructive caller is active.
- **P4-6b territory:** aggregate `eligible_count_hint` and `eligible_bytes_hint` cannot identify the exact eligible incarnation set after quarantine publication. The transition model is the only place that possesses exact grace-qualified intents; its permit must therefore bind a canonical rolling digest of those intents, the quarantine publication permit must preserve that digest, and proposal qualification must revalidate the bounded exact intent slice against it. The restricted publisher must independently observe the exact physically selected quarantine manifest before hard-publishing the immutable proposal. Crash before proposal publication may repeat mark/quarantine work and leak immutable support only; no locator-removal token or destructive caller may be introduced.
- **P4-6b red and proof:** the dedicated target first failed to compile on the absent proposal qualifier, non-constructible permit, and restricted hard-publication request/receipt boundary. Production now carries one domain-separated both-width rolling digest over every exact eligible intent from the two-mark transition permit through quarantine qualification, then independently revalidates the strictly ordered maximum-4,096 intent slice, frozen/effective grace, timing, authority basis, aggregate hints, and digest before encoding one immutable proposal. The first-authority publisher decodes the privately owned proposal bytes, holds the shared authority lock, reselects the exact nondegraded database plus physically selected quarantine manifest/generation, and appends through the existing immutable-GC hard-publication path without returning any locator-removal or Void authority. Exact retry returns the same durable receipt. A real file-backed disconnected test advances empty quarantine through a delta-only first mark and eligible second mark, proves stale authority and corrupt selected-manifest locators publish nothing, selects the exact quarantine, hard-publishes and retries one proposal, and rejects cancellation. Review also corrected the transition model's stale assumption that every nonempty candidate set has a compacted root; the frozen manifest/closure explicitly permits delta-only candidate authority. Malformed, substituted, reversed, truncated, stale, oversized, canceled, wrong-authority, corrupt-locator, and retry paths are covered at both hash widths. The post-review complete 25-target P4 matrix passes 221 integration tests and all 471 library tests pass. All-target checking, formatting, `git diff --check`, mdBook, the 436-fixture/95-route/38-doc contract gate, and all 28 suppression architecture tests pass; four initially flattened conversions were rewritten to preserve typed sources, leaving the reviewed inventory exactly 1,488. Strict Clippy remains the exact 110 historical library findings and reports no changed-production diagnostic. Source search confirms no service, live v3 GC, task, route, allocator, selected Void, locator removal, physical deletion, or destructive caller is active.
- **Next action:** begin P4-6c with a plural, guarded locator-removal boundary that consumes exact proposal authority, rechecks current locator/pin/lineage state, and retains one exact outcome per incarnation without publishing a receipt prematurely.
