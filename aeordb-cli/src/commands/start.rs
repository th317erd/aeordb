pub fn run(port: u16, database: &str) {
  println!(
    "AeorDB server starting on port {port} with database {database}...\n\
     (Server start not yet implemented — use the library directly for now)"
  );
}
