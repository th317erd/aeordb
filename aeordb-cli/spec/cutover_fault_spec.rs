//! P8 native cutover fault campaign target.
//!
//! Keep the executable matrix in the library's canonical rehearsal spec so
//! the required CLI gate and the subsystem regression target cannot drift.

#[path = "../../aeordb-lib/spec/engine/migration_cutover_rehearsal_spec.rs"]
mod migration_cutover_rehearsal_spec;
